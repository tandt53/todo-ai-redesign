# Task Archive

Rows moved out of `.claude/state/TASKS.md` by the orchestrator when it exceeded
the caps declared in `MANIFEST ## Limits`. Nothing here is live; the queue is
`TASKS.md`. Grouped by ISO week.

## 2026-W33 — F-001 voice-assistant-view (complete: structural 14/14, product APPROVED)

| ID | Title | Module | Feature | Agent | Pri | Depends | Status | Artifacts | Outcome |
|----|-------|--------|---------|-------|-----|---------|--------|-----------|---------|
| T-001 | Spec the voice-first assistant view | assistant | F-001 | spec-agent | P0 | — | DONE | docs/specs/assistant/F-001-voice-assistant-view.md, docs/specs/assistant/index.md | 13 AC / 6 tensions; cites UC-52,54; flagged 11-uc-conversation.md gap |
| T-002 | Gate 1 multi-lens spec review | assistant | F-001 | orchestrator | P0 | T-001 | DONE | docs/reports/gate1-review-F-001.md | GATE CLOSED at round cap: 2 rounds, 123 findings routed, 2 owner decisions, 0 unresolved HIGH |
| T-002b | Spec revision from Gate 1 findings | assistant | F-001 | spec-agent | P0 | T-002 | DONE | docs/specs/assistant/F-001-voice-assistant-view.md | rev2: 29 ACs (was 13), C1-C12 all closed, 3 contradictions gone, spec-check exit 0, 191 lines |
| T-002c | Gate 1 re-review (round 2 of max 2) | assistant | F-001 | orchestrator | P0 | T-002b | DONE | docs/reports/gate1-review-F-001.md | 9/9 lenses; r1 clusters all closed; 15H/21M/8L new -> 7 clusters, no conflicts |
| T-002d | Final spec revision (round-2 findings) | assistant | F-001 | spec-agent | P0 | T-002c | DONE | docs/specs/assistant/F-001-voice-assistant-view.md | rev3: R1-R7 + 21 same-pass edits landed, 198 lines, 29 ACs stable, spec-check exit 0 |
| T-003 | Design system (phase system) | _shared | F-001 | design-agent | P1 | T-002 | DONE | docs/design/_shared/DESIGN.md, docs/design/_shared/tokens.json, docs/design/_shared/components.md | Aurora-disciplined identity, 13 components, 47/47 WCAG pairs pass, DESIGN.md 44 lines |
| T-004 | Architecture + contracts | assistant | F-001 | architect-agent | P1 | T-002 | DONE | docs/specs/_shared/ARCHITECTURE.md, docs/specs/assistant/api-contracts.md, docs/specs/assistant/data-model.md, docs/specs/_shared/adr/, docs/specs/_shared/platform/ | 12 files, 7/7 OQs answered, R1-R7 zero-drift, harness smoke 2/2 pass |
| T-005 | Screen mockups (phase screens) | assistant | F-001 | design-agent | P1 | T-003 | DONE | docs/design/assistant/screens/voice-assistant-view.html, docs/design/assistant/screens/voice-assistant-view-ios.html, docs/design/assistant/screens/voice-assistant-view-android.html | 3 variants x 17 states, design-check 18/0/6skip, 17-testid catalogue, OQ1: inline list + drawer |
| T-006b | Backend prototype server per ADR-001 | assistant | F-001 | backend-agent | P2 | T-004 | DONE | src/assistant/api/ | 8 endpoints, 9 rules, 5 mid-flight pins absorbed, vitest 65/65, tsc clean, mutation check ran; also created root package.json/tsconfig per platform doc |
| T-007b | QA api: author test cases | assistant | F-001 | qa-api-agent | P2 | T-002,T-004 | DONE | docs/qa/assistant/F-001/api/, tests/assistant/api/F-001-voice-assistant-view.spec.ts | 39 P1 TCs, 16/16 api ACs, 23/23 error rows, 24-row fixture table; 5 contract gaps routed to architect |
| T-006c | Finish web implementation | assistant | F-001 | web-agent | P2 | T-006 | DONE | src/assistant/web/ | app shell + components + 94 tests; orchestrator re-ran: web 94/94, api 68/68; 22/22 testids; fixed 2 bugs in T-006 output |
| T-006d | Align confirm-chip labels with design copy | assistant | F-001 | backend-agent | P2 | T-006c | DONE | src/assistant/api/engine/turns.ts, src/assistant/api/ports/fixture-interpreter.ts, src/assistant/api/ports/fixture-table.ts | server-owned labels w/ real count; vi ừ/không added; 5 new tests |
| T-006e | Fix api test-harness port flake (426 vs 404) | assistant | F-001 | backend-agent | P3 | T-006b | DONE | src/assistant/api/__tests__/helpers.ts | root cause: supertest per-request server + Node keep-alive port reuse; fixed single-server-per-harness; orchestrator verified 5/5 clean re-runs, agent ran 20/20 |
| T-007 | QA web: author test cases | assistant | F-001 | qa-web-agent | P2 | T-002,T-005 | DONE | docs/qa/assistant/F-001/web/, tests/assistant/e2e/F-001-voice-assistant-view.spec.ts | 32 P1 TCs, 22/22 web ACs, tsc clean, 3 testid/affordance requests routed to design |
| T-008b | Fix C8 Ops + C9.3 spec sentence | assistant | F-001 | spec-agent | P1 | T-008 | DONE | docs/specs/assistant/F-001-voice-assistant-view.md | Ops section added, AC-7 baseline corrected to post-apply, spec-check exit 0 |
| T-008c | Fix C9.1/C9.2/C9.3 contract+data-model | assistant | F-001 | architect-agent | P1 | T-008 | DONE | docs/specs/assistant/api-contracts.md, docs/specs/assistant/data-model.md | 500 INTERNAL documented, boundary_late+last_foreground_at added, AC-7 baseline corrected |
| T-008d | Investigate+fix new AC-15 test timeout flake (orchestrator-found, not in review report) | assistant | F-001 | backend-agent | P1 | T-008 | DONE | src/assistant/api/__tests__/helpers.ts | root cause: IPv6 wildcard bind + supertest double-listen race + unrelated playwright server on same port; fixed w/ explicit 127.0.0.1 bind + awaited listening event; orchestrator verified 15/15 + agent 30/30 |
| T-008f | Retag 24 ACs to (api, web); mobile deferred to F-002+ | assistant | F-001 | spec-agent | P1 | T-008b | DONE | docs/specs/assistant/F-001-voice-assistant-view.md | 22 ACs retagged (api,web); AC-26/27 marked RESERVED for mobile follow-up; Out of Scope + Data table fixed; spec-check exit 0 |
| T-009 | Fix + run qa-api automation (execute phase) | assistant | F-001 | qa-api-agent | P1 | T-008e | DONE | tests/assistant/api/F-001-voice-assistant-view.spec.ts, docs/qa/assistant/runs/2026-08-16-api-execute.md | rewired handle-based port, fixed 3 script bugs + 1 false-green, 46/46, npm test:all 213/213 |
| T-009b | Sync TC-10/TC-30 markdown to corrected single-pending-question scenario | assistant | F-001 | qa-api-agent | P2 | T-009 | DONE | docs/qa/assistant/F-001/api/TC-10-serial-order-and-answer-binding.md, docs/qa/assistant/F-001/api/TC-30-explicit-close-declines-idempotent.md, docs/qa/assistant/F-001/api/index.md | docs now match the automation; why-note added so it does not read as a weakened test; F-001 api tiers 46/46 + 73/73 |
| T-010 | Bring up Playwright harness + run qa-web automation (execute phase) | assistant | F-001 | qa-web-agent | P1 | T-008e | DONE | tests/assistant/e2e/, docs/qa/assistant/runs/2026-08-16-web-execute.md | harness built from scratch (also created root playwright.config.ts, vitest.config.ts per platform doc delegation); 34/34 e2e pass (orchestrator re-ran independently, real chromium); fixed 4 script bugs; npm test:all 213/213 |
| T-011 | Fix C9: 4 missing assistant_turn fields in data-model.md | assistant | F-001 | architect-agent | P1 | T-008e | DONE | docs/specs/assistant/data-model.md | post_apply/created_ids/pending_op/caused_resolutions added; post_apply row closes the AC-7 baseline loop |
| T-012 | Gate 4 structural re-verify | assistant | F-001 | reviewer-agent | P2 | T-009,T-010,T-011 | DONE | docs/reports/review-F-001-2026-08-16-v3.md, docs/specs/_shared/LEARNINGS.md | STRUCTURAL-PASS 14/14; orchestrator independently re-verified test:all 213/213, tsc clean, no C12 mutation left behind |
| T-015a | Extend AC-19 (4.1.3) + confirm no other spec English-copy leaks | assistant | F-001 | spec-agent | P1 | T-014 | DONE | docs/specs/assistant/F-001-voice-assistant-view.md | AC-19 gained 4.1.3; added Naming convention paragraph (concept names vs UI copy/locale); 1 literal-string leak fixed in Out of Scope; spec-check exit 0 |
| T-015b | Translate bulkDeleteOptions to Vietnamese | assistant | F-001 | backend-agent | P1 | T-014 | DONE | src/assistant/api/engine/turns.ts | Xoá N việc / Giữ lại; qa-api automation reads options positionally, unaffected (46/46); flagged mixed-language risk in web headline for T-015c |
| T-015c | Translate all web UI copy to Vietnamese + add aria-live for outcomes (4.1.3) + fix leaked "turn" word | assistant | F-001 | web-agent | P1 | T-014 | DONE | src/assistant/web/ | full Vietnamese pass, role=log+aria-live+role=alert wiring, self-proved via mutation (5/98 fail on 3 targeted breaks), turn-leak fixed; 217/217, tsc clean |
| T-015f | Translate the contract-documented unsupported_query alternative text | assistant | F-001 | architect-agent | P1 | T-015e | DONE | docs/specs/assistant/data-model.md, docs/specs/assistant/api-contracts.md | alternative value now "danh sách và bộ lọc trên màn hình" in both docs |
| T-015g | Implement turns.ts to match the corrected contract | assistant | F-001 | backend-agent | P1 | T-015f | DONE | src/assistant/api/engine/turns.ts, src/assistant/api/__tests__/turn.test.ts | contract value implemented; api unit 73/73; left 1 docs/qa/-owned stale assertion for T-016 (correct scope boundary) |
| T-015d | Update 3 mockups: Vietnamese copy + fix listening-Undo discrepancy + boundary hairline + clarify caption | assistant | F-001 | design-agent | P1 | T-014 | DONE | docs/design/assistant/screens/ (3 files) | 17 states x 3 files Vietnamese; M2 fixed in listening+mic-hidden (orchestrator briefing error caught+corrected); M3+M4 fixed; design-check 18/0/6skip held |
| T-016 | Sync qa-web automation to translated strings; add 4.1.3 TC; re-execute | assistant | F-001 | qa-web-agent | P1 | T-015b,T-015c,T-015g | DONE | tests/assistant/e2e/F-001-voice-assistant-view.spec.ts, docs/qa/assistant/F-001/web/ (19 edited + TC-033/034 new), docs/qa/assistant/runs/2026-08-16-web-execute-v2.md | 36/36 (orchestrator re-ran, real chromium); 3 self-proved mutations; found spelling drift Xóa/Xoá + Đang nghĩ/Đang xử lý (non-blocking, routed to Gate 5) |
| T-017 | Gate 5: final product review re-verify | assistant | F-001 | product-agent | P2 | T-016,T-016b,T-015d | DONE | docs/reports/product-review-F-001-final-2026-08-16-v2.md | APPROVED; 0 HIGH, 2 MEDIUM (M5/M6, non-blocking); orchestrator re-verified test:all 217/217; screen-reader pass + M5/M6 routed to sign-off checklist |
| T-016b | Sync qa-api automation to translated strings; re-execute | assistant | F-001 | qa-api-agent | P1 | T-015f,T-015g | DONE | tests/assistant/api/F-001-voice-assistant-view.spec.ts, docs/qa/assistant/F-001/api/TC-14-unsupported-query-names-alternative.md, docs/qa/assistant/runs/2026-08-16-api-execute-v2.md | 1 stale assertion fixed (self-proved via revert-and-check); test:all 217/217 |
| T-023 | Fix BUG-001: offline local creates never replay | assistant | F-001 | web-agent | P1 | T-019 | DONE | src/assistant/_shared/controller.ts, src/assistant/_shared/api/client.ts, src/assistant/_shared/testing/fixtures.ts, src/assistant/web/__tests__/controller.test.ts | syncLocalTasks: 201+409 both ack, idempotent, creates replay before queued turn; also closed an adjacent loss path (offline cold open wiped stored local tasks); 8 tests mutation-proved; contract needed no change |

## 2026-W33 — F-001 residual rows (superseded, archived with the feature)

Each of these was superseded by a later row that is also in this archive:
T-006 by T-006c · T-008 by T-012 · T-008e by T-012 · T-014 by T-017 ·
T-015e by T-015f+T-015g · T-013 was a P3 nicety never dispatched.

| ID | Title | Module | Feature | Agent | Pri | Depends | Status | Artifacts | Outcome |
|----|-------|--------|---------|-------|-----|---------|--------|-----------|---------|
| T-006 | Web implementation | assistant | F-001 | web-agent | P2 | T-004,T-005 | PARTIAL | src/assistant/web/ | 12 files: model/reducer/api-client/seams/stores + 2 components; no React deps, no app shell, no tests; agent died on session limit |
| T-008 | Structural review C1-C14 | assistant | F-001 | reviewer-agent | P3 | T-006c,T-006b,T-006d,T-006e,T-007,T-007b | PARTIAL | docs/reports/review-F-001-2026-08-16.md, docs/specs/_shared/LEARNINGS.md | STRUCTURAL-FAIL 10/14; fails: C1,C2 (mobile scope, needs human), C8 (no Ops), C9 (3 doc/code drifts); code+tests need no changes |
| T-008e | Gate 3 structural re-verify | assistant | F-001 | reviewer-agent | P2 | T-008f,T-008c,T-008d | PARTIAL | docs/reports/review-F-001-2026-08-16-v2.md, docs/specs/_shared/LEARNINGS.md | STRUCTURAL-FAIL 12/14 (was 10/14); mobile+C8+3xC9 confirmed fixed; NEW: C5 (qa automation never executed, 2 bugs), C9 (4 more undocumented turn fields) |
| T-013 | Add exact 2-target delete row to canonical fixture table | assistant | F-001 | backend-agent | P3 | T-009 | PENDING | — | — |
| T-014 | Gate 3: final product review | assistant | F-001 | product-agent | P1 | T-012 | PARTIAL | docs/reports/product-review-F-001-final-2026-08-16.md | CHANGES REQUESTED 2H+4M+5L; H1 locale mismatch (owner: translate to Vietnamese), H2 WCAG 4.1.3 missing |
| T-015e | Translate UNSUPPORTED_QUERY_ALTERNATIVE to Vietnamese | assistant | F-001 | backend-agent | P2 | T-015c | BLOCKED | — | first attempt returned confused/hallucinated (claimed npm test:all doesn't exist, referenced an unrelated project) — orchestrator disbelieved the factual claims but the process point (contract embeds the literal English string) was valid and verified; redirected as T-015f+T-015g |
| T-035 | Clarify AC-2 wording to license the early-exit request flow | assistant | F-003 | spec-agent | P2 | T-033 | DONE | docs/specs/assistant/F-003-mobile-surface.md | AC-2 now licenses sequential-with-early-exit; the mic-denied/speech-unasked state named a legitimate resting state; spec-check exit 0 |
| T-043 | Invert or retire the now-vacuous PAINTED/StyleSheet drift detector | assistant | F-003 | qa-mobile-agent | P2 | T-040 | DONE | tests/assistant/mobile/F-003-mobile-surface.spec.ts, docs/qa/assistant/F-003/mobile/TC-032-touch-targets-hit-area.md, docs/qa/assistant/runs/2026-08-17-mobile-execute.md | retired as superseded, handoff PROVEN by mutation (literal reintroduced -> unit tier red); now parses design section Touch; permission-cta asserted absent so T-042 forces it; removed a self-written assertion that would have frozen an unstated rule |
| T-040 | styles.ts should import PAINTED instead of restating it | assistant | F-003 | mobile-agent | P2 | T-037 | DONE | src/assistant/mobile/model/touch.ts, src/assistant/mobile/components/styles.ts, src/assistant/mobile/__tests__/touch-keyboard-back.test.ts | paintedBox() single accessor, 5 boxes derived, zero literals left; retry corrected 96->80; kept 2 values explicit with stated reasons; mutation-proved |
| T-045 | Set PAINTED permission-cta to 112 | assistant | F-003 | mobile-agent | P3 | T-042 | DONE | src/assistant/mobile/model/touch.ts | 140 -> 112 with the diagnosis recorded; parsed-widths test changed from exact key set to superset so a new published row fails as "PAINTED has not adopted it" rather than "your list is stale"; 469/469 |
| T-046 | Assert cta=112 + the three stated floor invariants | assistant | F-003 | qa-mobile-agent | P3 | T-042 | DONE | tests/assistant/mobile/F-003-mobile-surface.spec.ts, docs/qa/assistant/F-003/mobile/TC-032-touch-targets-hit-area.md | asserted the shortest-label RULE not just the value; mutation-proved with 156 (multiple of 4, below longest label, still wrong = 140s exact shape); 124/124, test:all 469/469 |
| T-042 | Re-measure permission-cta floor + fix the rounding prose | assistant | F-003 | design-agent | P3 | T-041 | DONE | docs/design/_shared/components.md | cta floor = 112 from the shortest of 3 labels (PAINTED 140 over-stated ~26px); refused to tighten the other floors — slack absorbs Chromium-vs-RN text shaping; rule restated + general shortest-label clause |
| T-041 | Publish the 4 unsourced widths in components.md | assistant | F-003 | design-agent | P2 | T-037 | DONE | docs/design/_shared/components.md | 4 widths published in their own table (content floors, not touch minimums); refused to ratify retry=96, measured 81.9, published 80; task-row 320 re-based to narrowest supported device width |
| T-037 | Fix M1+M2: tautological AC-11 assertion, tested_by gap, unlinked touch-target copies | assistant | F-003 | qa-mobile-agent | P1 | T-036 | DONE | tests/assistant/mobile/F-003-mobile-surface.spec.ts, docs/qa/assistant/F-003/mobile/TC-032-touch-targets-hit-area.md, docs/qa/assistant/F-003/mobile/TC-036-system-back-never-destructive.md | found a 2nd tautology; real decision-table + 5 behavioural tests; mockup CSS parsed w/ non-vacuity guard, all 4 drift directions caught; 120/120, test:all 462/462 |
| T-056 | Scope the permission-copy parser to its own section (regression from T-052) | assistant | F-003 | mobile-agent | P1 | T-052 | DONE | src/assistant/mobile/__tests__/permissions.test.ts | root cause: SPK-REVERTED's escaped \| split into a 4th cell so it matched the permission table's shape; fixed by scoping to the heading's own level (future sections out of scope by construction, no exclusion list); 2-way mutation proof, cksum-verified restore per L-001; 469/469 |
| T-104 | Draw the iOS/Android variants of the app shell (carry the a11y id catalogues) | assistant | F-003 | design-agent | P2 | T-103 | DONE | docs/design/assistant/screens/app-shell-ios.html, docs/design/assistant/screens/app-shell-android.html | held back deliberately: they carry the identifier catalogues mobile tests parse, and drawing 3 variants of a proposal T-103 will change is waste |
| T-036 | Gate 3: product review for F-003 | assistant | F-003 | product-agent | P1 | T-032 | DONE | docs/reports/product-review-F-003-2026-08-17.md | APPROVED 0H/4M/5L; found a tautological assertion in QA automation (backIsBackgroundTransition(): true asserted toBe(true)) by mutation, orchestrator verified; answered all 3 gate questions |
| T-051 | Align both transcript ports to one interface-language source | assistant | F-001 | web-agent | P2 | T-067,T-068 | DONE | src/assistant/_shared/model/client-stores.ts, src/assistant/web/ports/web-speech-source.ts, src/assistant/mobile/ports/native/rn-transcript-source.ts | not started; the work moved into T-067 (web port) + T-068 (mobile port) because both touch the same files as the English copy change. Closes when they do — kept as its own row because AC-23 is satisfied by the PAIR reading one declared source, which neither task proves alone |
| T-038 | Record motion/haptics phase boundary + reduced-motion precondition | assistant | F-003 | design-agent | P2 | T-036 | DONE | docs/design/_shared/DESIGN.md, docs/design/_shared/tokens.json | motion stack declared-not-installed recorded as a phase boundary; reduced-motion + haptics made a precondition on the first mobile animation; DESIGN.md 46/100 lines |
| T-039 | Add Verification status block; leave AC-9/10/11/12 unticked pending device pass | assistant | F-003 | spec-agent | P1 | T-036 | DONE | docs/specs/assistant/F-003-mobile-surface.md | AC-1..8 ticked, AC-9..12 left unticked; Verification status placed right after the checkboxes; added beyond brief: a ticked box is not a device pass either (5 ticked ACs carry device residue); no counts embedded since numbers drift |
| T-020b | Decide android accessibility-identity attribute (resource-id vs contentDescription) | assistant | F-003 | architect-agent | P1 | T-020 | DONE | docs/specs/assistant/F-003-mobile-surface.md | resource-id (via testID) carries identity, contentDescription carries TalkBack announcement, non-conflation pinned; AC-12's two halves were mutually unsatisfiable as written |
| T-020c | Android mockup: move 22 ids to resource-id | assistant | F-003 | design-agent | P1 | T-020b | DONE | docs/design/assistant/screens/voice-assistant-view-android.html | 22 ids moved to resource-id, aria-label untouched (18 byte-identical), header note fixed; falsification-tested that the rename is what restores C14 detection |

| T-121b | Cross-subtree files from T-121, under an orchestrator grant | assistant | F-001 | orchestrator | P1 | T-121 | DONE | docs/specs/_shared/uc-coverage-map.md, tests/assistant/api/F-001-voice-assistant-view.spec.ts, docs/qa/assistant/F-001/api/TC-17-undo-delete-restores-all-fields.md | web-agent performed these under an explicit one-off grant in its briefing, because splitting the change would leave Today defined twice. It flagged the crossing itself. Recorded on the orchestrator's row rather than widening web-agent's writers permanently — a one-off grant has no representation in the map, and that is the gap, not the crossing |
| T-133 | components.md owes two rules the mockups now assert: Overdue heading colour, surface title names the collection | assistant | F-001 | design-agent | P2 | T-131 | DONE | docs/design/_shared/components.md | folded into T-135 |
| T-070a | QA api: English + retired undo phrase | assistant | F-001 | qa-api-agent | P1 | T-069,T-073 | DONE | tests/assistant/api/F-001-voice-assistant-view.spec.ts, docs/qa/assistant/F-001/api/utterance-intent-fixtures.json, docs/qa/assistant/F-001/api/TC-23-voice-undo-guard-no-task-no-ai.md, docs/qa/assistant/F-001/api/TC-24-voice-undo-no-applied-turn-refusal.md, docs/qa/assistant/runs/2026-08-17-api-english-resync.md | 47/47; saw that dropping a phrase from a closed list makes NO test fail, so it ADDED TC-23 step 7 asserting the retirement point-for-point (kind=turn not undo, AI counter +1, no task, undo window unspent) and mutation-proved it; refused `git checkout --` because T-073 edit was uncommitted — the exact trap qa-mobile fell into 20min later; resolved the L-004 twin against the CONTRACT, not either copy |
| T-071 | Recapture all 47 screenshots in English (web + both simulators) | assistant | F-001 | orchestrator | P2 | T-070a,T-070b,T-070c | DONE | output/screens-by-flow.html | 47 shots recaptured in English: 13 web from the running client, 34 from the installed apps on iPhone 16 Pro + Pixel 9 Pro; per-run user namespace so a previous run cannot feed the next one its data |
| T-076 | Restore lost catalogue work after a git-checkout revert (L-010) | assistant | F-001 | orchestrator | P1 | — | DONE | docs/design/_shared/components.md, docs/specs/_shared/LEARNINGS.md, docs/design/assistant/screens/voice-assistant-view-ios.html | qa-mobile ran `git checkout -- docs/design/_shared/components.md` to undo a mutation check; the file was TRACKED so the command SUCCEEDED and discarded 57 uncommitted lines — T-062's whole English rewrite AND T-052's ## Spoken frames (18 SPK rows AC-22 parses). 19 tests went red pointing at the parser, not the cause. Recovered components.md from a downstream agent's Read transcript (a LATER reader held the whole file; the writer's own transcript had only a truncated diff). A SECOND file was destroyed and I MISSED it — the iOS mockup, ~280 lines; qa-mobile-agent found and restored that one itself by re-running T-062's deterministic scripts against the HEAD blob, proving the replay faithful by reproducing the two mockups it never touched byte-for-byte. Both cksum-verified. L-010 written |
| T-130 | Creating a task while viewing Upcoming: what date, if any | assistant | F-001 | orchestrator | P1 | T-129 | DONE | docs/reports/owner-decision-2026-08-18-upcoming-create.md | OWNER DECIDED: save dateless, say where it went. Option 2 of design three, chosen rather than done silently. Component work is T-135 |
| T-135 | SaveNotice: say where a created task went when it leaves the surface | assistant | F-001 | design-agent | P1 | T-130 | DONE | docs/design/_shared/components.md, docs/design/assistant/screens/app-shell.html, docs/design/assistant/screens/app-shell-ios.html, docs/design/assistant/screens/app-shell-android.html | NOT a toast: on Upcoming the backdrop looks identical before and after, so a timed message re-creates the silence it exists to end. Persistent dismissible strip in the banner stack, no action (none can be built — no date UI exists), T-133 folded in. design-check 60/60 with a browser, 9 renders read by hand |
| T-136 | Two new SaveNotice ids: three pinned catalogue counts are red | assistant | F-001 | web-agent | P1 | T-135 | DONE | src/assistant/mobile/model/a11y.ts, src/assistant/mobile/model/touch.ts, src/assistant/mobile/__tests__/a11y.test.ts, src/assistant/web/__tests__/app.test.tsx | 743/743 back. Mobile has its own NOT_BUILT (SHELL_IDS_BLOCKED) the briefing did not name; an untouched assertion (47-11=36) held on its own, which is the proof the change is catalogue-only |

## 2026-W34 — closed leaf rows (archived 2026-08-18 at the DONE cap)

These two were the **only** DONE rows in the queue that no live row depends on. The other fifty cannot be archived without breaking C3, which resolves `Depends` against `TASKS.md` alone — see **T-149**.

| ID | Title | Module | Feature | Agent | Pri | Depends | Status | Artifacts | Outcome |
|----|-------|--------|---------|-------|-----|---------|--------|-----------|---------|
| T-144 | .claude/lib/tasks.js is CommonJS in an ESM project — the node task reader throws | assistant | — | orchestrator | P2 | — | DONE | .claude/lib/tasks.cjs | fixed upstream and delivered by upgrade: 4 files renamed .js to .cjs. Fixing the crash then exposed a SECOND bug it was hiding — the node reader matched T-\d+ so it dropped every lettered sub-task while the shell reader counted them. R9 17/17 |
| T-148 | Record the F-005 revision-2 log under the one-off cross-subtree grant | assistant | F-005 | orchestrator | P0 | T-143 | DONE | docs/reports/gate1-lenses/F-005-revision-2-log.md | Authored by spec-agent on T-143 because the T-143 briefing named this path; docs/reports/ belongs to the orchestrator (MANIFEST writers:). Recorded here rather than widening spec-agent permanently — the 'sanctioned once' mechanism MANIFEST already documents for T-121. The file stays where the round-2 lenses will look for it, beside the five round-1 returns it answers |

## 2026-W34 — Inbox-as-unfiled, and the message door's first half (archived 2026-08-19 at the DONE cap)

A **dependency-closed** cluster: nothing still in the live queue names any of these four, so C3 stays green. T-140 was closed against T-157's evidence rather than re-dispatched — it and T-157 amended the same clause of F-001 AC-31, and dispatching both would have put two agents in one AC.

| ID | Title | Module | Feature | Agent | Pri | Depends | Status | Artifacts | Outcome |
|----|-------|--------|---------|-------|-----|---------|--------|-----------|---------|
| T-137 | Inbox is UNFILED not undated: ADR-009 amendment 2, two axes | assistant | F-001 | architect-agent | P0 | — | DONE | docs/specs/_shared/adr/ADR-009-today-is-a-date.md, docs/specs/assistant/data-model.md, docs/specs/assistant/api-contracts.md | Amendment 1 table survives as the date axis; only its 3rd cell name was wrong. Refused both list_id options for a named isFiled predicate that must be answerable TRUE in a test. INV-INBOX-FILING with 3 carriers, not a comment. Measured Inbox 709 to 716 |
| T-138 | Design after two axes: Inbox regains day grouping, where the Inbox row sits, nested counts | assistant | F-001 | design-agent | P1 | T-137 | DONE | docs/design/_shared/components.md, docs/design/assistant/screens/app-shell.html, docs/design/assistant/screens/app-shell-ios.html, docs/design/assistant/screens/app-shell-android.html | menu splits views from containers, Inbox heads the filing group; nested counts shown as-is because nothing in the menu ever claimed to be a partition; LSM-CLEAR-AHEAD is DORMANT not dead — wakes with the first filed task. design-check 60/60, 13 renders read |
| T-139 | Implement Inbox-as-unfiled: inCollection, the isFiled seam, the disjointness suite | assistant | F-001 | web-agent | P1 | T-137,T-138 | DONE | src/assistant/_shared/model/tasks.ts, src/assistant/_shared/testing/fixtures.ts, src/assistant/web/components/, src/assistant/mobile/components/ | 771/771 (was 743), tsc clean, 6 mutations. isFiled reads list_id structurally — no field, no wire change; filedTask fixture makes it answerable TRUE so the invariant is proven not vacuous. COLLECTION_GROUPS is the single source of menu order. Mobile break has only a source scan — labelled as one |
| T-140 | Tapping a task in a message switches to the collection holding it | assistant | F-001 | spec-agent | P1 | T-157 | DONE | docs/specs/assistant/F-001-voice-assistant-view.md | Closed against T-157's evidence rather than re-dispatched — same clause of AC-31, and verified on disk: the amended bullet says activating the door 'switches to a collection that holds it' and then meets the postcondition unchanged. Provenance is docs/reports/owner-question-2026-08-18-assistant-created-tasks-are-invisible.md — an owner QUESTION doc, not a decision doc; the direction is the queue's record |
| T-156 | Port the declared-elements.sh SIGPIPE fix upstream to the template | assistant | — | orchestrator | P0 | T-154 | DONE | .claude/tools/spec-check/declared-elements.sh, .claude/eval/scenarios/tests/R13-declared-elements.sh | **Verified upstream 2026-08-21, read-only, in the template itself:** `templates/project-starter/.claude/tools/spec-check/declared-elements.sh:81` carries the `case` glob AND the L-016 comment, and `R13-declared-elements.sh` is beside it. The port arrived back here as commits f8023bf/0097bd1. **What closes this row is not the port — it is R13**: the scenario builds a fixture >100 KB, past the measured threshold (64 KB → 0, 72 KB → 141), so a future sync that reverts the line turns a scenario red instead of turning ten present fields into silent orphans. That is the difference between this row being done and being done again in a month: the defect is size-dependent, so every small spec would still have passed and told nobody. 17/17 scenarios green. **L-014's shape finally discharged — the remedy no longer depends on anyone remembering.** | grep -qF ...` under `set -uo pipefail`, and F-005 immediately reported **10 false orphans** — `title`, `note`, `priority`, `due_at` among them, every one of which a dozen ACs constrain. Re-applied locally the same hour and verified both directions (21/21 accounted, exit 0; a planted orphan still caught). **That re-application is not the fix — it is the third time the same patch has been written.** Until it is upstream, every `upgrade-project.sh` and every sync from `claude-agents-final` silently restores a check that fails hardest on the specs that are most complete, because the defect scales with haystack size. This is L-014's shape exactly: a remedy that depends on someone remembering. |
| T-172 | Two clocks in the test harness — fixtures read the wall clock while the harness pinned the seam | assistant | — | web-agent | P2 | T-163 | DONE | src/assistant/_shared/testing/fixtures.ts | Fixed inside T-163 and recorded because the DIAGNOSIS is the durable part: 771 tests were green only because the view also read the wall clock, agreeing with the fixture and disagreeing with the controller. Anchoring both to T0 turned 29 red at once — L-004 inside the instrument built to detect it. Durable half is L-023 |
| T-175 | `/account` was missing from the vite dev proxy, so the app crashed on load and the task list rendered empty | assistant | F-005 | web-agent | P2 | T-163 | DONE | src/assistant/web/vite.config.ts | Found by running the app: the browser's `GET /account` was answered by Vite with `index.html`, so `loadAccount` threw `Cannot read properties of null (reading 'timezone')` in `init()` and `refreshTasks()` never ran — three seeded tasks rendered as 'No tasks yet'. Fixed directly by the orchestrator (a one-line dev-server config entry, ops-class per ORCHESTRATION) with a comment naming the cause. **No unit test covers it because they use a fake server rather than this proxy** — web-agent owns whether a smoke case is worth adding |
| T-030 | Move the 7 permission-copy strings into components.md | assistant | F-003 | design-agent | P2 | T-027 | DONE | docs/design/_shared/components.md, docs/design/assistant/screens/voice-assistant-view-ios.html, docs/design/assistant/screens/voice-assistant-view-android.html | 7-row catalogue + 1 adjacent AC-4 string; found 2 real mockup drifts incl. an iOS message that violated AC-2 by not naming which permission; design-check 18/0 held |
| T-031 | Re-point permissions.ts to cite components.md | assistant | F-003 | mobile-agent | P2 | T-030 | DONE | src/assistant/mobile/model/permissions.ts, src/assistant/mobile/__tests__/permissions.test.ts | CATALOGUE keyed by design row IDs, template removed; tests PARSE components.md so a hand-copy cannot pass; 448/448, mutation-proved both ways; found an 8th unenumerated iOS tuple |
| T-033 | Decide + publish the 8th iOS permission row (mic denied + speech never asked) | assistant | F-003 | design-agent | P1 | T-031 | DONE | docs/design/_shared/components.md | IOS-MIC-UNASKED published; ruled row-not-spec-clause (forcing both dialogs would spend iOS last prompt on an inert grant); CTA=Settings, AND-DENIED keeps its exclusive label; recorded why the 4 iOS rows converge for two different reasons |
| T-034 | Implement the 8th row + CTA rule | assistant | F-003 | mobile-agent | P1 | T-033 | DONE | src/assistant/mobile/model/permissions.ts, src/assistant/mobile/controller.ts, src/assistant/mobile/__tests__/permissions.test.ts | IOS-MIC-UNASKED implemented, ctaTarget decoupled from canRequest, selection keys on full tuple; found+fixed a live bug (nothing-denied rendered IOS-SPEECH, claiming a never-asked grant was off); 453/453, 3 mutations proved |
| T-032 | Gate: F-003 structural re-verify | assistant | F-003 | reviewer-agent | P2 | T-028,T-029,T-030,T-034 | DONE | docs/reports/review-F-003-2026-08-17-v2.md, docs/specs/_shared/LEARNINGS.md | STRUCTURAL-PASS 14/14; falsified the parse-catalogue claim (4 tests red on a 1-token change); self-corrected 2 pass-1 claims; C7 conditional per ADR-007; L-008 appended |
| T-044 | Spec F-002 talk-back (the binding commitment from Gate 1 D1) | assistant | F-002 | spec-agent | P1 | T-032 | DONE | docs/specs/assistant/F-002-talk-back.md, docs/specs/assistant/index.md | 164 lines, 17 ACs, zero api surface; speaking is a message property not a 5th state; web has no screen-reader detection so talk-back is off-by-default there; spec-check exit 0 |
| T-049 | Spec revision from F-002 Gate 1 (C1-C12) | assistant | F-002 | spec-agent | P1 | T-047 | DONE | docs/specs/assistant/F-002-talk-back.md, docs/specs/assistant/index.md | rev2: 224 lines, 22 ACs, all 12 clusters + 16 MED/LOW; C4 fixed with a mandatory positive assertion; found live locale drift; F-004 framing corrected to reserved-not-committed |
| T-050 | Gate 1 re-review round 2 for F-002 | assistant | F-002 | orchestrator | P1 | T-049 | DONE | docs/reports/gate1-review-F-002.md, docs/reports/gate1-lenses/F-002/ | 7/7 lenses: 25H/14M/5L into 10 clusters; 31 of 38 r1 findings closed; ALL new HIGHs sit in the 4 ACs rev2 newly wrote; recovered 5 r1 findings lost in consolidation and persisted all 14 lens returns verbatim; 3 items escalated to human; round cap reached |
| T-026 | Mobile onForeground must also sync local creates | assistant | F-003 | mobile-agent | P2 | — | DONE | src/assistant/mobile/controller.ts, src/assistant/mobile/__tests__/lifecycle.test.ts, src/assistant/mobile/__tests__/_helpers.ts | syncLocalTasks inside the same gateForeground installer, creates before queued turn; 2 regression tests modelling silent reconnect (no onChange callback), mutation-proved |
| T-024 | Fix BUG-002: cold open must read session before accepting input | assistant | F-003 | mobile-agent | P1 | T-021 | DONE | src/assistant/mobile/controller.ts, src/assistant/mobile/__tests__/lifecycle.test.ts | root cause: gate duplicated in 2 entry points, drifted; consolidated into one installer; QA TC-030 green untouched; 5 regression tests mutation-proved |
| T-018 | Spec F-003 mobile surface (carries AC-26/27) | assistant | F-003 | spec-agent | P1 | — | DONE | docs/specs/assistant/F-003-mobile-surface.md, docs/specs/assistant/index.md | 137 lines, 12 ACs; parity table classifies all 29 F-001 ACs (21 as-is + 6 clause-added + 2 moved); ios/android split explicit; spec-check exit 0 |
| T-019 | Mobile implementation (React Native) | assistant | F-003 | mobile-agent | P2 | T-018 | DONE | src/assistant/mobile/, src/assistant/_shared/ | shared extraction (mobile controller EXTENDS shared, no 2nd reducer); 92 mobile tests; web 98/98 + api 73/73 + e2e 36/36 unbroken; found BUG-001 |
| T-025 | Invert QA KNOWN-OPEN pin now that BUG-001 is fixed | assistant | F-003 | qa-mobile-agent | P1 | — | DONE | tests/assistant/mobile/F-003-mobile-surface.spec.ts, docs/qa/assistant/F-003/mobile/TC-040-foreground-is-a-reconnect-for-offline-creates.md, docs/qa/assistant/F-003/mobile/index.md, docs/qa/assistant/runs/2026-08-17-mobile-execute.md | pin -> 5 contract-derived assertions; added TC-040 after finding every reconnect assertion passed regardless of onForeground; 111/111, 3 mutation checks |
| T-027 | Fix C9 (client.permission_state in data-model) + add announce port to platform doc | assistant | F-003 | architect-agent | P1 | T-022 | DONE | docs/specs/assistant/data-model.md, docs/specs/_shared/platform/mobile.md | permission_state row w/ both platform asymmetries; Announcer port + build-dont-author convention; routed 2 follow-ups |
| T-028 | ADR: accept the metro/image-size advisory with review conditions (C7, owner decision) | assistant | F-003 | architect-agent | P1 | T-022 | DONE | docs/specs/_shared/adr/ADR-007-accept-metro-image-size-advisory.md | 66 lines (6 over house limit, accepted by orchestrator rather than dropping a required pin); found the only offered fix is a DOWNGRADE to RN 0.72.17; C7 conditional pass bounded to the exact current audit shape |
| T-029 | Fix F-003 Data lead sentence (3rd client row is new, not pre-existing) | assistant | F-003 | spec-agent | P2 | T-027 | DONE | docs/specs/assistant/F-003-mobile-surface.md | lead sentence corrected; agent verified data-model.md:133 really carries the row before asserting it; spec-check exit 0 |
| T-052 | Closing spec revision for F-002 (D1-D10 + recovered r1) | assistant | F-002 | spec-agent | P1 | T-050 | DONE | docs/specs/assistant/F-002-talk-back.md, docs/design/_shared/components.md, docs/specs/assistant/data-model.md, docs/specs/assistant/index.md | rev3: 379 lines, AC-24 new, D1-D10 all closed; decision_log gained a real entry shape + `failed`/`waiting`; 3 owner decisions written in dated; D5 dissolved not exempted; found OQ6 (iOS cannot honour the DND half) and refused to decide it; spec-check 16/16 |
| T-057 | Simulator shell + first device run of the mobile client | assistant | F-003 | orchestrator | P1 | — | DONE | .mobile-app/, docs/qa/_shared/bugs/BUG-003-crypto-randomuuid-missing-on-device.md, docs/qa/_shared/bugs/BUG-004-conversation-never-scrolls-to-newest-message.md | Expo shell built and installed on iOS Simulator + Android emulator; 34 real device screenshots; found BUG-003 (crypto.randomUUID absent in Hermes, blocks every turn) and BUG-004 (no scroll to newest message, both clients) |
| T-058 | Fix BUG-003: uuid default must work on Hermes | assistant | F-003 | mobile-agent | P1 | T-057 | DONE | src/assistant/_shared/controller.ts, src/assistant/web/__tests__/controller.test.ts | defaultUuid() feature-detects then falls back to RFC-4122 v4; no dependency added; regression test constructs the controller with NO uuid dep after `delete globalThis.crypto` (delete not undefined — reproduces Hermes' ReferenceError); proved failing against the old default first; 469->474; re-verified on device with the harness workaround removed |
| T-062 | Owner decision: English-first — copy catalogue + 3 mockups | assistant | F-001 | design-agent | P1 | — | DONE | docs/design/_shared/components.md, docs/design/assistant/screens/voice-assistant-view.html, docs/design/assistant/screens/voice-assistant-view-ios.html, docs/design/assistant/screens/voice-assistant-view-android.html | 28 catalogue lines + ~120 strings across 3 mockups; ZERO row ids moved (proved by the shape of the red: all 8 found by id, failing only on content); re-measured every touch floor with real webfonts — CTA went UP 112->136 because 'Open Settings' is wider; re-invented the no-match mishearing (a VN homophone joke does not translate); wrote the house-words rule §Buttons never had |
| T-063 | Owner decision: English-first — ADR + AC-23/data-model language constant | assistant | F-002 | architect-agent | P1 | — | DONE | docs/specs/_shared/adr/ADR-008-english-first-this-phase.md, docs/specs/assistant/F-002-talk-back.md, docs/specs/assistant/data-model.md | ADR-008 supersedes the 2026-08-16 decision for the declared language ONLY, with review conditions that expire the no-i18n call; AC-23's six substantive clauses verified unchanged, only the value moved; caught that fixing the mobile literal alone would still violate AC-23 (the defect is the second source); OQ2 closed not deleted, re-opens with the 2nd language |
| T-064 | Drop the `hoàn tác` undo phrase: F-001 AC-5 + api-contracts.md:84 + ADR-006 (specs half; api-contracts:129 + data-model:95 alternative too) | assistant | F-001 | spec-agent | P1 | T-063 | DONE | docs/specs/assistant/F-001-voice-assistant-view.md, docs/specs/assistant/api-contracts.md, docs/specs/assistant/data-model.md, docs/specs/_shared/adr/ADR-006-voice-undo.md | all 4 sites agree; ADR-006 AMENDED not rewritten — and it noticed the ADR authorised its own amendment ("growing the list is a docs/design/product decision"); kept the rejected-option Vietnamese example as decision-time reasoning; contract now matches turns.ts byte-for-byte |
| T-073 | Drop `hoàn tác` from normalize.ts UNDO_PHRASES + retire the vacuous undo.test case | assistant | F-001 | backend-agent | P1 | T-064 | DONE | src/assistant/api/engine/normalize.ts, src/assistant/api/__tests__/undo.test.ts | the code half of AC-5's vocabulary change; my T-069 briefing wrongly described this file as diacritic handling and told the agent not to touch it |
| T-067 | English copy: web client + shared model (+ web half of T-051/AC-23) | assistant | F-001 | web-agent | P1 | T-062 | DONE | src/assistant/_shared/model/messages.ts, src/assistant/_shared/model/format.ts, src/assistant/_shared/model/client-stores.ts, src/assistant/_shared/controller.ts, src/assistant/web/index.html, src/assistant/web/components/ConversationPane.tsx, src/assistant/web/components/VoiceSurface.tsx, src/assistant/web/components/Chrome.tsx, src/assistant/web/components/Composer.tsx, src/assistant/web/ports/web-speech-source.ts, src/assistant/web/__tests__/interface-language.test.ts | 114/114; AC-23 solved by REMOVING the second source not correcting a literal — INTERFACE_LANGUAGE declared once in client-stores.ts; 3 tests assert the MECHANISM (port read as text contains no navigator.language and no BCP-47 literal) + parse data-model.md at run time, all mutation-proved; reported ~15 message families the catalogue has no English row for rather than silently inventing — note: TaskListPane.tsx was in this task's output and was later REPLACED by TasksSurface.tsx in T-107; path dropped from Files so C-checks verify what exists, the record of it is here |
| T-068 | English copy: mobile client + re-measured touch floors (+ mobile half of T-051/AC-23) | assistant | F-003 | mobile-agent | P1 | T-062 | DONE | src/assistant/mobile/model/permissions.ts, src/assistant/mobile/model/touch.ts, src/assistant/mobile/model/announce.ts, src/assistant/mobile/model/theme.ts, src/assistant/mobile/components/ConversationList.tsx, src/assistant/mobile/components/TaskList.tsx, src/assistant/mobile/components/Composer.tsx, src/assistant/mobile/components/VoiceSurface.tsx, src/assistant/mobile/components/Chrome.tsx, src/assistant/mobile/ports/native/rn-transcript-source.ts, src/assistant/mobile/boot.ts, src/assistant/mobile/__tests__/interface-language.test.ts, src/assistant/mobile/__tests__/permissions.test.ts, src/assistant/mobile/__tests__/touch-keyboard-back.test.ts, src/assistant/mobile/__tests__/a11y.test.ts, src/assistant/mobile/__tests__/parity.test.ts | 124/124; extracted the 8 permission rows programmatically instead of retyping; an escaped apostrophe passed every value assertion but failed the L-008 read-as-text guard, so it switched quoting — that guard did its job; deleted BootOptions.locale because a shell-supplied locale is the same second source moved one layer out; mutation-proved AC-23. MISREPORTED the mockups as still-Vietnamese (only a person's name remains) and did not flag 21 stale server-shaped fixtures |
| T-069 | English copy: api layer (confirm chips, fixture table) | assistant | F-001 | backend-agent | P1 | T-062 | DONE | src/assistant/api/engine/turns.ts, src/assistant/api/ports/fixture-table.ts, src/assistant/api/__tests__/questions.test.ts, src/assistant/api/__tests__/turn.test.ts, src/assistant/api/__tests__/undo.test.ts | 73/73; transcribed chip labels from design rather than composing them (M5's real fix); found a 3rd Vietnamese fixture row I missed ('ừ') and kept the canonical+colloquial shape instead of collapsing branches; CORRECTED MY BRIEFING — normalize.ts's Vietnamese line is the live UNDO_PHRASES list, not diacritic handling |
| T-070b | QA web+e2e: English copy constants, fixtures, TC docs | assistant | F-001 | qa-web-agent | P1 | T-067 | DONE | tests/assistant/e2e/F-001-voice-assistant-view.spec.ts, tests/assistant/pages/AssistantPage.ts, tests/harness/qa-test-server.ts, docs/qa/_shared/fixtures/web/assistant-web-fixtures.json, docs/qa/assistant/F-001/web/index.md, docs/qa/assistant/runs/2026-08-17-web-execute-v3.md | e2e 10/36 -> 36/36, confirmed x3 no flake; 22 TC docs; found that a surviving harness silently feeds run N the previous run data because freshUser restarts its counter at 1 (presented as 27 ordinary product failures); found that fixing the copy made a pre-existing flake REACHABLE — a red assertion had been masking it; removed one vacuously-true assertion on purpose rather than leaving it reading as coverage |
| T-070c | QA mobile: English + 112->136 floor + stale chip fixtures | assistant | F-003 | qa-mobile-agent | P1 | T-068 | DONE | tests/assistant/mobile/F-003-mobile-surface.spec.ts, docs/qa/assistant/F-003/mobile/TC-033-undo-is-one-gesture-by-touch.md, docs/qa/assistant/F-003/mobile/TC-004-parity-undo-shapes-refusal-idempotent.md, docs/qa/assistant/F-003/mobile/TC-005-parity-undo-skips-modified-nothing-reverted.md, docs/qa/assistant/F-003/mobile/TC-032-touch-targets-hit-area.md | 4 known red (TC-14 alternative, the 112->136 floor, the CTA-label-vs-mockup case, TC-037 announcement). Separately: 21 places across mobile unit tests + qa fabricate the server's confirm chips as 'Xóa 3 việc'/'Giữ lại' — strings the server can no longer emit. They pass because the fixture builds the turn itself, so they are green while asserting an impossible shape (L-004) |
| T-082 | Build the new-message affordance (web + shared) | assistant | F-001 | web-agent | P1 | T-080,T-081 | DONE | src/assistant/_shared/model/follow.ts, src/assistant/web/follow.ts, src/assistant/web/components/NewMessageAffordance.tsx, src/assistant/web/App.tsx, src/assistant/web/components/ConversationPane.tsx, src/assistant/web/components/icons.tsx, src/assistant/web/styles.css, src/assistant/web/__tests__/follow.test.ts, src/assistant/web/__tests__/app.test.tsx, src/assistant/web/__tests__/_helpers.ts | 138/138; answered the tier question instead of writing a test that cannot fail — jsdom has no layout, so it built a scrollHeight getter DERIVED FROM THE REAL MESSAGE NODES, and named that one property as the only reason the position clauses are falsifiable here; listed what only the browser tier can own; 7 mutants 7 killed incl. editing the published label upstream; found mobile had duplicated the shared follow logic under a DIFFERENT constant name |
| T-083 | Build the new-message affordance (mobile) | assistant | F-001 | mobile-agent | P1 | T-080,T-081 | DONE | src/assistant/mobile/model/follow.ts, src/assistant/mobile/components/useNewMessageFollow.ts, src/assistant/mobile/components/NewMessageAffordance.tsx, src/assistant/mobile/model/a11y.ts, src/assistant/mobile/model/announce.ts, src/assistant/mobile/ports/app-lifecycle.ts, src/assistant/mobile/boot.ts, src/assistant/mobile/index.ts, src/assistant/mobile/controller.ts | 161/161; merged the duplication and added a test asserting mobile re-declares none of it — that assertion IS the report that was missing while the two constants had different names; comparing before deleting found a REAL divergence not just the number: web distinguishes pure-append from history-replace, mobile did not, and AC-28 clean start is the reachable case (a boundary message contains no user turn, so the ownTurn shortcut misses it and the view would hold on content just discarded) |
| T-090 | Mobile is UNVERIFIED for the BUG-006 race — model tier cannot see it, needs a device pass | assistant | F-001 | mobile-agent | P1 | T-089 | DONE | src/assistant/mobile/model/follow.ts, src/assistant/mobile/components/useNewMessageFollow.ts | 599/599; red-first with the bug report own numbers (expected 623 to be 744 — the identical 121-unit shortfall); put the flight bookkeeping in the MODEL not the hook because a flag kept inside the hook is uncheckable until a device shows up, which is how mobile stayed green while web browser tier saw the defect; rejected onMomentumScrollBegin with a reason (RN emits it around PROGRAMMATIC scrolls too, so releasing on it would lower the flag on the very scroll that raised it and the fix would read as present while doing nothing); 8 mutations, 2 survived and both were real test defects |
| T-095 | Import todo-ai requirements into docs/specs/_source (verbatim, read-only) | assistant | — | orchestrator | P1 | — | DONE | docs/specs/_source/README.md, docs/specs/_source/todo-ai/, MANIFEST.md | 13 docs / ~3.9k lines; UC-20, UC-52, ADR-7, ADR-11 were cited by this repo since F-001 while living only in the other repository — every one of those citations was unresolvable from inside. Kept Vietnamese and read-only for the same reason docs/reports/ are: they are a record, and editing here would be L-004 |
| T-096 | Map the 54 UCs to this repo's features; say which are built, changed, dropped, missing | assistant | — | architect-agent | P1 | T-095 | DONE | docs/specs/_shared/uc-coverage-map.md | **Closed on evidence 2026-08-21, not on a return — the row sat IN_PROGRESS with nothing running.** `uc-coverage-map.md` is on disk at **34,934 bytes**. The dispatch's `---METRICS---` block is lost with whatever session ran it, so it reads as `status: unknown` on the dashboard; the work is real.  the F-docs cite UCs but nothing lists which UC has an F-doc. UC-31..UC-39 are the manual todo operations the owner just found missing |
| T-099 | A masked mutation in web AC-30 guard — same hole mobile found in its own | assistant | F-001 | web-agent | P1 | T-090 | DONE | src/assistant/web/__tests__/controller.test.ts | **Closed on evidence 2026-08-21, and its real output is a LEARNING rather than a patch.** **L-012 is the ORCHESTRATOR's artifact, not this agent's** — web-agent cannot write `{specs}/`, and the entry itself says it was *"recorded by the orchestrator because no reviewer was in the loop; the finding is theirs"*. It credits T-099 with confirming and sharpening mobile's finding, and `controller.test.ts:568` carries the mutation note the row asked for.  — |
| T-080 | AC for following new messages (F-001) | assistant | F-001 | spec-agent | P1 | — | DONE | docs/specs/assistant/F-001-voice-assistant-view.md | AC-30, 8 clauses; "at the bottom" defined by formula and sampled BEFORE the append (a post-append sample reports every user as not-at-bottom and silently disables the feature); singularity written as a count; rewrote clause (g) from a 3-item LIST into a quantifier because a list with a guard on two of three is the shape that produced BUG-001 and BUG-002; routed OQ8 to the owner instead of inventing a carve-out, then closed it; anchored the submit scroll to the APPEND not the gesture |
| T-081 | Design the new-message affordance + render into 3 mockups | assistant | F-001 | design-agent | P1 | — | DONE | docs/design/_shared/components.md, docs/design/assistant/screens/voice-assistant-view.html, docs/design/assistant/screens/voice-assistant-view-ios.html, docs/design/assistant/screens/voice-assistant-view-android.html | affordance NAMES ITS NEWEST REASON — a neutral count normally, and when a question is pending below the fold it quotes that question head in the existing `question` amber, so words AND accent both change and colour is never the sole carrier; zero-height dock so it never reflows the pane it protects; 2-line clamp after a 375px render truncated to "Waiting for your answer - Delete ..."; declined a Touch row that would be a 4th avoidable red |
| T-193 | Design audit: the elements are arranged badly, and only one feature is drawn at all | assistant | — | design-agent | P1 | — | DONE | docs/reports/design-audit-2026-08-21.md | **7 findings, every number a `getBoundingClientRect()` reading at 6 widths across 5 surfaces — it drove the app rather than reading the catalogue.** `design-check` ran first and returned **60/60 clean**, and **nothing in the report is something it could have caught: it tests horizontal OVERFLOW only, never underflow.** **Coverage question answered NO — 5 of the 7 are on the ONE feature that IS drawn.** Root cause found and verified by the orchestrator: **nothing is measured above 1280**, `tokens.json`'s widest declared breakpoint, **and the defect grows 1:1 past 1140px** — 140px of dead gutter at 1280 reads as padding, **780px at 1920 is 52% of the pane.** It also **rendered the mockups side by side at the same widths**, which split the findings cleanly into *design defect* (reproduces in `app-shell.html`) and *implementation drift* (mockup and catalogue both specify the right thing). |
| T-226 | Talk shows which task is open, so "it" means that one | assistant | F-005 | spec-agent | P1 | — | DONE | docs/specs/assistant/F-005-task-detail.md, docs/specs/assistant/api-contracts.md, docs/specs/assistant/index.md | **Owner request.** While a task detail is open, the conversation carries a visible reference to it, so *"move it to Friday"* resolves to that task — **and the assistant stays able to act on any OTHER task meanwhile.** The open task is a **hint for reference resolution, not a scope filter**; that half is the one most likely to be lost in implementation. **Measured gap: `POST /assistant/turn` carries no client UI state** (`session_id, client_turn_id, transcript, source, answer_to_turn_id, timezone`), so with a detail open *"move it"* is interpreted exactly as with nothing open and `F-001 AC-13` fires a clarify question listing candidates — the wrong answer at the moment the user is staring at one task. **Already specified, not to be redone:** `F-005 AC-45` (detail occupies the list's column at split+), `AC-48`/`F-001 AC-31` (activating a named task replaces the open detail), `F-001 AC-24` (closing is always available). **Needs care:** the new field is untrusted (ownership under ADR-005, plus an id whose task was deleted in flight), and **dedupe must re-serve the recorded outcome rather than re-resolve against whatever is open now** — the same sentence now means different things depending on screen state, which is new for this contract. **DONE — AC-49..AC-52, spec-check 22/22.** `open_task_id` goes in the request body beside `answer_to_turn_id` (same kind of field: per-turn context affecting this turn's processing), **not** as a second channel like `timezone`. An id that is deleted, absent or not the caller's is **silently treated as absent — not a 404** — because failing the turn would make an invisible race break a sentence the user did say. `open_task_id` joins `session_id` and `timezone` in the dedupe exclusion list. AC-50 is deliberately its own AC: **the natural reading of "bound to a task" is a restriction, and an implementer who reads it that way silently breaks the majority of turns.** |
| T-229 | Record in the specs the decisions that live only in design | assistant | — | spec-agent | P1 | — | DONE | docs/specs/assistant/F-001-voice-assistant-view.md, docs/specs/assistant/F-003-mobile-surface.md, docs/specs/assistant/data-model.md, docs/specs/assistant/index.md | **The owner asked whether their decisions had reached the specs; audited, most had not** — they were in design files and the task queue, and **QA writes tests from the spec, never from design or code**, so a behaviour with no AC is untested and left to whoever implements it. Five, all decided: **(1)** swipe-to-delete on mobile — the AC needed is the accessibility consequence, since neither VoiceOver nor TalkBack performs a custom swipe and a swipe-only delete does not exist for those users; **(2)** tap title = edit in place, tap row = open detail, checkbox = complete only; **(3)** crossing into the wide layout must CLOSE the Lists drawer — `hide-wide` hides the trigger and nothing clears the state, so both menus render with the rail unreachable behind the drawer's scrim; **(4)** `blocked` considered and rejected — record the reasoning, including that *waiting on someone* was the one candidate with real single-user value and was still declined; **(5)** no visible add affordance above the fold at split+, **accepted by the owner** over a shortcut or moving the row to the top, because speaking is the primary way in — recorded as a chosen consequence so a later reviewer does not file it as a bug. Must NOT touch the filing-axis table or add a list entity — T-228 is in that file concurrently. **DONE — F-001 AC-33..AC-36 plus F-003 AC-13, spec-check 13/13 and 4/4, nothing renumbered.** The `blocked` entry records **what was considered, not only the verdict**: *waiting on someone* is named as the one candidate with real single-user value and still declined. **Agent decided three things worth knowing:** AC-36 is tagged `(web, mobile)` though it is trivially true below split, so **both QA tiers discover the decision and nobody later wonders whether the phone has the same gap**; AC-35 and AC-36 are listed web-only in F-003's parity table rather than in the hold-identically set; and the `blocked` rejection went into F-001's Out of Scope rather than F-005's, because status is a foundational model concern and F-001 declares the baseline. **Owed if implementation has shipped:** neither the web nor the mobile suite asserts the no-gesture delete path, so AC-33 / F-003 AC-13 need a re-review against shipped code. |
| T-149 | The DONE cap and C3 are mutually unsatisfiable — validate-state.sh resolves Depends against TASKS.md only | assistant | — | orchestrator | P2 | — | DONE | .claude/hooks/validate-state.sh, .claude/eval/scenarios/tests/R9-state-contract.sh, .claude/state/TASKS-archive.md, MANIFEST.md | **The deadlock this row predicted arrived on 2026-08-21 and forced the fix: 61 DONE rows, cap 60, and ZERO archivable** — every DONE row was named, directly or transitively, by a row that stays, so the cap was satisfiable only by raising it, which MANIFEST forbids in writing. C3 now builds `ALL_IDS` from TASKS.md **and** TASKS-archive.md. **Verified in both directions, because a resolver that accepts everything is not a resolver:** a live row naming an archived id passes, a lettered archived id (`T-401b`) passes, and an id in neither file still FAILS. Then proven by reverting the fix and watching R9 go 16/17 with two named assertions red. Ported upstream the same hour. **Cap reverted 60 → 50** as the raise-note promised. **Two things found on the way, neither of them this task:** three template SAMPLE rows (`module: auth`) sat in the archive under ids T-002..T-004 that real rows already used — removed, and C4 widened to span both files, since archive-aware resolution needs archive-aware uniqueness or `Depends: T-002` resolves to two different rows. And **R9's own broken-queue fixture never copied `lib/tasks.sh`, so the validator died at its `source` line and exited non-zero because it could not RUN** — the assertion *"rejects a knowingly broken queue"* had been passing on the crash, not on the finding. L-012 inside the scenario that guards the validator. Fixed in both fixtures. |
| T-106 | What does mobile land on — Talk or Tasks? Design proposes, owner decides | assistant | F-001 | design-agent | P1 | T-105 | DONE | docs/design/_shared/information-architecture.md | **Closed on evidence 2026-08-21.** The IA is marked *"revised T-105/T-106"* at lines 3, 18 and 274, and §274 carries the answer this row asked for. **The owner half is what the proposal status records; the design work is done.**  not asked and not answered by the desktop decision |
| T-224 | Draw the picked phone layout: no time column, inline add row, floating mic | assistant | — | design-agent | P0 | T-221 | DONE | docs/design/assistant/screens/ (3 shells + index.html), docs/design/_shared/DESIGN.md, docs/design/_shared/tokens.json, docs/design/_shared/specimen.html | **Owner picked Option C** — *"nhìn gọn hơn, minimal hơn"*. No left time rail on phone; checkbox and title flush left; due date right; "Add a task" row at the end of the list; floating mic bottom right; swipe-left to delete; new task found by scroll-to-new + NEW tag. **Owner also asked whether iOS can have a similar floating button. It can, and `DESIGN.md:86` does not forbid it:** that rule is scoped to *primary create*, and in C the primary create is the inline row while the floating button is voice. Rule stands unchanged; only the shape differs (iOS takes the 44pt floor and a material/solid ground, not Material elevation — Things 3 and Apple Notes both ship one). **Three things genuinely open:** the phone top bar loses both its buttons and must be redrawn rather than left with holes; `DESIGN.md:88` (*row delete always visible on touch*) must be revised for the swipe, with a screen-reader path on BOTH platforms since neither VoiceOver nor TalkBack performs custom swipes; and the floating mic covers the end of a full list, so the full-list case must be drawn. **Rail stays at tablet/desktop, so the row has two layouts across breakpoints — agent states whether that is right rather than letting it pass.** Also folds in T-223: last pass measured the checkbox against the web floor of 40 only, never against iOS 44 or Android 48. **(h) Owner: the web panels look unrounded — "tôi mới thấy 2 góc được bo tròn thôi". They are counting correctly, and it is a defect from T-220.** All three columns DO carry `border-radius:r-md` at split+, but **no panel has `overflow:hidden`, and each panel's own first or last child is `bg.base` and square**: `.topbar` is `col-main`'s first child and paints over its top two corners; `.composer` is `col-panel`'s last child and paints over its bottom two. So each panel shows exactly the two corners nothing covers. **The radius is real; the clipping is missing.** Same structure in all three shells. **`overflow:hidden` is the trap** — focus rings use `outline` + `outline-offset` and therefore paint outside the element, so clipping the panel hides the ring on any control flush to its edge, which is a worse defect and one the a11y probe would not catch because the ring is still in the CSS. Matching corners on the children is the safe form. **(i) One checkbox shape everywhere — rounded square. Owner instruction, reason given: it should look pressable.** Three shapes exist today, not two: web `r-xs` 4, Android `r-xs` 4 with a 2px stroke, and **`.plat-ios .cbx` a full circle** (line 544, dropped). **The iOS circle is not an iOS convention being overruled, it is Apple Reminders being copied** — Things 3, the comparable `DESIGN.md:41` already cites, uses a rounded square on iOS, so the change costs no platform correctness. Android's 2px stroke may stay; that is M3 weight, not shape. **The judgement is the radius, not the find-and-replace:** `xs` is 4 on a 20px box and barely reads as rounded, which does not deliver the stated reason — agent picks at 1x render, and `radius.assign` names the checkbox under `xs` by name so any change must be reflected there. Folds in T-223 again since it is the same rule. **(j) Web adopts the same pattern as phone — owner: inline add row, Talk as a floating button.** Header becomes `hamburger · title` left and **`Search · ⋯`** right at every width, on every platform. **One conflict the agent must resolve, not draw around: at split+ the Talk panel is ALREADY a visible column** (`.col-panel`, composer and mic inside it), so a floating Talk button there points at something on screen. FAB belongs where Talk is not visible — phone, and a browser below 1024; at split+ the column IS the affordance. The width where one becomes the other must read as a feature changing shape, not a control vanishing. **Behind `⋯`:** Search (also its own icon — the one action worse by voice, since finding is typing and watching the list narrow), Sort, Hide completed (a persisting toggle, a habit not a query), Select tasks. **Filter deliberately absent:** the sentence the app already answers, plus the sidebar collections, already do it. **Search needs drawn states** — field, mid-query, no result — or it is another control with nothing behind it. **(k) `specimen.html` is a commit behind and the owner caught it.** Written last by `1e35182`, while `tokens.json` moved on to `7ef7800`. **`motion.mapping` appears 0 times and `.cbx` still carries `border-radius:50%`** — the specimen currently shows the circular checkbox that is being removed everywhere else, so it contradicts the system it documents. Updated inside this pass rather than after, since the checkbox shape and panel corners are changing now. Needs: the panels-on-canvas plate (the pattern is load-bearing and has no plate), the single checkbox shape, the motion mapping (as a table if animating on load would break *nothing moves without the user acting* — agent says which), and the `listColor` palette. **Also swept the other direction — anything the specimen asserts that the tokens no longer say; the stale circle is one instance and the class is the point.** **RETURNED PARTIAL and re-dispatched.** Done and verified: checkbox 3 rules -> 2, **zero circles in all ten files**, `r-sm` 8 on a 20px box; hit areas measured for the first time at **44 iOS / 48 Android / 40 web** (T-223 closed); panel children given matching corners **without `overflow:hidden`** so focus rings survive; specimen 3 contradictions fixed + 3 plates added (its 10 remaining `50%` are all legitimate — FAB, switch knob, mic, 7 swatches); `DESIGN.md:88` revised, reason recorded: the always-visible delete cost 44pt of every row for ~1% of taps. design-check 137/0/2. **Still open: both self-checks skipped with a FALSE reason** — the return said playwright-cli was not on PATH; it is at `/usr/local/bin/playwright-cli`, node resolves `playwright`, and the previous pass screenshotted these same files at 08:21. **Second consecutive pass with no visual review, and the owner had asked for it by name.** Also unrebuilt: `index.html`. **Round 2 returned DONE; re-dispatched again.** Browser checks did run this time and earned their cost: **two defects only a render finds** — due times doubled at desktop (a specificity bug: the `display:none` default sat after the container query in source order) and the red swipe panel showed at 1440/1920. Both fixed, `swipe-action{display:none}` verified at line 1108. **But the checkbox sweep is 4 of 10 files, reported as 10.** `task-detail` x3 and `voice-assistant-view` x3 still carry `.plat-ios .cbx{border-radius:r-circle}` and the old `r-xs`. **Second false completeness claim in two returns** (first: playwright not on PATH). Both are assertions written in the grammar of measurements, which is the dangerous form: a gap reported as a gap costs one re-dispatch, a gap reported as a finished check ships — here it would have shipped the very defect the owner raised. Recorded in `.claude/memory/design-agent.md`. **Round 3 finished the sweep before the process died to a machine sleep — verified independently: 0 circles in all ten files, base `.cbx` at `r-sm`, no `r-xs` left in the rule, hit-area pseudo present in all ten, design-check 137/0/2.** Still owed: Search states and the `⋯` menu, raised as T-225. |
| T-231 | Draw the four list operations the new spec requires | assistant | F-008 | design-agent | P1 | — | DONE | docs/design/assistant/screens/lists.html, docs/design/assistant/screens/lists-ios.html, docs/design/assistant/screens/lists-android.html, docs/design/assistant/screens/index.html | **F-008 was written AFTER design had drawn part of Lists, and it adds four operations design never drew.** Drawn today: `data-sheet="new"`, `data-sheet="taken"`, `v-menu-open`, `v-menu-empty`, `v-menu-failed`. **Missing: delete, rename, recolour, reposition.** The delete confirmation has spec'd copy — *"Move {n} tasks to Inbox and delete {name}?"* (AC-8) — and **zero pixels**. **The empty-list case is design's own call**: F-008 requires confirmation only for a non-empty list, and the spec'd sentence is wrong for an empty one. **There is also no surface holding these operations** — the drawer's rows show a name and a count and nothing else, so the route to them must be drawn and justified, and kept consistent with the task row's gestures since a user will try what they already learned. **Reposition is the trap:** drag-to-reorder is the obvious answer and fails keyboard and screen-reader users, exactly like swipe-to-delete, which now has `F-001 AC-33` for it. **No voice-initiated state to draw** — F-008 forbids the assistant renaming, recolouring or deleting a list. **DONE — 3 mockups, 8 states each, 18 new testids, none renamed, design-check 175/0/4.** AC-8's copy matches verbatim. **Agent decided five things:** the route is an always-visible three-dot overflow on each list row, **not a swipe — inside the drawer a swipe collides with the drawer's own dismiss gesture** (Google Tasks, Todoist and MS To Do all do this in sidebars); **deleting an empty list does not confirm**, since F-008 requires it only for non-empty and an empty list has nothing to lose; reposition is **Move up / Move down menu items**, which sidesteps the drag trap entirely; rename edits inline in the drawer; recolour opens a swatch row under the row. **PARTIAL on one thing, declared honestly: iOS and Android were not separately screenshotted** — web only. |
| T-221 | Phone: is the left time column worth 18.5% of the screen | assistant | — | design-agent | P0 | — | DONE | docs/design/assistant/explorations/mobile-row-and-entry.html | **Owner question, prototype-only — explicitly asked for a separate HTML to approve BEFORE the main design changes.** **Measured:** the phone time rail is 72px of a 390px screen, **18.5% of every line, reserved on every row** including the 4 of 18 fixture rows that render "—". (The fixture is authored, so 14/18 is evidence about the designer, not about users; the 18.5% is a fact about the layout either way.) **The owner reached the row question through the entry question and that link is the content:** a text field pinned to the bottom implies new tasks appear at the bottom, while a time rail sorts by time, so an untimed task does not go there — a real contradiction, not a preference. Three options at 390px: **A** keep rail + bottom field, **B** drop rail (checkbox and name flush left, due date right) + bottom field, **C** drop rail + inline new-task row + floating voice button. **Two written rules are in the way and must be named, not drawn over:** `DESIGN.md:86` *iOS has no FAB* and `DESIGN.md:88` *row delete is always visible on touch, because a hover-revealed control does not exist there*. **And swipe-to-delete is a lockout unless a second path exists** — VoiceOver and TalkBack cannot perform a custom swipe. Writes ONLY `docs/design/assistant/explorations/mobile-row-and-entry.html`; T-220 is running against the main design tree. |
| T-237 | The colour swatches are ovals, and the row keeps its dot | assistant | F-008 | design-agent | P1 | — | DONE | docs/design/assistant/screens/lists.html, docs/design/assistant/screens/lists-ios.html, docs/design/assistant/screens/lists-android.html, docs/design/assistant/screens/app-shell.html, docs/design/assistant/screens/app-shell-ios.html, docs/design/assistant/screens/app-shell-android.html, docs/design/_shared/DESIGN.md, docs/design/_shared/tokens.json | **Owner spotted it in the render.** Measured: all seven swatches are **36x40**. `.swatch` sets `width:36; height:36; min-height:40` and **`min-height` beats `height`**, stretching the circle. The `min-height` is there for the touch floor, **so deleting it trades a visible defect for an invisible one** — the fix is the one already used on the checkbox: paint at token size, expand the target with a pseudo, per platform. Other painted circles to be checked the same way (width vs height is mechanically checkable). **Second half is a question, answered, no drawing changes: the list row keeps the dot and does NOT take the colour as a background.** The reason is structural rather than taste — **the drawer already marks the active list with a background**, so a colour fill would put two meanings in one channel and the selected row would stop being legible; seven saturated rows would also force per-colour text colours to hold contrast. Recorded as a one-line rule so it is answered once. **DONE — verified 36x36 in all three list files, design-check 175/0/4.** **Two different bugs produced the same symptom, which is why fixing the measured one would not have finished it:** web was `min-height:40` beating `height:36` (36x40); **iOS and Android were flex-shrink** — no `flex:none`, and 7x36 + 6x8 = 300px in a 288px container, squeezing them to 34x36. Also found `app-shell`'s swatches round but **painted at 40/44/48, one size per platform for the same object**; unified to 36 with the hit area on a pseudo. 60 circles measured, 0 non-round. **Agent noted honestly that Android's mic button uses the same min-width/min-height anti-pattern but is square, so it left it alone.** iOS and Android were finally screenshotted and probed, closing the gap two earlier returns declared. |
| T-239 | The time column is gone on web only, not on iOS or Android | assistant | — | design-agent | P0 | — | DONE | docs/design/assistant/screens/app-shell-ios.html, docs/design/assistant/screens/app-shell-android.html | **Owner opened `app-shell-ios.html` and saw the old layout. Measured: at 1440 `row-time` is `none` in `app-shell.html` but `flex 96x44` (iOS) and `96x48` (Android), with `row-due` hidden in both.** T-227 listed all three files as modified and gave a measurement table as though it covered them. **Narrow fix — everything ELSE from that pass did land in all three:** header is `shell-lists-menu-button · shell-search-button · shell-overflow-button`, and `tasks-inline-add` and `assistant-voice-fab` are present in every shell. **That is what makes this one dangerous: a spot check on any of those confirms the pass and misses the defect — coverage is per change, not per pass.** **Also the orchestrator's miss:** the verification at the time measured `app-shell.html` only while the claim named three. Third false coverage claim from this agent in one day, recorded in `.claude/memory/design-agent.md`. **DONE — six-row table delivered and independently re-measured by the orchestrator: `row-time none` and `row-due block` in all three shells at both 430 and 1440.** design-check 175/0/4. **The agent found four rail-dependent leftovers the briefing did not name:** `.group` padding still indented by `calc(rail + gutter)`, the skeleton row still a grid with a rail track, `.inline-new` still padded to the old rail at wide, and a dead `.app[data-surface] .row-time .wd{display:inline}` that could never render. **Fixing only the task row would have left both platform files indented against a column that no longer exists.** |
| T-238 | Show four ways a list can carry its colour | assistant | F-008 | design-agent | P1 | — | DONE | docs/design/assistant/explorations/list-colour-carrier.html | **Owner asked twice; answering in prose was not enough** — *"có bị rối mắt ko"* is a question about looking, and this owner has caught every defect this session by looking. Prototype only, writes one file under `explorations/`. **A** dot before the name (drawn today), **B** whole row filled, **C** thin colour bar on the left edge, **D** dot plus a tint only while open. **Every option must show the ACTIVE-LIST state, because that is where the argument lives:** the drawer already marks the open list with a background, so B puts two meanings in one channel and the open list stops being identifiable — a lost function, not a lost aesthetic. Six lists each, not two, so density is honest; light and dark, since a saturated fill behaves differently across them. **B must be drawn at its best — an option drawn badly proves nothing.** Worst-case text contrast per option per theme reported as numbers, since needing per-colour text to stay legible is a maintenance cost rather than an opinion. **OWNER CONFIRMED A after seeing the render — no drawing changes; the rule already in `DESIGN.md` stands.** **Built by the orchestrator, not the agent.** The dispatch was killed at 25 minutes with zero bytes written, and the cause was this briefing: four options x six lists x two themes x the open state, plus 7-colour contrast per option per theme, is 8 renders and 56 calculations for a yes/no question. Rebuilt directly in one pass, 8.8K. **The render carried the argument the prose could not:** in A, C and D the open list is the row with a different ground; in B every row has a different ground, so *open* must borrow a second signal and becomes the weakest mark on the panel. **Contrast measured:** grey `#94949E` reaches only **2.66:1 against white text** (AA needs 4.5), so B would need a text colour chosen per swatch and re-chosen on every palette addition. **Option C — a bar on the left edge — was not in the original question and is a real alternative: it keeps the ground free. Set aside, not rejected.** |


<!-- archived 2026-08-23: DONE rows that NO row anywhere names in `Depends`.
     C3 resolves Depends against TASKS.md alone, so a row that a live row still
     names cannot be moved without turning one FAIL into many — measured today:
     archiving 38 rows by age produced 28 broken dependency links, which is the
     exact failure the 2026-08-18 note predicted. Only leaves move until T-149
     lands (ALL_IDS = TASKS.md union TASKS-archive.md). -->
| T-105 | Desktop: Tasks centre, chat right panel — the list becomes the primary object | assistant | F-001 | design-agent | P1 | T-101 | DONE | docs/design/_shared/information-architecture.md, docs/design/assistant/screens/app-shell.html, docs/design/assistant/screens/app-shell-ios.html, docs/design/assistant/screens/app-shell-android.html, docs/design/_shared/components.md, docs/design/_shared/tokens.json | docs/reports/owner-decision-2026-08-17-desktop-list-is-primary.md; supersedes the remove-at-every-width confirmation for desktop only |
| T-179 | § CarriedNotice — bottom placement and the two-lifetime rule, in the design system | assistant | F-005 | design-agent | P1 | T-177 | DONE | docs/reports/design-signoff-visual-language.md | **SUPERSEDED 2026-08-21 by the visual-language redesign, closed rather than run.** Both would have drawn against v1, which no longer exists — the owner replaced colour, type, spacing, shape and layout wholesale. What each owed is now inside T-204's ordered redraw. **Closing them rather than leaving them PENDING is the point: a queued task that draws against a retired system is worse than no task, because it looks like coverage.**  phase: system. Amends `docs/design/_shared/components.md § CarriedNotice` (## Placement, ## Multiplicity, the lifetime table) and the IA's frame-level entries. **The strip-order rule inverts rather than reverses:** it was written *outermost first* from the top; from the bottom the principle is unchanged and reads *a strip that is not about the surface it appears on stays furthest from the surface's content*. **The composer is the real constraint** — design's own note says § OfflineBanner docks above the composer at the bottom of Talk, so 'bottom' cannot mean the screen edge on the one surface where the app's primary input lives, and the region must move with the keyboard. Also owed: whether the 10s life gets any visual expression at all (a countdown ring is the obvious idea and is probably wrong — it draws the eye to the clock rather than the action). **Sharpened 2026-08-21 from T-177's return, which names three staleness points and one of them is a flat contradiction, not a drift:** `components.md` § CarriedNotice states *"There is no timer anywhere in this family, so WCAG 2.2.1 is not engaged"* — revision 5 puts a ten-second timer on two of its rows, so that sentence is now false where it stands and it is the sentence a reader consults to learn whether 2.2.1 applies. The other two are `## Placement` (top bar → bottom of frame, with the composer/keyboard rule on Talk) and the strip-order rule, which **inverts rather than reverses**. spec-agent deliberately did not touch this file — it is design's. **Third item added 2026-08-21 from the owner's answer to F-006's product lens (option A):** when `CN-UNDO` reaches its ten seconds, what replaces it **names the trash**. Copy is design's and joins this revision rather than starting its own. Note the constraint this inherits: the `§ Buttons` one-word-per-concept table binds **delete** to *removing a task* and has **no word for *stops being recoverable*** — the design lens raised that as F6(a) against F-006's AC-3, and the same gap bites this string. |
| T-192 | Audit: where does the project still not understand voice-first | assistant | — | product-agent | P1 | — | DONE | docs/reports/audit-voice-first-2026-08-21.md | **6 findings, and the gap is NOT where the word count pointed.** The word count was a pointer and was wrong about which document was at fault: the two lowest-voice specs are both **correct** — F-003 delegates through a parity table enumerating all 29 F-001 ACs, F-005's hand-first shape is the owner's own decision — while **the real defect lives in the spec with the HIGHEST voice count.** **F-002 `§ What speaks` has 14 speaking rows and every one is a reply to a turn the user issued.** The two things the app is already specified to say unprompted — the landing summary on open, a passed reminder — **are both specified as pixels. The app only ever speaks when spoken to.** Verified by the orchestrator: the section's rows are all reactions, and **F-004 has no spec file** — it is *reserved, not committed* — so design defers the owner's 2026-08-18 spoken-summary request **by citing a feature that exists only because F-002 deferred it.** Also delivered: **the inherited-premise check nobody had ever done** — two tables, 13 rows, saying which of the source product's voice premises survived. **And three honest 'this is fine' answers with what was checked**, which is what makes the other six credible. D1 escalated. |spoken|nói` vs `typed|by hand|keyboard` per spec:** F-002 **39 : 4** · F-001 **21 : 13** · F-006 **16 : 14** · **F-005 31 : 36 — the hand wins** · **F-003 3 : 8 — the MOBILE surface spec mentions voice three times.** Those last two are the shape of the question: **a product that says voice-first has its phone spec and its biggest feature written hand-first.** Whether that is wrong is the owner's call — the audit's job is to say where it happens, with counts, and to separate *deliberately hand-only* (structure, ordering, pickers — F-005 decided that on purpose) from *voice was simply not considered*. Also check `docs/specs/_source/` — the inherited product had a voice-first UI research doc and a conversation use-case doc, and whether this redesign kept their premises is checkable. **Output is a report in `{reports}/`, not spec edits.** Everything it finds becomes a task or an owner question. |
| T-208 | Two test pins now disagree with the drawn ids, both by design | assistant | — | web-agent | P1 | T-207 | DONE | src/assistant/web/__tests__/app.test.tsx | **DONE 2026-08-22 as part of the web drift cleanup.** The catalogue pin moved 47→76 and the built-catalogue pin 36→40; the five drawn `shell-carried-notice*` ids left `AHEAD_OF_MOCKUPS`, which is that list working as designed. A 24th mockup state was added so the four notice-item testids render. Web slice re-run by the orchestrator: **364/366**. — Named by design-agent in its own return rather than left to be discovered. **(a)** `src/assistant/web/__tests__/app.test.tsx` pins the union catalogue at **47**; it is **52** now, because the five `shell-carried-notice*` ids were **published but never drawn** and tranche 1 drew them. **(b)** the same file's `AHEAD_OF_MOCKUPS` list still names those five as undrawn, **so its own expiry check fires** — which is the list working correctly. Two one-line edits. **Do not "fix" them by removing ids from the mockups**: the drawing is now correct and the pin is what is stale. |
| T-209 | Four controls ship in the client with ids the catalogue does not declare | assistant | — | mobile-agent | P2 | T-207 | DONE | src/assistant/mobile/model/a11y.ts | **DONE 2026-08-22 inside the mobile drift cleanup — the four missing ids are declared and the catalogue test passes 28/28.** — `tasks-row-open` and the three `tasks-row-*-mark` ids exist in `src/` and are **absent from `a11y.ts`'s `SHELL_A11Y_IDS`**. design-agent drew those controls **without ids** rather than inventing them, because a mockup naming something the client does not declare fails the catalogue check — so the drawing is right and the declaration is missing. Add them, then the next screens pass can label the controls. |
| T-215 | Ground instead of border on typed fields, plus two defects in the same renders | assistant | — | design-agent | P1 | — | DONE | docs/design/assistant/screens/task-detail.html, docs/design/assistant/screens/task-detail-ios.html, docs/design/assistant/screens/task-detail-android.html, docs/design/assistant/screens/index.html, docs/design/_shared/components.md | All three landed. `design-check` **137/0**, testids **byte-identical before and after** on all three platforms, boxes in `detail-blank` at 390 **3 → 1**. **The finding that matters is one it made against the owner's own suggestion, and it is right: the ground is NOT the accessibility carrier and cannot be.** `bg.sunken` on `bg.base` measures **1.12:1 light, 1.08:1 dark** — **verified independently by the orchestrator** — against the **3:1** WCAG 1.4.11 wants for a control boundary. So what identifies the field is **the label above it**, and what marks engagement is **the focus ring** (2px accent, 9.56:1). **It wrote that constraint into `components.md § FLD-QUIET` so nobody later reads the ground as sufficient** — which is the difference between a decision and a defect. **Two defects it caught inside its own work, both invisible to every mechanical check:** the Android calendar's trailing week rendered `−3 −2 −1 0 1 2` instead of September 1–6, and the AM/PM selector measured 48×34.5 against Android's 48dp floor. **And it declined the briefing's instruction to DELETE the duplicate `Add a step`** — the id is bound at `TaskDetail.tsx:998` with two passing tests and an AC-14 traceability row, so deleting it would put the design in conflict with shipped green code; it made the two into one action in two moments instead. **Two pre-existing findings reported, not fixed:** step checkboxes are 20×20 on all three platforms, under every floor; and `detail-repeat-until` shows *Never* pressed while the until-date field is displayed. |
| T-218 | The typed fields sit on a different gutter from everything else | assistant | — | design-agent | P1 | — | DONE | docs/design/assistant/screens/task-detail.html, docs/design/assistant/screens/task-detail-ios.html, docs/design/assistant/screens/task-detail-android.html, docs/design/assistant/screens/app-shell.html, docs/design/assistant/screens/app-shell-ios.html, docs/design/assistant/screens/app-shell-android.html, docs/design/assistant/screens/index.html, docs/design/_shared/components.md | **The dispatch was interrupted and its `---METRICS---` return is lost — status derived from the disk instead (L-018's method, third use), and all three changes had landed.** Measured by the orchestrator: **the gutter is now one rule — title, note and the property block all run `16 → 374` at 390**, where they ran 4→386 / 4→386 / 16→374 before. The rule reached the app shell too, which the briefing permitted if it was genuinely systemic. **AM/PM is `flex-direction: row` and measures 48×48 — it kept the hit area it had been repaired for**, which was the one risk flagged. Placeholder is **`No name yet`**, which reads as a state rather than an instruction to go and do work. `design-check` **137/0**. `index.html` rebuilt. **Not verifiable without the return: its `visual_review:` and `a11y_review:` blocks, and its answer on the title's size** — the title measures 32px against 16 elsewhere, which is the heading and almost certainly right, but the owner raised size and **no recorded answer exists.** Carried to T-219 rather than assumed. |
| T-220 | Panels on a canvas, and the two self-checks that were skipped | assistant | — | design-agent | P0 | — | DONE | docs/design/_shared/tokens.json, docs/design/_shared/DESIGN.md, docs/design/assistant/screens/ (9 mockups + index.html) | **Owner 2026-08-22, two instructions.** **(a) The big layout regions stop being separated by lines.** Measured at 1440: exactly three long structural borders remain — `header.topbar` bottom **1020px**, **`aside.col-panel` LEFT 820px**, `div.composer` top **419px**. The owner's pattern replaces all three and **uses tokens that already exist**: page becomes `bg.sunken`, each content region a `bg.base` panel on top, same ground as each other, rounded, small gap. **It narrows a rule the ORCHESTRATOR caused** — `border.when_a_line_earns_it`'s third case is *a container holding a different KIND of thing*, which I proposed, and the owner's pattern is the better answer to the same problem. **(b) Re-run both self-checks.** T-218's return was lost to an interrupt, so **nothing drawn since has a recorded `visual_review:` or `a11y_review:`** — the owner noticed. Run them over the whole set, not just this change. **Carries three open items to fix or re-confirm:** T-216 (step checkboxes 20×20, under every floor), T-217 (`detail-repeat-until` shows *Never* pressed while an until-date is displayed), T-219 (the title's size, asked by the owner and never answered). **System + screens in one dispatch is allowed here and the reason is stated: the rule against it exists because a fresh system has no external standard to meet, and this system is authored, signed off, and being narrowed by an owner instruction.** |
| T-227 | Web drops the time column and the header loses its two buttons | assistant | — | design-agent | P0 | — | DONE | docs/design/assistant/screens/app-shell.html, docs/design/assistant/screens/app-shell-ios.html, docs/design/assistant/screens/app-shell-android.html | **CLOSED ON DISK EVIDENCE 2026-08-22 by the next orchestrator — the dispatching session ended without running step 5, leaving the row IN_PROGRESS while its follow-up T-239 was already DONE.** Verified in `app-shell.html`: base `.row` is now `display:flex` with the grid-with-rail override deleted (the inversion the row asked for), `.row-time{display:none}` at every width, `.list::before` removed, and the header rebuilt as `hamburger · title · spacer · shell-search-button · shell-overflow-button` with `shell-talk-button` and `assistant-add-task-button` gone. T-239 carried the same three changes to iOS and Android. **One thing the row asked to be RAISED was not raised anywhere on disk:** whether the right-aligned per-row date survives at 1920, where rows are far wider than a phone — the concern that moving the time from a left column to a right edge swaps a short scan for a long one. Re-raise it against T-225 or a new row rather than treat it as answered. — **Owner checked and web had not adopted the phone pattern.** Measured in the browser at 1440: base `.row` is still `grid-template-columns: var(--rail) 1fr auto`, overridden ONLY at `max-width:1023px`; `.row-time` renders 96x40; `.row-due` is `display:none`; the header still holds `shell-talk-button` and `assistant-add-task-button` and has neither Search nor `⋯`. The inline add row IS present (820x52) but sits at top=734, below the fold, under a large blue header button doing the same job. **T-224 defended keeping the rail at split+ and the argument was reasonable; the owner has decided otherwise and it is their call — not to be re-argued.** **The base rule is the bug:** the rail is the default and only the below-split query undoes it; invert so the flat row is the default. **Two judgement calls, not rules:** `.list::before` draws a spine belonging to the time column and now marks nothing; and moving the time from a left column to a right-aligned per-row date **replaces a scan down the left edge with a scan down the right** — the phone's answer may not survive at 1920 where rows are far wider, and that is a finding to raise rather than absorb. Search states and `⋯` contents stay with T-225. |
| T-230 | Spec the list actions: search, sort, hide completed, select | assistant | — | spec-agent | P1 | T-228 | DONE | docs/specs/assistant/F-009-list-actions.md, docs/specs/assistant/F-008-lists.md, docs/specs/assistant/index.md | **Four features with no spec at all** — `hide completed` does not appear in any file under `docs/specs/`. The header now ends in `Search · ⋯` and **nothing behind either control is specified**, so T-225 was about to draw a second feature ahead of its spec, the way Lists already was. **T-225 is re-pointed to depend on this rather than on the design pass.** Depends on T-228 because *Select tasks* includes **move to list**, which needs Lists to exist. **Search is the one that is worse by voice** — finding is typing and watching the list narrow — and needs states, not an icon: where the field appears, the list mid-query, and no result. **Hide completed is a persisting preference, not a query**, so it needs a home in whatever holds user settings. **Filter is deliberately excluded** — the sentence the app already answers, plus the sidebar collections, do that work; record the exclusion with the reason so it is not re-proposed. **Also carries three decisions closing F-008's open questions, taken by the orchestrator as reversible and cheap to overturn — write them into F-008 and mark the OQs closed against this task.** **OQ-1 → option (a):** undoing a voice-created list unfiles its tasks back to Inbox. The alternative leaves `list_id` dangling and read as Inbox, which is a silent inconsistency, and (a) matches AC-7's delete semantics so one rule covers both. **OQ-2 → yes:** the assistant may set `list_id`, adding it to `F-005 AC-36`'s allowlist. F-008's own permissions table already grants *move a task to a named list by voice*, so answering no would leave the spec contradicting itself; a misfiled task costs one move to fix and the per-turn undo already covers it. **OQ-3 → routed to architect-agent**, not a product decision. |
| T-241 | Two mobile chips still ignore the mockup, and rule 6 answers one of them outright | assistant | — | mobile-agent | P2 | T-232 | DONE | src/assistant/mobile/components/styles.ts | **DONE 2026-08-22 inside the mobile drift cleanup.** `chipOld` now `text.muted` with strikethrough and no ground, matching rule 6 and `.old` in the mockups; `badgeEdited` now `text.secondary` on `bg.sunken`, matching `.tag.edited`. **Web carries both divergences too — reported, not fixed, correctly. Filed as part of T-256.** — **Found by mobile-agent during T-232 and correctly left alone as unbriefed; filed here so it is not dropped with the return.** **(a) `chipOld` uses `danger`/`dangerTint`.** DESIGN.md rule 6 names this pair outright — *"added values now read in `text.primary` at semibold **against the old value struck through in `text.muted`**"* — and both mockups agree: `.old{color:var(--text-muted); text-decoration:line-through}` (`app-shell-ios.html:678`). So the old value should be muted and struck through, not red. **The T-232 briefing is what caused this miss:** it pointed the agent at rules 1 and 3 for `diff.remove`, which both pass, when rule 6 already gave the direct answer. The agent checked what it was pointed at. **(b) `badgeEdited` uses `accent`/`accentTint`** where the mockups draw `.tag.edited{background:var(--bg-sunken); color:var(--text-secondary)}` — an EDITED marker is not the assistant speaking, so accent violates rule 1 (*one signal per meaning*). **Neither is a regression from T-232** — the shim produced the same values — but neither is right, and (a) contradicts the very rule T-232 was about. Check whether web has the same two divergences before fixing mobile alone. |
| T-242 | The upcoming-row test measures a frozen date against a moving clock | assistant | — | mobile-agent | P1 | — | DONE | src/assistant/mobile/__tests__/shell.test.ts | **DONE 2026-08-22. Two lines, one file — `fixtures.ts` untouched, confirmed by diff.** Both real-clock reads now take the describe-scoped `now = 2026-08-16T09:00:00.000Z`, matching the sibling pattern at ~456. Full suite **1221/1240, 19 failures** — exactly one test flipped green, and the count re-run here rather than taken on report. **DATE-INDEPENDENCE VERIFIED BY THE ORCHESTRATOR, not accepted on claim** — the agent proved it at one faked date, so it was re-run here at three: 2026-08-23 (where the old version failed as `Today`), 2026-08-25 (where it failed differently again, the row having gone overdue and emptied the view) and 2027-03-01. **41/41 at every one.** `grep "new Date()"` over the file returns nothing, so no real-clock read survives. **One self-decision, and it was the right one:** the agent also threaded `now` through `tasksSurfaceView` on line 473, not just `groupTasks` on 476. `tasksSurfaceView` defaults its `now` to `nowDate()`, so from 24 Aug `inCollection` would have stopped placing the seed in upcoming and the test would have broken again for a second reason — the exact case the briefing flagged as "report it rather than widening". It is the same defect, the same line, the same fix pattern, so fixing it here was correct and it was declared rather than slipped in. — **Split from T-233 by owner decision 2026-08-22, raised to P1 — not because it is urgent but because it is the only failure in the suite that rewrites its own error message.** `src/assistant/mobile/__tests__/shell.test.ts:476` asserts `['Later']` and currently gets `['Tomorrow · Sun, Aug 23']`. **Cause:** `upcomingTask` (`src/assistant/_shared/testing/fixtures.ts:122`) builds its due date from the **frozen** `T0 = 2026-08-16` plus 7 days, landing on 23 Aug, while the assertion calls `groupTasks(view.tasks, 'upcoming', new Date())` on the **real clock**. A fixed mark measured against a moving one. **It changes failure mode as time passes:** green before 22 Aug; `Tomorrow` today; `Today` on 23 Aug; and from 24 Aug the row goes overdue, so `tasksSurfaceView(..., 'upcoming')` returns empty — `view.tasks` and `view.empty` break too, and the test then fails for a reason that has nothing to do with grouping. **That is the whole point of fixing it early:** whoever runs the suite next week meets what reads as a fresh regression from their own work. **Expected fix is one argument — pass `new Date(T0)` so both sides share a clock.** Do not change `upcomingTask` itself: `web/__tests__/shell.test.tsx:579` and `app.test.tsx` also consume it, so moving the fixture moves their ground too. **Check the sibling assertion at line ~461**, which already threads a fixed `now` through `groupTasks` — that is the pattern this test failed to follow, and it is in the same describe block. **Scope is this one test.** The other 19 failures stay on T-233. |
| T-243 | AC-15 covers mobile too, and stops calling itself Tab | assistant | F-009 | spec-agent | P1 | T-234 | DONE | docs/specs/assistant/F-009-list-actions.md | **DONE 2026-08-22. 8/8 criteria, verified here rather than taken on report.** F-009 revision 2: AC-15 is `(web, mobile)`, stated as sequential navigation with per-platform realisation — web keeps Tab order following DOM order and the `focusRing` token untouched; mobile names VoiceOver and TalkBack swipe navigation reaching checkbox, title and delete as separate elements, plus hardware-keyboard focus. **The clause that does the real work: "the row container does not collapse its children into a single focusable unit"** — that is the exact refactor that would silently break this, named in the AC so it cannot happen quietly. AC-15 also states in one clause that it is the guarantee AC-6's deferral depends on, so the next reader sees why the tag matters. `declared-elements.sh` exit 0 re-run here; AC-1..AC-15 plus AC-6a all present, none renumbered; `git diff --name-only` confirms F-009 is the only spec touched. — **OWNER DECIDED 2026-08-22 (option A of three).** T-234 added AC-15 and spec-agent tagged it `(web)` alone, reasoning that Tab is a keyboard concept and mobile focus order belongs to the platform accessibility tree. The reasoning is half right and the tag is wrong. **The gap it leaves:** no AC anywhere requires mobile rows and row controls to be reachable — grep confirms the specs name VoiceOver/TalkBack exactly once, in `F-001 AC-33`, for delete alone. **That matters because AC-6 now leans on it:** the reorder deferral is only acceptable while a screen-reader user can still reach every row and every control, and VoiceOver and TalkBack are mobile. AC-15 is currently written for the one platform where the deferral does not bite and absent from the two where it does. **Measured, so the fix is not oversold: the mobile implementation is already correct.** `TaskList.tsx` routes through `a11yProps()` — the checkbox carries a label, `role: 'checkbox'` and `state.checked`; the delete button carries a label and role; the row wrapper is a `View` with a label that does **not** set `accessible`, so children stay individually reachable. **Nothing is broken today. Nothing holds it in place either** — no AC requires it and no test asserts it, so wrapping the row in a `Pressable` to make it tappable (an ordinary React Native move) would drop the checkbox and delete out of VoiceOver with nothing turning red. |
| T-245 | Record the done-task selection rule the design already enforces | assistant | F-009 | spec-agent | P1 | T-225 | DONE | docs/specs/assistant/F-009-list-actions.md | **DONE 2026-08-22, verified on disk. F-009 revision 3, `declared-elements.sh` exit 0, AC-1..AC-15 plus AC-6a intact, none renumbered.** `AC-9` keeps *"on each row"* verbatim and gains a clause making completed rows explicit, plus the rejected alternative with its reason — the exclusion was defensible because bulk-complete on a done task is a no-op, and it lost because forty finished tasks would otherwise be forty single deletions. `AC-11` states uniform application as a **rule, not a branch**: *"Applied uniformly regardless of current status — an already-complete task receives the same operation with no special case, no error, and no skip."* **OQ-2 filed against AC-6 for drag-reorder of done tasks, argued both ways and deliberately NOT answered** — it even notes that AC-6's own *"reorder is cosmetic"* reasoning supports leaving done rows undraggable, while the selection decision's reasoning does not carry over because drag gates nothing destructive. **ORCHESTRATOR ERROR, recorded rather than hidden: this work was swept into commit `fcd1120` by a `git add -A` before it had been verified**, so it is committed under the unrelated message *"Complete and Done sit in the same toolbar…"*. The content is right and history is not being rewritten for it; the lesson is to stage paths, not `-A`, while an agent is writing. — **OWNER DECIDED 2026-08-22, both halves. UNBLOCKED.** *"Task đã xong thì hiển thị checkbox đã check, có thể chọn được."* and, on what bulk Complete does to an already-complete task: *"vẫn complete bình thường."* **So `AC-9`'s "on each row" STANDS UNQUALIFIED** — do not narrow it to "each open row". Completed tasks are selectable; bulk delete and bulk move reach them. **Bulk Complete applies normally to an already-done task — no special case, no error, no disabled button, and the toolbar count includes it like any other selected row.** The dev lens had named this as the gap that made option B unspecifiable (*"would require specifying the behavior of Complete on an already-done task, which neither spec nor design addresses"*); it is now answered and must be written down, not left implicit. **The design is what changes, not the AC.** **One thing the owner did NOT settle: drag-reorder of done tasks (`AC-6`).** The done row carries no `drag-handle` for the same reason it carried no selection control, and only the selection half was answered. **Do NOT extend the selection answer to drag** — record it as an open question against AC-6. |
| T-246 | The drag handle is the only new F-009 control without a testid | assistant | F-009 | design-agent | P2 | T-225 | DONE | docs/design/_shared/components.md, docs/design/assistant/screens/app-shell.html, docs/design/assistant/screens/app-shell-ios.html, docs/design/assistant/screens/app-shell-android.html | **DONE 2026-08-22, all four parts verified on disk by the orchestrator.** `tasks-drag-handle` published and drawn (3 mentions in the catalogue; one id on the control, per the house convention the tester lens named, not one per instance). The three LOWs are recorded: ConfirmDialog focus at `components.md:1291` (*"Cancel receives initial focus — this is the safe default"*), the single-task bypass in `§ ConfirmDialog` at 1270, and the search/hide-completed interaction at 1110. design-check held at 175/0/4; suite unchanged at 1221/1240. — **Gate 1.5 M2, tester lens.** 19 of the 20 new F-009 controls carry a `data-testid`; `.drag-handle` does not. Verified: **4 drag handles in the DOM, 0 with a testid.** AC-6 assertions would fall back to `getByLabel('Drag to reorder')` — selector priority 3 — breaking the testid-first convention every other F-009 assertion follows. **Add `tasks-drag-handle` to `components.md § Testid catalogue` and to the element in all three shell mockups.** Mechanical; no decision in it. **Fold in the three LOW findings while the file is open:** (a) record that ConfirmDialog focuses Cancel, which AC-12 does not state — the choice is right, it is just unwritten; (b) note in `§ ConfirmDialog` that AC-12's single-task bypass exists, or draw it as a state, since only the 3-task path is drawn; (c) state in `§ SearchField` that completed tasks appear in search results when hide-completed is off and the title matches — the mockup CSS hides them unconditionally at `app-shell.html:1159`, which the dev lens judged a demonstration shortcut rather than a design error, but AC-2 and the mockup currently disagree and the implementer needs one source. |
| T-248 | The selection inset moved the column instead of the ground, and the row has no left padding to grow | assistant | F-009 | design-agent | P1 | T-247 | DONE | docs/design/assistant/screens/app-shell.html, docs/design/assistant/screens/app-shell-ios.html, docs/design/assistant/screens/app-shell-android.html, docs/design/_shared/components.md | **DONE 2026-08-22. Re-measured by the orchestrator, not accepted on report: `cb=8, title=40` for EVERY row — the three selected AND the unselected "Buy milk". The column no longer jogs.** Fixed at the right level: `space.2` 8px horizontal padding on `.row` itself, so the painted ground fills the padding box in every state (hover and selected alike) with no per-state override. That also delivered the owner's *"cách xa lề trái hơn 1 chút"* in the same change. design-check 175/0/4; suite 1221/1240; radii unchanged (ground `md` 12, checkbox `sm` 8). This time the agent DID run the visual review it skipped last time. **The nesting-rule tradeoff it flagged is real but does not bite, checked here rather than taken either way:** at an 8px inset the formula wants `xs` 4 while the checkbox carries `sm` 8. But the rule exists against *"the concentric-corner mistake, visible as a gap that thickens at each corner"*, and these corners are not concentric — measured **8px across and 18px down** from the ground's corner. `grep` also confirms `design-check` does not enforce the rule mechanically. So nothing is broken; `sm` was set by owner decision and stays. **One thing left open and it is NOT new: in selection mode the done row's title sits at x=8 while every other title is at x=40**, because the done row has no checkbox to make a column. That is the same done-row question as T-245 and it resolves with it, not separately. |
| T-250 | AC-9 still names the exit control "Done", which is the word being retired | assistant | F-009 | spec-agent | P2 | T-249 | DONE | docs/specs/assistant/F-009-list-actions.md | **DONE 2026-08-22. F-009 revision 4, verified on disk: `declared-elements.sh` exit 0, AC-1..AC-15 unbroken, diff is 2 lines.** `AC-9` now reads *"Exit: **✕** icon button (accessible name \"Exit selection\") or deselect all"*, wording taken from `components.md`'s vocabulary table rather than invented — which is the whole point, since inventing a second phrasing is how the drift being fixed here started. **It grepped the word rather than skimming: 13 hits, 1 stale, 12 correctly meaning the task status or the `Done` collection.** **One judgement call and it is right: the flow diagram at line 60 keeps `D[Mark done]`** — that node names the *action outcome* (the task's status becomes done), not the exit control, and the exit is already a separate node `G[Exit select mode]`. Editing it on sight would have been the easy wrong move. — **Coupling the orchestrator created and should not leave for someone else to trip on.** T-245 wrote `AC-9` with *"Exit: **Done** button or deselect all"* — correct when written, and **stale the moment T-249 lands**, because the owner flagged `Complete` and `Done` as confusable in the same toolbar and design is now choosing a different word for the exit control. **Cause: the two dispatches were briefed minutes apart and only the design one was told about the copy clash.** The spec agent had no way to know. **When T-249 returns, update `AC-9` to whatever word it chose**, and check the same word is not left stale in the flow diagram at line ~60 (`C -->|Complete| D[Mark done] --> G[Exit select mode]`) or anywhere else in F-009. **Take the word from the design return, do not re-decide it** — `components.md` will carry it in the vocabulary table by then, and that table is the authority. |
| T-254 | The Talk button is drawn nowhere and rendered anyway, because its replacement was never built | assistant | — | web-agent | P2 | T-253 | DONE | — | **DONE 2026-08-22.** FAB built as `VoiceFab` in `Chrome.tsx`, renders `assistant-voice-fab`, hidden above split; `shell-talk-button` gone from web render — the 4 remaining mentions are comments recording the retirement. `app.test.tsx` "invents none" passes because the client caught up: expected set 40→41, invented `[]`. Verified on disk. — **NOT BLOCKED ON DESIGN — verified 2026-08-22: the FAB IS DRAWN.** At 390px on `app-shell-ios.html` state `phone-tasks`, an element of class `voice-fab` renders carrying `assistant-voice-fab`, and no `shell-talk-button` appears at that width. So the implementer has a target to build against and neither of these rows needs a design pass first. — **web-agent hit this and correctly refused to force it green.** `shell-talk-button` was retired from all three mockups by T-227, but `Chrome.tsx:46` still renders it, so the catalogue test reports an **invented** id. **Verified: `assistant-voice-fab`, its replacement, has ZERO implementations anywhere in `src/`.** So removing the Talk button now would strand narrow-width users with no route to Talk at all — a worse outcome than one red assertion, which is why the agent left it. **The two are sequentially dependent: build the FAB first, retire the button second.** Do not reorder them, and do not retire the button "temporarily" to get a green suite. **Depends on T-253 only for sequencing** — both touch the web client and should not run in parallel. |
| T-257 | A path-switch row now reports a floating action button's id | assistant | — | mobile-agent | P1 | T-255 | DONE | — | **DONE 2026-08-22.** `VoiceFab.tsx` created (52×52, accent, mic); `PathSwitch` now has **0** references to `voiceFab` — verified by grep. The id names the control it labels again. — **NOT BLOCKED ON DESIGN — verified 2026-08-22: the FAB IS DRAWN.** At 390px on `app-shell-ios.html` state `phone-tasks`, an element of class `voice-fab` renders carrying `assistant-voice-fab`, and no `shell-talk-button` appears at that width. So the implementer has a target to build against and neither of these rows needs a design pass first. — **Undeclared change inside T-233, caught by the orchestrator comparing the two agents rather than by reading either return.** `PathSwitch.tsx:36` and `touch.ts` were changed from `SHELL_A11Y_IDS.pathTalk` to `SHELL_A11Y_IDS.voiceFab`. **Verified: no FAB was built** — the only mention of `voiceFab` in mobile production code is that id assignment, and `PathSwitch` still renders its path-switch `Pressable`. **So the id claims a control that does not exist, on a control that is something else.** A testid that lies about what it labels is precisely what breaks a QA selector months later, and the catalogue check cannot catch it because the id is legitimately published — it is just attached to the wrong thing. **web-agent met the identical wall in the same hour and refused**, leaving its test red with the reason that `assistant-voice-fab` has zero implementations and retiring the Talk button would strand narrow-width users. **Two agents, one problem, opposite routes. The refusing one was right, and the same order applies here: build the FAB first, re-point the id second.** **Do not resolve this by reverting the id to `pathTalk` alone** — that restores a red test and loses the information that the Talk affordance is supposed to become a FAB below the split. Either build it, or revert and file the FAB with the design reference attached. Coordinate with T-254, which is the same job on web. |
| T-258 | The search mid-query state does not narrow by title — it hides the done row | assistant | F-009 | design-agent | P1 | — | IN_PROGRESS | — | **Found in the second visual-audit pass, on the one state that matters most. Measured, not eyeballed.** `app-shell.html` state `search-filtering` carries query **"bill"** and shows **4 rows, of which exactly 1 contains "bill"** — *Pay the electricity bill*. *Send the weekly report to Hanh*, *Gọi nha sĩ đặt lịch khám răng* and *Buy milk* match nothing. **Compared with `tasks-default` (5 rows) the only change is that the done row disappeared**, which is `AC-7` hide-completed, a different feature — and the dev lens had already flagged the CSS `.app[data-search="filtering"] .row.done{display:none}` as contradicting `AC-2`. **`AC-2` is live filtering by title.** So the state that exists to show the list narrowing shows a list that has not narrowed. `search-no-results` is correct by contrast: query "meeting", 0 rows. **Three lenses passed this and my own briefing called it *"the state the product lives or dies on"*.** The spec lens wrote *"AC-2: search-filtering state shows live narrowing with query 'bill'. Covered."* — it verified the state exists, not what it contains. **Worse, the tester lens proposed the check that would have missed it.** Its Q5 answer was to assert narrowing *by counting visible rows* — and the count does drop, 5 → 4. **A row-count assertion goes GREEN on this state while three of the four remaining rows do not match the query.** **Fix: draw the state with only the matching rows**, and make the visible set derive from the query rather than from a CSS rule that hides done rows. **Then tell qa-web-agent its proposed assertion is insufficient** — the test must check that every visible row contains the query, not that the count fell. |
| T-259 | The NEW badge still wears the colour rule 6 retired, in ten mockups | assistant | — | design-agent | P1 | — | DONE | — | **DONE 2026-08-22 across all three dispatches.** `.tag` is now `background:var(--bg-sunken); color:var(--text-primary)` in the app-shell, lists, task-detail and voice files — **and the detail/voice dispatch supplied the rendered proof the app-shell one could not**, since that is where `.tag` elements actually exist: measured `bg:rgb(242,242,244), color:rgb(17,19,24)`. The three dispatches were each given the same target treatment so none of them invented a variant — the failure mode that produced the 30 phantom variables in T-267. — **The design system violates its own rule, in its own drawings — and the client is the half that is right.** `DESIGN.md ## Colour rules` 6 retired green/`success` with this reasoning: *"rule 3 already forbids colour carrying a diff alone, so the label `NEW` was doing the work and the hue was decoration; added values now read in `text.primary` at semibold against the old value struck through in `text.muted`."* **Measured in `voice-assistant-view.html`:** the `NEW` badge renders `color: rgb(0,47,167)` on `background: rgb(230,234,246)` — accent on accent-tint. The rule says the hue is decoration; the drawing still paints it. **`.tag{background:var(--accent-tint); color:var(--accent)}` appears in 10 of the 12 mockups.** **Inconsistent inside one CSS block, which is the tell:** the very next line, `.tag.edited{background:var(--bg-sunken); color:var(--text-secondary)}`, follows rule 6 correctly. EDITED obeys and NEW does not. **The direction of this drift is the unusual part: the CLIENT is correct and the DRAWING is stale.** T-232 corrected mobile today — `styles.ts:217` is now `badgeNew: { color: c.text.primary, fontWeight: semibold }` with no ground. So mobile follows `DESIGN.md` and ten mockups do not. **Do not "fix" mobile back to match the mockups.** Fix the mockups, then re-check web, which still paints `.chip-old` and `.badge-edited` the old way too (T-256). |
| T-260 | The composer draws Send as active while the field is empty | assistant | — | design-agent | P2 | — | DONE | — | **DONE 2026-08-22.** The composer's empty state now shows Send unavailable by **two** signals, as rule 3 requires: `disabled:true`, `opacity:0.4`, `color:rgb(100,100,112)`, `cursor:not-allowed`. Identical on all three platforms. **Chose disabled-visible over hidden, with the reason declared — hiding it would shift the layout the moment the user types.** — **`components.md § Composer` enumerates the states as *"empty (placeholder \"Say or type what needs doing.\") · with-text (send activates)"*.** *Activates* on with-text means it is not active when empty. **Measured in `voice-assistant-view.html` default state:** the input value is `""` (placeholder showing) while `.composer-send` has `disabled: false`, no `aria-disabled`, `opacity: 1` and full `text-secondary` colour. So the drawing shows an active Send over an empty field. **`DESIGN.md ## Colour rules` 3 also bites: a disabled control loses opacity AND its border** — whatever the empty state should look like, it needs two signals, not none. **Decide and draw it: is Send hidden until there is text, or shown disabled?** Either is defensible; the current drawing is neither, and an implementer reading it ships a Send that appears to work on nothing. Check the same state on iOS and Android. |
| T-263 | Two Lists states render an empty overlay on Android, and the menu is empty on web too | assistant | F-008 | design-agent | P1 | — | DONE | — | **DONE 2026-08-22, verified on disk by the orchestrator.** `delete-confirm` now draws content on all three — leaf-text 14 web / 12 iOS / **14 Android, where the screen previously dimmed and drew nothing**. `menu-nonempty` draws 10 web / 16 iOS / 10 Android, where **web and Android previously rendered an empty container**. — **Found by comparing the same named state across the three platform files — a property nothing currently checks. Measured with the mockup dev toolbar removed.** **`delete-confirm` on Android: `overlays: ["scrim"]` and nothing else.** The screen dims and **no dialog is drawn at all**. iOS has `delete-sheet`; web has the full copy. **`menu-nonempty` on Android: `["scrim", "ctx-menu for-work"]` — the menu container exists and is EMPTY**, leaf-text count identical to the drawer with no menu open. **On web the context menu has no content either.** iOS is the only platform where both states are fully drawn (`action-sheet` with 6 items: Rename · Change colour · Move up · Move down · Delete · Cancel). **The delete confirmation is the worst of it, because of what the copy carries.** Web says *"Move 5 tasks to Inbox and delete Work?"* and *"The tasks are not deleted. They move to Inbox, where they were before you filed them."* — that reassurance IS F-008's delete semantics, and it is the thing a user needs before confirming a destructive action. **iOS shows only the button label "Move and delete"; Android shows nothing.** Two of three platforms ask the user to confirm a destructive action without telling them what it does. **Another design-check blind spot, worth folding into T-252:** the tool counts *distinct renderings* per state, and an overlay containing nothing still counts as distinct because the scrim differs. **A rule for "an overlay element that renders with no visible content" would catch this whole class.** |
| T-264 | The colour picker sits two rows below the list it edits | assistant | F-008 | design-agent | P1 | — | DONE | — | **DONE 2026-08-22, measured on all three platforms: Work y=410 → recolour panel y=462 → Home y=549 (551 Android).** The panel now sits attached to its own row instead of below the next one. — **Measured at 390px on all three platforms, and the same file gets it right elsewhere — which is what makes this unambiguous.** **`recolour`:** `Work` at y=410 → `Home` at y=462 → **`recolour-panel for-work` at y=514.** The colour picker for *Work* renders **below Home**, two rows from the list it edits. Its own class says `for-work`, so it knows its target; it is only placed wrong. **`rename` in the same file is correct:** `rename-form for-work` renders at **y=410**, replacing the Work row in place, with Home following at 547. **So the two editors for the same object behave differently: rename replaces the row, recolour appends after the next one.** **Why it matters beyond tidiness:** a swatch row captioned *"Colour for Work"* sitting directly under **Home** reads as belonging to Home. The caption is the only thing contradicting the position, and position wins in a list. **Fix: place the recolour panel where rename already goes** — attached to its own row. Identical on all three platforms, so one decision covers them. **Noted, not filed:** the swatches carry no names. Selection is signalled by a ring, so `DESIGN.md` rule 3 is satisfied for *state* — but the colours themselves are distinguishable by hue alone, which is inherent to a colour picker and only worth revisiting if list colour ever carries meaning elsewhere. |
| T-265 | The deleted-task screen preserves your typing and then offers no way to keep it | assistant | F-005 | design-agent | P1 | — | DONE | — | **DONE 2026-08-22, verified on disk.** A secondary **Copy text** action now sits beside the primary *Back to your tasks* on all three platforms, so the rescued text can be kept instead of displayed and discarded. **The copy was corrected too: *"It went from another window"* → *"It was deleted from another window while you were typing."*** — I had raised that as a low-confidence question and the agent judged it a slip, matching the heading and the vocabulary table. **It rejected the two alternatives with reasons: "Create a new task with this" as overreach with no spec for a creation flow from this state, and removing the text as unhelpful since the display is the context.** Both remain open to the owner. **One consequence the agent could not handle itself: the new `detail-copy-button` is drawn in three mockups and declared in ZERO places — 0 in `components.md`, 0 in `app.test.tsx`. Sent into the running `_shared` dispatch, which owns the catalogue.** — **Found by looking at the whole state and asking what happens next. Verified identical on all three platforms.** State `detail-deleted` does three things: it says **"This task was deleted."**, it explains **"It went from another window while you were typing. There is nothing here to save it to."**, and it shows the user's text under a **YOU TYPED** heading — *"Gọi nha sĩ đặt lịch khám răng — hỏi giá lấy cao răng"*. **Then the only action is "Back to your tasks".** Measured on web, iOS and Android: `actionsInPanel: ["Back to your tasks"]`, `editableFields: 0`, no copy / keep / save affordance anywhere in the panel. **So the design goes to the trouble of rescuing the text, displays it, and then the single exit discards it.** The user must hand-select from a non-editable ground or lose what they wrote — and the screen has already told them there is nowhere to save it, which makes the omission read as deliberate. **This is the same shape as the F-009 search state (T-258): a state that exists to do something and stops one step short.** **Options, none obviously right: a "Copy" action; a "Create a new task with this" action, which is the only one that actually completes the rescue; or accept the loss and stop displaying the text, which at least would not promise something it does not deliver.** **Lower confidence, raised as a question not a defect: "It went from another window" reads as a grammatical slip** for *"it was deleted from another window"*. Given this product keeps a binding vocabulary table, worth a second pair of eyes on that sentence. |
| T-267 | The whole retired v1 palette is hardcoded into web CSS under token-shaped names | assistant | — | web-agent | P0 | — | DONE | src/assistant/web/styles.css | **DONE 2026-08-22. Verified by the orchestrator IN A BROWSER, not from source — which matters here, because reading source is exactly what hid this.** Measured on a page loading the real `styles.css`: `--accent` = **`#002FA7`** light / **`#8AA6FF`** dark; `.btn-primary` background `rgb(0,47,167)` / `rgb(138,166,255)`; `.menu-row.active` is that accent at 7%; `.seg` on-state is `accentTint`. **The violet is gone.** **Static check is stronger than the criterion I set: 36 hex declarations in the CSS, every one matching `tokens.json` exactly — 0 names that do not exist as a token, 0 values that drift.** Grep for `primary`, `success`, `voice`, `question`, `diff-add`, `diff-remove`, `listening`, `thinking`: **0 each.** **The agent pushed back on one of my criteria and was right.** I wrote that `--color-*: #` must return nothing; it observed that the `:root` block is the legitimate entry point for token values into CSS and that the real test is whether those hexes match the tokens. **That is the better criterion and it is what I verified against.** **Three self-decisions, all declared and all resting on rule 1 or rule 6:** the arrival flash takes `accentTint` because the assistant changed that row; the two voice glows merge into one `color-mix(accent 35%)` because one accent now means the assistant on both halves of its turn; the gradient collapses to a solid accent because v2 retired it. — 
| T-270 | Web animations run at v1 speeds — the motion half of the same disguise | assistant | — | web-agent | P1 | — | DONE | — | **DONE 2026-08-23, verified on disk by the orchestrator before the agent reported.** `design-check` **201/5 → 202 passed, 0 failed**. The five motion literals now hold the token values with a comment naming each source: `micro: 100`, `standard: 180`, `emphasis: 280`; `celebrate`, `auroraBreath` and `easing-spring` deleted as retired. **And `FLASH_MS` now reads `tokensJson.motion.duration_ms.diffFlashHold + diffFlashFade` — the comment above it that claimed the number came from tokens.json has been true only since this change.** — 
| T-272 | Two one-line mockup fixes left over from the fan-out | assistant | — | design-agent | P2 | — | DONE | — | **DONE 2026-08-23.** `.tag` in `index.html` fixed — **all 10 files now carry one identical rule**, verified: `grep` returns 10× `background:var(--bg-sunken); color:var(--text-primary)` and nothing else. Three separate dispatches plus this leftover, and **none invented a variant, because each was handed the same target string** — the failure mode that produced 30 phantom variables, avoided by construction. Android CTA → *"Open app settings"*. **The loosened assertion is restored to a single value** and the agent quoted it before and after, as asked. **Useful nuance it found rather than assumed: iOS correctly says "Open Settings" because iOS routes to the SYSTEM Settings app, while Android opens the app's own info page.** So the two platforms legitimately differ, which is why the drift looked plausible. It reported and did not "fix" iOS. — 
| T-273 | The last red test is the web twin of a clock bug fixed on mobile this morning | assistant | — | web-agent | P1 | — | IN_PROGRESS | — | **The single remaining suite failure, and it is the orchestrator's scoping error coming back.** `shell.test.tsx` → *"the menu holds only rows this data model can fill"* expects `Today 2, Upcoming 1` and gets **`Today 3, Upcoming`** — one task moved out of Upcoming into Today. **Cause: the test seeds `upcomingTask`, whose due date is the frozen `T0 = 2026-08-16` plus seven days = 2026-08-23. Today is 2026-08-23.** The seeded "upcoming" task is due today. **This test went red DURING this session, when the clock rolled past midnight.** **T-242 fixed this exact defect on mobile this morning** by threading a fixed `now` through instead of reading the real clock. **Web shares the fixture and never got the same treatment, because T-242's scope was the one mobile test — my error, not any agent's.** **Every agent that saw this called it "pre-existing, unrelated". Pre-existing is right; unrelated is not.** **The T-242 note predicted the shape and I did not check for siblings:** *"from 24 Aug the row goes overdue and the assertions break for a different reason entirely."* That is two days away for this one too. |
| T-275 | design-check is blind to a CSS variable that is used but never defined | assistant | — | design-agent | P2 | T-252 | IN_PROGRESS | — | **Fifth blind spot, and the sharpest demonstration yet of why "0 failed" is not the same as "correct".** While `--dur-breath` was undefined and seven animations were inert, **`design-check` reported 202 passed, 0 failed.** The new `token-literal` check catches a variable holding a literal; **nothing catches a variable that is consumed and never declared.** **This is the third time today the tool said green over something broken:** the render tier skipping silently and printing `13 passed, 0 failed`; the catalogue assertions being scoped to the app shell so a control drawn elsewhere is unchecked; and now this. **Proposed assertion: every `var(--x)` in a stylesheet resolves to a declaration in that stylesheet or in `:root`.** It is a pure text check — no browser needed — and it would have caught this the moment the token was deleted. **Build it the way the other three were built: break it first.** Delete a variable declaration, watch it go red, restore it, watch it go green. |

<!-- archived 2026-08-23 (second pass): the oldest DONE rows, taken by queue order.
     Safe by age because C3 already resolves `Depends` against TASKS.md UNION
     TASKS-archive.md (validate-state.sh:193-196) — T-149's fix is in place. The 28
     broken links seen earlier today were caused by this file being truncated in the
     same script that removed the rows, not by the cap/C3 tension. -->
| T-101 | Redesign the app as a whole against the UC coverage map (information architecture first) | assistant | — | design-agent | P1 | T-096 | DONE | docs/design/_shared/information-architecture.md | **Closed on evidence 2026-08-21.** `information-architecture.md:3` names T-101 as its author, and §115 records what a later pass **reverses in T-101's own drawing** — only writable if the drawing exists.  docs/reports/owner-decision-2026-08-17-conversation-is-not-a-list.md |
| T-102 | Conversation surface drops the task list — CONFIRMED at every width by the owner | assistant | F-001 | design-agent | P1 | T-101 | DONE | docs/design/assistant/screens/app-shell.html | delivered inside T-101; owner confirmed no desktop split pane |
| T-103 | Restate AC-1/AC-4: the guarantee survives, the mechanism changes | assistant | F-001 | spec-agent | P1 | T-102 | DONE | docs/specs/assistant/F-001-voice-assistant-view.md, docs/specs/assistant/index.md | **Closed on evidence 2026-08-21.** F-001 revision 4 **credits T-103 by name** in both the spec and `index.md`. **The output shipped and the row never moved — it has been blocking T-113 since.**  dropping them loses the guarantee; leaving them contradicts the build |
| T-107 | Build the new shell on web (split at 1024, Tasks surface, AC-31/32, Settings) | assistant | F-001 | web-agent | P1 | T-103 | DONE | src/assistant/web/, src/assistant/_shared/model/ | web 145->170; the ONE layout branch is a container query on the app root, and a test greps the four shell sources for innerWidth/clientWidth/matchMedia requiring ZERO hits so no second width-selected mechanism can grow there; AC-31 flash is asserted to END not just start; AC-32 tests read the LIST and never the message, plus a third reads the message with an EMPTY list to hold AC-1 from the other side; NOT_BUILT map of 9 ids each with its justifying artifact, and a second test asserting none is rendered so the exclusion list cannot quietly become ids-we-stopped-checking |
| T-108 | Build the new shell on mobile (peer paths, Tasks surface, close D8) | assistant | F-003 | mobile-agent | P1 | T-103 | DONE | src/assistant/mobile/ | 690/690 tree; OQ9 is ONE declared value held by 3 tests incl. a source scan requiring exactly one declaration and one caller, mutation-proved; collapsed its own independently-built collections/day-grouping model onto web mid-flight rather than leave one fact in two homes; strengthened a check that a surviving mutation exposed — "each id is wired into a component" passed while the delete id existed only inside touchProps(), so it now strips measurement helpers before scanning: measured is not rendered; 8 of 29 ids unbuilt with a test that fails BOTH ways |
| T-121 | Make Today a date, in code — one unit across _shared/api/web/mobile | assistant | F-001 | web-agent | P1 | T-120 | DONE | src/assistant/_shared/model/tasks.ts, src/assistant/_shared/controller.ts, src/assistant/_shared/testing/fixtures.ts, src/assistant/api/app.ts, src/assistant/web/components/TasksSurface.tsx, src/assistant/mobile/components/TasksSurface.tsx, src/assistant/web/__tests__/collections.test.ts | splitting leaves a window where Today is defined twice; subtree crossing sanctioned for this task |
| T-124 | OWNER: assistant-created tasks are invisible on the default surface | assistant | F-001 | orchestrator | P1 | T-121 | DONE | docs/reports/owner-question-2026-08-18-assistant-created-tasks-are-invisible.md, docs/reports/owner-decision-2026-08-18-four-buckets.md | docs/reports/owner-question-2026-08-18-assistant-created-tasks-are-invisible.md. Talk to the assistant, add a task, open Tasks: not there. add-in-context covered the manual composer and missed the PRIMARY path in a voice-first product |
| T-126 | Four buckets: amend ADR-009 — Upcoming added, Inbox becomes undated-only, overdue folds into Today | assistant | F-001 | architect-agent | P1 | — | DONE | docs/specs/_shared/adr/ADR-009-today-is-a-date.md | four date predicates, total and disjoint, day-granular. Measured: 7 of 737 live rows change bucket, all Inbox→Today, all overdue. Upcoming has no member in the store |
| T-128 | Landing summary + Lists menu + day groups after overdue folded into Today | assistant | F-001 | design-agent | P1 | T-126 | DONE | docs/design/_shared/components.md | count_secondary word changed (title_list already spent "more"); rule 4 struck not deleted; open_all split from inbox_count (two facts, errors in opposite directions); Upcoming row; new Overdue group, Inbox+Done go flat — Done would have shipped a NEW falsehood; skeleton asserting a heading it cannot know |
| T-129 | Implement four buckets: Collection, inCollection day-granular, groupTasks, dueAtForCollection | assistant | F-001 | web-agent | P1 | T-126,T-128 | DONE | src/assistant/_shared/model/tasks.ts, src/assistant/web/components/, src/assistant/mobile/components/ | 743/743 (was 709), tsc clean, 6 mutations proved the new assertions. Unreadable due_at answers Inbox or it would be in NO bucket; groupTasks takes the collection; todayGroupLabel deleted; two silent fallbacks became exhaustive switches |
| T-131 | Draw the owed mockup states: Upcoming row, Overdue heading, flat Inbox/Done, LSM-CLEAR-AHEAD | assistant | F-001 | design-agent | P1 | T-128 | DONE | docs/design/assistant/screens/app-shell.html, docs/design/assistant/screens/app-shell-ios.html, docs/design/assistant/screens/app-shell-android.html | design-check 60/60 with a real browser, id catalogues byte-identical, 202 mockup-parsing tests. Rendering caught two falsehoods reading could not: static h1 Today over every collection, and an Overdue heading indistinguishable from a date heading |
| T-141 | F-005 task detail — spec, full scope | assistant | F-005 | spec-agent | P0 | — | DONE | docs/specs/assistant/F-005-task-detail.md, docs/specs/assistant/index.md | 37 ACs, recurrence carries 11. Impact section found 13 field-enumerating sites not 2, and that the assistant write allowlist IS the diff source — a field omitted there emits an empty diff, an F-001 failure caused by F-005 silence. The source audit OVERTURNED an invented decision. 10 open questions |
| T-240 | The keyboard covers the composer on both phones | assistant | F-003 | mobile-agent | P0 | — | DONE | src/assistant/mobile/components/useKeyboardInset.ts, src/assistant/mobile/components/TalkSurface.tsx | **Owner hit it on a real device pass; reproduced on the iOS simulator with the hardware keyboard disabled. It is worse than *covered* — the composer is not visible at all.** Measured: `assistant-composer-input` stays at **y=788, bottom=830 on an 874pt screen** with the keyboard up, and the iOS keyboard occupies from roughly y=610 down. **It does not lift by a single pixel.** **The composer IS inside a `KeyboardAvoidingView`** (`TalkSurface.tsx:138`, inside 83-139), so this is not a missing wrapper. What the KAV lacks is **`keyboardVerticalOffset`**, while a `ShellBar` sits above it — the standard cause of a short lift, though a short lift does not explain a zero lift. **Android is a second, different suspect:** the manifest sets `windowSoftInputMode="adjustResize"` AND the KAV uses `behavior="height"`, two mechanisms doing the same job, which is a known conflict. **Context that matters:** RN 0.86.2, Expo 57, `newArchEnabled=true` — `KeyboardAvoidingView` behaves differently under the new architecture and edge-to-edge. **Diagnose before patching; do not add an offset and call it fixed without measuring the lift on both platforms.** **DONE, verified by the orchestrator in a fresh render on both platforms.** **Root cause:** RN 0.86's `KeyboardAvoidingView` compares parent-relative `onLayout` coordinates against screen-absolute `keyboardFrame.screenY`; under Fabric the two systems diverge and the difference comes out exactly zero — which is why the composer did not move at all rather than moving too little. Replaced with a `useKeyboardInset` hook over `Keyboard.addListener` + `useSafeAreaInsets`. **No dependency added** — `react-native-keyboard-controller` and `reanimated` stayed out. **Android needed a second fix the agent got wrong once:** `endCoordinates.height` reports only the key area (312dp), missing Gboard's ~76dp toolbar strip, so the composer stayed sliced in half; `endCoordinates.screenY` is the keyboard's true visual top. **The tempting repair was to add 76dp and it was refused** — correct on this emulator, wrong on any keyboard with a different strip (owner rule, T-291). Final: iOS composer bottom 575pt vs Q key 590pt; Android 1824px vs toolbar ~1860px. `useKeyboardInset.ts` contains zero hardcoded numbers. 306/306 mobile tests pass. |
| T-284 | Build the inline add-task row on web and mobile | assistant | F-009 | web-agent | P0 | — | DONE | src/assistant/web/components/TasksSurface.tsx, src/assistant/web/styles.css, src/assistant/web/__tests__/app.test.tsx, package.json | **The owner asked for this first, noticed it missing first, and it is the ONE piece of the v3 design that needs no backend at all** — it calls the create path that already exists. `tasks-inline-add` is in `a11y.ts` and in two test files and **no component renders it**; `app.test.tsx`'s `NOT_BUILT` map records it as *§ InlineAdd (T-244) — not built*. Drawn in `docs/design/assistant/screens/app-shell*.html` at the end of the task list. **Web first in this row; mobile is T-285 so the two can run in parallel.** Remove the entry from `NOT_BUILT` when it renders — that map is the contract for what is still owed. **DONE — verified rendered and visible by the orchestrator at 1280 with tasks present**, not just asserted. 366/366 web tests, `tsc` clean. **Agent decided two things:** the inline row is the primary add path once the list has content while the old form stays for the empty-state CTA; and blur commits when the field has content, cancels when empty, matching the existing rename behaviour. **One correction to the return: the `tsc` error it called pre-existing was introduced this morning** by T-240's keyboard fix — `react-native-safe-area-context` was imported by mobile source but never declared at the repo root, which already declares `react-native`, `-svg` and `-web`. Added at the harness's version; `tsc` now exits 0. |
| T-281 | Bind the four surviving animations to the new token, and delete the aurora band | assistant | — | web-agent | P1 | T-274 | DONE | `src/assistant/web/styles.css` · `src/assistant/web/components/VoiceSurface.tsx` | **The assistant`s thinking dots are alive again — verified in a browser by the orchestrator, not by reading the variable chain.** Computed styles at 1.2s: `.tdots i` → `tBlink/1.2s`, `.waveform i` → `wave/1.2s`, `.st-thinking .mic` → `orbBreath/1.2s`, `.queued-note .dot-pulse` → `qPulse/1.2s`, and `.aurora-band` → **`none/0s`**, deleted rather than rebound as `motion.rule` requires. `check-undefined-vars.sh` FAIL(7) → PASS(14 files clean); suite 1240/1240; `tsc --noEmit` exit 0. **The agent named the mechanism precisely and it is worth keeping:** an alias consumed 7 times and declared 0 times makes every `var()` resolve to the CSS initial value, so `animation-duration` computes to `0s` — the rule is syntactically valid, passes every text check, and animates nothing. That is why the new check tests resolution rather than presence. Alias named `--dur-pulse` to match `--dur-micro`/`--dur-standard`/`--dur-emphasis`; the one surviving mention of `auroraBreath` is a comment recording what was retired, which the agent kept deliberately and said so. |
| T-300 | Screen 1 round 2, mobile: the same defects plus two of its own | assistant | F-003 | mobile-agent | P0 | — | DONE | src/assistant/mobile/components/TaskList.tsx, src/assistant/mobile/components/TasksSurface.tsx, src/assistant/mobile/components/AssistantScreen.tsx, src/assistant/mobile/components/styles.ts | **Shares items 2, 3, 5, 6 and 7 with T-299 — read that row and match its answers rather than inventing a second set.** The row alignment is worse here: the checkbox sits **36px** above the row's centre, against 11 on web, because the wrapped due date makes the row twice as tall. **Two defects are mobile-only.** **(a) Android is rendering iOS font sizes.** The last pass added per-platform sizing but `PlatformContext` is never provided by `AssistantScreen` or `.mobile-app/App.tsx`, so the default wins everywhere — the code is right and nothing supplies it. **(b) The header has no `Search` and no `⋯`.** Web has both controls placed (behind nothing yet); mobile has neither, so the two clients disagree about what the bar contains. **Place them; do not build what sits behind them.** |
| T-303 | Mobile: the empty block still hangs at the top of the screen | assistant | F-003 | mobile-agent | P1 | T-302 | DONE | src/assistant/mobile/components/styles.ts | **T-302 fixed the heading size and the icon's optical alignment, then ended without returning a report** — it modified `styles.ts` and stopped, so what it believed it had finished is unknown and everything here was verified by the orchestrator measuring, not by its account. **Remaining: `No tasks yet` sits just under the bar, a wide gap, `+ Add a task`, then the whole screen empty below.** Measured on iOS at 402x874: heading top ~138pt, add row ~187pt, nothing to 874. **The gap between the two lines is larger than the space above the heading, which inverts the grouping** — two lines that belong together read as separated. Mockup decides the vertical placement; spacing from tokens. **DONE, and the mockup answered the question rather than the build being wrong: `.empty` is `padding: s7 0 s6`, top-anchored, with no centring rule for `phone-tasks-empty`.** So the empty lower three quarters is what the design draws; if the owner dislikes it, that is a design change and not a defect. Heading-to-field gap moved from `space[6]` 32 to `space[3]` 12 — the 32 was a bottom padding meant to sit under a body paragraph that mobile's ET-FIRST drops, so it landed between the heading and the field and inverted the grouping. **Icon ink confirmed level (Lists centre y=246 against Today y=245.5) and both bar controls present.** **One correction to the orchestrator's own briefing: it asked for `No tasks yet` to be SMALLER than `Today`, which has no basis — the mockup sets the bar title at 17px and the empty heading at 28px, so the design deliberately makes it larger.** The bar title is persistent chrome; the empty heading is the screen's content. Build currently has them equal, matching neither. Owner's call. |
| T-301 | Web: the panels are square and a priority prints as punctuation | assistant | — | web-agent | P1 | — | DONE | src/assistant/web/styles.css, src/assistant/web/components/TasksSurface.tsx, src/assistant/web/components/ConversationPane.tsx | **Two defects seen in the render after T-299.** **(a)** The Tasks and Talk columns are square and touching, split by a thin vertical line — **the exact line the panels-on-a-canvas pattern replaced two days ago in the mockups.** `tokens.json § layout.panel` gives radius 12 and gap 4. Check every region, not the two that are visible. **(b)** A high-priority row renders `Call mom !!!` with the marks glued to the title, while the conversation block beside it renders the same value as the word `high`. **Two representations of one field, and the row's reads as something the user typed into the name.** |
| T-098 | Owner reversal: personal lists — a data-model change, not just UI | assistant | — | architect-agent | P1 | T-096 | DONE | `docs/specs/assistant/data-model.md` · `docs/specs/assistant/api-contracts.md` · `docs/specs/assistant/F-008-lists.md` | **Closed on evidence rather than dispatched — T-286 had already done it.** The row says *"task has no project/list/tag field; status is the only grouping"*, which was true when written and is not now. `data-model.md:233` states **"`lists` and `tasks.list_id` now exist (F-008)"**, `:373` defines the field with its FK, its null-means-Inbox rule, the step prohibition (AC-13) and the delete-cascade to `null`, and `:379` records that 0 existing rows carry one so no migration is needed. `api-contracts.md` carries `POST/GET/PATCH/DELETE /lists`. **Checked before dispatching an architect at it**, because a briefing built on this row would have asked for a data-model change that is already in the file — the same wasted-dispatch shape recorded in MEMORY today. |today|done|archived) is the only grouping. Touches every place that groups by status |
| T-142 | F-005 Gate 1 round 1 — multi-lens spec review | assistant | F-005 | orchestrator | P0 | T-141 | DONE | docs/reports/gate1-lenses/F-005-consolidated.md, docs/reports/gate1-lenses/F-005-tester.md, docs/reports/gate1-lenses/F-005-dev.md, docs/reports/gate1-lenses/F-005-architect.md, docs/reports/gate1-lenses/F-005-product.md, docs/reports/gate1-lenses/F-005-design.md, docs/reports/owner-decision-2026-08-18-f005-gate1.md | REJECT. 20 HIGH, 7 convergences found independently by 2+ lenses, 4 owner decisions taken |
| T-143 | F-005 revision 2 — Gate 1 round 1 findings + 4 owner answers | assistant | F-005 | spec-agent | P0 | T-142 | DONE | docs/specs/assistant/F-005-task-detail.md, docs/specs/assistant/index.md | Revision log at docs/reports/gate1-lenses/F-005-revision-2-log.md is recorded on T-148 — docs/reports/ is orchestrator-owned and the briefing named the path (see Drift Log). 64/64 round-1 findings dispositioned resolved (20 HIGH, 34 MED, 10 LOW — consolidation's header said LOW 8, its own table sums 10). 37→46 ACs, none renumbered or deleted; Impact 9→13 subsections; API Touch Points' false 'no new endpoints' corrected. declared-elements.sh exit 0 (18 fields), 46 AC ids no gaps/dups, ## Links block byte-identical (verified). Two new open questions: OQ11 (mobile reminder surfacing — a collision between owner answers 1 and 3, default taken), OQ12 (note indicator; rev-1's market claim was false and is withdrawn) |
| T-145 | F-001 AC-24 — the 'zero actions above the split' clause is false while the F-005 detail is open | assistant | F-001 | spec-agent | P0 | T-143 | DONE | docs/specs/assistant/F-001-voice-assistant-view.md | Revision 5, amended in place: bound unchanged (at most one action), its REASON moved off the per-width claim and the detail-open state given coverage — closing the detail is the one action, carrying AC-24's not-hidden/not-disabled condition. 32 ACs, none added/renumbered/deleted; declared-elements exit 0; ## Links byte-identical; no layout branch. AC-25/AC-31/AC-32 checked, consistent, unamended. Two states recorded not settled: OQ10 (F-005 AC-2 holds the detail open over its OWN failed write — a second instance of L-015), OQ11 (AC-31's door while the detail is open) |
| T-146 | F-005 Gate 1 round 2 — multi-lens spec review (LAST ROUND, cap reached after it) | assistant | F-005 | orchestrator | P0 | T-143, T-145, T-153 | DONE | docs/reports/gate1-lenses/F-005-r2-consolidated.md, docs/reports/gate1-lenses/F-005-r2-tester-api.md, docs/reports/gate1-lenses/F-005-r2-tester-web.md, docs/reports/gate1-lenses/F-005-r2-tester-mobile.md, docs/reports/gate1-lenses/F-005-r2-dev-backend.md, docs/reports/gate1-lenses/F-005-r2-dev-web.md, docs/reports/gate1-lenses/F-005-r2-dev-mobile.md, docs/reports/gate1-lenses/F-005-r2-architect.md, docs/reports/gate1-lenses/F-005-r2-design.md, docs/reports/gate1-lenses/F-005-r2-product.md | REJECT. 9 lenses (5 in round 1 — mobile tags made three new lenses applicable), 99 findings: 40 HIGH, 44 MED, 15 LOW. All nine returned a checked: list and examined all 48 ACs. 10 convergences; the clock/zone one was found independently by SIX lenses. The round-2-only question (is a claimed disposition really in the spec?) found 4 unlanded and 1 unexecutable. **ROUND CAP REACHED — escalated to the owner** |
| T-152 | information-architecture.md has no failure/empty/loading entry for the detail surface + the design-system debt F-005 named | assistant | F-005 | design-agent | P1 | T-143 | DONE | docs/design/_shared/components.md, docs/design/_shared/DESIGN.md, docs/design/_shared/information-architecture.md, docs/design/_shared/tokens.json | phase: system. **§ CarriedNotice is a SIBLING of § SaveNotice, not a widening** — SaveNotice clears on leaving the surface, which is precisely what AC-47 forbids; widening it would give one component two lifetimes told apart by a flag. Placed frame-level OUTSIDE the stacking layer so S3/S4/S5 slide under it, which is what makes 'visible on Settings' true rather than three-of-five. **Accent question DECIDED, not routed: the set is closed at five** — AC-9 and AC-39 carry their marks without colour, and DESIGN.md now says so as a rule. Row mark budget is ONE list of four with a fixed order. IA §2/§3/§4/§6 carry S6. § Buttons gains 'put back' plus a neutral variant (contrast computed: 13.28/13.34 label, 5.01/5.78 boundary). design-check 60/0/0 with token-drift reading 98 vars per mockup — non-zero, so L-007's silent-scan defect is excluded, and it re-verified that its own DESIGN.md edit did not move the threshold regex |
