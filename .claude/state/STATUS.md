# Project Status
<!-- Orchestrator reads this every session. Sub-agents do NOT read it. -->
<!-- Orchestrator updates it from sub-agent return summaries. -->
<!-- Keep under 100 lines. This is a live snapshot, not history. -->

## Last Updated
- **By**: orchestrator
- **At**: 2026-08-18
- **Session**: Gate 1 CLOSED for F-005 after three review rounds and four revisions. Next: T-157 (F-001 AC-31's gate, blocks architecture), then T-160. Earlier: T-143, T-145 and T-153 all DONE; two owner decisions taken (detail trap, message door). T-146 — Gate 1 round 2, the LAST round — is unblocked. Earlier note: T-143 returned DONE — F-005 revision 2 on disk, verified. Next: T-145 (blocking), then T-146 (Gate 1 round 2, last round). validate-state green; eval 17/17 — R9 passes for the first time.

---

## Phase
<!-- Current phase of development -->
`DISCOVERY` | `DESIGN` | `ARCHITECTURE` | `BUILD` | `VALIDATE` | `SHIP`

**Current**: DISCOVERY

---

## In-Flight
<!-- Orchestrator's working set: sub-agents currently dispatched. -->
<!-- Rule: orchestrator does not spawn two sub-agents whose {src}/{module}/ subtrees overlap. -->

| Task ID | Agent | Module | Feature | Subtree | Dispatched |
|---------|-------|--------|---------|---------|------------|
| T-245 | spec-agent | assistant | F-009 | docs/specs/assistant/ | 2026-08-22 |
| T-249 | design-agent | assistant | F-009 | docs/design/ | 2026-08-22 |
<!-- Example:
-->
| — | — | — | — | — | — |

---

## Blockers
<!-- Anything stopping progress. Orchestrator resolves these. -->

| ID | Description | Waiting On | Raised By |
|----|-------------|-----------|----------|
| design-check render tier | Playwright 1.62.1 expects chromium build 1234; the container ships 1194, so the browser will not launch and layout/state/contrast/overflow checks SKIP while the summary still reads "0 failed". Pass `DESIGN_CHECK_BROWSER=/opt/pw-browsers/chromium-1194/chrome-linux/chrome` — 13 passed becomes 175. | Nothing — workaround is known and works | orchestrator |

---

## Handover

**Start here: [docs/reports/HANDOVER-2026-08-18.md](../../reports/HANDOVER-2026-08-18.md).** Sessions before 2026-08-19 ran from a different repository; that file carries the day's four owner decisions, F-005's Gate 1 verdict (REJECT, 20 HIGH), the six open questions still owed an answer, and the queue rows whose premises went stale the same day. **Next action: T-143.**

---

## Agent Results
<!-- Sub-agents return summaries; orchestrator records them here. -->
<!-- Recent entries only — archive old results when the file grows. -->
| Date | Agent | Task | Status | Summary | Next Action |
|------|-------|------|--------|---------|-------------|
| 2026-08-22 | design-agent | T-248 | DONE | space.2 padding on .row, so the ground fills the padding box in every state. Re-measured here: cb=8, title=40 for all rows, selected and not. Delivers the owner's extra left margin in the same change. design-check 175/0/4, suite 1221/1240. | Owner asked: row content now at 8 while headings and the inline add row stay at 0 — does the split rail read as a deliberate indent? T-245 still blocked on the done-row product call. |
| 2026-08-22 | design-agent | T-247+T-246 | PARTIAL / DONE | Menu anchored (gap 68→4, overhang 82→0, all 3 platforms) and T-246 complete. But the checkbox inset was added to the CONTENT, so selecting a row shifts its checkbox and title 4px right and the columns jog — the owner's own 'layout ko đẹp' complaint in a new place. Caught by looking at the render; the agent measured inset 0→4 and declared 'Not checked: visual review'. | T-248 dispatched: the 4px must come from the ground extending outward, not the content moving in. |
| 2026-08-22 | 3 lenses (dev/tester/spec) | Gate 1.5 F-009 | DONE | **0 HIGH from all three — nothing blocks implementation.** 3 MED (two of them the same finding reached independently) + 3 LOW. All 14 briefed ACs have a drawn state. design-check 175/0/4. Report: docs/reports/gate15-lenses/F-009-consolidated.md | T-245 filed and BLOCKED on one owner product call; T-246 filed for the testid gap plus the three LOWs. Owner design signoff is the remaining gate and it blocks every implementer. |
| 2026-08-22 | design-agent | T-225 | DONE | phase: screens. 10 states x 3 shell mockups, 19 testids, 14 ACs. design-check 175/0/4; 54 states/53 renderings; suite 1221/1240 unchanged. The 1920 row gap is measured at 395px inside an 820px row. | Gate 1.5 dispatched (dev + tester + spec). Owner design signoff follows and blocks every implementer. |
| 2026-08-22 | design-agent | T-244 | DONE | phase: system. Six component sections published for F-009 (SearchField, Search empty state, OverflowMenu, SelectionMode, BulkActionToolbar, ConfirmDialog), 13 ACs covered, 20 new testids, 4 drifted shell ids reconciled with reasons. No tokens added, no screens drawn. design-check 175/0/4; suite 1221/1240 unchanged. | T-225 (screens) unblocked. T-233's a11y cluster now has an exact work-list and is a src/ fix, not a docs one. |
| 2026-08-22 | mobile-agent | T-242 | DONE | Frozen-date-vs-moving-clock fixed in two lines; fixtures.ts untouched. Suite 1220/1240 → 1221/1240, 19 failures. Date independence re-verified by the orchestrator at 23 Aug, 25 Aug and Mar 2027 — 41/41 each. Agent also threaded `now` through tasksSurfaceView, which the briefing had flagged as the likely second half of the defect. | Owner decisions A and B both closed. T-233 holds the remaining 19, most waiting on T-225. |
| 2026-08-22 | spec-agent | T-243 | DONE | F-009 rev 2: AC-15 re-tagged (web, mobile) as sequential navigation. Web unweakened; mobile names VoiceOver/TalkBack per-control reachability and forbids the row collapsing its children. Links itself to AC-6's deferral. declared-elements exit 0, numbering intact, F-001 untouched. | Owner decision A closed. T-242 still in flight. |
| 2026-08-22 | mobile-agent | T-232 | DONE | v1 colour shim deleted; 42 retired-key sites across 11 mobile files renamed to v2, each visual change citing the rule that decided it. 4 of 42 render differently (checkboxDone, TaskList tick, badgeNew, chipNew). Full suite re-run by the orchestrator: 1220/1240, identical to the T-233 baseline, zero new failures. | T-241 filed for two mockup divergences the agent found and left alone. T-233 (the 20 baseline failures) still PENDING at P2. |
| 2026-08-22 | spec-agent | T-234 | DONE | F-009 rev 1: AC-6 keeps drag as the only reorder and now carries the screen-reader deferral bounded to reorder alone; AC-15 new for Tab + visible focus; lists-vs-tasks inconsistency accepted in Out of Scope. 14→15 ACs, none renumbered. declared-elements exit 0. F-001 AC-33 untouched, verified by diff. | **Owner decision open: AC-15 is web-only, so mobile row/control reachability is specified nowhere — the promise AC-6's deferral rests on.** T-225 unblocked. |
| 2026-08-19 | qa-api-agent | T-166 | DONE | F-005 QA api: 35 TCs + 154 automated cases, api tier 202/202, the three __qa__ doors built as one implementation. TC-14/TC-17 fixed at the harness with the zoneless agent kept and used. Seven assertions red on first authoring, none weakened. | T-173 filed (AC-26 edge). T-167 still waits on T-165 (screens), which waits on T-164 (mobile, in flight). |
| 2026-08-19 | web-agent | T-163 | PARTIAL | F-005 web: S6 detail, receiver clause, AC-47's notice rules, one clock widened not duplicated, zero JS width reads. web 366/366, full suite 1017/1020 with all 3 failures owned elsewhere. PARTIAL for AC-3's proactive save, unadopted testids, placeholder copy. | T-164 (mobile) dispatched with the 8 items web named. T-171 filed for the AC-3 gap. |
| 2026-08-19 | backend-agent | T-162 | DONE | F-005 backend: ten endpoints (four new), api 202/202, tsc clean. Guard moved onto the write behind a branded type. Attribution of the red full suite PROVEN with a HEAD worktree, not asserted. | T-164 (mobile) still waits on T-163 (web, in flight). T-166 extended with the two docs/qa/ fixes it named; T-170 filed for the contract gap. Full-suite verdict held until web returns. |
| 2026-08-19 | design-agent | T-152 | DONE | phase: system. § CarriedNotice published as a sibling of § SaveNotice with the reason; the accent question decided (set closed at five, marks carried without colour); IA §2/§3/§4/§6 carry the detail surface; § Buttons gains 'put back' + a neutral variant. design-check 60/0/0, scan confirmed non-zero. | T-165 (screens) unblocked, which in turn unblocks T-167 (qa web+mobile). T-168/T-169 filed. Three owner-facing questions raised, held until backend+web return. |
| 2026-08-19 | architect-agent | T-160 | DONE | F-005 architecture: 6 ADRs, contracts + data model + three platform docs. All 12 recorded questions answered, none deferred. ADR-010 amends ADR-005 and creates the account entity its premise assumed. | Phase 4 unblocked: T-162 backend (P0), T-163 web, T-164 mobile, plus QA authoring. Design T-152 can run in parallel. |
| 2026-08-19 | spec-agent | T-157 | DONE | F-001 revision 7: AC-31's gate is now existence, not the collection on screen; both client predicates named by path. T-140 closed against the same evidence. Verified on disk. | **T-160 (architecture) is unblocked — the last thing holding it is gone.** T-161 filed; T-151/T-152 extended. |
| 2026-08-19 | spec-agent | T-159 | DONE | F-005 revision 4: 58/58 dispositioned (45 fixed, 12 recorded for architecture, 1 new OQ). Gate 1 closed. Agent stalled after finishing; work verified on disk and recorded on evidence. | T-157 (F-001 AC-31 gate) is next and blocks T-160 (architecture). |
| 2026-08-18 | orchestrator | T-155 | DONE | Gate 1 round 3 (targeted), 9 lenses: 58 findings vs round 2's 99. Dispositions landed; the amendments introduced the damage. Constraint held — no AC added. | **Owner decision needed: the third round has run and the residue is concentrated, not converged.** |
| 2026-08-18 | spec-agent | T-154 | DONE | F-005 revision 3: 99/99 findings dispositioned, **no AC added** (48 before and after). OQ6 closed on the owner's answer. Agent's PARTIAL was a checker defect, not a spec defect — reproduced, fixed, re-verified. | T-155 (targeted re-review) is unblocked; its dispatch set derives from the revision-3 log. T-156/157/158 filed. |
| 2026-08-18 | orchestrator | T-146 | DONE | Gate 1 round 2, 9 lenses: REJECT, 40 HIGH / 44 MED / 15 LOW. 10 convergences; clock+zone found by six lenses independently. Four claimed dispositions did not land; one (the accent directive) is unexecutable. No conflicts between lenses. | **ROUND CAP REACHED — the owner decides whether to spend a third round.** OQ6 is now blocking per the product lens. |
| 2026-08-18 | spec-agent | T-153 | DONE | Both owner composition-answers folded into F-005 (46→48 ACs, AC-2 narrowed) and F-001 (rev 6, 32 ACs unchanged, OQ10/OQ11 closed). Verified on disk; ## Links untouched on both. Orchestrator appended the round-2 addendum to the revision-2 log and recorded both memory entries. | **T-146 (Gate 1 round 2) is unblocked and is the next action — it is the LAST review round F-005 gets.** |
| 2026-08-18 | spec-agent | T-145 | DONE | F-001 revision 5 — AC-24's reachability clause amended in place. Bound unchanged; the reason moved off the per-width claim and the detail-open state got a route (close the detail). Verified: 32 ACs unchanged, declared-elements exit 0, ## Links byte-identical, +5/-2 lines. | T-146 (Gate 1 round 2) is now unblocked. OQ10 + OQ11 are owed the owner. T-150/T-151/T-152 filed. |
| 2026-08-18 | spec-agent | T-143 | DONE | F-005 revision 2. 64/64 round-1 findings resolved (20 HIGH). 37→46 ACs, none renumbered; Impact 9→13 subsections; the false "no new assistant endpoints" corrected and the restore path stated. Verified on disk: 3 artifacts present, declared-elements.sh exit 0, 46 AC ids no gaps/dups, ## Links byte-identical. | T-145 (F-001 AC-24 amendment, blocking) then T-146 (Gate 1 round 2 — last round). OQ11 + OQ12 are new and owed the owner. |

---

## Drift Log
- 2026-08-22 **T-218's dispatch was interrupted and its return is lost — no `---METRICS---`, no `visual_review:`, no `a11y_review:`.** Status derived from the disk rather than re-dispatched (L-018, third use), and **all three changes had landed**: the gutter is one rule at `16 → 374` for every block, AM/PM is `flex-direction: row` and still 48×48, the placeholder is `No name yet`, design-check 137/0. **What is genuinely lost is the self-review** — the two check blocks this project just made mandatory, and the agent's answer on the title's size. **A re-dispatch would have redone work that was already correct on disk; accepting it silently would have credited two self-checks that may never have run.** Recorded as done on evidence, with the unanswered half carried to T-219.
- 2026-08-21 **The visual-language redesign renamed two token keys and took 466 tests offline; two dispatches ran past it.** Measured: the suite runs **774 of 1240**, with **16 test files failing to load** — `theme.ts:45-46` reads `tokens.font.family.display`/`.body`, and commit 9e0aef7 renamed them to `ui`/`numeric`. **Why it survived: a design dispatch returns `tests_passing: 0, tests_total: 0` by design — it does not run the suite, and neither did the orchestrator between the two.** The lesson is not "run the tests" but which file it was: **`tokens.json` is imported by `src/` at runtime, so it is not a design-only artifact, and renaming a key in it is a code change wearing a design change's clothes.** T-207. Also: `a11y.test.ts` — the file that guards the mobile id catalogue — is one of the sixteen, so the id assertions design-agent most needed could not run, which is why its confidence is MEDIUM and honestly so.
- 2026-08-21 **Orchestrator error, caught by spec-agent (T-188): the round-2 consolidation dropped ten of thirty-seven findings, one of them HIGH.** Measured after the fact — the nine lens files carry 37 findings (4+7+4+3+4+4+3+4+4); the consolidation names 25 by id. The lost HIGH was **product F2**, an unbounded assistant read that speaks step titles and breaks ADR-013. **It survived only because the briefing told the agent to read the nine files rather than the consolidation** — which is L-009 paying for itself, one dispatch after I wrote the briefing line. **Second loss of this kind in one gate**: the first dropped a `would_not_be_a_finding_if:` field that a later briefing then told an agent to use. Same root, and it is mine: **the consolidation is written by the orchestrator under compaction pressure and has no count to check against, so a dropped finding leaves no trace at all.** Durable half is **L-028**, whose remedy is two cheap counts — state the per-lens totals in the consolidation, and have the revision agent reconcile before writing.
- 2026-08-21 **Six rows had been sitting `IN_PROGRESS` with nothing running, and five of them were finished.** Measured on disk rather than re-dispatched (L-018): `uc-coverage-map.md` is 34,934 bytes (T-096); F-001 revision 4 **credits T-103 by name** in both the spec and the index; `information-architecture.md:3` names **T-101** as its author and is marked *revised T-105/T-106*; L-012 and `controller.test.ts:568` carry **T-099**'s finding. **Only T-089 was genuinely unfinished** — BUG-006 still reads `| Status | open |` — so it went back to PENDING rather than being credited. **The cost was not the six rows: T-103 alone had been blocking T-113, and T-096 blocking three P1 rows, for days.** The generalisable half: **nothing in the validator compares `IN_PROGRESS` against `STATUS.md ## In-Flight`.** A row can claim to be running forever with no agent behind it and 818 checks stay green — a gap in the C-checks, not a housekeeping lapse.
- 2026-08-21 **Orchestrator error: the persisted product-lens file dropped a field the next dispatch needed.** T-187's briefing told spec-agent that product F4 carries a `would_not_be_a_finding_if:` line and to use it as the shape of the edit. **It does not — because I dropped it when compacting the lens return into `docs/reports/gate1-lenses/F-006-product.md`.** The agent looked, could not find it, used the owner decision instead, and flagged the discrepancy rather than inventing one. **Nothing was lost this time; the durable point is that the persisted lens file is what every downstream dispatch reads, so compacting a return is not archiving it — it is rewriting the input to the next agent.** L-009 says persist the individual returns because clustering drops what one lens saw alone; this is the same failure one level down, inside a single return. Going forward: when a lens return is persisted, the `would_not_be_a_finding_if:` and `directive:` fields are the two that must survive verbatim — they are the only ones a revision agent acts on.
- 2026-08-21 **T-177's return carried no `memory_entry:` / `agent_memory_entry:` field at all.** The completion protocol requires the field; `none` is a legitimate value and silence is not. Work verified real and recorded DONE on evidence. Noted because this is L-015's shape and the reason the memory system sat empty for the project's first two weeks: nobody noticed the field was missing. **Nothing to record on the orchestrator's side either way — but the absence is the signal, not the emptiness.**
- 2026-08-19 **A template sync erased the L-016 fix, and the check went back to failing hardest on the most complete specs.** Commit 0097bd1 restored `declared-elements.sh` line 73 to the `printf | grep -qF` pipeline under `set -uo pipefail`; F-005 then reported **10 false orphans** including `title`, `note`, `priority` and `due_at` — fields a dozen ACs each constrain. Caught here only because T-177's briefing names the check as a success criterion and it was run before dispatching; **had it been run by spec-agent instead, the agent would have spent a dispatch inventing ACs for fields that already have them.** Re-applied and verified both directions (21/21, exit 0; a planted orphan still caught). **T-156 raised P1 → P0** — this is the third writing of the same patch, and L-014's shape: a remedy that depends on someone remembering is not a remedy. Also worth noting the failure is *silent and size-dependent*: below ~64 KB of haystack the reverted code is correct, so a small spec would have passed and told nobody.
- 2026-08-19 **Commit b970aad carries a false message and it is not being rewritten.** Another session staged an eight-file `qa/**/automation/** → tests/**` move while this one was committing; a `git commit` with no pathspec swept them under a message about five metrics files. HEAD was briefly broken — files at `tests/`, configs still naming `qa/assistant/automation/` — and 8674719 closes that window and carries the correction. History left intact deliberately: rewriting while a second session is live in the same tree is the larger risk. **The rule that follows is `git add` by explicit path, never `-A`, in any repo that may have a second session in it.**
- 2026-08-19 **The red screen the owner saw on the simulator was caused by the orchestrator, not by the app.** To attribute the F-003 regression, `git stash push src/assistant/mobile src/assistant/_shared` ran while Metro was watching those exact paths with Fast Refresh on. Metro hot-swapped modules into the LIVE app twice (stash, then pop) and it red-boxed `TypeError: Cannot read property 'used' of undefined` in `carriedRows` — new `carried.ts` reading state built by the old reducer's `initialState`, which had no `undoOffer`. **Not reproducible on a clean bundle:** relaunched cold and re-drove the whole path — seed → Tasks → Inbox → delete → `Deleted "Buy milk". / Put back` → tap → `"Buy milk" is back on the list.` with the row restored. One ERROR line in the entire session log, sitting immediately after the Fast Refresh burst and before the relaunch. **The trap is that hardening `offer !== null` would have looked like the fix** — weakening correct code to silence an artefact of the measurement. Durable half is **L-025**. No task filed: there is no defect.
- 2026-08-19 **T-164's return never arrived — the session was compacted while mobile-agent was in flight, so there is no `---METRICS---` block, no `memory_entry:`, no `unresolved:`.** Status was derived from the disk instead (L-018's method, second use): source present and typechecking, `npx vitest run src/assistant/mobile` 299/306, full suite 1232/1240. Two defects found this way that the lost return would have had to disclose. **(1)** its own new test file calls `controller.dispatch2(...)` six times — a method that exists nowhere in `src/`; the real `dispatch` is `protected`. Plus `carriedRowFor`, `announcer.spoken`, `task({local:true})`, none of which exist. 20 tsc errors in that one file. **(2)** a live regression in F-003: `assistant-row-badge` is no longer rendered, so QA's `F-003-mobile-surface.spec.ts` drops 124/124 → 123/124. Attributed by stashing only `src/assistant/mobile` + `src/assistant/_shared` and re-running: 124/124 clean. **The generalisable half is not the defects, it is that neither would have been known at all if the status had been taken from the agent's word** — and the word was never delivered. Recorded PARTIAL, follow-up T-176. Separately: the app was built and driven on an iOS 18.6 simulator the same hour and the *shipped* behaviour is sound — four collections, per-row accessible names, AC-47's `Deleted "X". / Put back`. A broken test file and a working app are not a contradiction; they are why the tier exists.
- 2026-08-19 **Ran the app (web) for the first time since F-005 landed and it surfaced two defects no test had.** (1) `/account` was absent from the vite dev proxy, so `loadAccount` threw during `init()` and the task list rendered empty despite three seeded rows — the unit suites use a fake server, so nothing covered the proxy. Fixed directly (one config line, ops-class) and recorded as T-175. (2) The row's urgency mark renders **`!!!`**, the graduated scale **AC-9 forbids by name** and which design's TR-URGENCY replaced with a single `!` for `high` only — **and a test asserts `.toBe('!!!')`, so the forbidden rendering is protected by a green test.** The accessible name is correct, so AC-33's 4.1.3 case passes while AC-9's visual requirement fails. Filed as T-174; it is L-021's shape for the third time.
- 2026-08-19 **orchestrator error, caught by qa-api-agent (T-166): the T-166 briefing quoted a failure set of three and named `src/assistant/mobile/__tests__/shell.test.ts` as one of them.** The agent measured **ten** and reported that the named file was not present when it looked. This is L-022 recurring **after** L-022 was written — and it sharpens it: during parallel work even *naming a failing file* is unreliable, not just quoting a count, because the other agent is reshaping its own test files. The briefing should have said "measure your own baseline; mobile-agent is in flight in `src/assistant/mobile/`" and named no failure at all. The agent also counted **31** api-tagged ACs where the briefing said 30; 31 is correct.
- 2026-08-19 design-agent (T-152) **self-reported a mistake it caught and fixed**: it drafted the urgency mark as the graduated `!`/`!!`/`!!!` scale that AC-9 forbids **by name**, because its read of AC-9 had been truncated and the prohibition is in the tail. Nothing in the toolchain would have caught it — design-check knows tokens, contrast, testids and state coverage, not what an AC forbids, and C11/C14 both pass a graduated scale. Durable half is **L-021**; the role-specific half is in `.claude/memory/design-agent.md`.
- 2026-08-19 architect-agent (T-160) noted two things it correctly did not fix: **`MANIFEST Paths.unit_tests` resolves to `src/assistant/__tests__/`, which does not exist** — the real layout is per-layer (`src/assistant/{api,web,mobile}/__tests__/`) and the platform docs have used per-layer commands since F-001, so reviewer C9 will see the disagreement; and **`api/__tests__/tasks.test.ts:74` asserts `POST /tasks` rejects `reminder_at`, which is exactly the gap F-005 closes** — it must invert, and the contract names it so nobody meets it head-on and weakens it. Also: the return reported `tests_passing: 560` from `vitest run src/assistant`; the full suite is **771/771** (27 files) and was verified green by the orchestrator. Architecture wrote no code, so the suite is a baseline check rather than evidence of the work.
- 2026-08-19 T-159 (spec-agent): the harness reported `failed: stalled, no progress for 600s`, but **the entire task was on disk and complete** — 805 lines, 48 AC ids contiguous, `## Links` byte-identical, declared-elements exit 0, and a 22 KB revision log carrying all 58 findings with a finished closing section. The stall came after the work, during a verification read. Recorded DONE on evidence rather than re-dispatched: **a blind re-run would have amended an already-amended spec**, duplicating every fix. The `---METRICS---` block, `memory_entry:` and `unresolved:` are lost with the return, so this dispatch reads as `status: unknown` on the dashboard. Durable half is **L-018**.
- 2026-08-18 orchestrator edited `.claude/tools/spec-check/declared-elements.sh`: the `printf | grep -q` under `pipefail` reports SIGPIPE as failure past the 64 KB pipe buffer, so it named nine F-005 fields as unaccounted orphans while each appears 11-43 times in the ACs. Independently reproduced before accepting the agent's diagnosis (`grep -qF` returns 141 on the 118 KB haystack), fixed with a `case` glob that uses no pipe, and verified both directions (real orphan still caught). **Tooling infra, not a task artifact** — recorded here, with L-016 for the durable half and T-156 to port it upstream before the next upgrade erases it.
- 2026-08-18 `docs/specs/assistant/index.md` still described F-001 as revision 4: T-145 shipped revision 5 without updating the index. Caught and corrected in passing by T-153 because the AC-count edit had the file open. The index is the one file a fresh session reads to learn where a feature stands, so a spec that revises without touching it is a spec whose status is wrong everywhere it is quoted.
- 2026-08-18 T-145 (spec-agent): the return omitted `memory_entry:` / `agent_memory_entry:` entirely — the completion protocol requires the field, and `none` is a legitimate value while silence is not. Work verified real and recorded DONE on evidence. Noted because the memory system was empty for the whole project until 2026-08-18 for precisely this reason: nobody noticed the field was missing. The orchestrator recorded the entry the return should have carried (L-015 second occurrence).
- 2026-08-18 orchestrator: the DONE-row cap and C3 are in tension by construction — archiving is not deletion, but C3 resolves `Depends` against TASKS.md alone, so moving a row a live row still names converts one FAIL into eight (measured, then reverted). Of 52 DONE rows exactly **two** were archivable (T-144, T-148 — the only leaves); archiving both brought the queue to 50 and the validator to green, and R9 with it, since R9 runs the validator on the live state. That is the cap met by luck, not by design: the next DONE row re-opens it. **T-149** carries the real fix (`ALL_IDS = TASKS.md ∪ TASKS-archive.md`), which is template-owned.
- 2026-08-18 orchestrator wrote a T-143 briefing that told spec-agent to create `docs/reports/gate1-lenses/F-005-revision-2-log.md` — `docs/reports/` is orchestrator-owned (MANIFEST `writers:`), so C6 failed on a crossing the ORCHESTRATOR caused, not the agent. The file itself is right where it belongs: beside the five round-1 lens returns it answers, which is where T-146's lenses will look. Recorded on orchestrator row T-148 under the 'sanctioned once' mechanism MANIFEST documents for T-121, rather than widening spec-agent's subtree permanently. **The generalisable half:** a briefing can hand an agent a path outside its lane and nothing warns either party at dispatch time — C6 only catches it afterwards, and attributes it to the agent.
- 2026-08-17 two `tradeoff:` items are still being carried forward in qa-mobile-agent's returns but are STALE — verified closed by the orchestrator: the `Announcer` port IS in docs/specs/_shared/platform/mobile.md (added T-027, lines 16-17,25) and the permission copy IS in docs/design/_shared/components.md (moved T-030, extended T-033). Agents carry their own unresolved lists forward without re-checking; a future reviewer should not chase these.
- 2026-08-17 orchestrator created the repo's baseline commit b83d9c6 (441 files) to close LEARNINGS L-001: with zero commits, suite-can-fail.sh's `git checkout --` restore is a no-op on untracked files, so C12 reported PASS while leaving source mutated on four consecutive reviews. Verified the fix by mutating and restoring permissions.ts. .playwright-cli/ (5.1MB of console logs and screenshots) added to .gitignore rather than committed.
