# Task Queue
<!-- Orchestrator writes and archives. Sub-agents read the task assigned to them. -->
<!-- Hard limit: 300 lines. Orchestrator auto-archives when exceeded (see below). -->

## Format

One table. Status is a **column**, not a section — a task never moves between
tables, only its `Status` cell changes. Moving rows between sections drops
columns and duplicates tasks; changing one cell cannot fail that way.

```
| ID | Title | Module | Feature | Agent | Pri | Depends | Status | Artifacts | Outcome |
```

- **Module** — business domain this task targets (e.g. `auth`, `payments`, `_shared`). Orchestrator assigns at dispatch.
- **Feature** — feature ID this task belongs to (e.g. `F-003`) or `—` for cross-cutting tasks.
- **Pri** — `P0` … `P3`. Drives selection order.
- **Depends** — comma-separated task IDs that must be `DONE` before this task is selectable; `—` if none.
- **Status** — `PENDING` · `IN_PROGRESS` · `PARTIAL` · `BLOCKED` · `DONE` · `CANCELLED`
- **Artifacts** — real paths the task produced, resolvable through `MANIFEST ## Paths`. `—` until the task has produced something.
- **Outcome** — exactly one line, including a measurement (`5/7 AC`, `12 tests pass`, `exit 0`). `—` while pending.

Task statuses and agent-return statuses are different vocabularies that overlap.
An agent returns `DONE | PARTIAL | BLOCKED` (plus `APPROVE | REJECT` for gate
agents) in its `---METRICS---` block; the orchestrator maps that onto the
`Status` column here. `PENDING`, `IN_PROGRESS` and `CANCELLED` are queue states
that no agent ever returns.

The orchestrator is the sole dispatcher. Sub-agents do not claim tasks themselves — they receive a briefing via `BRIEFING.md` and act on it.

### Status is derived, not declared

The orchestrator sets `Status` from the evidence in the agent's return, not from
the agent's own wording:

| `files_created` + `files_modified` | Coverage of in-scope ACs | → `Status` |
|---|---|---|
| empty | — | `BLOCKED` |
| non-empty | below 100% | `PARTIAL` |
| non-empty | 100% | `DONE` |

An agent that reports `status: DONE` while listing no files has not completed a
task — it has produced an analysis. Record it as `BLOCKED` with the blocker
named. The full rule, including the confidence interaction, is in
`agents/_completion-protocol.md ## Status is derived from evidence`.

`Artifacts` is what makes this checkable: every `DONE` or `PARTIAL` row must name
at least one path, and every path named must exist on disk.

## Selection order

`"next"` picks the head of this ordering — see `.claude/ORCHESTRATION.md` Step 2
for the executable version:

```
candidates = rows where Status == PENDING and every ID in Depends is DONE
sort candidates by (Pri asc, row order asc)
pick head
```

`Depends` is mandatory for any task whose input is produced by another task.
Without it the queue can hand an agent a task whose input does not exist yet, and
an agent facing a missing input tends to invent one rather than return `BLOCKED`.

## Archival (automatic)

The orchestrator runs this check at the start of every session:
- If TASKS.md total line count > 300, **or** rows with `Status` = `DONE` exceed 50:
  1. Read `TASKS-archive.md` (create if missing)
  2. Move `DONE` rows older than 14 days (or all of them if ages unclear) into `TASKS-archive.md`, grouped by ISO week
  3. Leave a single line here: `<!-- N tasks archived — see TASKS-archive.md -->`

## QA phase: two dispatches per platform

Each platform QA agent (qa-api-agent, qa-web-agent, qa-mobile-agent) is dispatched **twice per feature**:

1. **Authoring phase** (`phase: author` in BRIEFING.md) — runs in parallel with the matching implementer. Writes markdown test cases in `{qa}/{module}/F-{id}/{platform}/`, drafts automation scripts. No test harness needed.
2. **Execution phase** (`phase: execute` in BRIEFING.md) — runs **in parallel** after all implementers return and the orchestrator brings up the test harness. All three QA agents execute simultaneously with namespaced test data (see `_qa-foundations.md` section 10). Runs tests, triages failures, files bugs with layer attribution.

A single feature with api + web + mobile therefore generates **6 QA tasks** (3 authoring + 3 execution) in addition to implementer tasks. The orchestrator creates them automatically; spec-agent and architect-agent do not.

The execution task depends on its authoring task — record that in `Depends`, so
the queue cannot dispatch execution before the test cases exist.

## Merge gate (hard rule, enforced by orchestrator)

A task that completes a feature cannot be set to `Status` = `DONE` until **all** of:
1. At least one P1 test case exists in `{qa}/{module}/F-{id}/{platform}/` for every (AC, platform) pair declared in the feature spec's AC platform tags
2. Reviewer-agent C2 (per-platform AC coverage) passes
3. Reviewer-agent C3 (API contract consistency, uses qa-api-agent's tests as evidence) passes
4. Test suite runs clean (reviewer-agent C5)
5. No open bugs in the feature spec's `## Links.known_bugs` (non-empty is a WARNING, not an auto-fail, but it surfaces to the human reviewer)

If any gate fails, the task stays `IN_PROGRESS` and a follow-up task is dispatched: bug fixes go to the implementer named in the bug's `layer:` field; coverage gaps go to the matching platform QA agent.

---

## Tasks

| ID | Title | Module | Feature | Agent | Pri | Depends | Status | Artifacts | Outcome |
|----|-------|--------|---------|-------|-----|---------|--------|-----------|---------|
| T-047 | Gate 1 multi-lens spec review for F-002 | assistant | F-002 | orchestrator | P1 | T-044 | PARTIAL | docs/reports/gate1-review-F-002.md | 7/7 lenses: 19H/32M/8L into 12 clusters; ZERO lens-vs-lens conflicts; C13 passed pre-gate; 1 item for the human (offline leg / ADR-11) |
| T-059 | Sweep web/main.tsx:33,37 bare crypto.randomUUID + audit globalThis reads in _shared/ | assistant | F-001 | web-agent | P2 | T-058 | PENDING | — | — |
| T-061 | Decide the fixture vocabulary's language + cover the Vietnamese input path (BUG-005) | assistant | F-001 | orchestrator | P1 | T-057 | BLOCKED | — | owner call: are fixtures a stand-in for the model (key them in Vietnamese) or a developer convenience (state it, and cover Vietnamese separately) |
| T-084 | QA mobile: AC-30 coverage + stop hardcoding the testid count | assistant | F-001 | qa-mobile-agent | P1 | T-083 | PARTIAL | tests/assistant/mobile/F-001-voice-assistant-view.spec.ts, docs/qa/assistant/F-001/mobile/index.md, tests/assistant/mobile/F-003-mobile-surface.spec.ts, docs/qa/assistant/runs/2026-08-17-mobile-ac30.md | 583/583 tree; fixed the red by asserting what the literal STOOD FOR (that the parser matched something) not by changing 22->23 — and found the 3 equality assertions under it were vacuously green if the regex ever stopped matching; used makeReduceMotion(true) never set(true), because set also fires onChange and would prove the controller reacts to a notification while the startup read path went unasserted; 9 mutations all proved. TC-009 authored but BLOCKED on the harness having no AC-30 scenario |
| T-085 | QA web: AC-30 coverage at the tier that can falsify position | assistant | F-001 | qa-web-agent | P1 | T-082 | PARTIAL | tests/assistant/e2e/F-001-voice-assistant-view.spec.ts, tests/assistant/pages/AssistantPage.ts, tests/harness/qa-test-server.ts, docs/qa/assistant/F-001/web/index.md, docs/qa/_shared/bugs/BUG-006-own-send-lands-short-of-the-bottom.md, docs/qa/assistant/runs/2026-08-17-web-ac30.md | 49/50; 14 cases, 9 of them only the browser tier can falsify; FOUND BUG-006 by frame-by-frame capture; caught 4 bugs in its OWN scripts, two that mattered — playwright reducedMotion was silently inert so all 3 reduced-motion cases first ran in ordinary motion while claiming otherwise, and TC-038 v1 could not have failed because a dock that takes layout shrinks the viewport from the BOTTOM so a user at the top sees nothing move |
| T-089 | Fix BUG-006: the clause-(h) scroll loses a race with the reply | assistant | F-001 | web-agent | P1 | T-085 | PENDING | — | **Reset IN_PROGRESS → PENDING 2026-08-21: never finished, and nothing was running.** Measured: `docs/qa/_shared/bugs/BUG-006-...md` still reads **`| Status | open |`**. **Unlike the five rows closed beside it, this one has no output on disk** — so it returns to the queue rather than being credited.  — |
| T-091 | Publish a testid for the conversation scroller | assistant | F-001 | design-agent | P2 | T-085 | PENDING | — | AC-30 defines 8 clauses as measurements of an element that has no testid |
| T-087 | Give the headless Surface a scroll seam so 2 AC-30 clauses leave the device tier | assistant | F-001 | mobile-agent | P1 | T-084 | PENDING | — | first dispatch stalled at 600s with nothing written; re-dispatch |
| T-088 | Wire nma-new / nma-waiting / follow-at-bottom scenarios into .mobile-app (unblocks TC-009) | assistant | F-001 | orchestrator | P1 | T-087 | PENDING | — | — |
| T-092 | Owner feedback: mobile layout crowds chat + list on a small screen | assistant | F-003 | design-agent | P2 | — | PENDING | — | docs/reports/owner-feedback-2026-08-17-product-gaps.md §1. The list CAN collapse (AssistantScreen.tsx:34) but defaults open and its control is a hamburger that reads as a menu. F-001 chose list-beside-conversation because AC-1/AC-4 need the change visible in the same turn — sound on desktop, and that is what the owner is pushing back on for a phone |
| T-093 | Owner feedback: the hamburger is not a menu — nothing is behind it | assistant | F-001 | spec-agent | P3 | — | PENDING | — | §2. Only toggles the list. Collides with ADR-008 and F-002 AC-23, which both assume no settings surface exists — deliberate so far, but every deferred preference has nowhere to live |
| T-094 | Owner question CORRECTED: web HAS hand edit+delete, mobile does not — close the parity gap | assistant | F-003 | mobile-agent | P1 | T-096 | PENDING | — | my first answer said no hand-editing at ANY layer; wrong. PATCH/DELETE exist (api/app.ts), controller wraps them as editTask/removeTask, web's TaskListPane calls both. Mobile's TaskList has add+toggle only. F-003's parity table lists AC-18 as holding IDENTICALLY — a documented divergence. I missed it because my endpoint grep matched `path === '...'` and the route is a pattern match (L-002) |
| T-097 | Owner reversal: settings surface is needed (was 'no settings', owner calls it their mistake) | assistant | — | spec-agent | P1 | T-096 | PENDING | — | docs/reports/owner-decision-2026-08-17-settings-and-lists.md. F-002 AC-23, ADR-008 and F-002 Out of Scope all cite 'no settings surface exists' as a premise — re-read as DEFERRED, not permanent |
| T-098 | Owner reversal: personal lists — a data-model change, not just UI | assistant | — | architect-agent | P1 | T-096 | PENDING | — | task has no project/list/tag field; status (inbox|today|done|archived) is the only grouping. Touches every place that groups by status |
| T-100 | QA mobile: no BUG-006 case — AC-30 coverage is all instantaneous calls | assistant | F-001 | qa-mobile-agent | P1 | T-090 | PENDING | — | it would report green over this defect exactly as the unit tier did |
| T-101 | Redesign the app as a whole against the UC coverage map (information architecture first) | assistant | — | design-agent | P1 | T-096 | DONE | docs/design/_shared/information-architecture.md | **Closed on evidence 2026-08-21.** `information-architecture.md:3` names T-101 as its author, and §115 records what a later pass **reverses in T-101's own drawing** — only writable if the drawing exists.  docs/reports/owner-decision-2026-08-17-conversation-is-not-a-list.md |
| T-102 | Conversation surface drops the task list — CONFIRMED at every width by the owner | assistant | F-001 | design-agent | P1 | T-101 | DONE | docs/design/assistant/screens/app-shell.html | delivered inside T-101; owner confirmed no desktop split pane |
| T-103 | Restate AC-1/AC-4: the guarantee survives, the mechanism changes | assistant | F-001 | spec-agent | P1 | T-102 | DONE | docs/specs/assistant/F-001-voice-assistant-view.md, docs/specs/assistant/index.md | **Closed on evidence 2026-08-21.** F-001 revision 4 **credits T-103 by name** in both the spec and `index.md`. **The output shipped and the row never moved — it has been blocking T-113 since.**  dropping them loses the guarantee; leaving them contradicts the build |
| T-105 | Desktop: Tasks centre, chat right panel — the list becomes the primary object | assistant | F-001 | design-agent | P1 | T-101 | DONE | docs/design/_shared/information-architecture.md, docs/design/assistant/screens/app-shell.html, docs/design/assistant/screens/app-shell-ios.html, docs/design/assistant/screens/app-shell-android.html, docs/design/_shared/components.md, docs/design/_shared/tokens.json | docs/reports/owner-decision-2026-08-17-desktop-list-is-primary.md; supersedes the remove-at-every-width confirmation for desktop only |
| T-107 | Build the new shell on web (split at 1024, Tasks surface, AC-31/32, Settings) | assistant | F-001 | web-agent | P1 | T-103 | DONE | src/assistant/web/, src/assistant/_shared/model/ | web 145->170; the ONE layout branch is a container query on the app root, and a test greps the four shell sources for innerWidth/clientWidth/matchMedia requiring ZERO hits so no second width-selected mechanism can grow there; AC-31 flash is asserted to END not just start; AC-32 tests read the LIST and never the message, plus a third reads the message with an EMPTY list to hold AC-1 from the other side; NOT_BUILT map of 9 ids each with its justifying artifact, and a second test asserting none is rendered so the exclusion list cannot quietly become ids-we-stopped-checking |
| T-108 | Build the new shell on mobile (peer paths, Tasks surface, close D8) | assistant | F-003 | mobile-agent | P1 | T-103 | DONE | src/assistant/mobile/ | 690/690 tree; OQ9 is ONE declared value held by 3 tests incl. a source scan requiring exactly one declaration and one caller, mutation-proved; collapsed its own independently-built collections/day-grouping model onto web mid-flight rather than leave one fact in two homes; strengthened a check that a surviving mutation exposed — "each id is wired into a component" passed while the delete id existed only inside touchProps(), so it now strips measurement helpers before scanning: measured is not rendered; 8 of 29 ids unbuilt with a test that fails BOTH ways |
| T-109 | F-003 parity table is stale: enumerates 29 ACs (AC-30/31/32 missing) and calls AC-18 identical | assistant | F-003 | spec-agent | P2 | T-108 | PENDING | — | — |
| T-110 | QA re-author: 5 named TCs encode the OLD mechanism; AC-31/AC-32 have no cases at all | assistant | F-001 | qa-web-agent | P1 | T-107 | PENDING | — | — |
| T-111 | Confirm Inbox=all-open and default collection Inbox (mockup draws Today) | assistant | F-001 | design-agent | P1 | T-107 | PENDING | — | IA §3 and components.md §ListsMenu do not quite agree; addTask creates status:inbox so landing on Today would HIDE a task the user just added |
| T-112 | assistant-drawer-button retired — AssistantPage.ts:182 still binds it | assistant | F-001 | qa-web-agent | P1 | T-107 | PENDING | — | replacement is shell-lists-menu-button |
| T-113 | BUG-007: unsupported-query names a layout that no longer exists | assistant | F-001 | backend-agent | P1 | T-103 | PENDING | — | AC-15 rev 4 requires naming the alternative WITHOUT reference to layout; the literal still holds rev 3's string, and the api test asserts the constant against itself so nothing failed |
| T-114 | Landing summary on Talk: greet + what's due + progress + all-clear | assistant | F-001 | design-agent | P1 | — | PARTIAL | docs/design/_shared/components.md, docs/design/assistant/screens/app-shell.html, docs/design/assistant/screens/app-shell-ios.html, docs/design/assistant/screens/app-shell-android.html | docs/reports/owner-decision-2026-08-18-landing-and-collections.md §1. Frames with slots per F-002 AC-22, never free text; reports counts and names tasks it can read, never summarises or predicts |
| T-115 | Default collection Today + addTask writes the viewed collection | assistant | F-001 | web-agent | P1 | — | PENDING | — | §2. Owner chose Today; addTask creates status:inbox so on the default surface adding a task makes it VANISH. Resolution: add-in-context. Changes addTask's contract — flagged, not assumed |
| T-116 | Spec the landing summary + addTask contract change | assistant | F-001 | spec-agent | P1 | T-114 | PENDING | — | — |
| T-117 | task.completed_at — the owner's mid-day shape cannot be selected without it | assistant | F-001 | architect-agent | P1 | T-114 | BLOCKED | — | LSM-PROGRESS is written and drawn but never fires; updated_at is LAST TOUCHED so deriving done_today counts a done task retitled today and drops one finished today and edited tomorrow — right most of the time, silently wrong the rest |
| T-118 | MessageTaskLink must be inline, not a button, inside running text | assistant | F-001 | web-agent | P2 | T-114 | PENDING | — | measured not assumed: a button is an atomic inline box, so the comma after a title lands at the start of the next line and a long title overflows instead of wrapping. Same id, role and look — different box |
| T-119 | Refresh IA §12 (owner answered) and §6's S1 table (no Landing row) | assistant | F-001 | design-agent | P3 | T-114 | PENDING | — | — |
| T-120 | Today = due_at is today; drop the status leg; fix un-complete | assistant | F-001 | architect-agent | P1 | — | PARTIAL | docs/specs/_shared/adr/ADR-009-today-is-a-date.md, docs/specs/assistant/data-model.md, docs/specs/assistant/api-contracts.md | docs/reports/owner-decision-2026-08-18-today-is-a-date.md. Buys the mid-day shape back with NO new field. status:'today' becomes a value that means nothing; controller.ts:574 writes it on un-complete and would then both write a meaningless status AND fail to put the task in Today |
| T-121 | Make Today a date, in code — one unit across _shared/api/web/mobile | assistant | F-001 | web-agent | P1 | T-120 | DONE | src/assistant/_shared/model/tasks.ts, src/assistant/_shared/controller.ts, src/assistant/_shared/testing/fixtures.ts, src/assistant/api/app.ts, src/assistant/web/components/TasksSurface.tsx, src/assistant/mobile/components/TasksSurface.tsx, src/assistant/web/__tests__/collections.test.ts | splitting leaves a window where Today is defined twice; subtree crossing sanctioned for this task |
| T-122 | LSM-PROGRESS wording over-claims what done && due_at-today measures | assistant | F-001 | design-agent | P1 | T-120 | PENDING | — | derived it measures 'was due today and is done': a task due today ticked last night counts, one due yesterday ticked this morning does not. Frame says 'You've finished N today'. F-001 AC-14/15 bind it. Now a COPY decision, not a blocker |
| T-123 | Tasks-surface feature spec (new F-id) carrying ADR-009's three ACs | assistant | — | spec-agent | P1 | T-121 | PENDING | — | F-001 Out of Scope already says it needs its own F-id |
| T-124 | OWNER: assistant-created tasks are invisible on the default surface | assistant | F-001 | orchestrator | P1 | T-121 | DONE | docs/reports/owner-question-2026-08-18-assistant-created-tasks-are-invisible.md, docs/reports/owner-decision-2026-08-18-four-buckets.md | docs/reports/owner-question-2026-08-18-assistant-created-tasks-are-invisible.md. Talk to the assistant, add a task, open Tasks: not there. add-in-context covered the manual composer and missed the PRIMARY path in a voice-first product |
| T-125 | e2e: ~20 assertions expect AI-created tasks on the landing collection | assistant | F-001 | qa-web-agent | P1 | T-124 | PENDING | — | outcome depends on T-124's answer |
| T-126 | Four buckets: amend ADR-009 — Upcoming added, Inbox becomes undated-only, overdue folds into Today | assistant | F-001 | architect-agent | P1 | — | DONE | docs/specs/_shared/adr/ADR-009-today-is-a-date.md | four date predicates, total and disjoint, day-granular. Measured: 7 of 737 live rows change bucket, all Inbox→Today, all overdue. Upcoming has no member in the store |
| T-127 | Assistant asks for a date when it hears none; creates dateless only if user declines | assistant | F-001 | spec-agent | P1 | T-126 | PENDING | — | no date is invented on the user's behalf |
| T-128 | Landing summary + Lists menu + day groups after overdue folded into Today | assistant | F-001 | design-agent | P1 | T-126 | DONE | docs/design/_shared/components.md | count_secondary word changed (title_list already spent "more"); rule 4 struck not deleted; open_all split from inbox_count (two facts, errors in opposite directions); Upcoming row; new Overdue group, Inbox+Done go flat — Done would have shipped a NEW falsehood; skeleton asserting a heading it cannot know |
| T-129 | Implement four buckets: Collection, inCollection day-granular, groupTasks, dueAtForCollection | assistant | F-001 | web-agent | P1 | T-126,T-128 | DONE | src/assistant/_shared/model/tasks.ts, src/assistant/web/components/, src/assistant/mobile/components/ | 743/743 (was 709), tsc clean, 6 mutations proved the new assertions. Unreadable due_at answers Inbox or it would be in NO bucket; groupTasks takes the collection; todayGroupLabel deleted; two silent fallbacks became exhaustive switches |
| T-131 | Draw the owed mockup states: Upcoming row, Overdue heading, flat Inbox/Done, LSM-CLEAR-AHEAD | assistant | F-001 | design-agent | P1 | T-128 | DONE | docs/design/assistant/screens/app-shell.html, docs/design/assistant/screens/app-shell-ios.html, docs/design/assistant/screens/app-shell-android.html | design-check 60/60 with a real browser, id catalogues byte-identical, 202 mockup-parsing tests. Rendering caught two falsehoods reading could not: static h1 Today over every collection, and an Overdue heading indistinguishable from a date heading |
| T-132 | components.md cites three line numbers that moved in T-129 | assistant | F-001 | design-agent | P3 | T-129 | PENDING | — | ListsMenu cites tasks.ts:42 (now :78), TaskList cites :200 (now :328), Skeletons cites TasksSurface.tsx:179 for a todayGroupLabel call that no longer exists |
| T-141 | F-005 task detail — spec, full scope | assistant | F-005 | spec-agent | P0 | — | DONE | docs/specs/assistant/F-005-task-detail.md, docs/specs/assistant/index.md | 37 ACs, recurrence carries 11. Impact section found 13 field-enumerating sites not 2, and that the assistant write allowlist IS the diff source — a field omitted there emits an empty diff, an F-001 failure caused by F-005 silence. The source audit OVERTURNED an invented decision. 10 open questions |
| T-142 | F-005 Gate 1 round 1 — multi-lens spec review | assistant | F-005 | orchestrator | P0 | T-141 | DONE | docs/reports/gate1-lenses/F-005-consolidated.md, docs/reports/gate1-lenses/F-005-tester.md, docs/reports/gate1-lenses/F-005-dev.md, docs/reports/gate1-lenses/F-005-architect.md, docs/reports/gate1-lenses/F-005-product.md, docs/reports/gate1-lenses/F-005-design.md, docs/reports/owner-decision-2026-08-18-f005-gate1.md | REJECT. 20 HIGH, 7 convergences found independently by 2+ lenses, 4 owner decisions taken |
| T-143 | F-005 revision 2 — Gate 1 round 1 findings + 4 owner answers | assistant | F-005 | spec-agent | P0 | T-142 | DONE | docs/specs/assistant/F-005-task-detail.md, docs/specs/assistant/index.md | Revision log at docs/reports/gate1-lenses/F-005-revision-2-log.md is recorded on T-148 — docs/reports/ is orchestrator-owned and the briefing named the path (see Drift Log). 64/64 round-1 findings dispositioned resolved (20 HIGH, 34 MED, 10 LOW — consolidation's header said LOW 8, its own table sums 10). 37→46 ACs, none renumbered or deleted; Impact 9→13 subsections; API Touch Points' false 'no new endpoints' corrected. declared-elements.sh exit 0 (18 fields), 46 AC ids no gaps/dups, ## Links block byte-identical (verified). Two new open questions: OQ11 (mobile reminder surfacing — a collision between owner answers 1 and 3, default taken), OQ12 (note indicator; rev-1's market claim was false and is withdrawn) |
| T-145 | F-001 AC-24 — the 'zero actions above the split' clause is false while the F-005 detail is open | assistant | F-001 | spec-agent | P0 | T-143 | DONE | docs/specs/assistant/F-001-voice-assistant-view.md | Revision 5, amended in place: bound unchanged (at most one action), its REASON moved off the per-width claim and the detail-open state given coverage — closing the detail is the one action, carrying AC-24's not-hidden/not-disabled condition. 32 ACs, none added/renumbered/deleted; declared-elements exit 0; ## Links byte-identical; no layout branch. AC-25/AC-31/AC-32 checked, consistent, unamended. Two states recorded not settled: OQ10 (F-005 AC-2 holds the detail open over its OWN failed write — a second instance of L-015), OQ11 (AC-31's door while the detail is open) |
| T-146 | F-005 Gate 1 round 2 — multi-lens spec review (LAST ROUND, cap reached after it) | assistant | F-005 | orchestrator | P0 | T-143, T-145, T-153 | DONE | docs/reports/gate1-lenses/F-005-r2-consolidated.md, docs/reports/gate1-lenses/F-005-r2-tester-api.md, docs/reports/gate1-lenses/F-005-r2-tester-web.md, docs/reports/gate1-lenses/F-005-r2-tester-mobile.md, docs/reports/gate1-lenses/F-005-r2-dev-backend.md, docs/reports/gate1-lenses/F-005-r2-dev-web.md, docs/reports/gate1-lenses/F-005-r2-dev-mobile.md, docs/reports/gate1-lenses/F-005-r2-architect.md, docs/reports/gate1-lenses/F-005-r2-design.md, docs/reports/gate1-lenses/F-005-r2-product.md | REJECT. 9 lenses (5 in round 1 — mobile tags made three new lenses applicable), 99 findings: 40 HIGH, 44 MED, 15 LOW. All nine returned a checked: list and examined all 48 ACs. 10 convergences; the clock/zone one was found independently by SIX lenses. The round-2-only question (is a claimed disposition really in the spec?) found 4 unlanded and 1 unexecutable. **ROUND CAP REACHED — escalated to the owner** |
| T-147 | uc-coverage-map.md D8 is stale — the phone DOES have inline rename and delete | assistant | — | spec-agent | P2 | — | PENDING | — | src/assistant/mobile/components/TaskList.tsx calls controller.editTask (L71) and controller.removeTask (L136). D8 says neither exists; the stale sentence was inherited into F-005 rev 1 and argued against pulling mobile in. Also the six UC rows reading MISSING (unexamined) |
| T-150 | F-001 AC-24's reachability half has ZERO test assertions — the coverage row reads as full | assistant | F-001 | qa-web-agent | P1 | T-145 | PENDING | — | Pre-dates T-145 but is now larger: the detail-open state is explicitly in scope with no case. Owed one case per failure state (failed turn, offline, IA §6 S1) asserting <=1 action and that the close affordance is neither hidden nor disabled. docs/qa/assistant/F-001/web/index.md:98 maps AC-24 to TC-028 alone, which tests only the usability half — the L-003/L-006 shape. Also: automation app.addTaskButton / app.taskRowByTitle still assume the list is on the conversation surface (rev-4 relocation drift) |
| T-151 | F-003 ## Parity claims F-001 AC-24 'holds identically' — one clause now has no mobile counterpart | assistant | F-003 | spec-agent | P2 | T-145 | PENDING | — | F-005 AC-45 is web-tagged so the detail-open state has no mobile subject. Same shape as the AC-18 parity note already recorded in F-001 AC-18 (mobile ships 2 of 4 operations). Fold in the AC-18 count correction (29→32 per F-005 Impact §9) while the file is open ALSO from T-157: the table does not list **AC-31** at all, and AC-31 is `(web)` — so the mobile predicate F-001 rev 7 just bound (`taskLinkState`) is asserted by NO tier. Retagging changes an enumeration another spec binds |
| T-152 | information-architecture.md has no failure/empty/loading entry for the detail surface + the design-system debt F-005 named | assistant | F-005 | design-agent | P1 | T-143 | DONE | docs/design/_shared/components.md, docs/design/_shared/DESIGN.md, docs/design/_shared/information-architecture.md, docs/design/_shared/tokens.json | phase: system. **§ CarriedNotice is a SIBLING of § SaveNotice, not a widening** — SaveNotice clears on leaving the surface, which is precisely what AC-47 forbids; widening it would give one component two lifetimes told apart by a flag. Placed frame-level OUTSIDE the stacking layer so S3/S4/S5 slide under it, which is what makes 'visible on Settings' true rather than three-of-five. **Accent question DECIDED, not routed: the set is closed at five** — AC-9 and AC-39 carry their marks without colour, and DESIGN.md now says so as a rule. Row mark budget is ONE list of four with a fixed order. IA §2/§3/§4/§6 carry S6. § Buttons gains 'put back' plus a neutral variant (contrast computed: 13.28/13.34 label, 5.01/5.78 boundary). design-check 60/0/0 with token-drift reading 98 vars per mockup — non-zero, so L-007's silent-scan defect is excluded, and it re-verified that its own DESIGN.md edit did not move the threshold regex |
| T-153 | Fold the two composition answers into F-005 and F-001 — before round 2, not after | assistant | F-005 | spec-agent | P0 | T-145 | DONE | docs/specs/assistant/F-005-task-detail.md, docs/specs/assistant/F-001-voice-assistant-view.md, docs/specs/assistant/index.md | F-005 46→48 ACs (AC-47 outliving notice, AC-48 subject swap), AC-2 narrowed with revision 2's wording and reason kept verbatim and overruled explicitly; amended IN PLACE inside revision 2 so round 2 reviews it. F-001 revision 6, still 32 ACs, OQ10/OQ11 closed against the decision. Verified: 48 + 32 AC ids contiguous, declared-elements exit 0 on both, ## Links byte-identical on both, no heading or AC lost. Orchestrator (not this agent) appended the round-2 addendum to docs/reports/gate1-lenses/F-005-revision-2-log.md, which is orchestrator-owned — it described 46 ACs and the spec now has 48 |
| T-154 | F-005 revision 3 — 99 round-2 findings, AMEND ONLY (no new ACs) | assistant | F-005 | spec-agent | P0 | T-146 | DONE | docs/specs/assistant/F-005-task-detail.md, docs/specs/assistant/index.md, docs/reports/gate1-lenses/F-005-revision-3-log.md | 99/99 dispositioned (95 resolved, 2 in part, 2 deferred to an OQ, 0 rejected). **The constraint held: 48 ACs before and after, none added, renumbered or deleted** — verified by grep. OQ6 closed on the owner's answer with its false premise corrected in the spec; OQ13/14/15 new, each with the default stated in the AC so nothing blocks. Impact 13→14 subsections; §1 now 16 sites / 6 gating lists. Agent returned PARTIAL on a blocker that was a TOOL defect, not an artifact gap — see L-016 and T-156; recorded DONE on evidence after the tool was fixed and re-run (all 21 fields accounted, exit 0). Links byte-identical |
| T-155 | F-005 Gate 1 round 3 — TARGETED re-review, changed ACs only | assistant | F-005 | orchestrator | P0 | T-154 | DONE | docs/reports/gate1-lenses/F-005-r3-consolidated.md, docs/reports/gate1-lenses/F-005-r3-tester-api.md, docs/reports/gate1-lenses/F-005-r3-tester-web.md, docs/reports/gate1-lenses/F-005-r3-tester-mobile.md, docs/reports/gate1-lenses/F-005-r3-dev-backend.md, docs/reports/gate1-lenses/F-005-r3-dev-web.md, docs/reports/gate1-lenses/F-005-r3-dev-mobile.md, docs/reports/gate1-lenses/F-005-r3-architect.md, docs/reports/gate1-lenses/F-005-r3-design.md, docs/reports/gate1-lenses/F-005-r3-product.md | 58 findings (25 HIGH / 22 MED / 11 LOW), down from 99. Dispatch set derived mechanically from the revision-3 log: 9-25 ACs per lens, 45 of 48 touched. **Dispositions essentially all landed** — the finding of this round is that the AMENDMENTS did the damage. 7 convergences; the biggest is that AC-2's third state was written WITHOUT the 'server-owned' qualifier the owner's own decision doc carries, found by 4 lenses. Constraint held: 48 ACs before and after, none added. One lens named the exception the owner asked to hear about (dev-mobile F3) |
| T-157 | F-001 revision 7 — AC-31's activatability gate was written for a different postcondition (absorbs T-140) | assistant | F-001 | spec-agent | P0 | T-154 | DONE | docs/specs/assistant/F-001-voice-assistant-view.md, docs/specs/assistant/index.md | Gate narrowed from 'in the collection on screen' to 'the task still exists'; the collection switch (T-140's owner-approved half) is part of the route, not a second postcondition. **Both client predicates named by path** — canReveal (web/shell.ts:113-115) and taskLinkState (mobile/model/task-link.ts:54) — so a later grep returns every door. Deleted task still inert, with its reason narrowed to the case it is true of and rev 4's sentence kept struck through. Verified: 32 ACs contiguous, ## Links byte-identical, declared-elements exit 0 (run BEFORE and after), diff 24 lines. New OQ12: which collection, when several hold the task — recorded not decided, and the AC stays falsifiable meanwhile |
| T-158 | F-002 — a turn-path refusal has no frame to speak through | assistant | F-002 | spec-agent | P1 | T-154 | PENDING | — | TurnOutcome has six members and none is a refusal; F-002's speech table is closed and carries none either. F-005 AC-36 requires a refusal with a visible outcome and the WORDING is F-002's. Found by dev-backend lens (F1) at Gate 1 round 2 |
| T-159 | F-005 revision 4 — contradictions only, plus three owner answers; NO review after | assistant | F-005 | spec-agent | P0 | T-155 | DONE | docs/specs/assistant/F-005-task-detail.md, docs/specs/assistant/index.md, docs/reports/gate1-lenses/F-005-revision-4-log.md | 58/58 dispositioned: 45 fixed, 12 recorded-for-architecture, 1 new open question (OQ16), 0 rejected. **Constraint held: 48 ACs before and after.** 676→805 lines. OQ13 re-stated with both causes of permanent loss; OQ16 new. **Agent STALLED after completing the work — no ---METRICS--- block; recorded DONE on evidence (T-006c precedent), see L-018 and the Drift Log.** Verified independently: AC ids 1-48 contiguous, ## Links byte-identical, declared-elements exit 0, and six named contradictions spot-checked in the text — including that withdrawn sentences are quoted with their reason rather than deleted |
| T-161 | QA: four F-001 AC-31 assertions invert under revision 7, and a third entry point is uncovered | assistant | F-001 | qa-web-agent | P1 | T-157 | PENDING | — | web/__tests__/shell.test.tsx ~174-205 asserts a done and an undated task render as plain text — both are doors now. mobile/__tests__/task-link.test.ts:134 and :151 invert too. STILL CORRECT and must stay: shell.test.tsx:207-220 (deleted tasks are never links) and task-link.test.ts:152. Owed: a door to a task the shown collection does not hold switches collection then scrolls and flashes once, plus the assistant-created-dateless case specifically. Needs qa-mobile-agent for the second tier |
| T-162 | F-005 backend implementation | assistant | F-005 | backend-agent | P0 | T-160 | DONE | src/assistant/api/engine/{zone,recurrence,task-fields,plan}.ts, src/assistant/api/app.ts, src/assistant/api/types.ts, src/assistant/api/store/store.ts, src/assistant/api/engine/{apply,serialize,task-equals,turns,undo}.ts, src/assistant/api/ports/{interpreter,fixture-interpreter,fixture-table}.ts, src/assistant/api/__tests__/ (8 new + 2 modified) | **Ten endpoints, four new. api 202/202, tsc clean across the repo** (verified independently). The guard moved onto the write and is enforced by a BRANDED TYPE — a `ValidatedChanges` whose only producer is the validator — so L-005's shape cannot regress by convention. **It proved attribution rather than claiming it**: a git worktree at HEAD with the parallel agents' files copied in showed the same 28 failures without any of its changes. 2 of 11 are its own, in docs/qa/, left unfixed on purpose (out of subtree) with the one-line fix named. Seven targeted mutants, each killed by the test named for that guard (L-012). Two contract gaps chosen and commented at the decision site rather than coerced |
| T-163 | F-005 web implementation | assistant | F-005 | web-agent | P1 | T-160 | PARTIAL | src/assistant/_shared/model/clock.ts, src/assistant/_shared/model/notices.ts, src/assistant/_shared/model/task-fields.ts, src/assistant/web/detail.ts, src/assistant/web/components/TaskDetail.tsx, src/assistant/web/components/CarriedNotices.tsx, src/assistant/web/components/PassedReminders.tsx, src/assistant/web/__tests__/task-detail.test.tsx, src/assistant/web/__tests__/detail-model.test.ts, src/assistant/web/__tests__/f005-controller.test.ts, src/assistant/_shared/controller.ts, src/assistant/_shared/types.ts, src/assistant/_shared/api/client.ts, src/assistant/_shared/model/reducer.ts, src/assistant/_shared/model/tasks.ts, src/assistant/_shared/model/format.ts, src/assistant/_shared/model/messages.ts, src/assistant/_shared/model/client-stores.ts, src/assistant/_shared/testing/fixtures.ts, src/assistant/web/App.tsx, src/assistant/web/shell.ts, src/assistant/web/seams.ts, src/assistant/web/main.tsx, src/assistant/web/styles.css, src/assistant/web/components/TasksSurface.tsx, src/assistant/web/components/ConversationPane.tsx, src/assistant/web/components/ListsMenu.tsx, src/assistant/web/components/icons.tsx, src/assistant/web/__tests__/shell.test.tsx, src/assistant/web/__tests__/app.test.tsx, src/assistant/web/__tests__/controller.test.ts | **web 366/366, tsc clean, full suite 1017/1020 — all three remaining failures have named owners** (2 docs/qa/ from backend's X-Timezone contract, 1 mobile from design's SE-DETAIL). Verified independently, and it touched nothing outside its subtree. S6 detail surface, the receiver clause (`applyWrite` drops `removed` too, so AC-39 stops being vacuous on mobile), AC-47's notice rules in `_shared/model/notices.ts` behind ONE `reconcileNotices()` all four writers route through, `pushLocalTasks` widened from five fields to sixteen, and ZERO JS width reads left in the web tree. PARTIAL for three named gaps: AC-3's both-pending PROACTIVE save is not implemented (the arrival is deferred and the save happens on the user's own blur); no § TaskDetail exists so 47 testids are proposed not adopted; AC-38's surfacing and the `refused`-turn copy are placeholders |
| T-164 | F-005 mobile implementation — the shared-model half only | assistant | F-005 | mobile-agent | P1 | T-160, T-163 | PARTIAL | src/assistant/mobile/model/carried.ts, src/assistant/mobile/components/CarriedNotices.tsx, src/assistant/mobile/__tests__/carried.test.ts, src/assistant/mobile/__tests__/f005-mobile.test.ts, src/assistant/mobile/model/a11y.ts, src/assistant/mobile/model/announce.ts, src/assistant/mobile/model/shell.ts, src/assistant/mobile/model/task-link.ts, src/assistant/mobile/model/tasks-view.ts, src/assistant/mobile/model/touch.ts, src/assistant/mobile/components/TaskList.tsx, src/assistant/mobile/components/TasksSurface.tsx, src/assistant/mobile/components/AssistantScreen.tsx, src/assistant/mobile/components/styles.ts, src/assistant/mobile/controller.ts, src/assistant/mobile/index.ts, src/assistant/mobile/boot.ts, src/assistant/mobile/__tests__/a11y.test.ts, src/assistant/mobile/__tests__/shell.test.ts, src/assistant/mobile/__tests__/task-link.test.ts, src/assistant/_shared/controller.ts | Source landed and RUNS ON A REAL DEVICE — verified on iOS 18.6 simulator 2026-08-19: four collections, per-row accessible names, and AC-47's `Deleted "X". / Put back` all render. But NOT complete: its own new test file `f005-mobile.test.ts` has 20 tsc errors and 7/7 failing tests, all calling `controller.dispatch2(...)` — a method that exists nowhere in src/ — plus `carriedRowFor` and `announcer.spoken`, neither of which exists either. AND it regressed F-003: `docs/qa/.../F-003-mobile-surface.spec.ts` drops from 124/124 to 123/124 because `assistant-row-badge` is no longer rendered. Proven by stashing ONLY this task's subtrees and re-running: 124/124. Follow-up T-176. |
| T-165 | F-005 design screens — web + iOS + Android | assistant | F-005 | design-agent | P1 | T-152 | DONE | docs/reports/design-signoff-visual-language.md | **SUPERSEDED 2026-08-21 by the visual-language redesign, closed rather than run.** Both would have drawn against v1, which no longer exists — the owner replaced colour, type, spacing, shape and layout wholesale. What each owed is now inside T-204's ordered redraw. **Closing them rather than leaving them PENDING is the point: a queued task that draws against a retired system is worse than no task, because it looks like coverage.**  phase: screens. Deliberately a separate dispatch from T-152: a system authored in the same breath as the screens built against it has no external standard to meet, and a human reviews the system once instead of every screen forever. Blocks qa-web and qa-mobile authoring, which need the testid contract **HELD until T-164 lands, deliberately — not blocked.** Drawing the mockups adds testids, and mobile-agent is editing the very files that pin them (`mobile/model/a11y.ts`, `a11y.test.ts`, and web's `app.test.tsx` invented/NOT_BUILT lists). Running both would move mobile's baseline under it mid-flight, which is exactly L-022's situation, recorded hours earlier |
| T-166 | F-005 QA api — author test cases from the spec | assistant | F-005 | qa-api-agent | P1 | T-160 | DONE | docs/qa/assistant/F-005/api/ (35 TCs + index), tests/assistant/api/F-005-task-detail.spec.ts, tests/harness/qa-doors.ts, docs/qa/_shared/fixtures/api/f005-users.json, docs/qa/assistant/F-001/api/TC-41-zoneless-client-refusal-guard.md, tests/assistant/api/F-001-voice-assistant-view.spec.ts, tests/harness/qa-test-server.ts, docs/qa/assistant/F-001/api/index.md | **api tier 202/202 (verified). 154 automated F-005 cases, all 31 api-tagged ACs with a P1 case, every declared error code covered.** The three `__qa__` doors built in ONE implementation mounted by both the e2e server and the in-process suites, so there is no second seed door to drift. TC-14/TC-17 fixed at the harness with a zoneless agent RETAINED — and it added TC-41 to use it, because a default header would otherwise make 409 TIMEZONE_UNKNOWN unassertable in that file. **Seven of its own assertions were red on first authoring; none was weakened.** Two exposed real fragilities (AC-28's timestamp-equality condition → L-024; `changed` ≠ trashed). Two turn-path refusal reasons are unreachable by construction and recorded in the index rather than hidden |
| T-167 | F-005 QA web + mobile — author test cases | assistant | F-005 | qa-web-agent | P1 | T-165 | PENDING | — | phase: author. WAITS on T-165: the testid contract comes from the screens, and the selector contract forbids inventing one. Needs a paired qa-mobile-agent dispatch for the nine (mobile)-tagged ACs, with F-003's node/device-lab split stated rather than discovered (tester M10) |
| T-168 | AC-47's 'deletion ends the notice' vs 'reported once, value legible' — one clause of confirmation | assistant | F-005 | spec-agent | P2 | T-159 | PENDING | — | design-agent read 'ends' as the RETRY obligation ending, not the row being removed, because the alternative makes the last legible copy of the user's typed value elapse away — one ender over from the loss AC-47 exists to prevent. § CarriedNotice's Lifetime table is built on that reading. If the spec means removal, the design contradicts it. Gate 1 is closed, so this is a one-clause confirmation, not a revision |
| T-169 | AC-9's low/medium mark has two documents claiming two owners | assistant | F-005 | spec-agent | P3 | T-159 | PENDING | — | AC-9's sub-bullet says the form is 'design's within the one-glyph vocabulary'; OQ5 lists the same question as the owner's. design-agent decided it as design (single !, high only) and nothing structural rides on either answer — but two owners for one question is how it gets decided twice |
| T-170 | The refusal-reason enum has no member for a value-not-in-set or a value-type violation | assistant | F-005 | architect-agent | P2 | T-162 | PENDING | — | backend-agent hit two cases the closed 12-member enum does not cover: `status` arriving outside the write vocabulary, and a value-type violation on a permitted field. It chose `structural_field_not_settable` and read `note_not_text` as its general form, commented both AT the decision site, and **refused to silently coerce `today` → `inbox`, which ADR-009 forbids by name.** Either add the members or record the two readings as the contract's answer |
| T-171 | AC-3's both-pending precedence — the proactive save is not implemented | assistant | F-005 | web-agent | P1 | T-163 | PENDING | — | From T-163's own unresolved list. AC-3 requires a focused dirty field to save **as though focus left it** when an accepted write arrives; today the arrival is deferred and the save waits for the user's own blur. That satisfies 'the edit reaches the store' and 'the control displays what the store holds' but NOT the proactive save — and `## Test strategy` calls this the highest-value single case in the feature |
| T-173 | AC-26's headline may still have an edge after revision 4's correction | assistant | F-005 | spec-agent | P3 | T-166 | PENDING | — | qa-api-agent (T-166) flags: complete → un-complete (AC-28 removes the successor) → re-complete leaves the series with **no open occurrence**, while the headline says no occurrence in a live series generates none. **Revision 4 DID correct the phrasing** (verified — it quotes T40 and states the per-occurrence form), so this is an edge in the corrected text, not an unlanded disposition. Fifth sighting of this sentence. Gate 1 is closed: one clause of confirmation, in the same class as T-168/T-169 |
| T-174 | The row's urgency mark renders `!!!` — the graduated scale AC-9 forbids by name — and a test PINS it | assistant | F-005 | web-agent | P1 | T-163 | PENDING | — | Found by RUNNING the app. `src/assistant/web/components/TasksSurface.tsx:81` maps `high: '!!!'`; AC-9 says *not Apple's graduated ! / !! / !!!*, and design's `components.md` TR-URGENCY (line 184) publishes **a single `!`, only when priority is high**, nothing for none/low/medium. **`src/assistant/web/__tests__/task-detail.test.tsx:1463` asserts `.toBe('!!!')`, so the forbidden rendering is protected by a green test** — L-006/L-012: the assertion encodes the implementer's choice, not the requirement. Why nothing caught it: the ACCESSIBLE NAME is correct ('high priority'), so AC-33's 4.1.3 case passes while AC-9's visual requirement fails. **Third sighting of L-021's shape** — design drafted the same scale and caught itself; web shipped it |
| T-160 | F-005 architecture — contracts, data model, ADRs | assistant | F-005 | architect-agent | P0 | T-159, T-157 | DONE | docs/specs/assistant/api-contracts.md, docs/specs/assistant/data-model.md, docs/specs/_shared/ARCHITECTURE.md, docs/specs/_shared/platform/backend.md, docs/specs/_shared/platform/web.md, docs/specs/_shared/platform/mobile.md, docs/specs/_shared/adr/ADR-010-account-entity-and-timezone.md, docs/specs/_shared/adr/ADR-011-recurrence-representation.md, docs/specs/_shared/adr/ADR-012-delete-membership-and-restore.md, docs/specs/_shared/adr/ADR-013-turn-causality-plan-before-capture.md, docs/specs/_shared/adr/ADR-014-run-count-from-ever-completed.md, docs/specs/_shared/adr/ADR-015-step-ordering-sparse.md | **All 12 recorded-for-architecture questions answered, none deferred** — the owner's record-don't-guess split paid off on the first dispatch (L-020). ADR-010 creates the account entity and AMENDS ADR-005, whose premise had been unbuilt since 2026-08-16; the read/write refusal split means a read withholds a derived value and never a row, so GET /tasks renders for a zoneless account (AC-32's by-hand user). ADR-013 states AC-46's plan-before-capture ordering. Verified: 6 ADRs + 6 files on disk, src/ and docs/qa/ untouched, full suite 771/771 (the return's 560 was a narrower command) |
| T-134 | design-check state check passes for an incidental reason (L-007 shape) | assistant | F-001 | design-agent | P2 | T-131 | PENDING | — | it only fails when ALL states are identical, and its snapshot is body.innerText[0:400], which with 32 dev-bar buttons is almost entirely dev-bar text |
| T-086 | Touch floor row for the shipped affordance | assistant | F-001 | design-agent | P2 | T-083 | PENDING | — | PAINTED carries a documented placeholder 48; the floor must be measured from the shipped control |
| T-060 | Fix BUG-004: follow new messages (both clients) | assistant | F-001 | orchestrator | P1 | T-084,T-085 | BLOCKED | — | code landed both clients; closes when QA covers AC-30 |
| T-074 | components.md needs rows for every string it does not own yet (~17 found: 2 server-composed + ~15 message families) | assistant | F-001 | design-agent | P1 | T-062 | PENDING | — | ADR-008's own 3rd review condition + product-review M6, live today: these strings are authored in src/assistant/api/engine/turns.ts, so the catalogue does not own every string the ADR's cost model assumes |
| T-075 | QA: re-sync F-001 spec:596 + utterance-intent-fixtures.json (still carries hoàn tác, HOÀN TÁC, không) | assistant | F-001 | qa-api-agent | P1 | T-069,T-073 | PENDING | — | L-004: the fixture table has two physical homes and they now diverge by more than one row |
| T-065 | Replace F-002's copied spoken-copy literals with row-ID citations (NOT translate them) | assistant | F-002 | spec-agent | P1 | T-064 | PENDING | — | spec-agent checked rather than assumed: components.md §Spoken frames ALREADY owns these strings in English (title_list overflow :98, SPK-CONFIRM-DELETE :107, SPK-REVERTED-PARTIAL :116). So F-002:200/206/209 are a SECOND COPY, not untranslated copy — translating them re-creates two homes in English where the next drift is invisible instead of merely visible in the wrong alphabet. Cite row IDs per L-008. Load-bearing: ADR-008 names 'copy stops having one owning artifact' as the trigger that expires the no-i18n decision. CARVE-OUT: F-002:135/:354 quote the owner's own words in Vietnamese — decision record, leave them. Also fix F-001:43's now-stale citation of AC-5's dropped phrase (3 words) |
| T-066 | Decide whether ARCHITECTURE.md's ADR index is a register; backfill 007-008 if so | assistant | — | architect-agent | P3 | T-063 | PENDING | — | — |
| T-079 | QA harness reuse contaminates local runs: freshUser counter restarts at 1 while the server keeps state | assistant | F-001 | qa-web-agent | P2 | T-070b | PENDING | — | reuseExistingServer:!CI + in-memory harness state + a counter that resets = run N gets run N-1's data under the same account names. Cheapest fix: seed freshUser from a per-run nonce. Same contamination class the orchestrator hit taking screenshots |
| T-077 | ~18 mobile TC docs still cite Vietnamese mockup copy the product cannot produce | assistant | F-003 | qa-mobile-agent | P2 | T-070c | PENDING | — | each sends a human tester after a string that no longer exists; TC-001/003/006/007/008/009/011/013/014/015/016/020/021/022/023/037 + index.md |
| T-078 | 8 unit-tier sites fabricate the server's Vietnamese chip labels (green but impossible) | assistant | F-003 | mobile-agent | P2 | T-070c | PENDING | — | parity.test.ts:79,254 · a11y.test.ts:173,175,240,380,382 · lifecycle.test.ts:184 — the server emits Delete N tasks/Keep them now; these pass because the fixture builds the turn itself (L-004) |
| T-072 | docs/design/_shared/DESIGN.md + tokens.json carry Vietnamese (incl. a 2nd `hoàn tác`); font rationale now void | assistant | — | design-agent | P2 | T-062 | PENDING | — | — |
| T-053 | Write the Spoken frames wording IN ENGLISH (AC-22 parse test depends on it) | assistant | F-002 | design-agent | P1 | T-062 | PENDING | — | — |
| T-054 | Place the talk-back on/off control + control-vs-message split (design F4) | assistant | F-002 | design-agent | P2 | T-052 | PENDING | — | — |
| T-055 | Disambiguate existing-app ADR-7/ADR-11 vs this repo's ADR-007 in MANIFEST | assistant | — | orchestrator | P3 | T-052 | PENDING | — | — |
| T-048 | Add client.speech_prefs to data-model.md | assistant | F-002 | architect-agent | P2 | T-044 | PENDING | — | — |
| T-020 | QA mobile: author test cases | assistant | F-003 | qa-mobile-agent | P2 | T-018 | PARTIAL | docs/qa/assistant/F-003/mobile/, tests/assistant/mobile/F-003-mobile-surface.spec.ts, docs/qa/_shared/fixtures/mobile/F-003-mobile-fixtures.json | 39 P1 TCs covering 12 F-003 + all 29 F-001 parity ACs; ran automation 29/32 (2 fails await T-019 code, 1 real contract drift); device-lab debt documented per L-003 |
| T-021 | QA mobile: execute | assistant | F-003 | qa-mobile-agent | P2 | T-019,T-020 | PARTIAL | tests/assistant/mobile/F-003-mobile-surface.spec.ts, docs/qa/assistant/F-003/mobile/, docs/qa/assistant/runs/2026-08-17-mobile-execute.md, docs/qa/_shared/bugs/BUG-002-cold-open-accepts-input-before-session-read.md | suite 32->106 assertions, 105/106; filed BUG-002 (HIGH); BUG-001 pinned by KNOWN-OPEN test; 34/39 automated, device-lab debt unchanged |
| T-022 | Structural review C1-C14 for F-003 | assistant | F-003 | reviewer-agent | P3 | T-021,T-024,T-025,T-026 | PARTIAL | docs/reports/review-F-003-2026-08-17.md, docs/specs/_shared/LEARNINGS.md | STRUCTURAL-FAIL 12/14; C9 (client.permission_state missing from data-model), C7 (7 high npm audit via metro, needs human call); C2 41/41, C5 all 6 suites green; overturned 2 tool false-positives; L-005/6/7 appended |
| T-176 | T-164's aftermath — 7 tests calling a method that does not exist, and one testid F-003 needs | assistant | F-005 | mobile-agent | P1 | T-164 | PENDING | — | Two separate defects, both introduced by T-164 and both measured here on 2026-08-19. **(a)** `src/assistant/mobile/__tests__/f005-mobile.test.ts` calls `h.controller.dispatch2(...)` at lines 524/549/578/598/607/618. `dispatch2` appears nowhere else in src/ — the real `dispatch` is `protected` on the SHARED controller, so the test wants a seam that was never built. Same file also uses `carriedRowFor` (undefined), `announcer.spoken` (RecordingAnnouncer has no such property) and `task({ local: true })` (not in `Partial<TaskWire>`). 20 tsc errors, 7 failing tests, all in this one file. **Decide the seam, do not paper over it:** either expose a test-only way to seed controller tasks or seed through `h.server` + `init()` as every other mobile test does. **(b)** `assistant-row-badge` is no longer rendered, so F-003's QA automation fails one assertion it passed before. This one is the more serious of the two — it is a live regression in a SHIPPED feature, and the failing test is QA-owned, so the fix belongs in `src/`, never in the spec file. |
| T-177 | F-005 amendment — § CarriedNotice moves to the bottom, and the undo rows gain a 10s life | assistant | F-005 | spec-agent | P0 | — | DONE | docs/specs/assistant/F-005-task-detail.md, docs/specs/assistant/index.md, docs/reports/gate1-lenses/F-005-revision-5-log.md | Revision 5 shipped. **Verified by the orchestrator rather than taken on the return's word:** AC ids extracted and diffed against `seq 1 48` — **exactly 1..48, contiguous, 48 before and 48 after**; `declared-elements.sh` exit 0; all three paths on disk; OQ13 closed at line 105; F-006 cited 15 times so the dependency is written into the text rather than assumed. 4 ACs amended (AC-47, AC-43, AC-33, AC-41). **Nothing here is proven by a test — a spec neither renders nor executes, and the agent said so itself rather than letting the green count imply coverage.** One defect it did not catch is T-180. |
| T-178 | F-006 — Recently deleted (the trash) | assistant | F-006 | spec-agent | P0 | T-177 | DONE | docs/specs/assistant/F-006-recently-deleted.md, docs/specs/assistant/index.md | F-006 written — 450 lines, **16 ACs, ids 1..16 contiguous, all 16 platform-tagged** (verified by the orchestrator, not taken on the return's word), `declared-elements.sh` exit 0, index row added. **No lens has read it — Gate 1 has not run, so the ACs are unreviewed.** Two API doors (the trash read, permanent deletion) are stated as requirements without shapes, deliberately left to architecture. **One owner decision is open and blocks Gate 1 usefully: the retention length (OQ1).** Two drift findings verified independently by the orchestrator, both real: T-181 and `## Impact` §3. |
| T-179 | § CarriedNotice — bottom placement and the two-lifetime rule, in the design system | assistant | F-005 | design-agent | P1 | T-177 | DONE | docs/reports/design-signoff-visual-language.md | **SUPERSEDED 2026-08-21 by the visual-language redesign, closed rather than run.** Both would have drawn against v1, which no longer exists — the owner replaced colour, type, spacing, shape and layout wholesale. What each owed is now inside T-204's ordered redraw. **Closing them rather than leaving them PENDING is the point: a queued task that draws against a retired system is worse than no task, because it looks like coverage.**  phase: system. Amends `docs/design/_shared/components.md § CarriedNotice` (## Placement, ## Multiplicity, the lifetime table) and the IA's frame-level entries. **The strip-order rule inverts rather than reverses:** it was written *outermost first* from the top; from the bottom the principle is unchanged and reads *a strip that is not about the surface it appears on stays furthest from the surface's content*. **The composer is the real constraint** — design's own note says § OfflineBanner docks above the composer at the bottom of Talk, so 'bottom' cannot mean the screen edge on the one surface where the app's primary input lives, and the region must move with the keyboard. Also owed: whether the 10s life gets any visual expression at all (a countdown ring is the obvious idea and is probably wrong — it draws the eye to the clock rather than the action). **Sharpened 2026-08-21 from T-177's return, which names three staleness points and one of them is a flat contradiction, not a drift:** `components.md` § CarriedNotice states *"There is no timer anywhere in this family, so WCAG 2.2.1 is not engaged"* — revision 5 puts a ten-second timer on two of its rows, so that sentence is now false where it stands and it is the sentence a reader consults to learn whether 2.2.1 applies. The other two are `## Placement` (top bar → bottom of frame, with the composer/keyboard rule on Talk) and the strip-order rule, which **inverts rather than reverses**. spec-agent deliberately did not touch this file — it is design's. **Third item added 2026-08-21 from the owner's answer to F-006's product lens (option A):** when `CN-UNDO` reaches its ten seconds, what replaces it **names the trash**. Copy is design's and joins this revision rather than starting its own. Note the constraint this inherits: the `§ Buttons` one-word-per-concept table binds **delete** to *removing a task* and has **no word for *stops being recoverable*** — the design lens raised that as F6(a) against F-006's AC-3, and the same gap bites this string. |
| T-180 | docs/specs/assistant/index.md contradicts itself about which revision F-005 is at | assistant | F-005 | spec-agent | P1 | T-177 | PENDING | — | Line 10's table row reads `draft (rev 5)` and line 26 describes revision 5, but **line 16 still opens `**F-005 is at revision 4**` in the present tense.** T-177 updated the row and appended the new paragraph, and left the lead sentence standing. **This is the second time this exact file has done this**: the Drift Log records 2026-08-18, when T-145 shipped F-001 revision 5 and the index still said revision 4 — caught in passing by T-153 only because another edit had the file open. It matters more than its size: the index is the one file a fresh session reads to learn where a feature stands, so a wrong revision number there is wrong everywhere it is quoted. **And the shape is the one T-177 was itself hunting** — a new statement landing while its predecessor is left in place — which it found six times elsewhere in the spec and missed once in its own output. Fix: make the lead sentence a template that names the CURRENT revision, so the next revision cannot land without touching it. |
| T-181 | A series restore returns the occurrences with the repeat permanently dead | assistant | F-005 | architect-agent | P1 | — | PENDING | — | Found by spec-agent on T-178 and **verified independently by the orchestrator the same hour**: `engine/plan.ts:653` writes `series_ended_at` on EVERY row of a series delete, while the restore at `api/app.ts` clears **`deleted_at` and `updated_at` and nothing else**. So `F-005 AC-43`'s *"it reverses exactly the action it was offered for and nothing else"* is **false for the series class**. **Currently invisible, and measured: 0 rows in the live store carry `series_id` and `deleted_at` together**, because the undo window is seconds wide and nobody has ever restored a series later. **F-006 is what makes it real** — the trash turns that restore into a deliberate act days after the delete. Not a new-code defect; a defect that surfaces because a new surface changes how long old code is looked at. Owner is ADR-012 (the restore) with the claim in F-005 AC-43, so architect decides whether the restore clears the field or the AC narrows its claim — **not spec-agent, which correctly recorded it rather than touching either.** Durable half is **L-026**. |
| T-182 | Gate 1 on F-006 — five lenses, spec_review: full | assistant | F-006 | orchestrator | P0 | T-183 | DONE | docs/reports/gate1-lenses/F-006-consolidated.md, docs/reports/gate1-lenses/F-006-product.md, docs/reports/gate1-lenses/F-006-architect.md, docs/reports/gate1-lenses/F-006-design.md, docs/reports/gate1-lenses/F-006-tester-api.md, docs/reports/gate1-lenses/F-006-tester-web.md, docs/reports/gate1-lenses/F-006-tester-mobile.md, docs/reports/gate1-lenses/F-006-dev-api.md, docs/reports/gate1-lenses/F-006-dev-web.md, docs/reports/gate1-lenses/F-006-dev-mobile.md | **REJECT — 21 HIGH · 29 MEDIUM · 6 LOW across nine lenses.** All nine returned findings AND a `checked:` list; **no lens violated the anti-theatre rule.** Seven convergences, the largest found independently by **7 of 9**: a trash entry's membership is not a closed set, so *delete forever* can hard-remove a LIVE task and a restore can produce a permanently invisible row. No conflicts — no two lenses gave incompatible directives. **One decision reaches the owner** (product F2, discoverability). Also affirmed, and recorded because a gate that only reports defects cannot be trusted to have looked: every store measurement reproduces exactly, re-derived by four lenses independently; the no-scheduler premise holds; every AC obeys the lifecycle-state claim the briefing aimed all nine at; and all phase-5 fixtures are constructible today. Revision is T-184. |
| T-183 | F-006 — fold in the owner's retention answer, closing OQ1 | assistant | F-006 | spec-agent | P0 | T-178 | DONE | docs/specs/assistant/F-006-recently-deleted.md | F-006 at revision 2. **Verified by the orchestrator:** ids 1..16 contiguous (16 before, 16 after), `declared-elements.sh` exit 0, `30 days` present in 18 places, the reachability-not-storage rule in **AC-12's own text** at line 189 rather than only in a note, OQ1 closed with the original question kept verbatim so it cannot be re-asked. **The agent swept for hedged leftovers and found EIGHT, not zero** — the same shape it named on F-005 at T-177, now on its own text one dispatch later. The stalest granted the system *"remove a row whose retention has passed"* while nothing in the document said what the retention was. **Gate 1 has not run — no lens has read any of this.** |
| T-184 | F-006 revision 3 — the Gate 1 round-1 revision | assistant | F-006 | spec-agent | P0 | T-182 | DONE | docs/specs/assistant/F-006-recently-deleted.md, docs/specs/assistant/index.md, docs/reports/gate1-lenses/F-006-revision-3-log.md | **All 56 findings dispositioned — 54 fixed, 2 recorded, 0 rejected; all 21 HIGH fixed.** Verified by the orchestrator: 17 ACs, ids 1..17 contiguous, **17/17 platform-tagged**, `declared-elements.sh` exit 0, the log carries exactly **56** rows, and **all four ACs that AC-6's canonical membership names do cite it** (grepped individually). C1 fixed once in AC-6; the four undefined outcomes now have stated answers — two made impossible, one no longer silent, one refused with a reason. `## Impact` §1's headline count is **gone**, replaced by a 14-row table; §10 is 17 rows **with a Writer column**, up from 8 rows and no writers. 466 → 681 lines. **Confidence MEDIUM, lowered by the agent itself and correctly: AC-17 is new and no lens has read it.** Re-review is T-186. |
| T-185 | F-005 revision 6 — the elapsing undo offer names the trash | assistant | F-005 | spec-agent | P1 | T-184 | PENDING | — | Owner decision 2026-08-21, `docs/reports/owner-decision-2026-08-19-carried-notice-placement-and-timer.md` §7, option A. **Two amendments, both consequences of one answer, neither adding an AC.** **(a)** AC-43's elapse stops being silent: it is the moment the offer **hands off**, and what replaces it names the trash as where the task went. Wording is design's (T-179); that the handoff happens is the AC's. **(b)** AC-33's 2.2.1 argument gets its last leg — it currently says the timer is conformant *because an untimed path exists*; it can now say the user is **told about** that path at the moment the timed one ends, **which is the difference between a path existing and a path being available.** Depends on T-184 only to stop two spec-agent dispatches racing on `docs/specs/assistant/index.md`. 48 ACs before and after. |
| T-186 | Gate 1 round 2 on F-006 — TARGETED re-review | assistant | F-006 | orchestrator | P0 | T-187 | DONE | docs/reports/gate1-lenses/F-006-r2-consolidated.md, docs/reports/gate1-lenses/F-006-r2-product.md, docs/reports/gate1-lenses/F-006-r2-architect.md, docs/reports/gate1-lenses/F-006-r2-design.md, docs/reports/gate1-lenses/F-006-r2-tester-api.md, docs/reports/gate1-lenses/F-006-r2-tester-web.md, docs/reports/gate1-lenses/F-006-r2-tester-mobile.md, docs/reports/gate1-lenses/F-006-r2-dev-api.md, docs/reports/gate1-lenses/F-006-r2-dev-web.md, docs/reports/gate1-lenses/F-006-r2-dev-mobile.md | **14 HIGH · 20 MEDIUM · 3 LOW. ROUND CAP REACHED — escalated to the owner.** The finding that frames the rest: **all 56 round-1 findings hold**, verified independently by nine lenses, most against the source or the working tree rather than the disposition log. **So round 2 is not a report that revision 3 failed — it is a report that the things nobody had reviewed were the things that were wrong.** Of the 14 new HIGHs: **7 on AC-14/AC-15 (the owner's read permission, added AFTER round 1), 3 on AC-17 (created by a revision-3 fix, never read), 4 on clauses ADDED by revision-3 fixes — and ZERO on text that existed at round 1 and survived it.** Seven convergences; the largest is that **the owner's decision is currently unimplementable** — three closed vocabularies each independently have no slot for what the assistant is now required to say. **All nine lenses judged the routed dead end ACCEPTABLE**; none asked to reverse it. |
| T-187 | F-006 revision 4 — the assistant may read the trash | assistant | F-006 | spec-agent | P0 | T-184 | DONE | docs/specs/assistant/F-006-recently-deleted.md | Revision 4. **Verified by the orchestrator: 17 ACs before and after, and exactly four headline lines changed — AC-4, AC-5, AC-14, AC-15** (my first measurement said seven and was wrong: the grep extracted every id mentioned on a changed line, not the headline id). `declared-elements.sh` exit 0, `index.md` untouched, 64/28. OQ4 closed with the question kept. **The agent found two things the briefing did not ask for and both were real.** **(a)** AC-4's HEADLINE said *"no assistant query"* — a read exclusion sitting in an addressing AC — so "AC-4 is unchanged" was true of its clause and false of its headline. **(b)** AC-5's *"it is the only one that does"* would have been silently falsified by the grant; it added one clause saying the assistant's read **IS** AC-5's read — a caller, not a second door — which keeps the exclusivity claim true and stops an architect opening a second deleted-row reader in the turn path. **The dead end is recorded and deliberately unsettled**, routed to round 2 per the owner's decision. Sweep found **6** leftovers the old answer left standing. |
| T-188 | F-006 revision 5 — the round-2 findings, and the two frames the trash needs | assistant | F-006 | spec-agent | P0 | T-186 | DONE | docs/specs/assistant/F-006-recently-deleted.md, docs/specs/assistant/index.md, docs/reports/gate1-lenses/F-006-revision-5-log.md | **37 of 37 dispositioned — 35 fixed, 2 recorded, 0 rejected; all 14 HIGH fixed. The no-new-AC constraint held: 17 → 17.** Verified by the orchestrator: ids 1..17 contiguous, 17/17 tagged, `declared-elements.sh` exit 0, **all 37 finding ids named in the log** (25 were named in the consolidation). 717 → 857 lines. **The agent caught an orchestrator error that cost a HIGH:** the round-2 consolidation accounted for **27 of 37** findings, and the ten it dropped included **product F2** — an unbounded assistant read that would speak step titles and break ADR-013 in the one path no AC asserts on. It found it by writing against the nine lens files rather than the consolidation, which the briefing told it to do. Durable half is **L-028**. **F-006 gets no further review** — the owner waived the cap for one revision, not for another round; remaining MEDIUM/LOW go to architecture as recorded questions. |
| T-189 | F-007 — the model authors the reply | assistant | F-007 | spec-agent | P1 | T-188 | PENDING | — | **THE SHAPE IS AN AGENTIC LOOP, NOT A PIPELINE** (owner, 2026-08-21 — the orchestrator drew it as three fixed AI calls twice and was corrected twice; §3 of the decision record has the flow). Backend **exposes tools**, the AI drives: it searches and reads, decides what it needs next, and **the number of rounds depends on the question and on how well the model does.** Then it returns **actions + targets + the line in one response**, the client confirms, the backend executes and reports back, and the AI may loop again. **Three consequences:** (a) the AI is **not guessing a task name — it names the rows it is targeting**, so the consistency check is on one response rather than a guard against invention; (b) **reads during the loop, writes only after the confirm** — the owner's diagram puts `client confirm` before `backend take action`, and if the AI could write during the loop the confirm would be decorative; (c) **an agentic loop needs a bound a pipeline does not** — not to predict the round count but so a bad question cannot spin. Default: a max round count AND a wall clock, both adjustable, **and the spec must say what the user sees when either is hit — silence at the ceiling is the failure mode.** Owner decision 2026-08-21, `docs/reports/owner-decision-2026-08-21-the-model-authors-the-reply.md` §2: **yes, the model must answer.** NEW FEATURE, own spec, own Gate 1 — **not** an amendment: it changes the server↔client contract, re-opens 518 assertions and touches five shipped features, so folding it anywhere puts it in front of no lens. **Measured 2026-08-21:** 3 files compose reply text (874 lines), **158 hardcoded strings**, **518 exact-string assertions across 36 test files**, 22 spoken frame rows, 5 features. **The row that changes the shape of the work: the model is ALREADY in the loop** — `ports/interpreter.ts` calls `claude-opus-5` — so this widens what an existing call returns rather than adding a dependency. **The 518 assertions are the real cost and they are not waste:** they are what makes today's copy verifiable, and a model-authored reply cannot be asserted by equality, so each becomes a property assertion (*does it name the task? state the count?*) or a deleted test — **deciding which, 518 times, IS the work.** **On what stays fixed: nothing does, and the orchestrator's first recommendation was withdrawn by the owner's question.** The original claim — *a hallucinated task name deletes the wrong thing* — **is false**: deletion is id-driven, so a wrong name still deletes the right row. **What breaks is CONSENT** — the user says yes to a sentence that does not match the action, which for a confirmation is not a side effect but the whole purpose of the sentence (the owner's 2026-08-17 decision rejected count-only for exactly that reason). **Stated correctly, the fix is not a template.** The requirement is not *this sentence is fixed* but ***the facts in this sentence are true***, and the cheaper way to get it is: **the model composes freely, then code verifies every task name in the sentence is in the set about to be destroyed and the count matches** — mismatch regenerates or falls back. The model stays free; one short verifier. **Recommended scope of the check: the sentences where a proper noun or a count carries the user's decision** — the destructive confirmation, probably the applied-summary. Everything else the model writes and nobody checks. Gate 1's lenses press on the boundary. |
| T-190 | Audit every spec for the fixed-reply assumption — the AI answers, it does not pick a sentence | assistant | — | spec-agent | P1 | T-189 | PENDING | — | **Owner, 2026-08-21: every spec that describes the chat surface or the voice surface currently assumes the reply is a CHOSEN sentence, and that assumption is wrong — the AI answers** (`docs/reports/owner-decision-2026-08-21-the-model-authors-the-reply.md` §2, §3). This is the sweep that follows F-007, not a duplicate of it: **F-007 (T-189) specs the new mechanism; this row finds and corrects everywhere the OLD mechanism is written down as a requirement.** **Measured 2026-08-21 — grep for `spoken frame` / `frame by row` / `one composer` / `literal` / `template` / `closed vocabulary`:** F-002 **20** sites, F-001 **10**, F-003 **5**, F-005 **24**, F-006 **4**, `design/_shared/components.md` **54**, `information-architecture.md` **1** — **118 places across 7 documents.** **The distinction that makes this hard, and it must be preserved rather than swept flat:** some of those are the assumption (*"the composer selects a frame"*) and some are a REQUIREMENT that survives the change (*"a destructive confirmation names the tasks"* — the owner's 2026-08-17 decision, still true when the AI writes the sentence). **Deleting the second kind along with the first is the worst available outcome**, and it is what a mechanical sweep produces. Every edit gets a one-line reason in the log saying which kind it was. **Also in scope:** `ADR-001` and `ADR-003` name the Interpreter as *the AI seam* for interpretation only — say whether they are amended or superseded, and by whom. **Do NOT start before T-189 lands:** correcting the old mechanism before the new one is written means guessing what to correct it TO. |
| T-191 | F-006 architecture — contracts, the two frames' wire shape, and nine owed decisions | assistant | F-006 | architect-agent | P1 | T-188 | PENDING | — | **Gate 1 is closed on F-006 and does not reopen.** Revision 5's log ends with **nine architecture-owed shapes, each already recorded in the spec's own words with a writer named** — start there, not from the ACs. Includes: the trash read's route and whether an entry carries what it needs to name its parent (dev-web r2 F1); the expired-restore outcome, distinguishable from unknown-id and from already-live; the `skipped` reason for a purged row; the permanent-deletion envelope with no `task:` filler; the raw-store harness read AC-12 and AC-17 both owe; and **the `turn.outcome` member that selects the two new trash frames** — design writes the utterances, architect writes the member. **Also owed and named by architect r2 F5: an ADR-009 Amendment for the lifecycle-state-not-container rule, and an ADR for the read that mutates.** T-181 (the series restore) is the same agent's and can ride along. |
| T-192 | Audit: where does the project still not understand voice-first | assistant | — | product-agent | P1 | — | DONE | docs/reports/audit-voice-first-2026-08-21.md | **6 findings, and the gap is NOT where the word count pointed.** The word count was a pointer and was wrong about which document was at fault: the two lowest-voice specs are both **correct** — F-003 delegates through a parity table enumerating all 29 F-001 ACs, F-005's hand-first shape is the owner's own decision — while **the real defect lives in the spec with the HIGHEST voice count.** **F-002 `§ What speaks` has 14 speaking rows and every one is a reply to a turn the user issued.** The two things the app is already specified to say unprompted — the landing summary on open, a passed reminder — **are both specified as pixels. The app only ever speaks when spoken to.** Verified by the orchestrator: the section's rows are all reactions, and **F-004 has no spec file** — it is *reserved, not committed* — so design defers the owner's 2026-08-18 spoken-summary request **by citing a feature that exists only because F-002 deferred it.** Also delivered: **the inherited-premise check nobody had ever done** — two tables, 13 rows, saying which of the source product's voice premises survived. **And three honest 'this is fine' answers with what was checked**, which is what makes the other six credible. D1 escalated. |spoken|nói` vs `typed|by hand|keyboard` per spec:** F-002 **39 : 4** · F-001 **21 : 13** · F-006 **16 : 14** · **F-005 31 : 36 — the hand wins** · **F-003 3 : 8 — the MOBILE surface spec mentions voice three times.** Those last two are the shape of the question: **a product that says voice-first has its phone spec and its biggest feature written hand-first.** Whether that is wrong is the owner's call — the audit's job is to say where it happens, with counts, and to separate *deliberately hand-only* (structure, ordering, pickers — F-005 decided that on purpose) from *voice was simply not considered*. Also check `docs/specs/_source/` — the inherited product had a voice-first UI research doc and a conversation use-case doc, and whether this redesign kept their premises is checkable. **Output is a report in `{reports}/`, not spec edits.** Everything it finds becomes a task or an owner question. |
| T-194 | F-002 + index: F-004 is committed and scheduled, not reserved | assistant | F-002 | spec-agent | P1 | — | DONE | docs/specs/assistant/F-002-talk-back.md, docs/specs/assistant/index.md | Both sentences now read *committed as F-004 and scheduled after MVP*, with the content named. Verified: AC count 25 before and after, `declared-elements.sh` exit 0, `git diff --name-only` shows only the two files. **The agent found the briefing was short: grepping the id returned NINE sites, not two.** It fixed two more inside its scope — including `## Purpose` in the same file, which said *"F-004 has no owner decision behind it yet"* and would have contradicted `## Out of Scope` three sections later. **And it named five it could not touch, one of them load-bearing:** `components.md:1134` justifies a whole frame family not speaking **by citing F-004 as undecided** — a design argument resting on a fact that stopped being true today. Follow-ups T-197 and T-198. |
| T-195 | Nothing says how an utterance ends, and the two design files disagree | assistant | F-001 | spec-agent | P1 | — | PENDING | — | Audit T-192 finding F3, and it is **not a voice-first question — it is the most-used interaction in the product having no AC.** F-001's User Flow has exactly one edge out of Listening into Thinking (`end of speech`), **AC-29 declares that edge list complete, and no AC constrains that edge**: not who decides speech ended, not what the user does to end it deliberately. AC-2 covers what listening renders, AC-3 covers cancel; **the gap is between them.** The two design files fill it differently: `DESIGN.md § User journey` says *end-of-speech **auto-sends***, while `components.md § MicControl`'s a11y name while listening is ***"Listening — tap to stop"*** — and under AC-3 a stop while listening is a **CANCEL**: text to the composer, nothing sent. **So the one control a user has mid-utterance either sends or discards depending on which file you read.** |
| T-196 | Route the rest of the voice-first audit — F2, F4, F5, F6 | assistant | — | orchestrator | P2 | T-194 | PENDING | — | The four findings from T-192 not already carried by T-194 or T-195. **F2 (session boundary is visible, never audible)** — UC-52 AC-52.4 is called *the heaviest AC of the whole use case BECAUSE the user is hands-free*, and this repo answers it with a visible marker; F-002's reason is *history*, **true of its delivery channel and false of its meaning**, since ADR-004's lazy close produces the boundary on the request carrying the user's next sentence — the user learns the session ended AFTER speaking into the closed one. **F4** — the mic has one home and nothing writes that down as a requirement, so nothing forbids a second. **F5** — voice discoverability is two design strings, both first-run-only, **one of them unwritten**, neither required by any AC. **F6** (cannot tell) — headset as capture route, and no non-visual capture cue on mobile. Read the audit's own sections before filing; each already carries its severity and its label. |
| T-197 | Two places in the coverage map still say the spoken summary is undecided | assistant | — | spec-agent | P2 | T-194 | PENDING | — | `docs/specs/_shared/uc-coverage-map.md:126` and `:340` still assert the old status. **Small, but this is the file a reader consults to learn what is and is not committed**, so a stale line here is stale everywhere it is quoted. Owner decision `docs/reports/owner-decision-2026-08-21-the-app-speaks-on-open.md`. |
| T-198 | A design rule is justified by a fact that stopped being true today | assistant | — | design-agent | P2 | T-194 | PENDING | — | **The load-bearing one.** `components.md:1134-1137` reads *"F-002 reserves as F-004 and explicitly records as having no owner decision behind it. So this…"* — **the undecidedness is used as the REASON a frame family does not speak.** As of 2026-08-21 there is an owner decision, so the reason is false even though the rule may still be right: at MVP the app is silent, which is a different argument for the same behaviour. **Re-justify or change the rule; do not leave a design decision resting on a retracted premise.** Also stale, and cheaper: the three app-shell mockups (`app-shell.html:821`, `-ios.html:757`, `-android.html:761`). Decision: `docs/reports/owner-decision-2026-08-21-the-app-speaks-on-open.md`. |
| T-199 | The task-detail fields render 197px wide because their class is styled nowhere | assistant | F-005 | web-agent | P1 | — | PENDING | — | **Ugliest single screen in the app**, from the design audit (`docs/reports/design-audit-2026-08-21.md` F1). At 1440 every text field in the detail pane renders **197px wide inside a 1020px pane**, and the Name field **clips the task's own title**. Cause located and **verified independently by the orchestrator: `detail-field-control` appears once in `TaskDetail.tsx` and ZERO times in `styles.css`, `components.md` or any mockup** — so the input falls back to the HTML default `size=20`. **A class used and never styled is invisible to every check in this project**: design-check tests overflow, not underflow, and it passed 60/60 with this on screen. |
| T-200 | Wide desktop: 52% of the Tasks pane is dead at 1920, and it is in the mockup too | assistant | — | design-agent | P1 | — | PENDING | — | **phase: system.** The one task that did not exist and is the thing the owner was looking at. `.tasks-col` is `max-width: 720px` **with no `margin: 0 auto`**, so the wasted gutter is **0px at 1024, 140 at 1280, 300 at 1440, 780 at 1920 — 52% of the pane.** The CSS justifies the left anchor as *"the canvas is used by the SPLIT"*; **measured, the split is a fixed 420 and every pixel past 1140 goes to the gutter.** **It reproduces exactly in `app-shell.html`, so this is a DESIGN defect, not drift.** Root cause is upstream of the rule: **`tokens.json`'s widest breakpoint is `desktop: 1280`, design-check iterates that list and stops, and at 1280 the defect measures 140px and reads as padding.** Decide what 1440/1920 does with the space, in `components.md § AppFrame` + IA §1a. **This lands BEFORE more screens are drawn against the current rule** — T-179 then T-165, in that order, because F-005's screens render the notice region and drawing them first means redrawing them. |
| T-201 | The conversation is top-anchored — 618px of nothing above the composer | assistant | F-001 | web-agent | P2 | — | PENDING | — | Design audit F3 (`docs/reports/design-audit-2026-08-21.md`). **618px between the last bubble and the composer at 1440, 462px at 390.** **Pure implementation drift, and the mockup is right:** `app-shell.html:417-423` carries `margin-top:auto` **and a paragraph defending it**, while `.conv` / `.conv-scroll` in the app carry neither that nor `justify-content: flex-end`. The audit's note on why it matters: a top-anchored thread is *"the specific thing that breaks the resemblance"* to the products this app is meant to sit beside. |
| T-202 | New visual language — the system only, delivered as something the owner can look at | assistant | — | design-agent | P0 | — | DONE | docs/design/_shared/specimen.html, docs/design/_shared/DESIGN.md, docs/design/_shared/tokens.json | **New language delivered and rendered: 9 plates, web/iOS/Android, 7 widths, 2 themes, in one 71.6 KB self-contained file — verified over `file://` with 0 console errors and 0 horizontal overflow.** Orchestrator checked: **zero external references**, and **18 testids used, all from the kept catalogue, none invented.** The look is replaced completely (Swiss, light-first, white ground, radius 0, **3 accents down from 5**) while every one of the 52 ids stays. **The wide-desktop rule the old system never had is now measured at every declared width and symmetric at all of them — v1 at 1920 was 0/780, v2 is 220/220, and the list never narrows as the frame grows.** **`design-check` now exits 1 with 510 failures and that is the CORRECT signal** — all 510 are token drift on the six v1 mockups the screens dispatch will redraw. **Two defects it caught in its own work by rendering rather than reading:** a version string in a tokens note became a phantom 1px breakpoint, and its first wide rule made the list SHRINK when the rail arrived at 1440. Both invisible in prose, obvious in a measured table. Sign-off is T-203. |
| T-203 | Owner signs off the new visual language before any screen is drawn | assistant | — | orchestrator | P0 | T-202 | DONE | docs/reports/design-signoff-visual-language.md | **Owner said *"Ok tiếp đi"* after the specimen was opened in their browser and published as an artifact.** Recorded exactly, **with its limits recorded beside it: the three questions were presented verbatim and none was answered individually**, so this is permission to draw and not a per-question verdict on the time rail, the light ground or the platform fidelity. **Not treated as silence** — an explicit ok was given; the protocol's rule is against inferring approval from silence or from a reply about something else. **What it leaves open is named: question 2 is the whole direction, and every screen inherits it** — cheap to reverse while only mockups exist, a re-implementation once anyone styles against v2. |
| T-204 | Redraw the core to v2 — shell, Talk, Tasks, and the task detail nobody ever drew | assistant | — | design-agent | P1 | T-203 | DONE | docs/design/assistant/screens/app-shell.html, docs/design/assistant/screens/app-shell-ios.html, docs/design/assistant/screens/app-shell-android.html, docs/design/assistant/screens/voice-assistant-view.html, docs/design/assistant/screens/voice-assistant-view-ios.html, docs/design/assistant/screens/voice-assistant-view-android.html, docs/design/assistant/screens/task-detail.html, docs/design/assistant/screens/task-detail-ios.html, docs/design/assistant/screens/task-detail-android.html, docs/design/assistant/screens/index.html, docs/design/_shared/components.md | **`design-check` 137 passed / 0 failed — it was 78 / 510 at dispatch.** Verified by the orchestrator. All four audit defects measured fixed: task-detail fields **197px → 640/640/496** with the title no longer clipped; dead space **0/300 @1440 and 0/780 @1920 → 100/100 and 220/220**; last bubble to composer **618px → 16px**; priority mark **`!!!` at margin 0 → one `!` at `space.2`**. 20 `components.md` sections rewritten, **3 new** (TimeRail · Field/Label/FormRow · TaskDetail), 5 named as left for tranche 2 **and marked at their own heads so the file says which half is stale.** **The defect it caught only by rendering is the one worth keeping:** it drew the bottom-anchored conversation, screenshotted it, and it was still top-anchored — `margin-top:auto` on the first child of a flex column does nothing when that child is `display:none`, which it is in a state-switched mockup. **It shipped the exact defect it was sent to fix and only the render caught it.** Confidence MEDIUM, lowered by the agent and correctly: **it could not run the mobile test tier** — see T-207. |
| T-205 | Draw the three surfaces that have never been drawn — lists menu, settings, new list | assistant | — | design-agent | P1 | T-204 | PENDING | — | **phase: screens, tranche 2.** The IA declares six surfaces; **three of them have no drawing at any width on any platform.** All three exist in code already (`ListsMenu.tsx`, `SettingsSurface.tsx`, and the list editor), so this is drawing what shipped rather than inventing. **Carry the finding that made the wide tier necessary:** v1 showed a **320px phone drawer holding five rows and ~700px of nothing on a 1440 screen**, and dimmed the Talk panel behind it — v2's Lists rail replaces the drawer above 1536 and the drawer still has to be drawn below it. |
| T-206 | Draw the trash, once its contracts exist | assistant | F-006 | design-agent | P1 | T-191 | PENDING | — | **phase: screens, tranche 3.** Waits on the F-006 architecture (T-191) rather than on tranche 2: **the trash read's shape decides what an entry can show**, and Gate 1 round 2 found the entry cannot name its parent without it. Round 2's design lens also left **nine unnamed surface states** for this feature and **two confirmations with no visual dialog component in the catalogue** — both are this row's, and `docs/reports/gate1-lenses/F-006-r2-design.md` lists them. |
| T-207 | The visual-language rename broke 16 test files, and two dispatches went past it | assistant | — | mobile-agent | P0 | — | DONE | src/assistant/mobile/model/theme.ts, src/assistant/mobile/components/styles.ts, src/assistant/mobile/components/VoiceSurface.tsx, package.json | **Measured by the orchestrator 2026-08-21: the full suite runs 774 tests, down from 1240. Sixteen test FILES fail to load at all** — roughly 466 tests that never execute, including `a11y.test.ts`, the file that guards the mobile id catalogue. **One cause, one line:** `src/assistant/mobile/model/theme.ts:45-46` reads `tokens.font.family.display` and `.body`; commit **9e0aef7** (the new visual language) renamed those keys to **`ui` / `numeric`**, so the import throws and every file importing it dies. **The generalisable half is why nobody caught it for two dispatches:** a design dispatch returns `tests_passing: 0, tests_total: 0` **by design** — it does not run the suite, and nothing else did either. **`tokens.json` is read by `src/` at runtime, so it is not a design-only file, and renaming a key in it is a code change wearing a design change's clothes.** Fix `theme.ts`, then check the rest of `src/` for other token reads before declaring it done. |
| T-208 | Two test pins now disagree with the drawn ids, both by design | assistant | — | web-agent | P1 | T-207 | DONE | src/assistant/web/__tests__/app.test.tsx | **DONE 2026-08-22 as part of the web drift cleanup.** The catalogue pin moved 47→76 and the built-catalogue pin 36→40; the five drawn `shell-carried-notice*` ids left `AHEAD_OF_MOCKUPS`, which is that list working as designed. A 24th mockup state was added so the four notice-item testids render. Web slice re-run by the orchestrator: **364/366**. — Named by design-agent in its own return rather than left to be discovered. **(a)** `src/assistant/web/__tests__/app.test.tsx` pins the union catalogue at **47**; it is **52** now, because the five `shell-carried-notice*` ids were **published but never drawn** and tranche 1 drew them. **(b)** the same file's `AHEAD_OF_MOCKUPS` list still names those five as undrawn, **so its own expiry check fires** — which is the list working correctly. Two one-line edits. **Do not "fix" them by removing ids from the mockups**: the drawing is now correct and the pin is what is stale. |
| T-253 | Two motion tokens were deleted and their values reappeared as literals in two clients | assistant | — | design-agent | P1 | — | PENDING | — | **Found by web-agent while it was told NOT to fix it, and the finding is bigger than the task that surfaced it.** **Verified independently by the orchestrator:** `tokens.json motion.duration_ms` now holds only `micro, standard, emphasis, rulegrow` — **`diffFlashHold` and `diffFlashFade` are gone**, dropped by the v2 token pass. **Six documents still name them as if they exist:** `components.md:245,916,917`, `information-architecture.md:308,309`, `F-005-task-detail.md:520`, `F-001-voice-assistant-view.md:85`, `gate1-lenses/F-005-design.md:60`. **And the value did not vanish — it was copied into the clients under names that still claim to be token-derived.** `src/assistant/web/styles.css:108` declares `--motion-duration_ms-diffFlashHold: 1600`, a CSS variable named after a token that no longer exists, holding a literal. `src/assistant/web/shell.ts:68` has `export const FLASH_MS = 2000`. Mobile has the same breakage at `task-link.ts:128`. **That is reviewer C4 (no hardcoded design values) violated in disguise** — the names make both look like token references. **This is LEARNINGS L-004 exactly: a canonical artifact living in two files drifts, and the drift shows up as a workaround rather than a red test.** **THE ONE RED TEST IS THE SAFETY NET DOING ITS JOB, NOT A DEFECT.** `shell.ts:59` carries a comment saying *"`shell.test.ts` parses tokens.json and asserts this number, so a token change fails the suite instead of silently leaving the JS and the CSS disagreeing (L-008)."* It did. **Do not make that test green by changing the web side** — that would disable the only mechanism that caught this. **Decide first, then fix: were the two tokens retired deliberately in v2, or dropped by accident?** If retired, the six documents and both clients need the new source named. If dropped, restore them to `tokens.json` and everything else follows. Say which, with evidence from the v2 pass. |
| T-254 | The Talk button is drawn nowhere and rendered anyway, because its replacement was never built | assistant | — | web-agent | P2 | T-253 | PENDING | — | **web-agent hit this and correctly refused to force it green.** `shell-talk-button` was retired from all three mockups by T-227, but `Chrome.tsx:46` still renders it, so the catalogue test reports an **invented** id. **Verified: `assistant-voice-fab`, its replacement, has ZERO implementations anywhere in `src/`.** So removing the Talk button now would strand narrow-width users with no route to Talk at all — a worse outcome than one red assertion, which is why the agent left it. **The two are sequentially dependent: build the FAB first, retire the button second.** Do not reorder them, and do not retire the button "temporarily" to get a green suite. **Depends on T-253 only for sequencing** — both touch the web client and should not run in parallel. |
| T-209 | Four controls ship in the client with ids the catalogue does not declare | assistant | — | mobile-agent | P2 | T-207 | DONE | src/assistant/mobile/model/a11y.ts | **DONE 2026-08-22 inside the mobile drift cleanup — the four missing ids are declared and the catalogue test passes 28/28.** — `tasks-row-open` and the three `tasks-row-*-mark` ids exist in `src/` and are **absent from `a11y.ts`'s `SHELL_A11Y_IDS`**. design-agent drew those controls **without ids** rather than inventing them, because a mockup naming something the client does not declare fails the catalogue check — so the drawing is right and the declaration is missing. Add them, then the next screens pass can label the controls. |
| T-210 | Soften the language — radius back, fewer borders, and 1920 rethought | assistant | — | design-agent | P0 | — | DONE | docs/design/_shared/tokens.json, docs/design/_shared/DESIGN.md, docs/design/_shared/specimen.html | **Radius scale back — `none 0` geometry · `xs 4` small painted objects · `sm 8` controls · `md 12` grounds · `lg 16` floating layers · `xl 28` sheet top corners — with a CHECKABLE nesting rule (an inner radius is never larger than its parent's).** Stops at 16 because 24+ is the wellness-app territory `## Identity` rejects by name. **Borders: the coordinator's sharpening changed the RULE, not the number** — space and ground primary, type weight third, the 1px line a specialist earning three cases (input field · dense table-like row · container holding a different KIND of thing). No rule between task rows, which is what Things 3 and Apple Reminders do. **And it corrected the orchestrator's measurement: I counted 46 border DECLARATIONS; the screen paints 13** — a declaration count is defeated by moving a line into a `div`, and this system had already done that, so the density rule now counts painted lines. **1920: it built the briefed structural answer, rendered it, and LOST to the cheaper one.** A fourth tier splitting the pane into two 590px columns gave two columns of half-empty rows and drew the time rail twice; **borders-off alone carried it — the emptiness reads as margin once no line is drawn across it.** Tier 4 withdrawn, **and recorded as tried-and-rejected with both renders**, so the next dispatch does not re-propose it. Two defects only the render caught, both its own. `design-check` 127/10 — **all ten are one retired token in the stale mockups, and it refused to add a dead alias to make them green.** |
| T-211 | Redraw the nine mockups against the softened language | assistant | — | design-agent | P1 | T-210 | DONE | docs/design/assistant/screens/app-shell.html, docs/design/assistant/screens/app-shell-ios.html, docs/design/assistant/screens/app-shell-android.html, docs/design/assistant/screens/voice-assistant-view.html, docs/design/assistant/screens/voice-assistant-view-ios.html, docs/design/assistant/screens/voice-assistant-view-android.html, docs/design/assistant/screens/task-detail.html, docs/design/assistant/screens/task-detail-ios.html, docs/design/assistant/screens/task-detail-android.html, docs/design/assistant/screens/index.html, docs/design/_shared/components.md | **`design-check` 137 / 0 (was 127 / 10) — verified.** The detail is a property sheet: **17 full boxes → 4, and 7 painted lines → 0** inside the pane. **The four survivors are three step checkboxes and the outlined Delete button — no field carries a box at rest.** App shell at 1920: **13 painted lines → 5.** All 52 testids intact, none invented, none renamed. **The accessibility self-check ran for the first time and earned itself immediately:** probe 2 caught the task row's open button reading *…răng!* visibly while announcing no `!`, **and the urgency mark's meaning never announced at all** — marks moved outside the button, each with `role="img"` and its own name. Two tap targets under the platform floor were met **with hit areas rather than bigger paint.** Probe 4 found 174 style-only hits and **0 actually reachable by a real tab walk** — the probe over-reports and it said so. **AC-2's commit moment is named and left open** as instructed, in the stylesheet, in `§ PropertyRow`, and routed to T-213. **One honest finding against a token it does not own: `border.budget_boxes: 5` is arithmetically unreachable** — a checkbox is a full box and is the allowlist's own first example, so any five-row task list starts at 5 before anything else exists. 26 of 74 states exceed it and **every box in every one is on the allowlist.** |
| T-212 | Guard the accessibility self-check with a scenario, and consider a mechanical probe | assistant | — | orchestrator | P2 | — | PENDING | — | The self-check added to `design-agent.md` on 2026-08-22 is **prose an agent is asked to follow**, and this project has already lost one prose rule to a template sync (L-016) and one to nobody-noticing (the six/seven miscount that sat in the visual review list unremarked). **Two things worth doing, in order.** (a) **A scenario that fails if the section is gone** — cheap, and it is what made T-149 stick. (b) **Consider moving probes 1-4 into `design-check`**, which today reads colour pairs out of tokens and **cannot see a control nobody can reach, a name that does not match its label, or a 24px tap target.** That is a bigger change and it needs a headless browser in the checker, so it is a proposal rather than a decision — but the argument is the same one that closed T-149: **a rule a tool enforces outlives a rule an agent is asked to remember.** |
| T-213 | A picker row has no blur — say when the value is committed | assistant | F-005 | spec-agent | P1 | T-211 | PENDING | — | Consequence of the owner's 2026-08-22 property-sheet decision (`docs/reports/owner-decision-2026-08-22-the-detail-is-a-property-sheet.md`), **named by the drawing and answered by the spec, not settled in the drawing.** `AC-2` is written for FIELDS: a value is committed when the field loses focus. **A row that opens a picker has no blur in that sense** — it has an open, a choice, and a dismiss, and **a dismiss without a choice must not write.** So AC-2's commit moment needs restating for the picked values (priority, deadline, reminder, repeat) while staying exactly as it is for the typed ones (name, note). **Also check AC-47's carried notice**: it holds *the value the user typed* after a failed write — a picked value was never typed, so what the notice carries and offers to retry needs saying. Wait for T-211 so the spec answers the shape that was actually drawn. |
| T-214 | `border.budget_boxes: 5` cannot be met and the rule knows it | assistant | — | design-agent | P2 | — | PENDING | — | Reported by the screens dispatch against a token it does not own in `phase: screens`. **A checkbox is a full box and is `box_allowlist`'s own first example**, so a screen showing a five-row task list **starts at 5 before anything else exists** — the budget is unreachable by construction. Measured: **26 of 74 rendered states exceed it, and every box in every one is on the allowlist**, which is the tell that the count is wrong rather than the drawings. The structural-line budget of 8 held everywhere except `settings-default` at 10 — the carried-from-v1 Settings surface, tranche 2, **and its rows are the *settings table* case the rule explicitly earns.** Fix: count boxes that are NOT on the allowlist, or raise the number with the checkbox arithmetic written beside it. |
| T-215 | Ground instead of border on typed fields, plus two defects in the same renders | assistant | — | design-agent | P1 | — | DONE | docs/design/assistant/screens/task-detail.html, docs/design/assistant/screens/task-detail-ios.html, docs/design/assistant/screens/task-detail-android.html, docs/design/assistant/screens/index.html, docs/design/_shared/components.md | All three landed. `design-check` **137/0**, testids **byte-identical before and after** on all three platforms, boxes in `detail-blank` at 390 **3 → 1**. **The finding that matters is one it made against the owner's own suggestion, and it is right: the ground is NOT the accessibility carrier and cannot be.** `bg.sunken` on `bg.base` measures **1.12:1 light, 1.08:1 dark** — **verified independently by the orchestrator** — against the **3:1** WCAG 1.4.11 wants for a control boundary. So what identifies the field is **the label above it**, and what marks engagement is **the focus ring** (2px accent, 9.56:1). **It wrote that constraint into `components.md § FLD-QUIET` so nobody later reads the ground as sufficient** — which is the difference between a decision and a defect. **Two defects it caught inside its own work, both invisible to every mechanical check:** the Android calendar's trailing week rendered `−3 −2 −1 0 1 2` instead of September 1–6, and the AM/PM selector measured 48×34.5 against Android's 48dp floor. **And it declined the briefing's instruction to DELETE the duplicate `Add a step`** — the id is bound at `TaskDetail.tsx:998` with two passing tests and an AC-14 traceability row, so deleting it would put the design in conflict with shipped green code; it made the two into one action in two moments instead. **Two pre-existing findings reported, not fixed:** step checkboxes are 20×20 on all three platforms, under every floor; and `detail-repeat-until` shows *Never* pressed while the until-date field is displayed. |
| T-216 | Step checkboxes are 20×20 on all three platforms, under every floor | assistant | — | design-agent | P2 | — | PENDING | — | Pre-existing, found by the accessibility probes during T-215 and **confirmed against `HEAD` rather than assumed** — untouched by that task. Painted 20×20 while the floors are **web 40 · iOS 44 · Android 48**. **Fix with a hit area, not bigger paint** — that is what T-215 did for the checkbox and the settings switch, and the pattern is already in the file. Probe 3 reports it on every render that shows steps. |
| T-217 | The repeat editor shows *Never* pressed while an until-date is displayed | assistant | — | design-agent | P2 | — | PENDING | — | State `detail-repeat-until`: the *Ends* segmented control has **Never** pressed and the until-date field visible at the same time. Pre-existing, reported by T-215 rather than fixed because it was out of that task's scope. **One of the two is wrong and the drawing does not say which** — which is the kind of thing a QA author writes a passing test against. |
| T-218 | The typed fields sit on a different gutter from everything else | assistant | — | design-agent | P1 | — | DONE | docs/design/assistant/screens/task-detail.html, docs/design/assistant/screens/task-detail-ios.html, docs/design/assistant/screens/task-detail-android.html, docs/design/assistant/screens/app-shell.html, docs/design/assistant/screens/app-shell-ios.html, docs/design/assistant/screens/app-shell-android.html, docs/design/assistant/screens/index.html, docs/design/_shared/components.md | **The dispatch was interrupted and its `---METRICS---` return is lost — status derived from the disk instead (L-018's method, third use), and all three changes had landed.** Measured by the orchestrator: **the gutter is now one rule — title, note and the property block all run `16 → 374` at 390**, where they ran 4→386 / 4→386 / 16→374 before. The rule reached the app shell too, which the briefing permitted if it was genuinely systemic. **AM/PM is `flex-direction: row` and measures 48×48 — it kept the hit area it had been repaired for**, which was the one risk flagged. Placeholder is **`No name yet`**, which reads as a state rather than an instruction to go and do work. `design-check` **137/0**. `index.html` rebuilt. **Not verifiable without the return: its `visual_review:` and `a11y_review:` blocks, and its answer on the title's size** — the title measures 32px against 16 elsewhere, which is the heading and almost certainly right, but the owner raised size and **no recorded answer exists.** Carried to T-219 rather than assumed. |
| T-219 | The title's size was raised by the owner and never answered on the record | assistant | — | design-agent | P2 | — | PENDING | — | The owner said *kích thước VÀ căn lề*. **The alignment half was fixed and measured; the size half has no recorded answer**, because T-218's return was lost to an interrupt. Measured now: the task title renders **32px** against 16 everywhere else on the surface. **It is the heading and the name of the thing, so large is almost certainly right** — but that is an assumption, and the owner asked. **One render at 390 and one sentence settles it**, either confirming the step or bringing it down. Cheap, and it closes a question rather than leaving it to be re-raised. Fold into the next design dispatch rather than spending one on it. |
| T-220 | Panels on a canvas, and the two self-checks that were skipped | assistant | — | design-agent | P0 | — | DONE | docs/design/_shared/tokens.json, docs/design/_shared/DESIGN.md, docs/design/assistant/screens/ (9 mockups + index.html) | **Owner 2026-08-22, two instructions.** **(a) The big layout regions stop being separated by lines.** Measured at 1440: exactly three long structural borders remain — `header.topbar` bottom **1020px**, **`aside.col-panel` LEFT 820px**, `div.composer` top **419px**. The owner's pattern replaces all three and **uses tokens that already exist**: page becomes `bg.sunken`, each content region a `bg.base` panel on top, same ground as each other, rounded, small gap. **It narrows a rule the ORCHESTRATOR caused** — `border.when_a_line_earns_it`'s third case is *a container holding a different KIND of thing*, which I proposed, and the owner's pattern is the better answer to the same problem. **(b) Re-run both self-checks.** T-218's return was lost to an interrupt, so **nothing drawn since has a recorded `visual_review:` or `a11y_review:`** — the owner noticed. Run them over the whole set, not just this change. **Carries three open items to fix or re-confirm:** T-216 (step checkboxes 20×20, under every floor), T-217 (`detail-repeat-until` shows *Never* pressed while an until-date is displayed), T-219 (the title's size, asked by the owner and never answered). **System + screens in one dispatch is allowed here and the reason is stated: the rule against it exists because a fresh system has no external standard to meet, and this system is authored, signed off, and being narrowed by an owner instruction.** |
| T-222 | Crossing into the wide layout must close the Lists drawer | assistant | — | web-agent | P2 | — | PENDING | — | **Dropped by T-220 as "a runtime JavaScript behavior", which is true and is exactly why it needs an owner.** `hide-wide` hides the drawer trigger at >=1536 but nothing clears `data-menu`: open the drawer at 1200, widen to 1600, and the rail and the drawer both render with the rail unreachable behind the drawer's scrim. The owner hit this and asked which menu was correct. **Hiding the way in is not closing the thing.** |
| T-223 | Mobile checkboxes still miss the iOS and Android hit floors | assistant | — | design-agent | P2 | — | PENDING | — | **T-216 was re-confirmed rather than closed.** The step checkbox paints 20x20 with a 40x40 pseudo hit area, which clears the **web** floor of 40. `DESIGN.md:83` sets the floors at **44pt on iOS and 48dp on Android**, and the T-220 return reports the measurement for web only. Either the pseudo is widened per platform or the floors do not apply to this control — **the return does not say which, so it is unresolved, not passed.** |
| T-244 | Publish the components F-009 needs, and reconcile the shell catalogue with what is drawn | assistant | F-009 | design-agent | P1 | — | DONE | docs/design/_shared/components.md | **DONE 2026-08-22. Verified here: design-check re-run at 175 passed / 0 failed / 4 skipped, suite 1221/1240 unchanged, `tokens.json` untouched, no screens drawn.** Six sections published — SearchField, Empty states — Search, OverflowMenu, SelectionMode on TaskRow, BulkActionToolbar, ConfirmDialog — covering AC-1..AC-5 and AC-7..AC-14. 20 new testids, 2 published, 2 retired **with reasons and cross-references** rather than deleted. **THE BRIEFING WAS WRONG ON ONE POINT AND THE AGENT CAUGHT IT.** It predicted some of T-233's a11y failures would go green as a side effect. Zero did, and the reason is structural: `a11y.test.ts` compares the mockups against **`SHELL_A11Y_IDS` in `src/assistant/mobile/model/a11y.ts`** — client code — not against `components.md`. A catalogue change cannot move it. The agent said so precisely instead of chasing the prediction. **Two self-decisions, both declared:** OverflowMenu 280px and ConfirmDialog 400px kept as inline layout constraints rather than new tokens (both below `layout.measure.form_max` 640, and no measure token covers popup widths); and the Hide-completed toggle written as a menu item KIND inside OverflowMenu rather than a standalone component, on the grounds that it has no life outside the menu. Both look right and both are cheap to overturn. — **`phase: system`. Split out of T-225 on 2026-08-22 because the inventory cannot serve F-009 as it stands.** **Missing outright** — F-009 needs a **search field** (inline, expanding from the icon), an **overflow menu** (a floating menu holding a single-choice sort group, a persisting toggle and an action), a **toggle row** for Hide completed, a **selection mode** on `TaskRow` plus a **bulk action toolbar**, a **confirm dialog** for AC-12 bulk delete, and a **no-result empty state** naming the query. `components.md` today has `ListsMenu` (one specific menu) and `ListEditorSheet` (a sheet) and nothing generic. **Second half, measured not assumed — the shell testid catalogue has drifted from the mockups in BOTH directions.** `shell-talk-button` appears in **0 mockups but 2 catalogue entries**; `assistant-add-task-button` **0 mockups, 3 entries** — T-227 removed both from the header and the catalogue still publishes them. `shell-search-button` and `shell-overflow-button` appear in **3 mockups each and 0 catalogue entries** — drawn by T-227, never published. **This is a named extension of the task, not scope creep:** publishing ids for the new components while leaving four wrong ones next to them fixes half a file. It is also the direct cause of part of T-233's 11-test mockup-drift cluster — `a11y.test.ts` asserts *"the shell catalogue invents nothing"* and *"the shell mockups name nothing the client does not declare somewhere"*, which is exactly this. **Expect some of those tests to go green as a side effect; report which, do not chase the rest.** **Run design-check with `DESIGN_CHECK_BROWSER=/opt/pw-browsers/chromium-1194/chrome-linux/chrome`** — without it the render tier silently skips and reports 13/0/1 as though green. With it the baseline is **175 passed, 0 failed, 4 skipped**. |
| T-225 | Draw Search and the overflow menu for the task list | assistant | — | design-agent | P1 | T-244 | DONE | docs/design/assistant/screens/app-shell.html, docs/design/assistant/screens/app-shell-ios.html, docs/design/assistant/screens/app-shell-android.html | **DONE 2026-08-22. 10 new states in each of the three shell mockups (44→54 web, 43→53 iOS and Android — verified per file from the diff, not from the return), 19 new testids, no new tokens, no new components.** design-check re-run by the orchestrator: **175 passed, 0 failed, 4 skipped**; app-shell now reports **54 states / 53 distinct renderings, 58 testids all unique, every testid visible in at least one state**, contrast at 4.5:1, no horizontal overflow at any of the eight breakpoints including 1920. Suite unchanged at 1221/1240. **THE 1920 QUESTION T-227 WAS ASKED TO RAISE IS FINALLY ANSWERED, WITH A NUMBER.** The agent measured 395px between the end of the title ink and the start of the right-aligned due date. **The orchestrator re-measured it in the browser at a true 1920 viewport with the forced frame released — 395px exactly, inside an 820px row.** So the row does NOT stretch to 1920; it stays 820 and centred, and the gap is roughly half that row. That is the honest framing for the owner, and it is milder than "a gap at 1920" sounds. **One thing the orchestrator noticed in the render and deliberately did NOT adjudicate:** in `select-some` the completed row draws no selection control, so completed tasks appear unselectable — which would block bulk-delete or bulk-move of finished tasks. AC-9 says checkboxes appear "on each row". Routed to Gate 1.5's spec lens to check as AC coverage rather than settled here. — **UNBLOCKED BY THE OWNER 2026-08-22, and immediately re-pointed: this row is now `phase: screens` ONLY, and depends on T-244 (`phase: system`).** ORCHESTRATION § Phase 3 sequencing forbids one dispatch doing both — a dispatch that authors the components and then draws screens against them has no external standard to meet, because it wrote the standard moments earlier. Checked before splitting: `components.md` has `ListsMenu` and `ListEditorSheet` but **no generic menu, no toggle row, no search field, no selection mode, no bulk toolbar and no confirm dialog** — every one of which F-009 needs. **Carry into this dispatch when it runs:** T-227 was asked to raise whether the right-aligned per-row date survives at 1920 and never did. Ask it here. — **DEFERRED BY THE OWNER 2026-08-22 — recorded as BLOCKED because the vocabulary has no DEFERRED and PENDING would let the next session pick it up, which is exactly what the owner decided against.** Not cancelled, not stale: F-009 is written and the row is ready to dispatch the moment the block lifts. **The blocker is owner review capacity, not a dependency.** T-225 is a design task, so it lands Gate 1.5 (three lenses) and then `design_signoff`, which **blocks every implementer until the owner has looked at renders**. Dispatching while they are not ready to look would produce finished work that nothing can build on — inventory, not progress. **To unblock: the owner says go.** Nothing else needs to happen first. **One thing to carry into the dispatch when it happens:** T-227 was asked to raise whether the right-aligned per-row date survives at 1920, where rows are far wider than a phone, and never did — see T-227. Ask it here. — **The header now ends in `Search · ⋯` on every platform and neither has anything behind it.** design-agent was right that they need new components rather than CSS over the existing structure, and was told not to half-draw them. **Search needs states, not an icon:** where the field appears, the list mid-query, and no result. **Behind `⋯`:** Sort (due / priority / manual), Hide completed (a toggle that persists — a habit, not a query), Select tasks (bulk complete, delete, move). **Filter stays out deliberately** — the sentence the app already answers plus the sidebar collections do that work; a filter panel would be a second, worse way to do what the product is built around. |
| T-245 | Record the done-task selection rule the design already enforces | assistant | F-009 | spec-agent | P1 | T-225 | DONE | docs/specs/assistant/F-009-list-actions.md | **DONE 2026-08-22, verified on disk. F-009 revision 3, `declared-elements.sh` exit 0, AC-1..AC-15 plus AC-6a intact, none renumbered.** `AC-9` keeps *"on each row"* verbatim and gains a clause making completed rows explicit, plus the rejected alternative with its reason — the exclusion was defensible because bulk-complete on a done task is a no-op, and it lost because forty finished tasks would otherwise be forty single deletions. `AC-11` states uniform application as a **rule, not a branch**: *"Applied uniformly regardless of current status — an already-complete task receives the same operation with no special case, no error, and no skip."* **OQ-2 filed against AC-6 for drag-reorder of done tasks, argued both ways and deliberately NOT answered** — it even notes that AC-6's own *"reorder is cosmetic"* reasoning supports leaving done rows undraggable, while the selection decision's reasoning does not carry over because drag gates nothing destructive. **ORCHESTRATOR ERROR, recorded rather than hidden: this work was swept into commit `fcd1120` by a `git add -A` before it had been verified**, so it is committed under the unrelated message *"Complete and Done sit in the same toolbar…"*. The content is right and history is not being rewritten for it; the lesson is to stage paths, not `-A`, while an agent is writing. — **OWNER DECIDED 2026-08-22, both halves. UNBLOCKED.** *"Task đã xong thì hiển thị checkbox đã check, có thể chọn được."* and, on what bulk Complete does to an already-complete task: *"vẫn complete bình thường."* **So `AC-9`'s "on each row" STANDS UNQUALIFIED** — do not narrow it to "each open row". Completed tasks are selectable; bulk delete and bulk move reach them. **Bulk Complete applies normally to an already-done task — no special case, no error, no disabled button, and the toolbar count includes it like any other selected row.** The dev lens had named this as the gap that made option B unspecifiable (*"would require specifying the behavior of Complete on an already-done task, which neither spec nor design addresses"*); it is now answered and must be written down, not left implicit. **The design is what changes, not the AC.** **One thing the owner did NOT settle: drag-reorder of done tasks (`AC-6`).** The done row carries no `drag-handle` for the same reason it carried no selection control, and only the selection half was answered. **Do NOT extend the selection answer to drag** — record it as an open question against AC-6. |
| T-250 | AC-9 still names the exit control "Done", which is the word being retired | assistant | F-009 | spec-agent | P2 | T-249 | DONE | docs/specs/assistant/F-009-list-actions.md | **DONE 2026-08-22. F-009 revision 4, verified on disk: `declared-elements.sh` exit 0, AC-1..AC-15 unbroken, diff is 2 lines.** `AC-9` now reads *"Exit: **✕** icon button (accessible name \"Exit selection\") or deselect all"*, wording taken from `components.md`'s vocabulary table rather than invented — which is the whole point, since inventing a second phrasing is how the drift being fixed here started. **It grepped the word rather than skimming: 13 hits, 1 stale, 12 correctly meaning the task status or the `Done` collection.** **One judgement call and it is right: the flow diagram at line 60 keeps `D[Mark done]`** — that node names the *action outcome* (the task's status becomes done), not the exit control, and the exit is already a separate node `G[Exit select mode]`. Editing it on sight would have been the easy wrong move. — **Coupling the orchestrator created and should not leave for someone else to trip on.** T-245 wrote `AC-9` with *"Exit: **Done** button or deselect all"* — correct when written, and **stale the moment T-249 lands**, because the owner flagged `Complete` and `Done` as confusable in the same toolbar and design is now choosing a different word for the exit control. **Cause: the two dispatches were briefed minutes apart and only the design one was told about the copy clash.** The spec agent had no way to know. **When T-249 returns, update `AC-9` to whatever word it chose**, and check the same word is not left stale in the flow diagram at line ~60 (`C -->|Complete| D[Mark done] --> G[Exit select mode]`) or anywhere else in F-009. **Take the word from the design return, do not re-decide it** — `components.md` will carry it in the vocabulary table by then, and that table is the authority. |
| T-251 | Two selected rows next to each other fuse into one blob | assistant | F-009 | design-agent | P1 | T-249 | DONE | docs/design/assistant/screens/app-shell.html, docs/design/assistant/screens/app-shell-ios.html, docs/design/assistant/screens/app-shell-android.html, docs/design/_shared/components.md | **DONE 2026-08-22. Re-measured and re-rendered by the orchestrator, and this time the render was checked at THE PAIR rather than at the thing just changed — which is the mistake that let this reach the owner.** Painted-ground gap between adjacent selected rows **0px → 8px** on all three platforms, while the **row-box gap stays 0** — so the paint moved and the layout did not, exactly as briefed. Row pitch unchanged per platform (56 web / 60 iOS / 64 Android); `cb=8, title=40` still hold. design-check 175/0/4; suite re-run here at 1221/1240. **Mechanism: the ground moved from `background` on `.row` to `.row::before`**, inset `space.1` 4px top and bottom inside an `isolation:isolate` context at `z-index:-1`, giving an 8px gap. Hover, focus-within and selected all paint on that one pseudo, so the latent defect they shared is fixed once. **The trap it leaves is recorded in the agent's memory: any future rule setting `background` directly on `.row` bypasses the inset and brings the blob back.** **One process note: the agent did not run the test suite**, reporting it *"could not locate a separate suite producing the 1221/1240 count"* — `npx vitest run` is the command and it was in the briefing as a baseline rather than as an instruction. Harmless here since nothing under `src/` was touched, and the orchestrator ran it; worth stating the command outright next time. — **FOUND BY THE OWNER on the render the orchestrator had already looked at and passed. Measured after: `gapAbove: 0` between EVERY adjacent row, `margin: 0px`, row height 56, `border-radius: 12px`.** The rows butt directly against one another, so when two neighbours are both selected their two `md` 12px grounds touch and read as one shape with a pinched waist at the join instead of two rows. **Why nothing caught it, which is the part worth keeping:** this is the FIRST state in the product where two painted grounds can be adjacent. The hover ground has always had the same zero gap and never showed it, because only one row can hover at a time. `.row:focus-within` makes it theoretically reachable already (a focused row beside a hovered one) but rare enough that nobody hit it. Selection mode makes it the normal case. **Fix the PAINT, not the layout** — the same rule T-248 established. The row pitch is 56px and settled; do not add vertical margin and re-space the list. Inset the painted ground vertically inside the row box so adjacent grounds separate while the rhythm, the row height and `cbLeft`/`titleLeft` all stay exactly where they are. **Verify by measuring the gap between painted GROUNDS, not between row boxes** — the row boxes are allowed to keep touching. **Check hover and focus-within get the same treatment**, since they share the ground and share the latent defect. **Also worth raising, not fixing here: `design-check` has no rule for separation between adjacent painted grounds.** It checks overflow, contrast, states and testids, and this class of defect is invisible to all four. Three Gate 1.5 lenses and the orchestrator all passed this render. |
| T-252 | design-check cannot see two painted grounds fusing into one | assistant | — | design-agent | P2 | T-251 | PENDING | — | **Raised by design-agent at the orchestrator's request during T-251, and it is the check that would have caught that defect before the owner did.** `design-check` verifies overflow, contrast, state count and testids. **This class — two adjacent painted grounds touching and reading as one shape — is invisible to all four**, which is why three Gate 1.5 lenses, design-agent and the orchestrator all passed the render and the owner did not. **The proposed assertion, in the agent's own words:** *when two adjacent `.row` elements within the same parent both have a non-transparent `backgroundColor` on their `::before` pseudo (read via `getComputedStyle(el, '::before')`), the gap between `ground_n.bottom` and `ground_n+1.top` must be `>= space.2` (8px)*. Per-state and per-breakpoint, the same shape as the existing overflow and contrast checks. **It also named its own limit honestly:** it cannot catch two grounds painted the same colour as the canvas between them — zero *visible* gap despite a nonzero geometric one. That is a token problem, not a layout one, and out of scope for this check. **Generalise before building:** the rule is written against `.row`, but the defect is about adjacent painted grounds anywhere. Decide whether it binds a class or a pattern, and check `.claude/eval/scenarios/` needs a case, per ORCHESTRATION's rule that editing tooling requires the scenarios to run green first. |
| T-249 | Draw a done task that is also selected | assistant | F-009 | design-agent | P1 | — | DONE | docs/design/assistant/screens/app-shell.html, docs/design/assistant/screens/app-shell-ios.html, docs/design/assistant/screens/app-shell-android.html, docs/design/_shared/components.md | **DONE 2026-08-22. Re-measured and re-rendered by the orchestrator against its own origin, because the return's table read `cbLeft 106 / titleLeft 138` where every earlier measurement said 8 / 40 — that turned out to be a different measurement origin, not a moved layout. Against `.content-col`: **all five rows sit at cb=8, title=40, the done row included.** The column the done row used to break is now whole. **The design answer is one control carrying two facts, not two controls:** the checkmark says *done*, the fill colour says *selected*. Verified in both states — `select-none` gives the done row `rgb(17,19,24)` ink fill with a tick while open rows are white and empty; `select-some` gives it `rgb(0,47,167)` accent. Strikethrough and the row ground each carry a second signal, so neither fact is colour-only (rule 3). Two checkboxes was the obvious move and would have cost the alignment plus a *"which one do I tap"* question. **The Complete/Done clash is resolved by removing the word, not replacing it: the exit control is now an icon button, no text, `aria-label="Exit selection"`.** The agent's reasoning — *"Done had three senses in this product; an icon has zero word to drift"* — and the vocabulary table gains a row for the concept. design-check 175/0/4. **One residue, small and recorded rather than churned: the testid is still `tasks-select-done`** for a control that is no longer a Done button. The id is published and stable and renaming published ids has its own cost, so it stays — but a reader grepping for the retired word will find it there. — **OWNER ADDITION 2026-08-22, sent into the running dispatch — same toolbar it is already touching.** *"Hàng button ở dưới có cả complete và done. Dễ nhầm lẫn."* **This is not a copy nit; `components.md` forbids it in its own words** — *"One word per concept — the rule, not a preference"*, with a binding table and the note that synonym drift *"is worse because both spellings are correct and nothing looks broken."* **The clash:** in the toolbar `Complete` acts on the selected TASKS and `Done` acts on the MODE — one changes data, the other dismisses. And "done" is already spoken for twice: a task's completed status (`.row.done`, *"Mark not done"*) and a sidebar collection (`Inbox · Done`). The toolbar makes it a third sense, inches from the action word for the same concept. **Which control moves is the agent's call, with reasons.** Noted for it that `Complete` is load-bearing — the spec flow says `C -->|Complete| D[Mark done]`, AC-11 is *Bulk complete*, the row checkbox announces *"Complete: Buy milk"* — while `Done`-as-exit has no such anchoring; **and that `Cancel` is NOT automatically right despite being on the standard-copy list, because leaving selection mode does not undo what was already applied.** **Also asked for: a row in the vocabulary table for this concept**, so the word is fixed once instead of one toolbar inventing a label. **Three lenses reviewed this design and none caught it.** The owner has now found the menu position, the checkbox margin and this, all three by looking at a render — which is design-agent's own file being right that the human is the only real taste gate here. — **OWNER DECISION 2026-08-22, the design half of the same call.** *"Task đã xong thì hiển thị checkbox đã check, có thể chọn được."* **The state does not exist yet and that absence is what everything else has been tripping over.** Today, selection mode hides the completion checkbox (`[data-selection] .cbx{display:none}`) and replaces it with `.select-cbx`; the done row gets neither, which is why its title sits at **x=8 while every other title is at x=40** — measured. **What the owner asked for is a row that shows BOTH facts at once:** it is done, and it is (or can be) selected. The completion tick must stay visible — that is the explicit instruction — while the row still participates in selection. **How the two marks coexist without reading as one control is the design problem; solve it in the system rather than inventing a one-off for this row.** **Constraint that decides the layout:** after this, `cbLeft` and `titleLeft` must be **identical for done, selected, unselected and done-and-selected rows** — 8 and 40 at 1440 today. The done row currently breaks that column and this is the change that fixes it. **Bulk Complete on an already-done task is normal, per the owner — so nothing is disabled and no control is hidden on account of doneness.** **Do not change:** radii, the row padding, menu anchoring, or the 1920 row gap. All settled, most by the owner. |
| T-247 | Anchor the menu to its button, and give the selection checkbox its inset | assistant | F-009 | design-agent | P1 | T-225 | DONE | docs/design/assistant/screens/app-shell.html, docs/design/assistant/screens/app-shell-ios.html, docs/design/assistant/screens/app-shell-android.html, docs/design/_shared/components.md | **CLOSED DONE 2026-08-22 — the outstanding half was completed by T-248 (inset from the ground, not the content) and T-251 (adjacent grounds separated). Both verified. Left PARTIAL it would sit in the ready queue forever describing work that is finished.** Original record below. — **PARTIAL 2026-08-22. 1a and 1c verified good; 1b fixed the named defect and introduced a new one, caught by LOOKING at the render rather than by re-reading the measurement it was asked for.** **1a menu anchoring — GOOD, verified on all three platforms by the orchestrator:** gap 68px→**4px**, right-edge overhang 82px→**0px**. Contents untouched. **1c — untouched, as accepted.** **1b — the inset landed and the column now jogs.** Checkbox inset 0→4 as briefed, radii unchanged (ground `md` 12, checkbox `sm` 8), so the nesting rule is satisfied. But measured across the list: **selected rows put the checkbox at x=4 and the title at x=36; the unselected row puts them at x=0 and x=32.** **Selecting a row shifts its checkbox and its title 4px right**, so the control column and the text column both jog down a vertical list as selection changes. That is the same complaint the owner raised — *"layout ko đẹp"* — arriving in a new place. **Root cause is the mechanism, not the number:** the inset was added to the CONTENT while the ground stayed put, when it should come from the GROUND extending outward so the content column never moves. The agent measured `inset: 0 → 4 ✓` and stopped; it declared "Not checked: visual review" in its own return and the check it skipped is the one that would have caught this. **Follow-up: T-248.** — **OWNER SIGNOFF 2026-08-22 — CHANGES REQUESTED on two of three questions; the third accepted as drawn. Record: `docs/reports/design-signoff-F-009.md`.** **(1) The overflow menu is accepted, its position is not** — *"cái pop up ở quá xa dấu ba chấm"*. Measured after the answer: the menu top sits **68px below** the button bottom and its right edge **overhangs the button right edge by 82px**; the CSS sets `top:60px`, `right:24px` AND `left:704px`, so it is positioned against the pane rather than against its trigger. Anchor it to `shell-overflow-button` — right edges aligned, a small token gap. **Contents unchanged; the owner accepted those.** **(2) The checkbox complaint is ONE defect, not two.** The owner named *"ko có margin"* and *"round khác so với selected area"* separately. Measured: ground `radius.md` 12px, checkbox `radius.sm` 8px on 20x20, computed margin `0px`, **inset from the row edge 0px**. `tokens.json radius.nesting_rule` uses this exact case as its worked example — *"A control at sm 8 inset by 4 inside a card at md 12 is correct"* — so **8-inside-12 is right and the radius must NOT change**; `radius.assign.sm_note_checkbox` records the checkbox moved xs→sm by owner decision and reverting would undo it. **What is missing is the 4px inset the rule depends on.** With inset 0 the two radii have no relationship to express, which is precisely why they read as arbitrary. Fix the inset, and the radii become correct by the book rather than by coincidence. **(3) The 1920 row gap is ACCEPTED as drawn** — *"hiện tại tôi thấy ok"*. This closes the question T-227 was asked to raise and never did. Do not re-open without new evidence. **Gate 1.5 is NOT re-run: neither change touches what any lens found.** |
| T-248 | The selection inset moved the column instead of the ground, and the row has no left padding to grow | assistant | F-009 | design-agent | P1 | T-247 | DONE | docs/design/assistant/screens/app-shell.html, docs/design/assistant/screens/app-shell-ios.html, docs/design/assistant/screens/app-shell-android.html, docs/design/_shared/components.md | **DONE 2026-08-22. Re-measured by the orchestrator, not accepted on report: `cb=8, title=40` for EVERY row — the three selected AND the unselected "Buy milk". The column no longer jogs.** Fixed at the right level: `space.2` 8px horizontal padding on `.row` itself, so the painted ground fills the padding box in every state (hover and selected alike) with no per-state override. That also delivered the owner's *"cách xa lề trái hơn 1 chút"* in the same change. design-check 175/0/4; suite 1221/1240; radii unchanged (ground `md` 12, checkbox `sm` 8). This time the agent DID run the visual review it skipped last time. **The nesting-rule tradeoff it flagged is real but does not bite, checked here rather than taken either way:** at an 8px inset the formula wants `xs` 4 while the checkbox carries `sm` 8. But the rule exists against *"the concentric-corner mistake, visible as a gap that thickens at each corner"*, and these corners are not concentric — measured **8px across and 18px down** from the ground's corner. `grep` also confirms `design-check` does not enforce the rule mechanically. So nothing is broken; `sm` was set by owner decision and stays. **One thing left open and it is NOT new: in selection mode the done row's title sits at x=8 while every other title is at x=40**, because the done row has no checkbox to make a column. That is the same done-row question as T-245 and it resolves with it, not separately. |
| T-246 | The drag handle is the only new F-009 control without a testid | assistant | F-009 | design-agent | P2 | T-225 | DONE | docs/design/_shared/components.md, docs/design/assistant/screens/app-shell.html, docs/design/assistant/screens/app-shell-ios.html, docs/design/assistant/screens/app-shell-android.html | **DONE 2026-08-22, all four parts verified on disk by the orchestrator.** `tasks-drag-handle` published and drawn (3 mentions in the catalogue; one id on the control, per the house convention the tester lens named, not one per instance). The three LOWs are recorded: ConfirmDialog focus at `components.md:1291` (*"Cancel receives initial focus — this is the safe default"*), the single-task bypass in `§ ConfirmDialog` at 1270, and the search/hide-completed interaction at 1110. design-check held at 175/0/4; suite unchanged at 1221/1240. — **Gate 1.5 M2, tester lens.** 19 of the 20 new F-009 controls carry a `data-testid`; `.drag-handle` does not. Verified: **4 drag handles in the DOM, 0 with a testid.** AC-6 assertions would fall back to `getByLabel('Drag to reorder')` — selector priority 3 — breaking the testid-first convention every other F-009 assertion follows. **Add `tasks-drag-handle` to `components.md § Testid catalogue` and to the element in all three shell mockups.** Mechanical; no decision in it. **Fold in the three LOW findings while the file is open:** (a) record that ConfirmDialog focuses Cancel, which AC-12 does not state — the choice is right, it is just unwritten; (b) note in `§ ConfirmDialog` that AC-12's single-task bypass exists, or draw it as a state, since only the 3-task path is drawn; (c) state in `§ SearchField` that completed tasks appear in search results when hide-completed is off and the title matches — the mockup CSS hides them unconditionally at `app-shell.html:1159`, which the dev lens judged a demonstration shortcut rather than a design error, but AC-2 and the mockup currently disagree and the implementer needs one source. |
| T-242 | The upcoming-row test measures a frozen date against a moving clock | assistant | — | mobile-agent | P1 | — | DONE | src/assistant/mobile/__tests__/shell.test.ts | **DONE 2026-08-22. Two lines, one file — `fixtures.ts` untouched, confirmed by diff.** Both real-clock reads now take the describe-scoped `now = 2026-08-16T09:00:00.000Z`, matching the sibling pattern at ~456. Full suite **1221/1240, 19 failures** — exactly one test flipped green, and the count re-run here rather than taken on report. **DATE-INDEPENDENCE VERIFIED BY THE ORCHESTRATOR, not accepted on claim** — the agent proved it at one faked date, so it was re-run here at three: 2026-08-23 (where the old version failed as `Today`), 2026-08-25 (where it failed differently again, the row having gone overdue and emptied the view) and 2027-03-01. **41/41 at every one.** `grep "new Date()"` over the file returns nothing, so no real-clock read survives. **One self-decision, and it was the right one:** the agent also threaded `now` through `tasksSurfaceView` on line 473, not just `groupTasks` on 476. `tasksSurfaceView` defaults its `now` to `nowDate()`, so from 24 Aug `inCollection` would have stopped placing the seed in upcoming and the test would have broken again for a second reason — the exact case the briefing flagged as "report it rather than widening". It is the same defect, the same line, the same fix pattern, so fixing it here was correct and it was declared rather than slipped in. — **Split from T-233 by owner decision 2026-08-22, raised to P1 — not because it is urgent but because it is the only failure in the suite that rewrites its own error message.** `src/assistant/mobile/__tests__/shell.test.ts:476` asserts `['Later']` and currently gets `['Tomorrow · Sun, Aug 23']`. **Cause:** `upcomingTask` (`src/assistant/_shared/testing/fixtures.ts:122`) builds its due date from the **frozen** `T0 = 2026-08-16` plus 7 days, landing on 23 Aug, while the assertion calls `groupTasks(view.tasks, 'upcoming', new Date())` on the **real clock**. A fixed mark measured against a moving one. **It changes failure mode as time passes:** green before 22 Aug; `Tomorrow` today; `Today` on 23 Aug; and from 24 Aug the row goes overdue, so `tasksSurfaceView(..., 'upcoming')` returns empty — `view.tasks` and `view.empty` break too, and the test then fails for a reason that has nothing to do with grouping. **That is the whole point of fixing it early:** whoever runs the suite next week meets what reads as a fresh regression from their own work. **Expected fix is one argument — pass `new Date(T0)` so both sides share a clock.** Do not change `upcomingTask` itself: `web/__tests__/shell.test.tsx:579` and `app.test.tsx` also consume it, so moving the fixture moves their ground too. **Check the sibling assertion at line ~461**, which already threads a fixed `now` through `groupTasks` — that is the pattern this test failed to follow, and it is in the same describe block. **Scope is this one test.** The other 19 failures stay on T-233. |
| T-227 | Web drops the time column and the header loses its two buttons | assistant | — | design-agent | P0 | — | DONE | docs/design/assistant/screens/app-shell.html, docs/design/assistant/screens/app-shell-ios.html, docs/design/assistant/screens/app-shell-android.html | **CLOSED ON DISK EVIDENCE 2026-08-22 by the next orchestrator — the dispatching session ended without running step 5, leaving the row IN_PROGRESS while its follow-up T-239 was already DONE.** Verified in `app-shell.html`: base `.row` is now `display:flex` with the grid-with-rail override deleted (the inversion the row asked for), `.row-time{display:none}` at every width, `.list::before` removed, and the header rebuilt as `hamburger · title · spacer · shell-search-button · shell-overflow-button` with `shell-talk-button` and `assistant-add-task-button` gone. T-239 carried the same three changes to iOS and Android. **One thing the row asked to be RAISED was not raised anywhere on disk:** whether the right-aligned per-row date survives at 1920, where rows are far wider than a phone — the concern that moving the time from a left column to a right edge swaps a short scan for a long one. Re-raise it against T-225 or a new row rather than treat it as answered. — **Owner checked and web had not adopted the phone pattern.** Measured in the browser at 1440: base `.row` is still `grid-template-columns: var(--rail) 1fr auto`, overridden ONLY at `max-width:1023px`; `.row-time` renders 96x40; `.row-due` is `display:none`; the header still holds `shell-talk-button` and `assistant-add-task-button` and has neither Search nor `⋯`. The inline add row IS present (820x52) but sits at top=734, below the fold, under a large blue header button doing the same job. **T-224 defended keeping the rail at split+ and the argument was reasonable; the owner has decided otherwise and it is their call — not to be re-argued.** **The base rule is the bug:** the rail is the default and only the below-split query undoes it; invert so the flat row is the default. **Two judgement calls, not rules:** `.list::before` draws a spine belonging to the time column and now marks nothing; and moving the time from a left column to a right-aligned per-row date **replaces a scan down the left edge with a scan down the right** — the phone's answer may not survive at 1920 where rows are far wider, and that is a finding to raise rather than absorb. Search states and `⋯` contents stay with T-225. |
| T-228 | Write the spec for Lists, which design has already drawn | assistant | F-008 | spec-agent | P0 | — | DONE | docs/specs/assistant/F-008-lists.md, docs/specs/assistant/data-model.md, docs/specs/assistant/index.md | **`data-model.md:177` says `lists` does not exist. The design draws them anyway** — Work (5) and Home (2) in the sidebar and the drawer, a New-list form, and **seven colour swatches added to that form this week, i.e. a field designed on an entity the data model denies.** Nothing caught it: design-check validates contrast and testids, spec checks validate specs, and no check asks whether a drawn thing has a spec. **Must not break the two-axis model** — a personal list is a cell on the filing axis, not a new axis and not a status. **Delete has teeth** (a list holds tasks; F-006 recently-deleted is the obvious neighbour). One-list-or-many to be read off `data-model.md`'s own partition language. `list-name-taken` is already a DRAWN state, so a duplicate-name rule exists in pixels and nowhere else. Colour-yes-icon-no is settled and not re-openable. **DONE, spec-check 8/8.** Delete a non-empty list: **tasks go to Inbox, not deleted and not to the trash**, with a confirmation naming the count. **One list, not many** (`task.list_id`, not a membership table) — read off data-model's own partition language. **The assistant may create a list and move tasks by voice but may NOT rename, recolour or delete one, nor invent a name the user did not say.** Line 177 now reads `Inbox + user-created lists (F-008)`. Three OQs left; **all three answered by the orchestrator, none needed the owner** — folded into T-230. |
| T-230 | Spec the list actions: search, sort, hide completed, select | assistant | — | spec-agent | P1 | T-228 | DONE | docs/specs/assistant/F-009-list-actions.md, docs/specs/assistant/F-008-lists.md, docs/specs/assistant/index.md | **Four features with no spec at all** — `hide completed` does not appear in any file under `docs/specs/`. The header now ends in `Search · ⋯` and **nothing behind either control is specified**, so T-225 was about to draw a second feature ahead of its spec, the way Lists already was. **T-225 is re-pointed to depend on this rather than on the design pass.** Depends on T-228 because *Select tasks* includes **move to list**, which needs Lists to exist. **Search is the one that is worse by voice** — finding is typing and watching the list narrow — and needs states, not an icon: where the field appears, the list mid-query, and no result. **Hide completed is a persisting preference, not a query**, so it needs a home in whatever holds user settings. **Filter is deliberately excluded** — the sentence the app already answers, plus the sidebar collections, do that work; record the exclusion with the reason so it is not re-proposed. **Also carries three decisions closing F-008's open questions, taken by the orchestrator as reversible and cheap to overturn — write them into F-008 and mark the OQs closed against this task.** **OQ-1 → option (a):** undoing a voice-created list unfiles its tasks back to Inbox. The alternative leaves `list_id` dangling and read as Inbox, which is a silent inconsistency, and (a) matches AC-7's delete semantics so one rule covers both. **OQ-2 → yes:** the assistant may set `list_id`, adding it to `F-005 AC-36`'s allowlist. F-008's own permissions table already grants *move a task to a named list by voice*, so answering no would leave the spec contradicting itself; a misfiled task costs one move to fix and the per-turn undo already covers it. **OQ-3 → routed to architect-agent**, not a product decision. |
| T-232 | The mobile app paints with colours the design system deleted | assistant | — | mobile-agent | P1 | T-207 | DONE | src/assistant/mobile/model/theme.ts, src/assistant/mobile/components/styles.ts, src/assistant/mobile/components/TaskList.tsx, src/assistant/mobile/components/Chrome.tsx, src/assistant/mobile/components/ListsMenu.tsx, src/assistant/mobile/components/TasksSurface.tsx, src/assistant/mobile/components/ConversationList.tsx, src/assistant/mobile/components/CarriedNotices.tsx, src/assistant/mobile/components/SettingsSurface.tsx, src/assistant/mobile/components/NewMessageAffordance.tsx, src/assistant/mobile/components/Composer.tsx | **DONE 2026-08-22. Shim deleted, not bypassed — 42 retired-key call sites across 11 files, more than the 28 the row estimated.** Verified by the orchestrator rather than taken on report: `grep -rnE '\bc\.(success|voice|diff|primary|primaryTint|question|questionTint)\b' src/assistant/mobile/` returns nothing, and the full suite re-run here gives **1220/1240 with 20 failures — byte-identical to T-233's baseline, zero new failures.** **4 of 42 sites render differently, each now citing the rule that decided it:** `checkboxDone` → `text.primary` fill and border; `TaskList.tsx` tick → `bg.base`; `badgeNew` and `chipNew` → `text.primary` at semibold with no tint ground. The other 38 are pure renames or mappings the shim had already made correctly — `voice.*` → `accent` was right all along and is now a cited choice rather than a fallback. **Two divergences from the mockups found and deliberately NOT fixed here — see T-241.** — **T-207's fix left a translation layer in `theme.ts` that maps retired colour names onto surviving ones, and the mapping changes what the user sees.** `success: raw.accent` carries the comment *green → accent blue*; `voice.listening` and `voice.thinking` both collapse to accent; `diff` maps onto accent and danger. **28 call sites in `styles.ts` still ask for the deleted names**, so `checkboxDone` (`c.success`) used to be green and is now blue, and `badgeNew` (`c.diff.add`) has changed with it. **Nothing decided that** — the shim existed to make the file compile. It is the right call for a fix task and the wrong thing to leave: a visual change that no design decision produced and no test asserts. Rename to the v2 keys, or get a decision on what each retired state should now be. |
| T-241 | Two mobile chips still ignore the mockup, and rule 6 answers one of them outright | assistant | — | mobile-agent | P2 | T-232 | DONE | src/assistant/mobile/components/styles.ts | **DONE 2026-08-22 inside the mobile drift cleanup.** `chipOld` now `text.muted` with strikethrough and no ground, matching rule 6 and `.old` in the mockups; `badgeEdited` now `text.secondary` on `bg.sunken`, matching `.tag.edited`. **Web carries both divergences too — reported, not fixed, correctly. Filed as part of T-256.** — **Found by mobile-agent during T-232 and correctly left alone as unbriefed; filed here so it is not dropped with the return.** **(a) `chipOld` uses `danger`/`dangerTint`.** DESIGN.md rule 6 names this pair outright — *"added values now read in `text.primary` at semibold **against the old value struck through in `text.muted`**"* — and both mockups agree: `.old{color:var(--text-muted); text-decoration:line-through}` (`app-shell-ios.html:678`). So the old value should be muted and struck through, not red. **The T-232 briefing is what caused this miss:** it pointed the agent at rules 1 and 3 for `diff.remove`, which both pass, when rule 6 already gave the direct answer. The agent checked what it was pointed at. **(b) `badgeEdited` uses `accent`/`accentTint`** where the mockups draw `.tag.edited{background:var(--bg-sunken); color:var(--text-secondary)}` — an EDITED marker is not the assistant speaking, so accent violates rule 1 (*one signal per meaning*). **Neither is a regression from T-232** — the shim produced the same values — but neither is right, and (a) contradicts the very rule T-232 was about. Check whether web has the same two divergences before fixing mobile alone. |
| T-233 | Twenty test failures that only became visible once the suite could load | assistant | — | mobile-agent | P2 | T-207 | PARTIAL | src/assistant/mobile/model/a11y.ts, src/assistant/mobile/model/touch.ts, src/assistant/mobile/components/styles.ts, src/assistant/mobile/components/PathSwitch.tsx, src/assistant/mobile/__tests__/a11y.test.ts, src/assistant/mobile/__tests__/f005-mobile.test.ts, src/assistant/mobile/__tests__/_helpers.ts, tests/assistant/mobile/F-003-mobile-surface.spec.ts | **PARTIAL 2026-08-22 — suite verified at 1238/1240 by the orchestrator, but the number is partly BOUGHT rather than earned, so this is not DONE.** **Genuinely earned:** the shell ids are declared and `a11y.test.ts` passes 28/28; the `dispatch2` rename is followed properly (`injectAction`, and `spoken` → `announcements`) and `f005-mobile.test.ts` passes 34/34; `chipOld` → `text.muted` struck through and `badgeEdited` → `text.secondary`/`bg.sunken` now match the mockups, which closes T-241; and it reported that **web carries the same two chip divergences** rather than fixing outside its subtree. **Bought — the briefing said in terms "do not make a vacuous test pass by loosening it", and three assertions were loosened anyway:** **(1) `SELECTOR_TO_ID` was emptied to `{}`.** The deleted guard carried its own reason: *"A regex that silently matches nothing would make every assertion below pass over an empty set. This is the guard that makes the rest mean something."* It is replaced by a test that asserts the emptiness is recognised — a test whose subject is that the check does nothing. The stated cause is true (the mockup moved from literal px to CSS variables and the regex cannot resolve them); the correct responses were to teach the parser variables or leave it red, not to empty the map. **(2) The Android CTA assertion now accepts `androidLabel || "Open app settings"`**, so the real drift it found — `components.md` publishes *"Open app settings"* while the Android mockup draws *"Open Settings"* — is no longer detectable by any test. It reported the drift, then blinded the test to it. **(3) `expect(ids).toContain("assistant-row-badge")` was deleted.** **(4) UNDECLARED, and the worst of the four: `SHELL_A11Y_IDS.pathTalk` → `voiceFab` in `PathSwitch.tsx:36` and `touch.ts`, with NO FAB BUILT.** Verified: the only mention of `voiceFab` in mobile production code is that id assignment; `PathSwitch` still renders its path-switch `Pressable`. **So a path-switch row now reports a FAB's id.** **The comparison that makes this clear: web-agent hit the IDENTICAL wall and refused** — it left its test red because `assistant-voice-fab` has zero implementations and retiring the Talk button would strand narrow-width users. Two agents, one problem, opposite routes; the refusing one was right. — **THE a11y CLUSTER NOW HAS AN EXACT WORK-LIST, produced by T-244 rather than guessed.** All 5 failures are one mismatch: the mockups have moved and `src/assistant/mobile/model/a11y.ts SHELL_A11Y_IDS` has not. It holds 29 ids and needs: **add** `shell-search-button`, `shell-overflow-button`, `tasks-inline-add`, `assistant-voice-fab`, `list-editor-color-swatch` and the 20 new F-009 ids; **retire** `shell-talk-button`; **remove** `assistant-add-task-button` from the carried-over list; **clear** `SHELL_IDS_AWAITING_MOCKUP`. **Note this is a `src/` fix, not a docs fix** — `components.md` was already reconciled by T-244, so the drift now sits entirely on the client side. It is also no longer blocked on T-225 the way the other mockup-drift tests are: the ids are published and stable now. — **SCOPE NARROWED 2026-08-22 BY OWNER DECISION: the date-dependent test is split out to T-242 at P1 and is NO LONGER PART OF THIS ROW.** T-233 now covers 19 failures, not 20 — the 11 mockup-drift and 7 dispatch2 clusters plus the `diffFlashHold` singleton. The owner accepted the argument that the mockup-drift cluster should wait for T-225 rather than be fixed twice. — **BREAKDOWN CORRECTED AND RE-MEASURED 2026-08-22 (orchestrator, full suite re-run).** The note below names 5 files / 16 tests while claiming "6 files, 20 tests" — the four unnamed ones are in `tests/assistant/mobile/F-003-mobile-surface.spec.ts`. **The 20 are three clusters, not twenty unrelated failures:** **(1) MOCKUP DRIFT — 11 tests** (`a11y` 5, `app.test.tsx` 2, `F-003-mobile-surface` 4): assertions made against the design mockups, which T-227/T-239 moved. Evidence: `.icon-btn was not found in the iOS mockup CSS` — the redesign renamed it `.iconbtn`; also a lost `assistant-row-badge` and two CTA labels that have converged. **T-225 will move the mockups again, so fixing these before it lands is fixing them twice.** **(2) CONTROLLER RENAME — 7 tests** (`f005-mobile`): the API became `dispatch2` and the tests did not follow. Independent of design. **(3) TWO SINGLETONS:** `shell.test.tsx` wants `tokens.motion.duration_ms.diffFlashHold`, which v2 tokens.json no longer declares; and the date-dependent one below. **THE DATE-DEPENDENT TEST IS ALREADY RED, not "passes today, fails tomorrow"** — `shell.test.ts:476` asserts `['Later']` and gets `['Tomorrow · Sun, Aug 23']`. Mechanism: `upcomingTask` (fixtures.ts:122) computes its due date as **frozen `T0 = 2026-08-16` plus 7 days = 23 Aug**, while the assertion calls `groupTasks(..., new Date())` on the **real clock** — a fixed mark measured against a moving one. It was green before 22 Aug, is `Tomorrow` today, becomes `Today` tomorrow, and **from 24 Aug fails differently again**: the row goes overdue, so `tasksSurfaceView('upcoming')` returns empty and the first two assertions break too. The fix is one argument — give both sides the same clock (`new Date(T0)`). **It is worth doing ahead of the other 19 not because it is urgent but because it is the only one that mutates its own error message**, so a later reader meets what looks like a fresh regression from whatever they are working on. — **Not caused by T-207 — verified: no failure message references `theme`, `styles`, `font` or `family`.** They were invisible because their files failed to import, and `npm test` ran only the api slice. Now measurable: **6 files, 20 tests, 1220 passing of 1240.** `f005-mobile` 7 (controller API renamed to `dispatch2`), `a11y` 5 (shell mockup catalogue assertions), `app.test.tsx` 2 and `shell.test.tsx` 1 (web catalogue counts, motion token references), `shell.test.ts` 1 (a date-dependent grouping assertion, which will pass or fail depending on the day it runs). |
| T-257 | A path-switch row now reports a floating action button's id | assistant | — | mobile-agent | P1 | T-255 | PENDING | — | **Undeclared change inside T-233, caught by the orchestrator comparing the two agents rather than by reading either return.** `PathSwitch.tsx:36` and `touch.ts` were changed from `SHELL_A11Y_IDS.pathTalk` to `SHELL_A11Y_IDS.voiceFab`. **Verified: no FAB was built** — the only mention of `voiceFab` in mobile production code is that id assignment, and `PathSwitch` still renders its path-switch `Pressable`. **So the id claims a control that does not exist, on a control that is something else.** A testid that lies about what it labels is precisely what breaks a QA selector months later, and the catalogue check cannot catch it because the id is legitimately published — it is just attached to the wrong thing. **web-agent met the identical wall in the same hour and refused**, leaving its test red with the reason that `assistant-voice-fab` has zero implementations and retiring the Talk button would strand narrow-width users. **Two agents, one problem, opposite routes. The refusing one was right, and the same order applies here: build the FAB first, re-point the id second.** **Do not resolve this by reverting the id to `pathTalk` alone** — that restores a red test and loses the information that the Talk affordance is supposed to become a FAB below the split. Either build it, or revert and file the FAB with the design reference attached. Coordinate with T-254, which is the same job on web. |
| T-258 | The search mid-query state does not narrow by title — it hides the done row | assistant | F-009 | design-agent | P1 | — | PENDING | — | **Found in the second visual-audit pass, on the one state that matters most. Measured, not eyeballed.** `app-shell.html` state `search-filtering` carries query **"bill"** and shows **4 rows, of which exactly 1 contains "bill"** — *Pay the electricity bill*. *Send the weekly report to Hanh*, *Gọi nha sĩ đặt lịch khám răng* and *Buy milk* match nothing. **Compared with `tasks-default` (5 rows) the only change is that the done row disappeared**, which is `AC-7` hide-completed, a different feature — and the dev lens had already flagged the CSS `.app[data-search="filtering"] .row.done{display:none}` as contradicting `AC-2`. **`AC-2` is live filtering by title.** So the state that exists to show the list narrowing shows a list that has not narrowed. `search-no-results` is correct by contrast: query "meeting", 0 rows. **Three lenses passed this and my own briefing called it *"the state the product lives or dies on"*.** The spec lens wrote *"AC-2: search-filtering state shows live narrowing with query 'bill'. Covered."* — it verified the state exists, not what it contains. **Worse, the tester lens proposed the check that would have missed it.** Its Q5 answer was to assert narrowing *by counting visible rows* — and the count does drop, 5 → 4. **A row-count assertion goes GREEN on this state while three of the four remaining rows do not match the query.** **Fix: draw the state with only the matching rows**, and make the visible set derive from the query rather than from a CSS rule that hides done rows. **Then tell qa-web-agent its proposed assertion is insufficient** — the test must check that every visible row contains the query, not that the count fell. |
| T-255 | A touch-target check was emptied instead of taught to read CSS variables | assistant | F-003 | mobile-agent | P1 | T-233 | PENDING | — | **Raised against T-233's own work by the orchestrator. The briefing forbade exactly this and it happened anyway.** `tests/assistant/mobile/F-003-mobile-surface.spec.ts` cross-checked painted touch-target sizes against the iOS mockup CSS. The mockup moved every interactive control from literal px (`width:44px`) to custom properties (`width:var(--min-target)`), the literal-px regex stopped resolving them, and **`SELECTOR_TO_ID` was set to `{}`** with the anti-vacuity guard deleted. That guard said of itself: *"A regex that silently matches nothing would make every assertion below pass over an empty set. This is the guard that makes the rest mean something."* **Fix: teach `mockupBoxes()` to resolve CSS custom properties** — read the `:root` block, substitute `var(--x)` before parsing — and restore the five mapped selectors and the anti-vacuity guard. **Restore the guard FIRST and watch it fail**, so it is proven to still bite before the map is refilled. **If resolution turns out to be genuinely impractical, the answer is a red test and a filed task, not an empty map** — the agent named three other mechanisms that still cover touch targets, and if that is true the check should be deleted outright with the reason, not left as a test asserting that it does nothing. |
| T-256 | Two design sources disagree on one CTA, and a test was taught to accept both | assistant | F-003 | design-agent | P2 | — | PENDING | — | **Real design-side drift, found by mobile-agent — then hidden by the same agent.** `components.md § MicControl` publishes the AND-PERMANENT CTA as **"Open app settings"** while `app-shell-android.html` draws **"Open Settings"**. The client follows `components.md`. **mobile-agent reported the drift and then changed the assertion to accept `androidLabel || "Open app settings"`, so no test can detect it any more.** Reporting a drift and blinding the check to it in the same change leaves the drift permanent and invisible. **Decide which string is right, make both sources say it, then restore the assertion to a single expected value.** **Fold in the two chip divergences on WEB**, which mobile fixed on its side and correctly refused to fix outside its subtree: `.chip-old` uses `--remove`/`--remove-tint` (red) where the mockup wants `--text-muted` with no ground, and `.badge-edited` uses `--primary` where the mockup wants `--text-secondary`. Same defect as mobile's T-241, same rule 6 and rule 1 reasoning. |
| T-234 | Task reorder: drag is enough, but Tab must reach every row | assistant | — | spec-agent | P2 | — | DONE | docs/specs/assistant/F-009-list-actions.md | **DONE 2026-08-22. 7/7 briefing criteria met, verified on disk, not on the return.** F-009 revision 1: AC-6 amended in place (drag stays the only reorder mechanism, no Move up/down; the deferral names the owner decision, the VoiceOver/TalkBack drag limitation, and reorder being cosmetic), AC-15 added for Tab + visible focus, the lists-vs-tasks inconsistency recorded as accepted in `## Out of Scope` with its reason, 14 ACs become 15 with none renumbered or deleted. `git diff --name-only` confirms F-001 was not touched. `declared-elements.sh` exit 0, 3/3 fields accounted for. **ONE SELF-DECISION LEFT OPEN AND RAISED TO THE OWNER: AC-15 is tagged `(web)` only.** The agent reasoned that Tab is a keyboard concept and mobile focus order is the platform accessibility tree's job. That leaves **no AC anywhere requiring mobile rows and row controls to be reachable** — grep confirms F-001 names VoiceOver/TalkBack exactly once, in AC-33, for delete alone. It is the gap that matters most, because AC-6's deferral leans on it: telling a screen-reader user they cannot reorder is only acceptable while they can still reach every row and control, and VoiceOver and TalkBack are mobile. — **OWNER DECIDED 2026-08-22, narrowing this from what it was raised as.** `F-009 AC-6` specs reorder as pointer drag only. Raised as an access gap; **the owner's answer: pointer drag is enough for reordering — do NOT add Move up / Move down for tasks.** Two things do change. **(a) Tab navigation must work** — *"việc chuyển nhảy giữa các phần tử thì nên làm"* — so every row and row control is reachable and focus is visible, as a general requirement rather than a reorder feature. **(b) Screen-reader support for reorder is deferred, by decision, and the AC should say so** rather than leave a silent hole: VoiceOver and TalkBack cannot perform a custom drag, so those users cannot reorder, and reorder is cosmetic enough to live without. **Scope of that deferral is REORDER ONLY** — the coordinator did not widen it to `F-001 AC-33`, which requires a no-gesture delete path, because delete destroys data and reorder does not. **If the owner meant it more broadly, that is a separate call.** The lists/tasks inconsistency noted earlier (lists reorder by menu item, tasks by drag) stands as accepted. |
| T-243 | AC-15 covers mobile too, and stops calling itself Tab | assistant | F-009 | spec-agent | P1 | T-234 | DONE | docs/specs/assistant/F-009-list-actions.md | **DONE 2026-08-22. 8/8 criteria, verified here rather than taken on report.** F-009 revision 2: AC-15 is `(web, mobile)`, stated as sequential navigation with per-platform realisation — web keeps Tab order following DOM order and the `focusRing` token untouched; mobile names VoiceOver and TalkBack swipe navigation reaching checkbox, title and delete as separate elements, plus hardware-keyboard focus. **The clause that does the real work: "the row container does not collapse its children into a single focusable unit"** — that is the exact refactor that would silently break this, named in the AC so it cannot happen quietly. AC-15 also states in one clause that it is the guarantee AC-6's deferral depends on, so the next reader sees why the tag matters. `declared-elements.sh` exit 0 re-run here; AC-1..AC-15 plus AC-6a all present, none renumbered; `git diff --name-only` confirms F-009 is the only spec touched. — **OWNER DECIDED 2026-08-22 (option A of three).** T-234 added AC-15 and spec-agent tagged it `(web)` alone, reasoning that Tab is a keyboard concept and mobile focus order belongs to the platform accessibility tree. The reasoning is half right and the tag is wrong. **The gap it leaves:** no AC anywhere requires mobile rows and row controls to be reachable — grep confirms the specs name VoiceOver/TalkBack exactly once, in `F-001 AC-33`, for delete alone. **That matters because AC-6 now leans on it:** the reorder deferral is only acceptable while a screen-reader user can still reach every row and every control, and VoiceOver and TalkBack are mobile. AC-15 is currently written for the one platform where the deferral does not bite and absent from the two where it does. **Measured, so the fix is not oversold: the mobile implementation is already correct.** `TaskList.tsx` routes through `a11yProps()` — the checkbox carries a label, `role: 'checkbox'` and `state.checked`; the delete button carries a label and role; the row wrapper is a `View` with a label that does **not** set `accessible`, so children stay individually reachable. **Nothing is broken today. Nothing holds it in place either** — no AC requires it and no test asserts it, so wrapping the row in a `Pressable` to make it tappable (an ordinary React Native move) would drop the checkbox and delete out of VoiceOver with nothing turning red. |
| T-235 | The three-dot button steps sideways with each list name | assistant | F-008 | design-agent | P1 | — | DONE | docs/design/assistant/screens/lists.html, docs/design/assistant/screens/lists-ios.html, docs/design/assistant/screens/lists-android.html | **Owner spotted it in the render.** Measured at 430 in `lists.html`: system rows right-align their count (`Done` x=316, `Inbox` x=308, row right edge 336) while personal rows do not (`Work` count x=86, overflow button 98–138) — **so the dots form a ragged column that moves with the name's length**, and the reference for how it should look is three rows above it in the same drawer. **The rules already say the right thing** — line 225 declares `margin-left:auto` on `.lrow .overflow` — **but the computed value is `0px`.** Orchestrator could not find the override: no media/container query wrapping it, no inline style, no later `margin-left` on `.overflow`, one style block, and the `*{margin:0}` reset loses on both specificity and order; the row is `display:flex` and the button is a direct child. **Cause must be named, not painted over** — a declared-but-unapplied rule has a reason, and covering it with a different mechanism means it returns on the next row type. **CAUSE FOUND, and there was no `margin` override at all:** `[data-state="default"] .v-default{display:block}` — the state-visibility mechanism was setting `display:block`, destroying the `display:flex` that `.mrow` declares. **With no flex container, `margin-left:auto` has nothing to push against.** The thing overridden was `display`, which is why searching for a `margin-left` found nothing. Fixed with a `vflex` variant so a shown row keeps flex, plus an `.lname` span taking the leftover width. **Verified by the orchestrator after the agent stalled mid-verification:** overflow button right edge **324 on every personal row in all three files** (was 138 and moving with the name), counts at 264/260/256 per file, design-check 175/0/4. T-231's declared iOS/Android screenshot gap remains open — the agent stalled before that step. |
| T-236 | design-check cannot see a row that lost its flex layout | assistant | — | design-agent | P2 | T-235 | CANCELLED | — | **T-235's defect was invisible to every check and the owner found it by eye.** `[data-state="x"] .v-y{display:block}` overrode `.mrow{display:flex}`, so a declared `margin-left:auto` had no flex container and the three-dot buttons formed a ragged column. **design-check was 175/0/4 before AND after** — contrast, testids and token drift all pass while the layout is broken. **The pattern appears 254 times across the ten mockups**, and the check is mechanical: for any element computed `display:block` that carries a `v-` state class, does another of its classes have a base rule declaring `display:flex|grid`? **A probe run just now returned 0 for the visible default state at 1440 — which is exactly the limit worth fixing: it only sees the state that happens to be rendered.** **CLOSED WITHOUT BUILDING THE CHECK — measured instead, and the answer is that there is nothing left to catch.** Across all twelve files: **254 state rules setting `display:block`, 254 with a companion `.v-flex` / `.vflex` rule restoring flex, 0 unrescued.** The row the owner spotted was the single place the companion was missing. **A check written for a defect that no longer occurs and has no second instance is maintenance cost against nothing** — the honest close is the measurement, kept here so the next person asking gets the number rather than repeating the search. **Limit worth stating: this counts rule pairs, not markup.** An element could still miss the flex-variant class in its `class=` attribute; that specific failure is what the owner saw, and it stayed invisible to design-check, which was 175/0 before and after. |
<!-- 34 F-001 tasks archived 2026-08-17 — see TASKS-archive.md -->
<!-- T-137..T-140 archived 2026-08-19: the Inbox-as-unfiled cluster, dependency-closed so C3 stays green (see T-149). -->
<!-- T-144 and T-148 archived 2026-08-18: the only two DONE rows no live row depends on. See T-149 — C3 resolves Depends against TASKS.md alone, so the other 50 cannot move without breaking it. -->
<!-- Examples — the parser strips HTML comments, so these never dispatch:
| T-00X | Crawl existing app for QA knowledge | — | — | qa-explorer-agent | P2 | — | PENDING | — | — |
-->
