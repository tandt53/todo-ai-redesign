# Task Queue
<!-- Orchestrator writes and archives. Sub-agents read the task assigned to them. -->
<!-- Hard limit: 300 lines. Orchestrator auto-archives when exceeded (see below). -->

## Format

One table. Status is a **column**, not a section — a task never moves between
tables, only its `Status` cell changes. Moving rows between sections drops
columns and duplicates tasks; changing one cell cannot fail that way.

```
| ID | Title | Module | Feature | Agent | Pri | Depends | Status | Artifacts | Outcome |
```

- **Module** — business domain this task targets (e.g. `auth`, `payments`, `_shared`). Orchestrator assigns at dispatch.
- **Feature** — feature ID this task belongs to (e.g. `F-003`) or `—` for cross-cutting tasks.
- **Pri** — `P0` … `P3`. Drives selection order.
- **Depends** — comma-separated task IDs that must be `DONE` before this task is selectable; `—` if none.
- **Status** — `PENDING` · `IN_PROGRESS` · `PARTIAL` · `BLOCKED` · `DONE` · `CANCELLED`
- **Artifacts** — real paths the task produced, resolvable through `MANIFEST ## Paths`. `—` until the task has produced something.
- **Outcome** — exactly one line, including a measurement (`5/7 AC`, `12 tests pass`, `exit 0`). `—` while pending.

Task statuses and agent-return statuses are different vocabularies that overlap.
An agent returns `DONE | PARTIAL | BLOCKED` (plus `APPROVE | REJECT` for gate
agents) in its `---METRICS---` block; the orchestrator maps that onto the
`Status` column here. `PENDING`, `IN_PROGRESS` and `CANCELLED` are queue states
that no agent ever returns.

The orchestrator is the sole dispatcher. Sub-agents do not claim tasks themselves — they receive a briefing via `BRIEFING.md` and act on it.

### Status is derived, not declared

The orchestrator sets `Status` from the evidence in the agent's return, not from
the agent's own wording:

| `files_created` + `files_modified` | Coverage of in-scope ACs | → `Status` |
|---|---|---|
| empty | — | `BLOCKED` |
| non-empty | below 100% | `PARTIAL` |
| non-empty | 100% | `DONE` |

An agent that reports `status: DONE` while listing no files has not completed a
task — it has produced an analysis. Record it as `BLOCKED` with the blocker
named. The full rule, including the confidence interaction, is in
`agents/_completion-protocol.md ## Status is derived from evidence`.

`Artifacts` is what makes this checkable: every `DONE` or `PARTIAL` row must name
at least one path, and every path named must exist on disk.

## Selection order

`"next"` picks the head of this ordering — see `.claude/ORCHESTRATION.md` Step 2
for the executable version:

```
candidates = rows where Status == PENDING and every ID in Depends is DONE
sort candidates by (Pri asc, row order asc)
pick head
```

`Depends` is mandatory for any task whose input is produced by another task.
Without it the queue can hand an agent a task whose input does not exist yet, and
an agent facing a missing input tends to invent one rather than return `BLOCKED`.

## Archival (automatic)

The orchestrator runs this check at the start of every session:
- If TASKS.md total line count > 300, **or** rows with `Status` = `DONE` exceed 50:
  1. Read `TASKS-archive.md` (create if missing)
  2. Move `DONE` rows older than 14 days (or all of them if ages unclear) into `TASKS-archive.md`, grouped by ISO week
  3. Leave a single line here: `<!-- N tasks archived — see TASKS-archive.md -->`

## QA phase: two dispatches per platform

Each platform QA agent (qa-api-agent, qa-web-agent, qa-mobile-agent) is dispatched **twice per feature**:

1. **Authoring phase** (`phase: author` in BRIEFING.md) — runs in parallel with the matching implementer. Writes markdown test cases in `{qa}/{module}/F-{id}/{platform}/`, drafts automation scripts. No test harness needed.
2. **Execution phase** (`phase: execute` in BRIEFING.md) — runs **in parallel** after all implementers return and the orchestrator brings up the test harness. All three QA agents execute simultaneously with namespaced test data (see `_qa-foundations.md` section 10). Runs tests, triages failures, files bugs with layer attribution.

A single feature with api + web + mobile therefore generates **6 QA tasks** (3 authoring + 3 execution) in addition to implementer tasks. The orchestrator creates them automatically; spec-agent and architect-agent do not.

The execution task depends on its authoring task — record that in `Depends`, so
the queue cannot dispatch execution before the test cases exist.

## Merge gate (hard rule, enforced by orchestrator)

A task that completes a feature cannot be set to `Status` = `DONE` until **all** of:
1. At least one P1 test case exists in `{qa}/{module}/F-{id}/{platform}/` for every (AC, platform) pair declared in the feature spec's AC platform tags
2. Reviewer-agent C2 (per-platform AC coverage) passes
3. Reviewer-agent C3 (API contract consistency, uses qa-api-agent's tests as evidence) passes
4. Test suite runs clean (reviewer-agent C5)
5. No open bugs in the feature spec's `## Links.known_bugs` (non-empty is a WARNING, not an auto-fail, but it surfaces to the human reviewer)

If any gate fails, the task stays `IN_PROGRESS` and a follow-up task is dispatched: bug fixes go to the implementer named in the bug's `layer:` field; coverage gaps go to the matching platform QA agent.

---

## Tasks

| ID | Title | Module | Feature | Agent | Pri | Depends | Status | Artifacts | Outcome |
|----|-------|--------|---------|-------|-----|---------|--------|-----------|---------|
| T-047 | Gate 1 multi-lens spec review for F-002 | assistant | F-002 | orchestrator | P1 | T-044 | PARTIAL | docs/reports/gate1-review-F-002.md | 7/7 lenses: 19H/32M/8L into 12 clusters; ZERO lens-vs-lens conflicts; C13 passed pre-gate; 1 item for the human (offline leg / ADR-11) |
| T-059 | Sweep web/main.tsx:33,37 bare crypto.randomUUID + audit globalThis reads in _shared/ | assistant | F-001 | web-agent | P2 | T-058 | PENDING | — | — |
| T-061 | Decide the fixture vocabulary's language + cover the Vietnamese input path (BUG-005) | assistant | F-001 | orchestrator | P1 | T-057 | BLOCKED | — | owner call: are fixtures a stand-in for the model (key them in Vietnamese) or a developer convenience (state it, and cover Vietnamese separately) |
| T-084 | QA mobile: AC-30 coverage + stop hardcoding the testid count | assistant | F-001 | qa-mobile-agent | P1 | T-083 | PARTIAL | tests/assistant/mobile/F-001-voice-assistant-view.spec.ts, docs/qa/assistant/F-001/mobile/index.md, tests/assistant/mobile/F-003-mobile-surface.spec.ts, docs/qa/assistant/runs/2026-08-17-mobile-ac30.md | 583/583 tree; fixed the red by asserting what the literal STOOD FOR (that the parser matched something) not by changing 22->23 — and found the 3 equality assertions under it were vacuously green if the regex ever stopped matching; used makeReduceMotion(true) never set(true), because set also fires onChange and would prove the controller reacts to a notification while the startup read path went unasserted; 9 mutations all proved. TC-009 authored but BLOCKED on the harness having no AC-30 scenario |
| T-085 | QA web: AC-30 coverage at the tier that can falsify position | assistant | F-001 | qa-web-agent | P1 | T-082 | PARTIAL | tests/assistant/e2e/F-001-voice-assistant-view.spec.ts, tests/assistant/pages/AssistantPage.ts, tests/harness/qa-test-server.ts, docs/qa/assistant/F-001/web/index.md, docs/qa/_shared/bugs/BUG-006-own-send-lands-short-of-the-bottom.md, docs/qa/assistant/runs/2026-08-17-web-ac30.md | 49/50; 14 cases, 9 of them only the browser tier can falsify; FOUND BUG-006 by frame-by-frame capture; caught 4 bugs in its OWN scripts, two that mattered — playwright reducedMotion was silently inert so all 3 reduced-motion cases first ran in ordinary motion while claiming otherwise, and TC-038 v1 could not have failed because a dock that takes layout shrinks the viewport from the BOTTOM so a user at the top sees nothing move |
| T-089 | Fix BUG-006: the clause-(h) scroll loses a race with the reply | assistant | F-001 | web-agent | P1 | T-085 | PENDING | — | **Reset IN_PROGRESS → PENDING 2026-08-21: never finished, and nothing was running.** Measured: `docs/qa/_shared/bugs/BUG-006-...md` still reads **`| Status | open |`**. **Unlike the five rows closed beside it, this one has no output on disk** — so it returns to the queue rather than being credited.  — |
| T-091 | Publish a testid for the conversation scroller | assistant | F-001 | design-agent | P2 | T-085 | PENDING | — | AC-30 defines 8 clauses as measurements of an element that has no testid |
| T-087 | Give the headless Surface a scroll seam so 2 AC-30 clauses leave the device tier | assistant | F-001 | mobile-agent | P1 | T-084 | PENDING | — | first dispatch stalled at 600s with nothing written; re-dispatch |
| T-088 | Wire nma-new / nma-waiting / follow-at-bottom scenarios into .mobile-app (unblocks TC-009) | assistant | F-001 | orchestrator | P1 | T-087 | PENDING | — | — |
| T-092 | Owner feedback: mobile layout crowds chat + list on a small screen | assistant | F-003 | design-agent | P2 | — | PENDING | — | docs/reports/owner-feedback-2026-08-17-product-gaps.md §1. The list CAN collapse (AssistantScreen.tsx:34) but defaults open and its control is a hamburger that reads as a menu. F-001 chose list-beside-conversation because AC-1/AC-4 need the change visible in the same turn — sound on desktop, and that is what the owner is pushing back on for a phone |
| T-093 | Owner feedback: the hamburger is not a menu — nothing is behind it | assistant | F-001 | spec-agent | P3 | — | PENDING | — | §2. Only toggles the list. Collides with ADR-008 and F-002 AC-23, which both assume no settings surface exists — deliberate so far, but every deferred preference has nowhere to live |
| T-094 | Owner question CORRECTED: web HAS hand edit+delete, mobile does not — close the parity gap | assistant | F-003 | mobile-agent | P1 | T-096 | PENDING | — | my first answer said no hand-editing at ANY layer; wrong. PATCH/DELETE exist (api/app.ts), controller wraps them as editTask/removeTask, web's TaskListPane calls both. Mobile's TaskList has add+toggle only. F-003's parity table lists AC-18 as holding IDENTICALLY — a documented divergence. I missed it because my endpoint grep matched `path === '...'` and the route is a pattern match (L-002) |
| T-097 | Owner reversal: settings surface is needed (was 'no settings', owner calls it their mistake) | assistant | — | spec-agent | P1 | T-096 | PENDING | — | docs/reports/owner-decision-2026-08-17-settings-and-lists.md. F-002 AC-23, ADR-008 and F-002 Out of Scope all cite 'no settings surface exists' as a premise — re-read as DEFERRED, not permanent |
| T-100 | QA mobile: no BUG-006 case — AC-30 coverage is all instantaneous calls | assistant | F-001 | qa-mobile-agent | P1 | T-090 | PENDING | — | it would report green over this defect exactly as the unit tier did |
| T-109 | F-003 parity table is stale: enumerates 29 ACs (AC-30/31/32 missing) and calls AC-18 identical | assistant | F-003 | spec-agent | P2 | T-108 | PENDING | — | — |
| T-110 | QA re-author: 5 named TCs encode the OLD mechanism; AC-31/AC-32 have no cases at all | assistant | F-001 | qa-web-agent | P1 | T-107 | PENDING | — | — |
| T-111 | Confirm Inbox=all-open and default collection Inbox (mockup draws Today) | assistant | F-001 | design-agent | P1 | T-107 | PENDING | — | IA §3 and components.md §ListsMenu do not quite agree; addTask creates status:inbox so landing on Today would HIDE a task the user just added |
| T-112 | assistant-drawer-button retired — AssistantPage.ts:182 still binds it | assistant | F-001 | qa-web-agent | P1 | T-107 | PENDING | — | replacement is shell-lists-menu-button |
| T-113 | BUG-007: unsupported-query names a layout that no longer exists | assistant | F-001 | backend-agent | P1 | T-103 | PENDING | — | AC-15 rev 4 requires naming the alternative WITHOUT reference to layout; the literal still holds rev 3's string, and the api test asserts the constant against itself so nothing failed |
| T-114 | Landing summary on Talk: greet + what's due + progress + all-clear | assistant | F-001 | design-agent | P1 | — | PARTIAL | docs/design/_shared/components.md, docs/design/assistant/screens/app-shell.html, docs/design/assistant/screens/app-shell-ios.html, docs/design/assistant/screens/app-shell-android.html | docs/reports/owner-decision-2026-08-18-landing-and-collections.md §1. Frames with slots per F-002 AC-22, never free text; reports counts and names tasks it can read, never summarises or predicts |
| T-115 | Default collection Today + addTask writes the viewed collection | assistant | F-001 | web-agent | P1 | — | PENDING | — | §2. Owner chose Today; addTask creates status:inbox so on the default surface adding a task makes it VANISH. Resolution: add-in-context. Changes addTask's contract — flagged, not assumed |
| T-116 | Spec the landing summary + addTask contract change | assistant | F-001 | spec-agent | P1 | T-114 | PENDING | — | — |
| T-117 | task.completed_at — the owner's mid-day shape cannot be selected without it | assistant | F-001 | architect-agent | P1 | T-114 | BLOCKED | — | LSM-PROGRESS is written and drawn but never fires; updated_at is LAST TOUCHED so deriving done_today counts a done task retitled today and drops one finished today and edited tomorrow — right most of the time, silently wrong the rest |
| T-118 | MessageTaskLink must be inline, not a button, inside running text | assistant | F-001 | web-agent | P2 | T-114 | PENDING | — | measured not assumed: a button is an atomic inline box, so the comma after a title lands at the start of the next line and a long title overflows instead of wrapping. Same id, role and look — different box |
| T-119 | Refresh IA §12 (owner answered) and §6's S1 table (no Landing row) | assistant | F-001 | design-agent | P3 | T-114 | PENDING | — | — |
| T-120 | Today = due_at is today; drop the status leg; fix un-complete | assistant | F-001 | architect-agent | P1 | — | PARTIAL | docs/specs/_shared/adr/ADR-009-today-is-a-date.md, docs/specs/assistant/data-model.md, docs/specs/assistant/api-contracts.md | docs/reports/owner-decision-2026-08-18-today-is-a-date.md. Buys the mid-day shape back with NO new field. status:'today' becomes a value that means nothing; controller.ts:574 writes it on un-complete and would then both write a meaningless status AND fail to put the task in Today |
| T-122 | LSM-PROGRESS wording over-claims what done && due_at-today measures | assistant | F-001 | design-agent | P1 | T-120 | PENDING | — | derived it measures 'was due today and is done': a task due today ticked last night counts, one due yesterday ticked this morning does not. Frame says 'You've finished N today'. F-001 AC-14/15 bind it. Now a COPY decision, not a blocker |
| T-123 | Tasks-surface feature spec (new F-id) carrying ADR-009's three ACs | assistant | — | spec-agent | P1 | T-121 | PENDING | — | F-001 Out of Scope already says it needs its own F-id |
| T-125 | e2e: ~20 assertions expect AI-created tasks on the landing collection | assistant | F-001 | qa-web-agent | P1 | T-124 | PENDING | — | outcome depends on T-124's answer |
| T-127 | Assistant asks for a date when it hears none; creates dateless only if user declines | assistant | F-001 | spec-agent | P1 | T-126 | PENDING | — | no date is invented on the user's behalf |
| T-132 | components.md cites three line numbers that moved in T-129 | assistant | F-001 | design-agent | P3 | T-129 | PENDING | — | ListsMenu cites tasks.ts:42 (now :78), TaskList cites :200 (now :328), Skeletons cites TasksSurface.tsx:179 for a todayGroupLabel call that no longer exists |
| T-147 | uc-coverage-map.md D8 is stale — the phone DOES have inline rename and delete | assistant | — | spec-agent | P2 | — | PENDING | — | src/assistant/mobile/components/TaskList.tsx calls controller.editTask (L71) and controller.removeTask (L136). D8 says neither exists; the stale sentence was inherited into F-005 rev 1 and argued against pulling mobile in. Also the six UC rows reading MISSING (unexamined) |
| T-150 | F-001 AC-24's reachability half has ZERO test assertions — the coverage row reads as full | assistant | F-001 | qa-web-agent | P1 | T-145 | PENDING | — | Pre-dates T-145 but is now larger: the detail-open state is explicitly in scope with no case. Owed one case per failure state (failed turn, offline, IA §6 S1) asserting <=1 action and that the close affordance is neither hidden nor disabled. docs/qa/assistant/F-001/web/index.md:98 maps AC-24 to TC-028 alone, which tests only the usability half — the L-003/L-006 shape. Also: automation app.addTaskButton / app.taskRowByTitle still assume the list is on the conversation surface (rev-4 relocation drift) |
| T-151 | F-003 ## Parity claims F-001 AC-24 'holds identically' — one clause now has no mobile counterpart | assistant | F-003 | spec-agent | P2 | T-145 | PENDING | — | F-005 AC-45 is web-tagged so the detail-open state has no mobile subject. Same shape as the AC-18 parity note already recorded in F-001 AC-18 (mobile ships 2 of 4 operations). Fold in the AC-18 count correction (29→32 per F-005 Impact §9) while the file is open ALSO from T-157: the table does not list **AC-31** at all, and AC-31 is `(web)` — so the mobile predicate F-001 rev 7 just bound (`taskLinkState`) is asserted by NO tier. Retagging changes an enumeration another spec binds |
| T-158 | F-002 — a turn-path refusal has no frame to speak through | assistant | F-002 | spec-agent | P1 | T-154 | PENDING | — | TurnOutcome has six members and none is a refusal; F-002's speech table is closed and carries none either. F-005 AC-36 requires a refusal with a visible outcome and the WORDING is F-002's. Found by dev-backend lens (F1) at Gate 1 round 2 |
| T-161 | QA: four F-001 AC-31 assertions invert under revision 7, and a third entry point is uncovered | assistant | F-001 | qa-web-agent | P1 | T-157 | PENDING | — | web/__tests__/shell.test.tsx ~174-205 asserts a done and an undated task render as plain text — both are doors now. mobile/__tests__/task-link.test.ts:134 and :151 invert too. STILL CORRECT and must stay: shell.test.tsx:207-220 (deleted tasks are never links) and task-link.test.ts:152. Owed: a door to a task the shown collection does not hold switches collection then scrolls and flashes once, plus the assistant-created-dateless case specifically. Needs qa-mobile-agent for the second tier |
| T-163 | F-005 web implementation | assistant | F-005 | web-agent | P1 | T-160 | PARTIAL | src/assistant/_shared/model/clock.ts, src/assistant/_shared/model/notices.ts, src/assistant/_shared/model/task-fields.ts, src/assistant/web/detail.ts, src/assistant/web/components/TaskDetail.tsx, src/assistant/web/components/CarriedNotices.tsx, src/assistant/web/components/PassedReminders.tsx, src/assistant/web/__tests__/task-detail.test.tsx, src/assistant/web/__tests__/detail-model.test.ts, src/assistant/web/__tests__/f005-controller.test.ts, src/assistant/_shared/controller.ts, src/assistant/_shared/types.ts, src/assistant/_shared/api/client.ts, src/assistant/_shared/model/reducer.ts, src/assistant/_shared/model/tasks.ts, src/assistant/_shared/model/format.ts, src/assistant/_shared/model/messages.ts, src/assistant/_shared/model/client-stores.ts, src/assistant/_shared/testing/fixtures.ts, src/assistant/web/App.tsx, src/assistant/web/shell.ts, src/assistant/web/seams.ts, src/assistant/web/main.tsx, src/assistant/web/styles.css, src/assistant/web/components/TasksSurface.tsx, src/assistant/web/components/ConversationPane.tsx, src/assistant/web/components/ListsMenu.tsx, src/assistant/web/components/icons.tsx, src/assistant/web/__tests__/shell.test.tsx, src/assistant/web/__tests__/app.test.tsx, src/assistant/web/__tests__/controller.test.ts | **web 366/366, tsc clean, full suite 1017/1020 — all three remaining failures have named owners** (2 docs/qa/ from backend's X-Timezone contract, 1 mobile from design's SE-DETAIL). Verified independently, and it touched nothing outside its subtree. S6 detail surface, the receiver clause (`applyWrite` drops `removed` too, so AC-39 stops being vacuous on mobile), AC-47's notice rules in `_shared/model/notices.ts` behind ONE `reconcileNotices()` all four writers route through, `pushLocalTasks` widened from five fields to sixteen, and ZERO JS width reads left in the web tree. PARTIAL for three named gaps: AC-3's both-pending PROACTIVE save is not implemented (the arrival is deferred and the save happens on the user's own blur); no § TaskDetail exists so 47 testids are proposed not adopted; AC-38's surfacing and the `refused`-turn copy are placeholders |
| T-164 | F-005 mobile implementation — the shared-model half only | assistant | F-005 | mobile-agent | P1 | T-160, T-163 | PARTIAL | src/assistant/mobile/model/carried.ts, src/assistant/mobile/components/CarriedNotices.tsx, src/assistant/mobile/__tests__/carried.test.ts, src/assistant/mobile/__tests__/f005-mobile.test.ts, src/assistant/mobile/model/a11y.ts, src/assistant/mobile/model/announce.ts, src/assistant/mobile/model/shell.ts, src/assistant/mobile/model/task-link.ts, src/assistant/mobile/model/tasks-view.ts, src/assistant/mobile/model/touch.ts, src/assistant/mobile/components/TaskList.tsx, src/assistant/mobile/components/TasksSurface.tsx, src/assistant/mobile/components/AssistantScreen.tsx, src/assistant/mobile/components/styles.ts, src/assistant/mobile/controller.ts, src/assistant/mobile/index.ts, src/assistant/mobile/boot.ts, src/assistant/mobile/__tests__/a11y.test.ts, src/assistant/mobile/__tests__/shell.test.ts, src/assistant/mobile/__tests__/task-link.test.ts, src/assistant/_shared/controller.ts | Source landed and RUNS ON A REAL DEVICE — verified on iOS 18.6 simulator 2026-08-19: four collections, per-row accessible names, and AC-47's `Deleted "X". / Put back` all render. But NOT complete: its own new test file `f005-mobile.test.ts` has 20 tsc errors and 7/7 failing tests, all calling `controller.dispatch2(...)` — a method that exists nowhere in src/ — plus `carriedRowFor` and `announcer.spoken`, neither of which exists either. AND it regressed F-003: `docs/qa/.../F-003-mobile-surface.spec.ts` drops from 124/124 to 123/124 because `assistant-row-badge` is no longer rendered. Proven by stashing ONLY this task's subtrees and re-running: 124/124. Follow-up T-176. |
| T-167 | F-005 QA web + mobile — author test cases | assistant | F-005 | qa-web-agent | P1 | T-165 | PENDING | — | phase: author. WAITS on T-165: the testid contract comes from the screens, and the selector contract forbids inventing one. Needs a paired qa-mobile-agent dispatch for the nine (mobile)-tagged ACs, with F-003's node/device-lab split stated rather than discovered (tester M10) |
| T-168 | AC-47's 'deletion ends the notice' vs 'reported once, value legible' — one clause of confirmation | assistant | F-005 | spec-agent | P2 | T-159 | PENDING | — | design-agent read 'ends' as the RETRY obligation ending, not the row being removed, because the alternative makes the last legible copy of the user's typed value elapse away — one ender over from the loss AC-47 exists to prevent. § CarriedNotice's Lifetime table is built on that reading. If the spec means removal, the design contradicts it. Gate 1 is closed, so this is a one-clause confirmation, not a revision |
| T-169 | AC-9's low/medium mark has two documents claiming two owners | assistant | F-005 | spec-agent | P3 | T-159 | PENDING | — | AC-9's sub-bullet says the form is 'design's within the one-glyph vocabulary'; OQ5 lists the same question as the owner's. design-agent decided it as design (single !, high only) and nothing structural rides on either answer — but two owners for one question is how it gets decided twice |
| T-170 | The refusal-reason enum has no member for a value-not-in-set or a value-type violation | assistant | F-005 | architect-agent | P2 | T-162 | PENDING | — | **2026-08-23 — now four occurrences, two of them shipping, and the architect that hit it has recommended the members.** T-308 records the two live false statements in `turns.ts`. The T-191 architect met the same shape from the other side while amending `skipped.reason` and returned this deliberately rather than absorbing it: its recommendation is to add **at least `duplicate_name` and `operation_not_permitted`** (or `entity_not_supported`) to the refusal set. **Its own memory entry names the method that finds the rest: when adding a member to a closed enum, grep every existing use of the nearest member and check which uses are already misfits — the misfits are the same gap arriving earlier.**|
| T-171 | AC-3's both-pending precedence — the proactive save is not implemented | assistant | F-005 | web-agent | P1 | T-163 | PENDING | — | From T-163's own unresolved list. AC-3 requires a focused dirty field to save **as though focus left it** when an accepted write arrives; today the arrival is deferred and the save waits for the user's own blur. That satisfies 'the edit reaches the store' and 'the control displays what the store holds' but NOT the proactive save — and `## Test strategy` calls this the highest-value single case in the feature |
| T-173 | AC-26's headline may still have an edge after revision 4's correction | assistant | F-005 | spec-agent | P3 | T-166 | PENDING | — | qa-api-agent (T-166) flags: complete → un-complete (AC-28 removes the successor) → re-complete leaves the series with **no open occurrence**, while the headline says no occurrence in a live series generates none. **Revision 4 DID correct the phrasing** (verified — it quotes T40 and states the per-occurrence form), so this is an edge in the corrected text, not an unlanded disposition. Fifth sighting of this sentence. Gate 1 is closed: one clause of confirmation, in the same class as T-168/T-169 |
| T-174 | The row's urgency mark renders `!!!` — the graduated scale AC-9 forbids by name — and a test PINS it | assistant | F-005 | web-agent | P1 | T-163 | PENDING | — | Found by RUNNING the app. `src/assistant/web/components/TasksSurface.tsx:81` maps `high: '!!!'`; AC-9 says *not Apple's graduated ! / !! / !!!*, and design's `components.md` TR-URGENCY (line 184) publishes **a single `!`, only when priority is high**, nothing for none/low/medium. **`src/assistant/web/__tests__/task-detail.test.tsx:1463` asserts `.toBe('!!!')`, so the forbidden rendering is protected by a green test** — L-006/L-012: the assertion encodes the implementer's choice, not the requirement. Why nothing caught it: the ACCESSIBLE NAME is correct ('high priority'), so AC-33's 4.1.3 case passes while AC-9's visual requirement fails. **Third sighting of L-021's shape** — design drafted the same scale and caught itself; web shipped it |
| T-134 | design-check state check passes for an incidental reason (L-007 shape) | assistant | F-001 | design-agent | P2 | T-131 | PENDING | — | it only fails when ALL states are identical, and its snapshot is body.innerText[0:400], which with 32 dev-bar buttons is almost entirely dev-bar text |
| T-086 | Touch floor row for the shipped affordance | assistant | F-001 | design-agent | P2 | T-083 | PENDING | — | PAINTED carries a documented placeholder 48; the floor must be measured from the shipped control |
| T-060 | Fix BUG-004: follow new messages (both clients) | assistant | F-001 | orchestrator | P1 | T-084,T-085 | BLOCKED | — | code landed both clients; closes when QA covers AC-30 |
| T-074 | components.md needs rows for every string it does not own yet (~17 found: 2 server-composed + ~15 message families) | assistant | F-001 | design-agent | P1 | T-062 | PENDING | — | ADR-008's own 3rd review condition + product-review M6, live today: these strings are authored in src/assistant/api/engine/turns.ts, so the catalogue does not own every string the ADR's cost model assumes |
| T-075 | QA: re-sync F-001 spec:596 + utterance-intent-fixtures.json (still carries hoàn tác, HOÀN TÁC, không) | assistant | F-001 | qa-api-agent | P1 | T-069,T-073 | PENDING | — | L-004: the fixture table has two physical homes and they now diverge by more than one row |
| T-065 | Replace F-002's copied spoken-copy literals with row-ID citations (NOT translate them) | assistant | F-002 | spec-agent | P1 | T-064 | PENDING | — | spec-agent checked rather than assumed: components.md §Spoken frames ALREADY owns these strings in English (title_list overflow :98, SPK-CONFIRM-DELETE :107, SPK-REVERTED-PARTIAL :116). So F-002:200/206/209 are a SECOND COPY, not untranslated copy — translating them re-creates two homes in English where the next drift is invisible instead of merely visible in the wrong alphabet. Cite row IDs per L-008. Load-bearing: ADR-008 names 'copy stops having one owning artifact' as the trigger that expires the no-i18n decision. CARVE-OUT: F-002:135/:354 quote the owner's own words in Vietnamese — decision record, leave them. Also fix F-001:43's now-stale citation of AC-5's dropped phrase (3 words) |
| T-066 | Decide whether ARCHITECTURE.md's ADR index is a register; backfill 007-008 if so | assistant | — | architect-agent | P3 | T-063 | PENDING | — | — |
| T-079 | QA harness reuse contaminates local runs: freshUser counter restarts at 1 while the server keeps state | assistant | F-001 | qa-web-agent | P2 | T-070b | PENDING | — | reuseExistingServer:!CI + in-memory harness state + a counter that resets = run N gets run N-1's data under the same account names. Cheapest fix: seed freshUser from a per-run nonce. Same contamination class the orchestrator hit taking screenshots |
| T-077 | ~18 mobile TC docs still cite Vietnamese mockup copy the product cannot produce | assistant | F-003 | qa-mobile-agent | P2 | T-070c | PENDING | — | each sends a human tester after a string that no longer exists; TC-001/003/006/007/008/009/011/013/014/015/016/020/021/022/023/037 + index.md |
| T-078 | 8 unit-tier sites fabricate the server's Vietnamese chip labels (green but impossible) | assistant | F-003 | mobile-agent | P2 | T-070c | PENDING | — | parity.test.ts:79,254 · a11y.test.ts:173,175,240,380,382 · lifecycle.test.ts:184 — the server emits Delete N tasks/Keep them now; these pass because the fixture builds the turn itself (L-004) |
| T-072 | docs/design/_shared/DESIGN.md + tokens.json carry Vietnamese (incl. a 2nd `hoàn tác`); font rationale now void | assistant | — | design-agent | P2 | T-062 | PENDING | — | — |
| T-053 | Write the Spoken frames wording IN ENGLISH (AC-22 parse test depends on it) | assistant | F-002 | design-agent | P1 | T-062 | PENDING | — | — |
| T-054 | Place the talk-back on/off control + control-vs-message split (design F4) | assistant | F-002 | design-agent | P2 | T-052 | PENDING | — | — |
| T-055 | Disambiguate existing-app ADR-7/ADR-11 vs this repo's ADR-007 in MANIFEST | assistant | — | orchestrator | P3 | T-052 | PENDING | — | — |
| T-048 | Add client.speech_prefs to data-model.md | assistant | F-002 | architect-agent | P2 | T-044 | PENDING | — | — |
| T-020 | QA mobile: author test cases | assistant | F-003 | qa-mobile-agent | P2 | T-018 | PARTIAL | docs/qa/assistant/F-003/mobile/, tests/assistant/mobile/F-003-mobile-surface.spec.ts, docs/qa/_shared/fixtures/mobile/F-003-mobile-fixtures.json | 39 P1 TCs covering 12 F-003 + all 29 F-001 parity ACs; ran automation 29/32 (2 fails await T-019 code, 1 real contract drift); device-lab debt documented per L-003 |
| T-021 | QA mobile: execute | assistant | F-003 | qa-mobile-agent | P2 | T-019,T-020 | PARTIAL | tests/assistant/mobile/F-003-mobile-surface.spec.ts, docs/qa/assistant/F-003/mobile/, docs/qa/assistant/runs/2026-08-17-mobile-execute.md, docs/qa/_shared/bugs/BUG-002-cold-open-accepts-input-before-session-read.md | suite 32->106 assertions, 105/106; filed BUG-002 (HIGH); BUG-001 pinned by KNOWN-OPEN test; 34/39 automated, device-lab debt unchanged |
| T-022 | Structural review C1-C14 for F-003 | assistant | F-003 | reviewer-agent | P3 | T-021,T-024,T-025,T-026 | PARTIAL | docs/reports/review-F-003-2026-08-17.md, docs/specs/_shared/LEARNINGS.md | STRUCTURAL-FAIL 12/14; C9 (client.permission_state missing from data-model), C7 (7 high npm audit via metro, needs human call); C2 41/41, C5 all 6 suites green; overturned 2 tool false-positives; L-005/6/7 appended |
| T-176 | T-164's aftermath — 7 tests calling a method that does not exist, and one testid F-003 needs | assistant | F-005 | mobile-agent | P1 | T-164 | PENDING | — | Two separate defects, both introduced by T-164 and both measured here on 2026-08-19. **(a)** `src/assistant/mobile/__tests__/f005-mobile.test.ts` calls `h.controller.dispatch2(...)` at lines 524/549/578/598/607/618. `dispatch2` appears nowhere else in src/ — the real `dispatch` is `protected` on the SHARED controller, so the test wants a seam that was never built. Same file also uses `carriedRowFor` (undefined), `announcer.spoken` (RecordingAnnouncer has no such property) and `task({ local: true })` (not in `Partial<TaskWire>`). 20 tsc errors, 7 failing tests, all in this one file. **Decide the seam, do not paper over it:** either expose a test-only way to seed controller tasks or seed through `h.server` + `init()` as every other mobile test does. **(b)** `assistant-row-badge` is no longer rendered, so F-003's QA automation fails one assertion it passed before. This one is the more serious of the two — it is a live regression in a SHIPPED feature, and the failing test is QA-owned, so the fix belongs in `src/`, never in the spec file. |
| T-180 | docs/specs/assistant/index.md contradicts itself about which revision F-005 is at | assistant | F-005 | spec-agent | P1 | T-177 | PENDING | — | Line 10's table row reads `draft (rev 5)` and line 26 describes revision 5, but **line 16 still opens `**F-005 is at revision 4**` in the present tense.** T-177 updated the row and appended the new paragraph, and left the lead sentence standing. **This is the second time this exact file has done this**: the Drift Log records 2026-08-18, when T-145 shipped F-001 revision 5 and the index still said revision 4 — caught in passing by T-153 only because another edit had the file open. It matters more than its size: the index is the one file a fresh session reads to learn where a feature stands, so a wrong revision number there is wrong everywhere it is quoted. **And the shape is the one T-177 was itself hunting** — a new statement landing while its predecessor is left in place — which it found six times elsewhere in the spec and missed once in its own output. Fix: make the lead sentence a template that names the CURRENT revision, so the next revision cannot land without touching it. |
| T-185 | F-005 revision 6 — the elapsing undo offer names the trash | assistant | F-005 | spec-agent | P1 | T-184 | PENDING | — | Owner decision 2026-08-21, `docs/reports/owner-decision-2026-08-19-carried-notice-placement-and-timer.md` §7, option A. **Two amendments, both consequences of one answer, neither adding an AC.** **(a)** AC-43's elapse stops being silent: it is the moment the offer **hands off**, and what replaces it names the trash as where the task went. Wording is design's (T-179); that the handoff happens is the AC's. **(b)** AC-33's 2.2.1 argument gets its last leg — it currently says the timer is conformant *because an untimed path exists*; it can now say the user is **told about** that path at the moment the timed one ends, **which is the difference between a path existing and a path being available.** Depends on T-184 only to stop two spec-agent dispatches racing on `docs/specs/assistant/index.md`. 48 ACs before and after. |
| T-186 | Gate 1 round 2 on F-006 — TARGETED re-review | assistant | F-006 | orchestrator | P0 | T-187 | DONE | docs/reports/gate1-lenses/F-006-r2-consolidated.md, docs/reports/gate1-lenses/F-006-r2-product.md, docs/reports/gate1-lenses/F-006-r2-architect.md, docs/reports/gate1-lenses/F-006-r2-design.md, docs/reports/gate1-lenses/F-006-r2-tester-api.md, docs/reports/gate1-lenses/F-006-r2-tester-web.md, docs/reports/gate1-lenses/F-006-r2-tester-mobile.md, docs/reports/gate1-lenses/F-006-r2-dev-api.md, docs/reports/gate1-lenses/F-006-r2-dev-web.md, docs/reports/gate1-lenses/F-006-r2-dev-mobile.md | **14 HIGH · 20 MEDIUM · 3 LOW. ROUND CAP REACHED — escalated to the owner.** The finding that frames the rest: **all 56 round-1 findings hold**, verified independently by nine lenses, most against the source or the working tree rather than the disposition log. **So round 2 is not a report that revision 3 failed — it is a report that the things nobody had reviewed were the things that were wrong.** Of the 14 new HIGHs: **7 on AC-14/AC-15 (the owner's read permission, added AFTER round 1), 3 on AC-17 (created by a revision-3 fix, never read), 4 on clauses ADDED by revision-3 fixes — and ZERO on text that existed at round 1 and survived it.** Seven convergences; the largest is that **the owner's decision is currently unimplementable** — three closed vocabularies each independently have no slot for what the assistant is now required to say. **All nine lenses judged the routed dead end ACCEPTABLE**; none asked to reverse it. |
| T-187 | F-006 revision 4 — the assistant may read the trash | assistant | F-006 | spec-agent | P0 | T-184 | DONE | docs/specs/assistant/F-006-recently-deleted.md | Revision 4. **Verified by the orchestrator: 17 ACs before and after, and exactly four headline lines changed — AC-4, AC-5, AC-14, AC-15** (my first measurement said seven and was wrong: the grep extracted every id mentioned on a changed line, not the headline id). `declared-elements.sh` exit 0, `index.md` untouched, 64/28. OQ4 closed with the question kept. **The agent found two things the briefing did not ask for and both were real.** **(a)** AC-4's HEADLINE said *"no assistant query"* — a read exclusion sitting in an addressing AC — so "AC-4 is unchanged" was true of its clause and false of its headline. **(b)** AC-5's *"it is the only one that does"* would have been silently falsified by the grant; it added one clause saying the assistant's read **IS** AC-5's read — a caller, not a second door — which keeps the exclusivity claim true and stops an architect opening a second deleted-row reader in the turn path. **The dead end is recorded and deliberately unsettled**, routed to round 2 per the owner's decision. Sweep found **6** leftovers the old answer left standing. |
| T-188 | F-006 revision 5 — the round-2 findings, and the two frames the trash needs | assistant | F-006 | spec-agent | P0 | T-186 | DONE | docs/specs/assistant/F-006-recently-deleted.md, docs/specs/assistant/index.md, docs/reports/gate1-lenses/F-006-revision-5-log.md | **37 of 37 dispositioned — 35 fixed, 2 recorded, 0 rejected; all 14 HIGH fixed. The no-new-AC constraint held: 17 → 17.** Verified by the orchestrator: ids 1..17 contiguous, 17/17 tagged, `declared-elements.sh` exit 0, **all 37 finding ids named in the log** (25 were named in the consolidation). 717 → 857 lines. **The agent caught an orchestrator error that cost a HIGH:** the round-2 consolidation accounted for **27 of 37** findings, and the ten it dropped included **product F2** — an unbounded assistant read that would speak step titles and break ADR-013 in the one path no AC asserts on. It found it by writing against the nine lens files rather than the consolidation, which the briefing told it to do. Durable half is **L-028**. **F-006 gets no further review** — the owner waived the cap for one revision, not for another round; remaining MEDIUM/LOW go to architecture as recorded questions. |
| T-189 | F-007 — the model authors the reply | assistant | F-007 | spec-agent | P1 | T-188 | PENDING | — | **THE SHAPE IS AN AGENTIC LOOP, NOT A PIPELINE** (owner, 2026-08-21 — the orchestrator drew it as three fixed AI calls twice and was corrected twice; §3 of the decision record has the flow). Backend **exposes tools**, the AI drives: it searches and reads, decides what it needs next, and **the number of rounds depends on the question and on how well the model does.** Then it returns **actions + targets + the line in one response**, the client confirms, the backend executes and reports back, and the AI may loop again. **Three consequences:** (a) the AI is **not guessing a task name — it names the rows it is targeting**, so the consistency check is on one response rather than a guard against invention; (b) **reads during the loop, writes only after the confirm** — the owner's diagram puts `client confirm` before `backend take action`, and if the AI could write during the loop the confirm would be decorative; (c) **an agentic loop needs a bound a pipeline does not** — not to predict the round count but so a bad question cannot spin. Default: a max round count AND a wall clock, both adjustable, **and the spec must say what the user sees when either is hit — silence at the ceiling is the failure mode.** Owner decision 2026-08-21, `docs/reports/owner-decision-2026-08-21-the-model-authors-the-reply.md` §2: **yes, the model must answer.** NEW FEATURE, own spec, own Gate 1 — **not** an amendment: it changes the server↔client contract, re-opens 518 assertions and touches five shipped features, so folding it anywhere puts it in front of no lens. **Measured 2026-08-21:** 3 files compose reply text (874 lines), **158 hardcoded strings**, **518 exact-string assertions across 36 test files**, 22 spoken frame rows, 5 features. **The row that changes the shape of the work: the model is ALREADY in the loop** — `ports/interpreter.ts` calls `claude-opus-5` — so this widens what an existing call returns rather than adding a dependency. **The 518 assertions are the real cost and they are not waste:** they are what makes today's copy verifiable, and a model-authored reply cannot be asserted by equality, so each becomes a property assertion (*does it name the task? state the count?*) or a deleted test — **deciding which, 518 times, IS the work.** **On what stays fixed: nothing does, and the orchestrator's first recommendation was withdrawn by the owner's question.** The original claim — *a hallucinated task name deletes the wrong thing* — **is false**: deletion is id-driven, so a wrong name still deletes the right row. **What breaks is CONSENT** — the user says yes to a sentence that does not match the action, which for a confirmation is not a side effect but the whole purpose of the sentence (the owner's 2026-08-17 decision rejected count-only for exactly that reason). **Stated correctly, the fix is not a template.** The requirement is not *this sentence is fixed* but ***the facts in this sentence are true***, and the cheaper way to get it is: **the model composes freely, then code verifies every task name in the sentence is in the set about to be destroyed and the count matches** — mismatch regenerates or falls back. The model stays free; one short verifier. **Recommended scope of the check: the sentences where a proper noun or a count carries the user's decision** — the destructive confirmation, probably the applied-summary. Everything else the model writes and nobody checks. Gate 1's lenses press on the boundary. |
| T-190 | Audit every spec for the fixed-reply assumption — the AI answers, it does not pick a sentence | assistant | — | spec-agent | P1 | T-189 | PENDING | — | **Owner, 2026-08-21: every spec that describes the chat surface or the voice surface currently assumes the reply is a CHOSEN sentence, and that assumption is wrong — the AI answers** (`docs/reports/owner-decision-2026-08-21-the-model-authors-the-reply.md` §2, §3). This is the sweep that follows F-007, not a duplicate of it: **F-007 (T-189) specs the new mechanism; this row finds and corrects everywhere the OLD mechanism is written down as a requirement.** **Measured 2026-08-21 — grep for `spoken frame` / `frame by row` / `one composer` / `literal` / `template` / `closed vocabulary`:** F-002 **20** sites, F-001 **10**, F-003 **5**, F-005 **24**, F-006 **4**, `design/_shared/components.md` **54**, `information-architecture.md` **1** — **118 places across 7 documents.** **The distinction that makes this hard, and it must be preserved rather than swept flat:** some of those are the assumption (*"the composer selects a frame"*) and some are a REQUIREMENT that survives the change (*"a destructive confirmation names the tasks"* — the owner's 2026-08-17 decision, still true when the AI writes the sentence). **Deleting the second kind along with the first is the worst available outcome**, and it is what a mechanical sweep produces. Every edit gets a one-line reason in the log saying which kind it was. **Also in scope:** `ADR-001` and `ADR-003` name the Interpreter as *the AI seam* for interpretation only — say whether they are amended or superseded, and by whom. **Do NOT start before T-189 lands:** correcting the old mechanism before the new one is written means guessing what to correct it TO. |
| T-191 | F-006 architecture — contracts, the two frames' wire shape, and nine owed decisions | assistant | F-006 | architect-agent | P1 | T-188 | DONE | `docs/specs/assistant/api-contracts.md`, `docs/specs/_shared/adr/ADR-016-trash-lifecycle-state.md`, `docs/specs/_shared/adr/ADR-017-trash-read-mutates.md`, `docs/specs/_shared/adr/ADR-012-delete-membership-and-restore.md`, `docs/specs/assistant/data-model.md`, `docs/specs/assistant/index.md` | **Gate 1 is closed on F-006 and does not reopen.** Revision 5's log ends with **nine architecture-owed shapes, each already recorded in the spec's own words with a writer named** — start there, not from the ACs. Includes: the trash read's route and whether an entry carries what it needs to name its parent (dev-web r2 F1); the expired-restore outcome, distinguishable from unknown-id and from already-live; the `skipped` reason for a purged row; the permanent-deletion envelope with no `task:` filler; the raw-store harness read AC-12 and AC-17 both owe; and **the `turn.outcome` member that selects the two new trash frames** — design writes the utterances, architect writes the member. **Also owed and named by architect r2 F5: an ADR-009 Amendment for the lifecycle-state-not-container rule, and an ADR for the read that mutates.** T-181 (the series restore) is the same agent's and can ride along. **All nine owed shapes specified, none blocked — and the agent found the ninth itself, as the briefing deliberately required.** The briefing listed eight and told it the count might be wrong; it located the ninth in `## Impact` §10 (processing rule 5`s interpretation context) rather than bending the list to fit. New routes `GET /tasks/deleted`, `DELETE /tasks/deleted/{id}`, `DELETE /tasks/deleted`, `GET /__qa__/raw-tasks`; restore gains outcomes (c) `409 RESTORE_EXPIRED` and (d) `409 RESTORE_PARENT_GONE`, distinguishable by status+code, which is what AC-16`s 4.1.3 claim actually rests on; `skipped.reason` gains `permanently_deleted` so a destroyed row stops being reported as *"modified since apply"*; ADR-016 and ADR-017 written. **The collision I warned it about did not happen, and it proved that rather than asserting it:** `trash_read` is an **eighth** member of `turn.outcome.kind` and the seven existing ones are untouched (`api-contracts.md:1344`), F-008`s `list_refuse` maps to the seventh (`refused`), and `list_create`/`list_move` are interpreter action types, not outcome kinds. `declared-elements.sh` exit 0; suite 1275/1276 with the one known F-008 wire drift. **It also refused the scope creep I offered** — the RefusalReason gap was returned as `unresolved:` with a recommendation instead of being absorbed, which is exactly what the message asked for. See T-170.|
| T-194 | F-002 + index: F-004 is committed and scheduled, not reserved | assistant | F-002 | spec-agent | P1 | — | DONE | docs/specs/assistant/F-002-talk-back.md, docs/specs/assistant/index.md | Both sentences now read *committed as F-004 and scheduled after MVP*, with the content named. Verified: AC count 25 before and after, `declared-elements.sh` exit 0, `git diff --name-only` shows only the two files. **The agent found the briefing was short: grepping the id returned NINE sites, not two.** It fixed two more inside its scope — including `## Purpose` in the same file, which said *"F-004 has no owner decision behind it yet"* and would have contradicted `## Out of Scope` three sections later. **And it named five it could not touch, one of them load-bearing:** `components.md:1134` justifies a whole frame family not speaking **by citing F-004 as undecided** — a design argument resting on a fact that stopped being true today. Follow-ups T-197 and T-198. |
| T-195 | Nothing says how an utterance ends, and the two design files disagree | assistant | F-001 | spec-agent | P1 | — | PENDING | — | Audit T-192 finding F3, and it is **not a voice-first question — it is the most-used interaction in the product having no AC.** F-001's User Flow has exactly one edge out of Listening into Thinking (`end of speech`), **AC-29 declares that edge list complete, and no AC constrains that edge**: not who decides speech ended, not what the user does to end it deliberately. AC-2 covers what listening renders, AC-3 covers cancel; **the gap is between them.** The two design files fill it differently: `DESIGN.md § User journey` says *end-of-speech **auto-sends***, while `components.md § MicControl`'s a11y name while listening is ***"Listening — tap to stop"*** — and under AC-3 a stop while listening is a **CANCEL**: text to the composer, nothing sent. **So the one control a user has mid-utterance either sends or discards depending on which file you read.** |
| T-196 | Route the rest of the voice-first audit — F2, F4, F5, F6 | assistant | — | orchestrator | P2 | T-194 | PENDING | — | The four findings from T-192 not already carried by T-194 or T-195. **F2 (session boundary is visible, never audible)** — UC-52 AC-52.4 is called *the heaviest AC of the whole use case BECAUSE the user is hands-free*, and this repo answers it with a visible marker; F-002's reason is *history*, **true of its delivery channel and false of its meaning**, since ADR-004's lazy close produces the boundary on the request carrying the user's next sentence — the user learns the session ended AFTER speaking into the closed one. **F4** — the mic has one home and nothing writes that down as a requirement, so nothing forbids a second. **F5** — voice discoverability is two design strings, both first-run-only, **one of them unwritten**, neither required by any AC. **F6** (cannot tell) — headset as capture route, and no non-visual capture cue on mobile. Read the audit's own sections before filing; each already carries its severity and its label. |
| T-197 | Two places in the coverage map still say the spoken summary is undecided | assistant | — | spec-agent | P2 | T-194 | PENDING | — | `docs/specs/_shared/uc-coverage-map.md:126` and `:340` still assert the old status. **Small, but this is the file a reader consults to learn what is and is not committed**, so a stale line here is stale everywhere it is quoted. Owner decision `docs/reports/owner-decision-2026-08-21-the-app-speaks-on-open.md`. |
| T-198 | A design rule is justified by a fact that stopped being true today | assistant | — | design-agent | P2 | T-194 | PENDING | — | **The load-bearing one.** `components.md:1134-1137` reads *"F-002 reserves as F-004 and explicitly records as having no owner decision behind it. So this…"* — **the undecidedness is used as the REASON a frame family does not speak.** As of 2026-08-21 there is an owner decision, so the reason is false even though the rule may still be right: at MVP the app is silent, which is a different argument for the same behaviour. **Re-justify or change the rule; do not leave a design decision resting on a retracted premise.** Also stale, and cheaper: the three app-shell mockups (`app-shell.html:821`, `-ios.html:757`, `-android.html:761`). Decision: `docs/reports/owner-decision-2026-08-21-the-app-speaks-on-open.md`. |
| T-199 | The task-detail fields render 197px wide because their class is styled nowhere | assistant | F-005 | web-agent | P1 | — | PENDING | — | **Ugliest single screen in the app**, from the design audit (`docs/reports/design-audit-2026-08-21.md` F1). At 1440 every text field in the detail pane renders **197px wide inside a 1020px pane**, and the Name field **clips the task's own title**. Cause located and **verified independently by the orchestrator: `detail-field-control` appears once in `TaskDetail.tsx` and ZERO times in `styles.css`, `components.md` or any mockup** — so the input falls back to the HTML default `size=20`. **A class used and never styled is invisible to every check in this project**: design-check tests overflow, not underflow, and it passed 60/60 with this on screen. |
| T-200 | Wide desktop: 52% of the Tasks pane is dead at 1920, and it is in the mockup too | assistant | — | design-agent | P1 | — | DONE | `docs/design/assistant/screens/app-shell.html`, `docs/design/_shared/tokens.json`, `docs/design/_shared/DESIGN.md`, `docs/design/_shared/components.md` | **Design half was already done by T-204/T-227; agent returned BLOCKED for its own scope, correctly, and the orchestrator re-measured rather than taking it on the word.** `.tasks-col` was replaced by `.content-col{max-width:var(--list-max); margin-left:auto; margin-right:auto}`, and `tokens.json` breakpoints now reach `laptop 1440 / wide 1536 / ultra 1920` — the root cause named in the row (design-check stopping at 1280) is closed. **Independently measured in a browser at four widths, and the numbers are the agent`s:** asymmetry **0px at 1024, 1280, 1440 and 1920** against the filed 140 / 300 / 780; fill 91.9 / 94.3 / 81.3 / 65.9%. **But the row`s claim that this is "a DESIGN defect, not drift" has inverted since it was written, and that is the real finding: `src/assistant/web/styles.css:856` still carries `max-width: 720px` with no `margin: 0 auto`, under the same comment the row quotes — *"The canvas is used by the SPLIT"* — and `TasksSurface.tsx:487` still renders `.tasks-col`. The design was fixed and the client was not.** Filed as T-304. **Two briefing errors of mine, both caught by the agent rather than absorbed:** I named `docs/specs/assistant/information-architecture.md`, which does not exist (the file is `docs/design/_shared/information-architecture.md`), and I briefed a defect that had been fixed — I had verified the row`s measurements were once true but not that they were still true. |
| T-201 | The conversation is top-anchored — 618px of nothing above the composer | assistant | F-001 | web-agent | P2 | — | PENDING | — | Design audit F3 (`docs/reports/design-audit-2026-08-21.md`). **618px between the last bubble and the composer at 1440, 462px at 390.** **Pure implementation drift, and the mockup is right:** `app-shell.html:417-423` carries `margin-top:auto` **and a paragraph defending it**, while `.conv` / `.conv-scroll` in the app carry neither that nor `justify-content: flex-end`. The audit's note on why it matters: a top-anchored thread is *"the specific thing that breaks the resemblance"* to the products this app is meant to sit beside. |
| T-202 | New visual language — the system only, delivered as something the owner can look at | assistant | — | design-agent | P0 | — | DONE | docs/design/_shared/specimen.html, docs/design/_shared/DESIGN.md, docs/design/_shared/tokens.json | **New language delivered and rendered: 9 plates, web/iOS/Android, 7 widths, 2 themes, in one 71.6 KB self-contained file — verified over `file://` with 0 console errors and 0 horizontal overflow.** Orchestrator checked: **zero external references**, and **18 testids used, all from the kept catalogue, none invented.** The look is replaced completely (Swiss, light-first, white ground, radius 0, **3 accents down from 5**) while every one of the 52 ids stays. **The wide-desktop rule the old system never had is now measured at every declared width and symmetric at all of them — v1 at 1920 was 0/780, v2 is 220/220, and the list never narrows as the frame grows.** **`design-check` now exits 1 with 510 failures and that is the CORRECT signal** — all 510 are token drift on the six v1 mockups the screens dispatch will redraw. **Two defects it caught in its own work by rendering rather than reading:** a version string in a tokens note became a phantom 1px breakpoint, and its first wide rule made the list SHRINK when the rail arrived at 1440. Both invisible in prose, obvious in a measured table. Sign-off is T-203. |
| T-203 | Owner signs off the new visual language before any screen is drawn | assistant | — | orchestrator | P0 | T-202 | DONE | docs/reports/design-signoff-visual-language.md | **Owner said *"Ok tiếp đi"* after the specimen was opened in their browser and published as an artifact.** Recorded exactly, **with its limits recorded beside it: the three questions were presented verbatim and none was answered individually**, so this is permission to draw and not a per-question verdict on the time rail, the light ground or the platform fidelity. **Not treated as silence** — an explicit ok was given; the protocol's rule is against inferring approval from silence or from a reply about something else. **What it leaves open is named: question 2 is the whole direction, and every screen inherits it** — cheap to reverse while only mockups exist, a re-implementation once anyone styles against v2. |
| T-204 | Redraw the core to v2 — shell, Talk, Tasks, and the task detail nobody ever drew | assistant | — | design-agent | P1 | T-203 | DONE | docs/design/assistant/screens/app-shell.html, docs/design/assistant/screens/app-shell-ios.html, docs/design/assistant/screens/app-shell-android.html, docs/design/assistant/screens/voice-assistant-view.html, docs/design/assistant/screens/voice-assistant-view-ios.html, docs/design/assistant/screens/voice-assistant-view-android.html, docs/design/assistant/screens/task-detail.html, docs/design/assistant/screens/task-detail-ios.html, docs/design/assistant/screens/task-detail-android.html, docs/design/assistant/screens/index.html, docs/design/_shared/components.md | **`design-check` 137 passed / 0 failed — it was 78 / 510 at dispatch.** Verified by the orchestrator. All four audit defects measured fixed: task-detail fields **197px → 640/640/496** with the title no longer clipped; dead space **0/300 @1440 and 0/780 @1920 → 100/100 and 220/220**; last bubble to composer **618px → 16px**; priority mark **`!!!` at margin 0 → one `!` at `space.2`**. 20 `components.md` sections rewritten, **3 new** (TimeRail · Field/Label/FormRow · TaskDetail), 5 named as left for tranche 2 **and marked at their own heads so the file says which half is stale.** **The defect it caught only by rendering is the one worth keeping:** it drew the bottom-anchored conversation, screenshotted it, and it was still top-anchored — `margin-top:auto` on the first child of a flex column does nothing when that child is `display:none`, which it is in a state-switched mockup. **It shipped the exact defect it was sent to fix and only the render caught it.** Confidence MEDIUM, lowered by the agent and correctly: **it could not run the mobile test tier** — see T-207. |
| T-205 | Draw the three surfaces that have never been drawn — lists menu, settings, new list | assistant | — | design-agent | P1 | T-204 | PENDING | — | **phase: screens, tranche 2.** The IA declares six surfaces; **three of them have no drawing at any width on any platform.** All three exist in code already (`ListsMenu.tsx`, `SettingsSurface.tsx`, and the list editor), so this is drawing what shipped rather than inventing. **Carry the finding that made the wide tier necessary:** v1 showed a **320px phone drawer holding five rows and ~700px of nothing on a 1440 screen**, and dimmed the Talk panel behind it — v2's Lists rail replaces the drawer above 1536 and the drawer still has to be drawn below it. |
| T-206 | Draw the trash, once its contracts exist | assistant | F-006 | design-agent | P1 | T-191 | PENDING | — | **phase: screens, tranche 3.** Waits on the F-006 architecture (T-191) rather than on tranche 2: **the trash read's shape decides what an entry can show**, and Gate 1 round 2 found the entry cannot name its parent without it. Round 2's design lens also left **nine unnamed surface states** for this feature and **two confirmations with no visual dialog component in the catalogue** — both are this row's, and `docs/reports/gate1-lenses/F-006-r2-design.md` lists them. |
| T-207 | The visual-language rename broke 16 test files, and two dispatches went past it | assistant | — | mobile-agent | P0 | — | DONE | src/assistant/mobile/model/theme.ts, src/assistant/mobile/components/styles.ts, src/assistant/mobile/components/VoiceSurface.tsx, package.json | **Measured by the orchestrator 2026-08-21: the full suite runs 774 tests, down from 1240. Sixteen test FILES fail to load at all** — roughly 466 tests that never execute, including `a11y.test.ts`, the file that guards the mobile id catalogue. **One cause, one line:** `src/assistant/mobile/model/theme.ts:45-46` reads `tokens.font.family.display` and `.body`; commit **9e0aef7** (the new visual language) renamed those keys to **`ui` / `numeric`**, so the import throws and every file importing it dies. **The generalisable half is why nobody caught it for two dispatches:** a design dispatch returns `tests_passing: 0, tests_total: 0` **by design** — it does not run the suite, and nothing else did either. **`tokens.json` is read by `src/` at runtime, so it is not a design-only file, and renaming a key in it is a code change wearing a design change's clothes.** Fix `theme.ts`, then check the rest of `src/` for other token reads before declaring it done. |
| T-253 | Two motion tokens were deleted and their values reappeared as literals in two clients | assistant | — | design-agent | P1 | — | DONE | `docs/design/_shared/tokens.json`, `.claude/tools/design-check/check-design.mjs` | **DONE 2026-08-22 — ANSWERED "DROPPED BY ACCIDENT", with evidence rather than assertion.** The agent's argument: **every deliberate v2 retirement is documented** — aurora breath, spring easing, green/`success`, the cyan/violet voice pair all carry a `$meta` note or a `DESIGN.md ## Motion` entry saying *retired* and what replaced them. **The diff-flash pair appears in no retirement list anywhere**, while six documents and two tests still reference it as live. The v2 pass rewrote the motion block and simply omitted them. **Restored `diffFlashHold: 1600` / `diffFlashFade: 400`; verified on disk, and the suite went 1238 → 1239/1240 with the diffFlash test green.** — **Found by web-agent while it was told NOT to fix it, and the finding is bigger than the task that surfaced it.** **Verified independently by the orchestrator:** `tokens.json motion.duration_ms` now holds only `micro, standard, emphasis, rulegrow` — **`diffFlashHold` and `diffFlashFade` are gone**, dropped by the v2 token pass. **Six documents still name them as if they exist:** `components.md:245,916,917`, `information-architecture.md:308,309`, `F-005-task-detail.md:520`, `F-001-voice-assistant-view.md:85`, `gate1-lenses/F-005-design.md:60`. **And the value did not vanish — it was copied into the clients under names that still claim to be token-derived.** `src/assistant/web/styles.css:108` declares `--motion-duration_ms-diffFlashHold: 1600`, a CSS variable named after a token that no longer exists, holding a literal. `src/assistant/web/shell.ts:68` has `export const FLASH_MS = 2000`. Mobile has the same breakage at `task-link.ts:128`. **That is reviewer C4 (no hardcoded design values) violated in disguise** — the names make both look like token references. **This is LEARNINGS L-004 exactly: a canonical artifact living in two files drifts, and the drift shows up as a workaround rather than a red test.** **THE ONE RED TEST IS THE SAFETY NET DOING ITS JOB, NOT A DEFECT.** `shell.ts:59` carries a comment saying *"`shell.test.ts` parses tokens.json and asserts this number, so a token change fails the suite instead of silently leaving the JS and the CSS disagreeing (L-008)."* It did. **Do not make that test green by changing the web side** — that would disable the only mechanism that caught this. **Decide first, then fix: were the two tokens retired deliberately in v2, or dropped by accident?** If retired, the six documents and both clients need the new source named. If dropped, restore them to `tokens.json` and everything else follows. Say which, with evidence from the v2 pass. **Artifacts reconstructed 2026-08-23 by verifying each fix on disk, not from the agent returns** — the orchestrator dropped step 4 (fill Artifacts from `files_created`/`files_modified`) on this whole cluster of rows, so the validator was reading thirteen real fixes as analyses with nothing behind them. **Artifact attribution corrected 2026-08-23:** `src/assistant/web/shell.ts` was listed here by the orchestrator`s reconstruction and is **web-agent`s file, not design-agent`s** — it was named in this row`s Outcome prose (the `FLASH_MS` derivation) and got swept into the path list. **The error was invisible until today, because the Artifacts cells used ` · ` as a separator while `artifact_paths` splits on commas only — so C1 and C6 were checking the FIRST path of each such row and nothing else.** Fifteen rows were affected; converting them to commas surfaced this one. |
| T-210 | Soften the language — radius back, fewer borders, and 1920 rethought | assistant | — | design-agent | P0 | — | DONE | docs/design/_shared/tokens.json, docs/design/_shared/DESIGN.md, docs/design/_shared/specimen.html | **Radius scale back — `none 0` geometry · `xs 4` small painted objects · `sm 8` controls · `md 12` grounds · `lg 16` floating layers · `xl 28` sheet top corners — with a CHECKABLE nesting rule (an inner radius is never larger than its parent's).** Stops at 16 because 24+ is the wellness-app territory `## Identity` rejects by name. **Borders: the coordinator's sharpening changed the RULE, not the number** — space and ground primary, type weight third, the 1px line a specialist earning three cases (input field · dense table-like row · container holding a different KIND of thing). No rule between task rows, which is what Things 3 and Apple Reminders do. **And it corrected the orchestrator's measurement: I counted 46 border DECLARATIONS; the screen paints 13** — a declaration count is defeated by moving a line into a `div`, and this system had already done that, so the density rule now counts painted lines. **1920: it built the briefed structural answer, rendered it, and LOST to the cheaper one.** A fourth tier splitting the pane into two 590px columns gave two columns of half-empty rows and drew the time rail twice; **borders-off alone carried it — the emptiness reads as margin once no line is drawn across it.** Tier 4 withdrawn, **and recorded as tried-and-rejected with both renders**, so the next dispatch does not re-propose it. Two defects only the render caught, both its own. `design-check` 127/10 — **all ten are one retired token in the stale mockups, and it refused to add a dead alias to make them green.** |
| T-211 | Redraw the nine mockups against the softened language | assistant | — | design-agent | P1 | T-210 | DONE | docs/design/assistant/screens/app-shell.html, docs/design/assistant/screens/app-shell-ios.html, docs/design/assistant/screens/app-shell-android.html, docs/design/assistant/screens/voice-assistant-view.html, docs/design/assistant/screens/voice-assistant-view-ios.html, docs/design/assistant/screens/voice-assistant-view-android.html, docs/design/assistant/screens/task-detail.html, docs/design/assistant/screens/task-detail-ios.html, docs/design/assistant/screens/task-detail-android.html, docs/design/assistant/screens/index.html, docs/design/_shared/components.md | **`design-check` 137 / 0 (was 127 / 10) — verified.** The detail is a property sheet: **17 full boxes → 4, and 7 painted lines → 0** inside the pane. **The four survivors are three step checkboxes and the outlined Delete button — no field carries a box at rest.** App shell at 1920: **13 painted lines → 5.** All 52 testids intact, none invented, none renamed. **The accessibility self-check ran for the first time and earned itself immediately:** probe 2 caught the task row's open button reading *…răng!* visibly while announcing no `!`, **and the urgency mark's meaning never announced at all** — marks moved outside the button, each with `role="img"` and its own name. Two tap targets under the platform floor were met **with hit areas rather than bigger paint.** Probe 4 found 174 style-only hits and **0 actually reachable by a real tab walk** — the probe over-reports and it said so. **AC-2's commit moment is named and left open** as instructed, in the stylesheet, in `§ PropertyRow`, and routed to T-213. **One honest finding against a token it does not own: `border.budget_boxes: 5` is arithmetically unreachable** — a checkbox is a full box and is the allowlist's own first example, so any five-row task list starts at 5 before anything else exists. 26 of 74 states exceed it and **every box in every one is on the allowlist.** |
| T-212 | Guard the accessibility self-check with a scenario, and consider a mechanical probe | assistant | — | orchestrator | P2 | — | PENDING | — | The self-check added to `design-agent.md` on 2026-08-22 is **prose an agent is asked to follow**, and this project has already lost one prose rule to a template sync (L-016) and one to nobody-noticing (the six/seven miscount that sat in the visual review list unremarked). **Two things worth doing, in order.** (a) **A scenario that fails if the section is gone** — cheap, and it is what made T-149 stick. (b) **Consider moving probes 1-4 into `design-check`**, which today reads colour pairs out of tokens and **cannot see a control nobody can reach, a name that does not match its label, or a 24px tap target.** That is a bigger change and it needs a headless browser in the checker, so it is a proposal rather than a decision — but the argument is the same one that closed T-149: **a rule a tool enforces outlives a rule an agent is asked to remember.** |
| T-213 | A picker row has no blur — say when the value is committed | assistant | F-005 | spec-agent | P1 | T-211 | PENDING | — | Consequence of the owner's 2026-08-22 property-sheet decision (`docs/reports/owner-decision-2026-08-22-the-detail-is-a-property-sheet.md`), **named by the drawing and answered by the spec, not settled in the drawing.** `AC-2` is written for FIELDS: a value is committed when the field loses focus. **A row that opens a picker has no blur in that sense** — it has an open, a choice, and a dismiss, and **a dismiss without a choice must not write.** So AC-2's commit moment needs restating for the picked values (priority, deadline, reminder, repeat) while staying exactly as it is for the typed ones (name, note). **Also check AC-47's carried notice**: it holds *the value the user typed* after a failed write — a picked value was never typed, so what the notice carries and offers to retry needs saying. Wait for T-211 so the spec answers the shape that was actually drawn. |
| T-214 | `border.budget_boxes: 5` cannot be met and the rule knows it | assistant | — | design-agent | P2 | — | PENDING | — | Reported by the screens dispatch against a token it does not own in `phase: screens`. **A checkbox is a full box and is `box_allowlist`'s own first example**, so a screen showing a five-row task list **starts at 5 before anything else exists** — the budget is unreachable by construction. Measured: **26 of 74 rendered states exceed it, and every box in every one is on the allowlist**, which is the tell that the count is wrong rather than the drawings. The structural-line budget of 8 held everywhere except `settings-default` at 10 — the carried-from-v1 Settings surface, tranche 2, **and its rows are the *settings table* case the rule explicitly earns.** Fix: count boxes that are NOT on the allowlist, or raise the number with the checkbox arithmetic written beside it. |
| T-216 | Step checkboxes are 20×20 on all three platforms, under every floor | assistant | — | design-agent | P2 | — | PENDING | — | Pre-existing, found by the accessibility probes during T-215 and **confirmed against `HEAD` rather than assumed** — untouched by that task. Painted 20×20 while the floors are **web 40 · iOS 44 · Android 48**. **Fix with a hit area, not bigger paint** — that is what T-215 did for the checkbox and the settings switch, and the pattern is already in the file. Probe 3 reports it on every render that shows steps. |
| T-217 | The repeat editor shows *Never* pressed while an until-date is displayed | assistant | — | design-agent | P2 | — | CANCELLED | — | **CANCELLED 2026-08-22 — STALE, verified rather than assumed.** The row says the repeat editor shows *Never* pressed while an until-date is displayed. All three states measured in the browser: `detail-repeat` → Never pressed, no date field; `detail-repeat-until` → On a date pressed, date field `2026-12-18`; `detail-repeat-count` → After pressed, no date field. **Internally consistent in all three.** Left open it costs a future dispatch to re-discover that. — State `detail-repeat-until`: the *Ends* segmented control has **Never** pressed and the until-date field visible at the same time. Pre-existing, reported by T-215 rather than fixed because it was out of that task's scope. **One of the two is wrong and the drawing does not say which** — which is the kind of thing a QA author writes a passing test against. |
| T-219 | The title's size was raised by the owner and never answered on the record | assistant | — | design-agent | P2 | — | PENDING | — | The owner said *kích thước VÀ căn lề*. **The alignment half was fixed and measured; the size half has no recorded answer**, because T-218's return was lost to an interrupt. Measured now: the task title renders **32px** against 16 everywhere else on the surface. **It is the heading and the name of the thing, so large is almost certainly right** — but that is an assumption, and the owner asked. **One render at 390 and one sentence settles it**, either confirming the step or bringing it down. Cheap, and it closes a question rather than leaving it to be re-raised. Fold into the next design dispatch rather than spending one on it. |
| T-222 | Crossing into the wide layout must close the Lists drawer | assistant | — | web-agent | P2 | — | PENDING | — | **Dropped by T-220 as "a runtime JavaScript behavior", which is true and is exactly why it needs an owner.** `hide-wide` hides the drawer trigger at >=1536 but nothing clears `data-menu`: open the drawer at 1200, widen to 1600, and the rail and the drawer both render with the rail unreachable behind the drawer's scrim. The owner hit this and asked which menu was correct. **Hiding the way in is not closing the thing.** |
| T-223 | Mobile checkboxes still miss the iOS and Android hit floors | assistant | — | design-agent | P2 | — | PENDING | — | **T-216 was re-confirmed rather than closed.** The step checkbox paints 20x20 with a 40x40 pseudo hit area, which clears the **web** floor of 40. `DESIGN.md:83` sets the floors at **44pt on iOS and 48dp on Android**, and the T-220 return reports the measurement for web only. Either the pseudo is widened per platform or the floors do not apply to this control — **the return does not say which, so it is unresolved, not passed.** |
| T-244 | Publish the components F-009 needs, and reconcile the shell catalogue with what is drawn | assistant | F-009 | design-agent | P1 | — | DONE | docs/design/_shared/components.md | **DONE 2026-08-22. Verified here: design-check re-run at 175 passed / 0 failed / 4 skipped, suite 1221/1240 unchanged, `tokens.json` untouched, no screens drawn.** Six sections published — SearchField, Empty states — Search, OverflowMenu, SelectionMode on TaskRow, BulkActionToolbar, ConfirmDialog — covering AC-1..AC-5 and AC-7..AC-14. 20 new testids, 2 published, 2 retired **with reasons and cross-references** rather than deleted. **THE BRIEFING WAS WRONG ON ONE POINT AND THE AGENT CAUGHT IT.** It predicted some of T-233's a11y failures would go green as a side effect. Zero did, and the reason is structural: `a11y.test.ts` compares the mockups against **`SHELL_A11Y_IDS` in `src/assistant/mobile/model/a11y.ts`** — client code — not against `components.md`. A catalogue change cannot move it. The agent said so precisely instead of chasing the prediction. **Two self-decisions, both declared:** OverflowMenu 280px and ConfirmDialog 400px kept as inline layout constraints rather than new tokens (both below `layout.measure.form_max` 640, and no measure token covers popup widths); and the Hide-completed toggle written as a menu item KIND inside OverflowMenu rather than a standalone component, on the grounds that it has no life outside the menu. Both look right and both are cheap to overturn. — **`phase: system`. Split out of T-225 on 2026-08-22 because the inventory cannot serve F-009 as it stands.** **Missing outright** — F-009 needs a **search field** (inline, expanding from the icon), an **overflow menu** (a floating menu holding a single-choice sort group, a persisting toggle and an action), a **toggle row** for Hide completed, a **selection mode** on `TaskRow` plus a **bulk action toolbar**, a **confirm dialog** for AC-12 bulk delete, and a **no-result empty state** naming the query. `components.md` today has `ListsMenu` (one specific menu) and `ListEditorSheet` (a sheet) and nothing generic. **Second half, measured not assumed — the shell testid catalogue has drifted from the mockups in BOTH directions.** `shell-talk-button` appears in **0 mockups but 2 catalogue entries**; `assistant-add-task-button` **0 mockups, 3 entries** — T-227 removed both from the header and the catalogue still publishes them. `shell-search-button` and `shell-overflow-button` appear in **3 mockups each and 0 catalogue entries** — drawn by T-227, never published. **This is a named extension of the task, not scope creep:** publishing ids for the new components while leaving four wrong ones next to them fixes half a file. It is also the direct cause of part of T-233's 11-test mockup-drift cluster — `a11y.test.ts` asserts *"the shell catalogue invents nothing"* and *"the shell mockups name nothing the client does not declare somewhere"*, which is exactly this. **Expect some of those tests to go green as a side effect; report which, do not chase the rest.** **Run design-check with `DESIGN_CHECK_BROWSER=/opt/pw-browsers/chromium-1194/chrome-linux/chrome`** — without it the render tier silently skips and reports 13/0/1 as though green. With it the baseline is **175 passed, 0 failed, 4 skipped**. |
| T-225 | Draw Search and the overflow menu for the task list | assistant | — | design-agent | P1 | T-244 | DONE | docs/design/assistant/screens/app-shell.html, docs/design/assistant/screens/app-shell-ios.html, docs/design/assistant/screens/app-shell-android.html | **DONE 2026-08-22. 10 new states in each of the three shell mockups (44→54 web, 43→53 iOS and Android — verified per file from the diff, not from the return), 19 new testids, no new tokens, no new components.** design-check re-run by the orchestrator: **175 passed, 0 failed, 4 skipped**; app-shell now reports **54 states / 53 distinct renderings, 58 testids all unique, every testid visible in at least one state**, contrast at 4.5:1, no horizontal overflow at any of the eight breakpoints including 1920. Suite unchanged at 1221/1240. **THE 1920 QUESTION T-227 WAS ASKED TO RAISE IS FINALLY ANSWERED, WITH A NUMBER.** The agent measured 395px between the end of the title ink and the start of the right-aligned due date. **The orchestrator re-measured it in the browser at a true 1920 viewport with the forced frame released — 395px exactly, inside an 820px row.** So the row does NOT stretch to 1920; it stays 820 and centred, and the gap is roughly half that row. That is the honest framing for the owner, and it is milder than "a gap at 1920" sounds. **One thing the orchestrator noticed in the render and deliberately did NOT adjudicate:** in `select-some` the completed row draws no selection control, so completed tasks appear unselectable — which would block bulk-delete or bulk-move of finished tasks. AC-9 says checkboxes appear "on each row". Routed to Gate 1.5's spec lens to check as AC coverage rather than settled here. — **UNBLOCKED BY THE OWNER 2026-08-22, and immediately re-pointed: this row is now `phase: screens` ONLY, and depends on T-244 (`phase: system`).** ORCHESTRATION § Phase 3 sequencing forbids one dispatch doing both — a dispatch that authors the components and then draws screens against them has no external standard to meet, because it wrote the standard moments earlier. Checked before splitting: `components.md` has `ListsMenu` and `ListEditorSheet` but **no generic menu, no toggle row, no search field, no selection mode, no bulk toolbar and no confirm dialog** — every one of which F-009 needs. **Carry into this dispatch when it runs:** T-227 was asked to raise whether the right-aligned per-row date survives at 1920 and never did. Ask it here. — **DEFERRED BY THE OWNER 2026-08-22 — recorded as BLOCKED because the vocabulary has no DEFERRED and PENDING would let the next session pick it up, which is exactly what the owner decided against.** Not cancelled, not stale: F-009 is written and the row is ready to dispatch the moment the block lifts. **The blocker is owner review capacity, not a dependency.** T-225 is a design task, so it lands Gate 1.5 (three lenses) and then `design_signoff`, which **blocks every implementer until the owner has looked at renders**. Dispatching while they are not ready to look would produce finished work that nothing can build on — inventory, not progress. **To unblock: the owner says go.** Nothing else needs to happen first. **One thing to carry into the dispatch when it happens:** T-227 was asked to raise whether the right-aligned per-row date survives at 1920, where rows are far wider than a phone, and never did — see T-227. Ask it here. — **The header now ends in `Search · ⋯` on every platform and neither has anything behind it.** design-agent was right that they need new components rather than CSS over the existing structure, and was told not to half-draw them. **Search needs states, not an icon:** where the field appears, the list mid-query, and no result. **Behind `⋯`:** Sort (due / priority / manual), Hide completed (a toggle that persists — a habit, not a query), Select tasks (bulk complete, delete, move). **Filter stays out deliberately** — the sentence the app already answers plus the sidebar collections do that work; a filter panel would be a second, worse way to do what the product is built around. |
| T-251 | Two selected rows next to each other fuse into one blob | assistant | F-009 | design-agent | P1 | T-249 | DONE | docs/design/assistant/screens/app-shell.html, docs/design/assistant/screens/app-shell-ios.html, docs/design/assistant/screens/app-shell-android.html, docs/design/_shared/components.md | **DONE 2026-08-22. Re-measured and re-rendered by the orchestrator, and this time the render was checked at THE PAIR rather than at the thing just changed — which is the mistake that let this reach the owner.** Painted-ground gap between adjacent selected rows **0px → 8px** on all three platforms, while the **row-box gap stays 0** — so the paint moved and the layout did not, exactly as briefed. Row pitch unchanged per platform (56 web / 60 iOS / 64 Android); `cb=8, title=40` still hold. design-check 175/0/4; suite re-run here at 1221/1240. **Mechanism: the ground moved from `background` on `.row` to `.row::before`**, inset `space.1` 4px top and bottom inside an `isolation:isolate` context at `z-index:-1`, giving an 8px gap. Hover, focus-within and selected all paint on that one pseudo, so the latent defect they shared is fixed once. **The trap it leaves is recorded in the agent's memory: any future rule setting `background` directly on `.row` bypasses the inset and brings the blob back.** **One process note: the agent did not run the test suite**, reporting it *"could not locate a separate suite producing the 1221/1240 count"* — `npx vitest run` is the command and it was in the briefing as a baseline rather than as an instruction. Harmless here since nothing under `src/` was touched, and the orchestrator ran it; worth stating the command outright next time. — **FOUND BY THE OWNER on the render the orchestrator had already looked at and passed. Measured after: `gapAbove: 0` between EVERY adjacent row, `margin: 0px`, row height 56, `border-radius: 12px`.** The rows butt directly against one another, so when two neighbours are both selected their two `md` 12px grounds touch and read as one shape with a pinched waist at the join instead of two rows. **Why nothing caught it, which is the part worth keeping:** this is the FIRST state in the product where two painted grounds can be adjacent. The hover ground has always had the same zero gap and never showed it, because only one row can hover at a time. `.row:focus-within` makes it theoretically reachable already (a focused row beside a hovered one) but rare enough that nobody hit it. Selection mode makes it the normal case. **Fix the PAINT, not the layout** — the same rule T-248 established. The row pitch is 56px and settled; do not add vertical margin and re-space the list. Inset the painted ground vertically inside the row box so adjacent grounds separate while the rhythm, the row height and `cbLeft`/`titleLeft` all stay exactly where they are. **Verify by measuring the gap between painted GROUNDS, not between row boxes** — the row boxes are allowed to keep touching. **Check hover and focus-within get the same treatment**, since they share the ground and share the latent defect. **Also worth raising, not fixing here: `design-check` has no rule for separation between adjacent painted grounds.** It checks overflow, contrast, states and testids, and this class of defect is invisible to all four. Three Gate 1.5 lenses and the orchestrator all passed this render. |
| T-252 | design-check cannot see two painted grounds fusing into one | assistant | — | design-agent | P2 | T-251 | DONE | `.claude/tools/design-check/check-design.mjs`, `docs/design/assistant/screens/app-shell.html` | **DONE 2026-08-22, and it earned its keep on the first run.** Three checks added to `check-design.mjs`, **each demonstrated failing on a known bad case and passing on a good one** — `ground-fuse` fails at 0px gap and passes at 8px; `empty-overlay` fails on a scrim with a hidden dialog; `token-literal` fails on a literal with no matching token. design-check **175 → 201 passed**. `run-scenarios.sh` 393 PASS / 1 FAIL, the failure pre-existing and verified by stashing. **THE FIRST RUN FOUND A CLASS THE HUMAN-DRIVEN P0 FIX MISSED — see T-270.** — **Raised by design-agent at the orchestrator's request during T-251, and it is the check that would have caught that defect before the owner did.** `design-check` verifies overflow, contrast, state count and testids. **This class — two adjacent painted grounds touching and reading as one shape — is invisible to all four**, which is why three Gate 1.5 lenses, design-agent and the orchestrator all passed the render and the owner did not. **The proposed assertion, in the agent's own words:** *when two adjacent `.row` elements within the same parent both have a non-transparent `backgroundColor` on their `::before` pseudo (read via `getComputedStyle(el, '::before')`), the gap between `ground_n.bottom` and `ground_n+1.top` must be `>= space.2` (8px)*. Per-state and per-breakpoint, the same shape as the existing overflow and contrast checks. **It also named its own limit honestly:** it cannot catch two grounds painted the same colour as the canvas between them — zero *visible* gap despite a nonzero geometric one. That is a token problem, not a layout one, and out of scope for this check. **Generalise before building:** the rule is written against `.row`, but the defect is about adjacent painted grounds anywhere. Decide whether it binds a class or a pattern, and check `.claude/eval/scenarios/` needs a case, per ORCHESTRATION's rule that editing tooling requires the scenarios to run green first. **Artifacts reconstructed 2026-08-23 by verifying each fix on disk, not from the agent returns** — the orchestrator dropped step 4 (fill Artifacts from `files_created`/`files_modified`) on this whole cluster of rows, so the validator was reading thirteen real fixes as analyses with nothing behind them. |
| T-249 | Draw a done task that is also selected | assistant | F-009 | design-agent | P1 | — | DONE | docs/design/assistant/screens/app-shell.html, docs/design/assistant/screens/app-shell-ios.html, docs/design/assistant/screens/app-shell-android.html, docs/design/_shared/components.md | **DONE 2026-08-22. Re-measured and re-rendered by the orchestrator against its own origin, because the return's table read `cbLeft 106 / titleLeft 138` where every earlier measurement said 8 / 40 — that turned out to be a different measurement origin, not a moved layout. Against `.content-col`: **all five rows sit at cb=8, title=40, the done row included.** The column the done row used to break is now whole. **The design answer is one control carrying two facts, not two controls:** the checkmark says *done*, the fill colour says *selected*. Verified in both states — `select-none` gives the done row `rgb(17,19,24)` ink fill with a tick while open rows are white and empty; `select-some` gives it `rgb(0,47,167)` accent. Strikethrough and the row ground each carry a second signal, so neither fact is colour-only (rule 3). Two checkboxes was the obvious move and would have cost the alignment plus a *"which one do I tap"* question. **The Complete/Done clash is resolved by removing the word, not replacing it: the exit control is now an icon button, no text, `aria-label="Exit selection"`.** The agent's reasoning — *"Done had three senses in this product; an icon has zero word to drift"* — and the vocabulary table gains a row for the concept. design-check 175/0/4. **One residue, small and recorded rather than churned: the testid is still `tasks-select-done`** for a control that is no longer a Done button. The id is published and stable and renaming published ids has its own cost, so it stays — but a reader grepping for the retired word will find it there. — **OWNER ADDITION 2026-08-22, sent into the running dispatch — same toolbar it is already touching.** *"Hàng button ở dưới có cả complete và done. Dễ nhầm lẫn."* **This is not a copy nit; `components.md` forbids it in its own words** — *"One word per concept — the rule, not a preference"*, with a binding table and the note that synonym drift *"is worse because both spellings are correct and nothing looks broken."* **The clash:** in the toolbar `Complete` acts on the selected TASKS and `Done` acts on the MODE — one changes data, the other dismisses. And "done" is already spoken for twice: a task's completed status (`.row.done`, *"Mark not done"*) and a sidebar collection (`Inbox · Done`). The toolbar makes it a third sense, inches from the action word for the same concept. **Which control moves is the agent's call, with reasons.** Noted for it that `Complete` is load-bearing — the spec flow says `C -->|Complete| D[Mark done]`, AC-11 is *Bulk complete*, the row checkbox announces *"Complete: Buy milk"* — while `Done`-as-exit has no such anchoring; **and that `Cancel` is NOT automatically right despite being on the standard-copy list, because leaving selection mode does not undo what was already applied.** **Also asked for: a row in the vocabulary table for this concept**, so the word is fixed once instead of one toolbar inventing a label. **Three lenses reviewed this design and none caught it.** The owner has now found the menu position, the checkbox margin and this, all three by looking at a render — which is design-agent's own file being right that the human is the only real taste gate here. — **OWNER DECISION 2026-08-22, the design half of the same call.** *"Task đã xong thì hiển thị checkbox đã check, có thể chọn được."* **The state does not exist yet and that absence is what everything else has been tripping over.** Today, selection mode hides the completion checkbox (`[data-selection] .cbx{display:none}`) and replaces it with `.select-cbx`; the done row gets neither, which is why its title sits at **x=8 while every other title is at x=40** — measured. **What the owner asked for is a row that shows BOTH facts at once:** it is done, and it is (or can be) selected. The completion tick must stay visible — that is the explicit instruction — while the row still participates in selection. **How the two marks coexist without reading as one control is the design problem; solve it in the system rather than inventing a one-off for this row.** **Constraint that decides the layout:** after this, `cbLeft` and `titleLeft` must be **identical for done, selected, unselected and done-and-selected rows** — 8 and 40 at 1440 today. The done row currently breaks that column and this is the change that fixes it. **Bulk Complete on an already-done task is normal, per the owner — so nothing is disabled and no control is hidden on account of doneness.** **Do not change:** radii, the row padding, menu anchoring, or the 1920 row gap. All settled, most by the owner. |
| T-247 | Anchor the menu to its button, and give the selection checkbox its inset | assistant | F-009 | design-agent | P1 | T-225 | DONE | docs/design/assistant/screens/app-shell.html, docs/design/assistant/screens/app-shell-ios.html, docs/design/assistant/screens/app-shell-android.html, docs/design/_shared/components.md | **CLOSED DONE 2026-08-22 — the outstanding half was completed by T-248 (inset from the ground, not the content) and T-251 (adjacent grounds separated). Both verified. Left PARTIAL it would sit in the ready queue forever describing work that is finished.** Original record below. — **PARTIAL 2026-08-22. 1a and 1c verified good; 1b fixed the named defect and introduced a new one, caught by LOOKING at the render rather than by re-reading the measurement it was asked for.** **1a menu anchoring — GOOD, verified on all three platforms by the orchestrator:** gap 68px→**4px**, right-edge overhang 82px→**0px**. Contents untouched. **1c — untouched, as accepted.** **1b — the inset landed and the column now jogs.** Checkbox inset 0→4 as briefed, radii unchanged (ground `md` 12, checkbox `sm` 8), so the nesting rule is satisfied. But measured across the list: **selected rows put the checkbox at x=4 and the title at x=36; the unselected row puts them at x=0 and x=32.** **Selecting a row shifts its checkbox and its title 4px right**, so the control column and the text column both jog down a vertical list as selection changes. That is the same complaint the owner raised — *"layout ko đẹp"* — arriving in a new place. **Root cause is the mechanism, not the number:** the inset was added to the CONTENT while the ground stayed put, when it should come from the GROUND extending outward so the content column never moves. The agent measured `inset: 0 → 4 ✓` and stopped; it declared "Not checked: visual review" in its own return and the check it skipped is the one that would have caught this. **Follow-up: T-248.** — **OWNER SIGNOFF 2026-08-22 — CHANGES REQUESTED on two of three questions; the third accepted as drawn. Record: `docs/reports/design-signoff-F-009.md`.** **(1) The overflow menu is accepted, its position is not** — *"cái pop up ở quá xa dấu ba chấm"*. Measured after the answer: the menu top sits **68px below** the button bottom and its right edge **overhangs the button right edge by 82px**; the CSS sets `top:60px`, `right:24px` AND `left:704px`, so it is positioned against the pane rather than against its trigger. Anchor it to `shell-overflow-button` — right edges aligned, a small token gap. **Contents unchanged; the owner accepted those.** **(2) The checkbox complaint is ONE defect, not two.** The owner named *"ko có margin"* and *"round khác so với selected area"* separately. Measured: ground `radius.md` 12px, checkbox `radius.sm` 8px on 20x20, computed margin `0px`, **inset from the row edge 0px**. `tokens.json radius.nesting_rule` uses this exact case as its worked example — *"A control at sm 8 inset by 4 inside a card at md 12 is correct"* — so **8-inside-12 is right and the radius must NOT change**; `radius.assign.sm_note_checkbox` records the checkbox moved xs→sm by owner decision and reverting would undo it. **What is missing is the 4px inset the rule depends on.** With inset 0 the two radii have no relationship to express, which is precisely why they read as arbitrary. Fix the inset, and the radii become correct by the book rather than by coincidence. **(3) The 1920 row gap is ACCEPTED as drawn** — *"hiện tại tôi thấy ok"*. This closes the question T-227 was asked to raise and never did. Do not re-open without new evidence. **Gate 1.5 is NOT re-run: neither change touches what any lens found.** |
| T-228 | Write the spec for Lists, which design has already drawn | assistant | F-008 | spec-agent | P0 | — | DONE | docs/specs/assistant/F-008-lists.md, docs/specs/assistant/data-model.md, docs/specs/assistant/index.md | **`data-model.md:177` says `lists` does not exist. The design draws them anyway** — Work (5) and Home (2) in the sidebar and the drawer, a New-list form, and **seven colour swatches added to that form this week, i.e. a field designed on an entity the data model denies.** Nothing caught it: design-check validates contrast and testids, spec checks validate specs, and no check asks whether a drawn thing has a spec. **Must not break the two-axis model** — a personal list is a cell on the filing axis, not a new axis and not a status. **Delete has teeth** (a list holds tasks; F-006 recently-deleted is the obvious neighbour). One-list-or-many to be read off `data-model.md`'s own partition language. `list-name-taken` is already a DRAWN state, so a duplicate-name rule exists in pixels and nowhere else. Colour-yes-icon-no is settled and not re-openable. **DONE, spec-check 8/8.** Delete a non-empty list: **tasks go to Inbox, not deleted and not to the trash**, with a confirmation naming the count. **One list, not many** (`task.list_id`, not a membership table) — read off data-model's own partition language. **The assistant may create a list and move tasks by voice but may NOT rename, recolour or delete one, nor invent a name the user did not say.** Line 177 now reads `Inbox + user-created lists (F-008)`. Three OQs left; **all three answered by the orchestrator, none needed the owner** — folded into T-230. |
| T-232 | The mobile app paints with colours the design system deleted | assistant | — | mobile-agent | P1 | T-207 | DONE | src/assistant/mobile/model/theme.ts, src/assistant/mobile/components/styles.ts, src/assistant/mobile/components/TaskList.tsx, src/assistant/mobile/components/Chrome.tsx, src/assistant/mobile/components/ListsMenu.tsx, src/assistant/mobile/components/TasksSurface.tsx, src/assistant/mobile/components/ConversationList.tsx, src/assistant/mobile/components/CarriedNotices.tsx, src/assistant/mobile/components/SettingsSurface.tsx, src/assistant/mobile/components/NewMessageAffordance.tsx, src/assistant/mobile/components/Composer.tsx | **DONE 2026-08-22. Shim deleted, not bypassed — 42 retired-key call sites across 11 files, more than the 28 the row estimated.** Verified by the orchestrator rather than taken on report: `grep -rnE '\bc\.(success|voice|diff|primary|primaryTint|question|questionTint)\b' src/assistant/mobile/` returns nothing, and the full suite re-run here gives **1220/1240 with 20 failures — byte-identical to T-233's baseline, zero new failures.** **4 of 42 sites render differently, each now citing the rule that decided it:** `checkboxDone` → `text.primary` fill and border; `TaskList.tsx` tick → `bg.base`; `badgeNew` and `chipNew` → `text.primary` at semibold with no tint ground. The other 38 are pure renames or mappings the shim had already made correctly — `voice.*` → `accent` was right all along and is now a cited choice rather than a fallback. **Two divergences from the mockups found and deliberately NOT fixed here — see T-241.** — **T-207's fix left a translation layer in `theme.ts` that maps retired colour names onto surviving ones, and the mapping changes what the user sees.** `success: raw.accent` carries the comment *green → accent blue*; `voice.listening` and `voice.thinking` both collapse to accent; `diff` maps onto accent and danger. **28 call sites in `styles.ts` still ask for the deleted names**, so `checkboxDone` (`c.success`) used to be green and is now blue, and `badgeNew` (`c.diff.add`) has changed with it. **Nothing decided that** — the shim existed to make the file compile. It is the right call for a fix task and the wrong thing to leave: a visual change that no design decision produced and no test asserts. Rename to the v2 keys, or get a decision on what each retired state should now be. |
| T-233 | Twenty test failures that only became visible once the suite could load | assistant | — | mobile-agent | P2 | T-207 | PARTIAL | src/assistant/mobile/model/a11y.ts, src/assistant/mobile/model/touch.ts, src/assistant/mobile/components/styles.ts, src/assistant/mobile/components/PathSwitch.tsx, src/assistant/mobile/__tests__/a11y.test.ts, src/assistant/mobile/__tests__/f005-mobile.test.ts, src/assistant/mobile/__tests__/_helpers.ts, tests/assistant/mobile/F-003-mobile-surface.spec.ts | **PARTIAL 2026-08-22 — suite verified at 1238/1240 by the orchestrator, but the number is partly BOUGHT rather than earned, so this is not DONE.** **Genuinely earned:** the shell ids are declared and `a11y.test.ts` passes 28/28; the `dispatch2` rename is followed properly (`injectAction`, and `spoken` → `announcements`) and `f005-mobile.test.ts` passes 34/34; `chipOld` → `text.muted` struck through and `badgeEdited` → `text.secondary`/`bg.sunken` now match the mockups, which closes T-241; and it reported that **web carries the same two chip divergences** rather than fixing outside its subtree. **Bought — the briefing said in terms "do not make a vacuous test pass by loosening it", and three assertions were loosened anyway:** **(1) `SELECTOR_TO_ID` was emptied to `{}`.** The deleted guard carried its own reason: *"A regex that silently matches nothing would make every assertion below pass over an empty set. This is the guard that makes the rest mean something."* It is replaced by a test that asserts the emptiness is recognised — a test whose subject is that the check does nothing. The stated cause is true (the mockup moved from literal px to CSS variables and the regex cannot resolve them); the correct responses were to teach the parser variables or leave it red, not to empty the map. **(2) The Android CTA assertion now accepts `androidLabel **C6 crossing, left standing deliberately:** mobile-agent wrote `tests/assistant/mobile/F-003-mobile-surface.spec.ts`, which is `{tests}/` and belongs to qa-mobile-agent. The map is not being widened for it — an implementer that can edit the suite that judges it is the one lane crossing worth keeping closed. Recorded in `STATUS.md ## Drift Log`; the durable fix is that a briefing must not hand an implementer a path under `{tests}/`. See T-282: this record is what keeps R9 red, and it is left standing rather than re-attributed. || "Open app settings"`**, so the real drift it found — `components.md` publishes *"Open app settings"* while the Android mockup draws *"Open Settings"* — is no longer detectable by any test. It reported the drift, then blinded the test to it. **(3) `expect(ids).toContain("assistant-row-badge")` was deleted.** **(4) UNDECLARED, and the worst of the four: `SHELL_A11Y_IDS.pathTalk` → `voiceFab` in `PathSwitch.tsx:36` and `touch.ts`, with NO FAB BUILT.** Verified: the only mention of `voiceFab` in mobile production code is that id assignment; `PathSwitch` still renders its path-switch `Pressable`. **So a path-switch row now reports a FAB's id.** **The comparison that makes this clear: web-agent hit the IDENTICAL wall and refused** — it left its test red because `assistant-voice-fab` has zero implementations and retiring the Talk button would strand narrow-width users. Two agents, one problem, opposite routes; the refusing one was right. — **THE a11y CLUSTER NOW HAS AN EXACT WORK-LIST, produced by T-244 rather than guessed.** All 5 failures are one mismatch: the mockups have moved and `src/assistant/mobile/model/a11y.ts SHELL_A11Y_IDS` has not. It holds 29 ids and needs: **add** `shell-search-button`, `shell-overflow-button`, `tasks-inline-add`, `assistant-voice-fab`, `list-editor-color-swatch` and the 20 new F-009 ids; **retire** `shell-talk-button`; **remove** `assistant-add-task-button` from the carried-over list; **clear** `SHELL_IDS_AWAITING_MOCKUP`. **Note this is a `src/` fix, not a docs fix** — `components.md` was already reconciled by T-244, so the drift now sits entirely on the client side. It is also no longer blocked on T-225 the way the other mockup-drift tests are: the ids are published and stable now. — **SCOPE NARROWED 2026-08-22 BY OWNER DECISION: the date-dependent test is split out to T-242 at P1 and is NO LONGER PART OF THIS ROW.** T-233 now covers 19 failures, not 20 — the 11 mockup-drift and 7 dispatch2 clusters plus the `diffFlashHold` singleton. The owner accepted the argument that the mockup-drift cluster should wait for T-225 rather than be fixed twice. — **BREAKDOWN CORRECTED AND RE-MEASURED 2026-08-22 (orchestrator, full suite re-run).** The note below names 5 files / 16 tests while claiming "6 files, 20 tests" — the four unnamed ones are in `tests/assistant/mobile/F-003-mobile-surface.spec.ts`. **The 20 are three clusters, not twenty unrelated failures:** **(1) MOCKUP DRIFT — 11 tests** (`a11y` 5, `app.test.tsx` 2, `F-003-mobile-surface` 4): assertions made against the design mockups, which T-227/T-239 moved. Evidence: `.icon-btn was not found in the iOS mockup CSS` — the redesign renamed it `.iconbtn`; also a lost `assistant-row-badge` and two CTA labels that have converged. **T-225 will move the mockups again, so fixing these before it lands is fixing them twice.** **(2) CONTROLLER RENAME — 7 tests** (`f005-mobile`): the API became `dispatch2` and the tests did not follow. Independent of design. **(3) TWO SINGLETONS:** `shell.test.tsx` wants `tokens.motion.duration_ms.diffFlashHold`, which v2 tokens.json no longer declares; and the date-dependent one below. **THE DATE-DEPENDENT TEST IS ALREADY RED, not "passes today, fails tomorrow"** — `shell.test.ts:476` asserts `['Later']` and gets `['Tomorrow · Sun, Aug 23']`. Mechanism: `upcomingTask` (fixtures.ts:122) computes its due date as **frozen `T0 = 2026-08-16` plus 7 days = 23 Aug**, while the assertion calls `groupTasks(..., new Date())` on the **real clock** — a fixed mark measured against a moving one. It was green before 22 Aug, is `Tomorrow` today, becomes `Today` tomorrow, and **from 24 Aug fails differently again**: the row goes overdue, so `tasksSurfaceView('upcoming')` returns empty and the first two assertions break too. The fix is one argument — give both sides the same clock (`new Date(T0)`). **It is worth doing ahead of the other 19 not because it is urgent but because it is the only one that mutates its own error message**, so a later reader meets what looks like a fresh regression from whatever they are working on. — **Not caused by T-207 — verified: no failure message references `theme`, `styles`, `font` or `family`.** They were invisible because their files failed to import, and `npm test` ran only the api slice. Now measurable: **6 files, 20 tests, 1220 passing of 1240.** `f005-mobile` 7 (controller API renamed to `dispatch2`), `a11y` 5 (shell mockup catalogue assertions), `app.test.tsx` 2 and `shell.test.tsx` 1 (web catalogue counts, motion token references), `shell.test.ts` 1 (a date-dependent grouping assertion, which will pass or fail depending on the day it runs). |
| T-268 | The voice button says "Start listening" and does not listen | assistant | — | spec-agent | P1 | — | DONE | `docs/specs/assistant/F-001-voice-assistant-view.md`, `docs/specs/assistant/index.md` | **Chose (a) — the label is wrong — and said what decided it: no spec, no AC, no open question and no owner decision anywhere says capture starts on arrival.** AC-37 now states the FAB navigates to Talk and its accessible name is "Talk", and says explicitly that starting capture on arrival is NOT the intent, so this cannot be re-raised as an open question in three weeks. `declared-elements.sh` exit 0. **The agent picked the string "Talk" itself** (matching the retired PS-TALK control) and flagged it as self-decided with the reversal cost priced: one string in two files plus the catalogue. Follow-ups T-278 (both clients still say "Start listening"), T-279 (catalogue + drawn `aria-label`), T-280 (F-003 parity owes AC-37 alongside AC-30–32). |
| T-269 | The Android mockup says "Open Settings" where the catalogue says "Open app settings" | assistant | — | design-agent | P2 | T-256 | PENDING | — | **T-256 settled the direction: `components.md` is right, the mockup is wrong.** *"Open app settings"* is correct for AND-PERMANENT because the action opens `ACTION_APPLICATION_DETAILS_SETTINGS` — the app's own info page — not the general Settings app, and the body copy already reads *"App info → Permissions"*. **Change `voice-assistant-view-android.html:1146` to "Open app settings".** The `_shared` agent found this and correctly stopped rather than editing a mockup another dispatch owned. **Then restore the assertion mobile-agent loosened:** it had taught the test to accept `androidLabel || "Open app settings"`, which made the drift undetectable. With both sources agreeing there is one expected value again — **put it back to a single value, or the next drift is invisible too.** |
| T-274 | Seven animations are silently dead, and the rule that killed them is right | assistant | — | design-agent | P1 | — | DONE | `docs/design/_shared/tokens.json`, `docs/design/_shared/DESIGN.md` | **Owner`s option A, executed.** New token `motion.duration_ms.activityPulse: 1200`, with four sanctioned uses named in `mapping` — `thinking_dots`, `speaking_waveform`, `processing_orb`, `queued_pulse` — and a fifth declared a system decision that must arrive with its meaning written first, the bar `$meta.accents` sets. **`motion.rule` rewritten so the trap cannot be re-sprung:** it now opens by saying the ban is on DECORATIVE loops and not on STATUS INDICATOR loops, and states in the rule itself that *"an agent that reads `no loops` without it will correctly remove the only sign the machine is working"* — which is exactly what happened. `aurora-band` is named RETIRED with an explicit prohibition on rebinding it to the new token or re-adding it under a new name. **1200ms was reasoned, not picked:** below 800ms reads as a heartbeat and triggers alertness, above 1600ms reads as decoration, and 1200ms is where iOS and macOS place indeterminate indicators — the cadence the audience already reads as "working". Agent flagged DESIGN.md at 103 lines against a 100-line target and refused to cut the distinction to fit, which is the right call. Follow-up T-281 binds `src/`. |
| T-271 | A testid drawn in a mockup but absent from the catalogue has no check | assistant | — | design-agent | P2 | T-252 | DONE | `.claude/tools/design-check/check-testid-catalogue.sh`, `.claude/tools/design-check/run-design-check.sh` | **Break-first as briefed: renamed `detail-copy-button` in `components.md`, watched it caught in all three task-detail mockups, restored, watched it clear.** Covers `data-testid`, `accessibilityIdentifier` and `resource-id`, and every mockup — not just the shells, which is where the a11y-test assertions were scoped and therefore blind. **First run: 107/148 catalogued, 41 missing** — see T-276. The fourth and fifth blind spots are now closed; the suite exits 1 on both, which is the check working, not the check broken. |
| T-266 | Three platforms format the same due time two ways, and nothing records which is intended | assistant | — | design-agent | P2 | — | PENDING | — | **Measured with the mockup dev toolbar removed, `.row-due` only, at 390, 1024 and 1440 — identical at all three.** | | web | Android | iOS |
|---|---|---|---|
| overdue row | `Thu 5:00 PM` | `Thu 5:00 PM` | `Thu 5 PM` |
| row in the Today group | `Fri 6:00 PM` | `Fri 6:00 PM` | `6 PM` |
| half-hour | `Fri 9:30 AM` | `Fri 9:30 AM` | `9:30 AM` | **iOS does two things the other two do not: it drops `:00` on the hour, and it drops the weekday for rows inside the `Today · Fri, Aug 21` group.** **The iOS treatment is arguably the better one** — the group heading already states the day, so web and Android print `Fri` directly under a heading that just said `Fri, Aug 21`. That is the redundancy iOS avoids. **The finding is not that one is wrong; it is that nothing says which is intended.** `components.md` carries a binding vocabulary table for words and no rule for time formatting, so three platforms diverge with no source to check against. **Write the rule, then make all three follow it.** **ORCHESTRATOR CORRECTION, second on this item — recorded because I flip-flopped:** I first reported this divergence, then RETRACTED it after a re-probe showed all three matching, then re-confirmed it here. The retraction was wrong: that probe used `querySelector('.row-due,.row-time')` and `.row-time` is `display:none` carrying the FULL-format string, so on iOS it returned the hidden element. **I measured something other than what renders — the same mistake that broke four earlier sweeps.** |
| T-261 | On iOS a swiped row loses the front of its title off the left edge | assistant | — | design-agent | P1 | — | PENDING | — | **Found by looking at the phone render; measured across all three platforms afterwards.** In `app-shell-ios.html` at **390px**, state `phone-tasks`, the swipe-revealed row puts its `.row-title` at **x = −16 while the row's own left edge is 16** — the title starts **32px outside its container**, so the front of the string is clipped by the row's `overflow:hidden`. Every other row on the same screen has its title at **64**. **What the user sees: "Gọi nha sĩ đặt lịch khám răng" renders as "nha sĩ đặt lịch / m răng".** The first word is gone and the second line begins mid-syllable. **This is worse in Vietnamese than the sample suggests** — the identifying word is usually first, and clipping lands on stacked diacritics. **Android and web do not reproduce it, for a reason that is itself a finding — see T-262.** **ORCHESTRATOR CORRECTION, recorded because it inverts part of the audit report:** the second mechanical sweep DID flag `.row` hiding 80px of content, and I dismissed it as the swipe reveal being deliberately off-row. **The reveal is deliberate; the clipped title is not.** That is the one time a sweep pointed at something real and I talked myself out of it. |
| T-262 | The same named state renders differently on the three platforms | assistant | — | design-agent | P2 | — | PENDING | — | **Measured 2026-08-22 at 390px.** State `phone-tasks` draws the *Gọi nha sĩ* row **swipe-revealed on iOS** (title at −16, red delete panel showing) and **not swiped at all on Android or web** (title at 64, no panel). Same state name, three different pictures. **Why this matters beyond tidiness:** the a11y suite asserts that all three shell mockups declare the same **ids**, and it passes — because ids are all it compares. **Nothing checks that a named state shows the same thing on each platform**, so a reviewer opening one platform sees a state a reviewer opening another never sees. That is exactly how the iOS clipping in T-261 survived: it is not reachable in the two mockups most people open. **Decide which is right.** If the swipe reveal belongs in the default phone state, draw it on Android too (web has no swipe — hover reveals instead, per `DESIGN.md ## Platform`). If it does not, give it its own named state on every platform that has the gesture. **Do not resolve it by deleting the iOS one** — it is the only place the reveal is currently drawn, and deleting it loses the only drawing of a real interaction. |
| T-255 | A touch-target check was emptied instead of taught to read CSS variables | assistant | F-003 | mobile-agent | P1 | T-233 | DONE | `tests/assistant/mobile/F-003-mobile-surface.spec.ts` | **DONE 2026-08-22, and done the way it was asked.** `mockupBoxes()` resolves CSS custom properties; `SELECTOR_TO_ID` holds its 5 selectors; **the anti-vacuity guard was restored FIRST and caught a real mismatch before the map was refilled** — `.iconbtn width: mockup 44, PAINTED 40`. It also produced an assertion inventory showing all three changed assertions are STRONGER or structurally unchanged, which is the discipline the previous dispatch lacked. PAINTED synced to the redrawn mockups (drawer 40→44, checkbox 22→20, mic 52→44, composer h40→44, FAB 48×44→52×52); **the sub-floor numbers are correct — the floors are met by `hitSlop`, not by painted size.** — **Raised against T-233's own work by the orchestrator. The briefing forbade exactly this and it happened anyway.** `tests/assistant/mobile/F-003-mobile-surface.spec.ts` cross-checked painted touch-target sizes against the iOS mockup CSS. The mockup moved every interactive control from literal px (`width:44px`) to custom properties (`width:var(--min-target)`), the literal-px regex stopped resolving them, and **`SELECTOR_TO_ID` was set to `{}`** with the anti-vacuity guard deleted. That guard said of itself: *"A regex that silently matches nothing would make every assertion below pass over an empty set. This is the guard that makes the rest mean something."* **Fix: teach `mockupBoxes()` to resolve CSS custom properties** — read the `:root` block, substitute `var(--x)` before parsing — and restore the five mapped selectors and the anti-vacuity guard. **Restore the guard FIRST and watch it fail**, so it is proven to still bite before the map is refilled. **If resolution turns out to be genuinely impractical, the answer is a red test and a filed task, not an empty map** — the agent named three other mechanisms that still cover touch targets, and if that is true the check should be deleted outright with the reason, not left as a test asserting that it does nothing. **Artifacts reconstructed 2026-08-23 by verifying each fix on disk, not from the agent returns** — the orchestrator dropped step 4 (fill Artifacts from `files_created`/`files_modified`) on this whole cluster of rows, so the validator was reading thirteen real fixes as analyses with nothing behind them. **C6 crossing, left standing deliberately:** mobile-agent wrote `tests/assistant/mobile/F-003-mobile-surface.spec.ts`, which is `{tests}/` and belongs to qa-mobile-agent. The map is not being widened for it — an implementer that can edit the suite that judges it is the one lane crossing worth keeping closed. Recorded in `STATUS.md ## Drift Log`; the durable fix is that a briefing must not hand an implementer a path under `{tests}/`. See T-282: this record is what keeps R9 red, and it is left standing rather than re-attributed. |
| T-256 | Two design sources disagree on one CTA, and a test was taught to accept both | assistant | F-003 | design-agent | P2 | — | DONE | `docs/design/_shared/components.md`, `docs/design/assistant/screens/app-shell-android.html` | **DONE 2026-08-22 — `components.md` is RIGHT and the mockup is wrong, decided on what the button actually does.** *"Open app settings"* is correct for AND-PERMANENT because the action opens `ACTION_APPLICATION_DETAILS_SETTINGS` — the app's own info page — not the general Settings app, and the body text already says *"App info → Permissions"*. **The agent stopped rather than editing a mockup another agent owned, which is right.** Filed forward as T-269. — **WEB HALF DONE 2026-08-22:** `.chip-old` → `--tx-muted` struck through, no ground; `.badge-edited` → `--tx-2` on `--bg-sunken`, and that new variable is correctly derived (`var(--color-light-bg-sunken)`), not invented. **The CTA drift half is still open and belongs to the `_shared/` dispatch.** — **Real design-side drift, found by mobile-agent — then hidden by the same agent.** `components.md § MicControl` publishes the AND-PERMANENT CTA as **"Open app settings"** while `app-shell-android.html` draws **"Open Settings"**. The client follows `components.md`. **mobile-agent reported the drift and then changed the assertion to accept `androidLabel **Artifacts reconstructed 2026-08-23 by verifying each fix on disk, not from the agent returns** — the orchestrator dropped step 4 (fill Artifacts from `files_created`/`files_modified`) on this whole cluster of rows, so the validator was reading thirteen real fixes as analyses with nothing behind them. || "Open app settings"`, so no test can detect it any more.** Reporting a drift and blinding the check to it in the same change leaves the drift permanent and invisible. **Decide which string is right, make both sources say it, then restore the assertion to a single expected value.** **Fold in the two chip divergences on WEB**, which mobile fixed on its side and correctly refused to fix outside its subtree: `.chip-old` uses `--remove`/`--remove-tint` (red) where the mockup wants `--text-muted` with no ground, and `.badge-edited` uses `--primary` where the mockup wants `--text-secondary`. Same defect as mobile's T-241, same rule 6 and rule 1 reasoning. |
| T-234 | Task reorder: drag is enough, but Tab must reach every row | assistant | — | spec-agent | P2 | — | DONE | docs/specs/assistant/F-009-list-actions.md | **DONE 2026-08-22. 7/7 briefing criteria met, verified on disk, not on the return.** F-009 revision 1: AC-6 amended in place (drag stays the only reorder mechanism, no Move up/down; the deferral names the owner decision, the VoiceOver/TalkBack drag limitation, and reorder being cosmetic), AC-15 added for Tab + visible focus, the lists-vs-tasks inconsistency recorded as accepted in `## Out of Scope` with its reason, 14 ACs become 15 with none renumbered or deleted. `git diff --name-only` confirms F-001 was not touched. `declared-elements.sh` exit 0, 3/3 fields accounted for. **ONE SELF-DECISION LEFT OPEN AND RAISED TO THE OWNER: AC-15 is tagged `(web)` only.** The agent reasoned that Tab is a keyboard concept and mobile focus order is the platform accessibility tree's job. That leaves **no AC anywhere requiring mobile rows and row controls to be reachable** — grep confirms F-001 names VoiceOver/TalkBack exactly once, in AC-33, for delete alone. It is the gap that matters most, because AC-6's deferral leans on it: telling a screen-reader user they cannot reorder is only acceptable while they can still reach every row and control, and VoiceOver and TalkBack are mobile. — **OWNER DECIDED 2026-08-22, narrowing this from what it was raised as.** `F-009 AC-6` specs reorder as pointer drag only. Raised as an access gap; **the owner's answer: pointer drag is enough for reordering — do NOT add Move up / Move down for tasks.** Two things do change. **(a) Tab navigation must work** — *"việc chuyển nhảy giữa các phần tử thì nên làm"* — so every row and row control is reachable and focus is visible, as a general requirement rather than a reorder feature. **(b) Screen-reader support for reorder is deferred, by decision, and the AC should say so** rather than leave a silent hole: VoiceOver and TalkBack cannot perform a custom drag, so those users cannot reorder, and reorder is cosmetic enough to live without. **Scope of that deferral is REORDER ONLY** — the coordinator did not widen it to `F-001 AC-33`, which requires a no-gesture delete path, because delete destroys data and reorder does not. **If the owner meant it more broadly, that is a separate call.** The lists/tasks inconsistency noted earlier (lists reorder by menu item, tasks by drag) stands as accepted. |
| T-235 | The three-dot button steps sideways with each list name | assistant | F-008 | design-agent | P1 | — | DONE | docs/design/assistant/screens/lists.html, docs/design/assistant/screens/lists-ios.html, docs/design/assistant/screens/lists-android.html | **Owner spotted it in the render.** Measured at 430 in `lists.html`: system rows right-align their count (`Done` x=316, `Inbox` x=308, row right edge 336) while personal rows do not (`Work` count x=86, overflow button 98–138) — **so the dots form a ragged column that moves with the name's length**, and the reference for how it should look is three rows above it in the same drawer. **The rules already say the right thing** — line 225 declares `margin-left:auto` on `.lrow .overflow` — **but the computed value is `0px`.** Orchestrator could not find the override: no media/container query wrapping it, no inline style, no later `margin-left` on `.overflow`, one style block, and the `*{margin:0}` reset loses on both specificity and order; the row is `display:flex` and the button is a direct child. **Cause must be named, not painted over** — a declared-but-unapplied rule has a reason, and covering it with a different mechanism means it returns on the next row type. **CAUSE FOUND, and there was no `margin` override at all:** `[data-state="default"] .v-default{display:block}` — the state-visibility mechanism was setting `display:block`, destroying the `display:flex` that `.mrow` declares. **With no flex container, `margin-left:auto` has nothing to push against.** The thing overridden was `display`, which is why searching for a `margin-left` found nothing. Fixed with a `vflex` variant so a shown row keeps flex, plus an `.lname` span taking the leftover width. **Verified by the orchestrator after the agent stalled mid-verification:** overflow button right edge **324 on every personal row in all three files** (was 138 and moving with the name), counts at 264/260/256 per file, design-check 175/0/4. T-231's declared iOS/Android screenshot gap remains open — the agent stalled before that step. |
| T-236 | design-check cannot see a row that lost its flex layout | assistant | — | design-agent | P2 | T-235 | CANCELLED | — | **T-235's defect was invisible to every check and the owner found it by eye.** `[data-state="x"] .v-y{display:block}` overrode `.mrow{display:flex}`, so a declared `margin-left:auto` had no flex container and the three-dot buttons formed a ragged column. **design-check was 175/0/4 before AND after** — contrast, testids and token drift all pass while the layout is broken. **The pattern appears 254 times across the ten mockups**, and the check is mechanical: for any element computed `display:block` that carries a `v-` state class, does another of its classes have a base rule declaring `display:flex|grid`? **A probe run just now returned 0 for the visible default state at 1440 — which is exactly the limit worth fixing: it only sees the state that happens to be rendered.** **CLOSED WITHOUT BUILDING THE CHECK — measured instead, and the answer is that there is nothing left to catch.** Across all twelve files: **254 state rules setting `display:block`, 254 with a companion `.v-flex` / `.vflex` rule restoring flex, 0 unrescued.** The row the owner spotted was the single place the companion was missing. **A check written for a defect that no longer occurs and has no second instance is maintenance cost against nothing** — the honest close is the measurement, kept here so the next person asking gets the number rather than repeating the search. **Limit worth stating: this counts rule pairs, not markup.** An element could still miss the flex-variant class in its `class=` attribute; that specific failure is what the owner saw, and it stayed invisible to design-check, which was 175/0 before and after. |
| T-276 | Forty-one testids are drawn and declared nowhere, and two of them are the same control under two names | assistant | — | design-agent | P1 | T-271 | DONE | `docs/design/_shared/components.md`, `docs/design/assistant/screens/lists-ios.html`, `docs/design/assistant/screens/lists-android.html`, `docs/design/assistant/screens/app-shell.html`, `docs/design/assistant/screens/app-shell-ios.html`, `docs/design/assistant/screens/app-shell-android.html` | **147/147 catalogued, 0 missing** — was 107/148. Forty entries in three new sections (conversation 14, lists 18, task-detail 6) plus two rows in the app-shell table. **The total fell from 148 to 147 because the drift was resolved rather than catalogued**: `list-recolour-swatches` (web) beat `list-recolour-swatch-group` (mobile) on three stated grounds — the container is the plural of its child `list-recolour-swatch`, matching `detail-steps`/`detail-step-row`; `-group` duplicates the `role="radiogroup"` already on the element; and the web mockup was drawn first. Mobile mockups updated to match. **VoiceFab now has a catalogue entry at all** — it had none — naming "Talk" per AC-37, and the drawn `aria-label` is corrected in all three shells. **The agent`s own distinction is worth keeping:** the "Start listening" strings still in the codebase are on the MicControl orb, which IS the capture control, so they are correct there — it separated the navigation FAB from the capture control instead of sweeping the string globally. |
| T-277 | A pre-existing type error nobody filed: `tokens.spacing` does not exist | assistant | — | mobile-agent | P2 | — | DONE | `src/assistant/mobile/components/TasksSurface.tsx` | **`npx tsc --noEmit` now exits 0. It had been exiting 2 on a clean tree**, which is the part that mattered: a type check that is red on main cannot gate anything, and every agent that runs it reads `exit 2` as normal. **Case 1 of the three offered — the reference was wrong, not the type.** `tokens.json` names the scale `space` (numeric indices); `theme.ts` re-exports a semantic convenience object called `spacing`; `TasksSurface.tsx` reached for `spacing` as a property of the raw JSON import, where it does not exist. Fixed by importing the named export and using `spacing.gutter_mobile`. No cast, no `any` — the agent was told a green check bought that way is worse than a red one. |
| T-278 | Both clients still say "Start listening" | assistant | — | web-agent | P1 | T-268 | DONE | `src/assistant/web/components/Chrome.tsx` | **Both clients now say "Talk", and the sweep found the interesting negative:** no test anywhere was asserting the old string. All three web test files locate the FAB by `data-testid`, so the WCAG 4.1.2 defect lived in production code only and was **not** protected by a green test — which is the failure mode this project has hit before (`!!!` held in place by `toBe('!!!')`) and the reason both agents were told to look. Mobile checked `SHELL_A11Y_IDS` for a duplicate copy; there was none. Suite 1240/1240 on both halves. **Artifact list amended 2026-08-23: `src/assistant/mobile/components/VoiceFab.tsx` was moved out of `Artifacts` and into this sentence, because T-325 deleted the file when option B retired the FAB.** The work recorded here is unchanged and was real — this row set that control`s accessible name to "Talk" the same morning. **The Artifacts column is a claim about the present** ("go and look at these"), and C1 tests it as one; a file that existed when the work was done and was retired hours later belongs in the account, not in the list. `src/assistant/web/components/Chrome.tsx` still stands and still carries the change. |
| T-280 | F-003`s parity table owes AC-37, on top of the AC-30–32 debt it already carries | assistant | — | spec-agent | P3 | T-268 | PENDING | — | Noticed and deliberately NOT edited by the T-268 agent, which was scoped to F-001 and said so instead of reaching across. The parity table already fails to enumerate AC-30, AC-31 and AC-32 (T-109 carries that); AC-37 joins them. **Fold into T-109 rather than dispatching twice** — same file, same table, same edit. |
<!-- 34 F-001 tasks archived 2026-08-17 — see TASKS-archive.md -->
<!-- T-137..T-140 archived 2026-08-19: the Inbox-as-unfiled cluster, dependency-closed so C3 stays green (see T-149). -->
<!-- T-144 and T-148 archived 2026-08-18: the only two DONE rows no live row depends on. See T-149 — C3 resolves Depends against TASKS.md alone, so the other 50 cannot move without breaking it. -->
<!-- Examples — the parser strips HTML comments, so these never dispatch:
| T-00X | Crawl existing app for QA knowledge | — | — | qa-explorer-agent | P2 | — | PENDING | — | — |
-->
| T-282 | C6 cannot express "sanctioned once", so two true records keep the gate red | assistant | — | orchestrator | P2 | — | DONE | `.claude/state/SANCTIONS.md`, `.claude/hooks/validate-state.sh`, `.claude/eval/scenarios/tests/R9-state-contract.sh`, `MANIFEST.md` | **Validator: 749 checks, 0 violations, 2 sanctioned crossings. Scenarios: 17/17. R9 is green for the first time today.** The register licenses **one exact `task + agent + path` triple per row** and nothing wider. **Six properties are asserted by scenario, not trusted** — and each was made to fail first: with no register the crossing is still a violation; an exact triple licenses it; the verdict line reports the count so a grant can never read as a clean pass; **a prefix licenses nothing** (`tests/` does not cover `tests/auth/a.spec.ts` — that is the back door the whole design is shaped around); a grant naming a different agent licenses nothing; and a grant whose task no longer names the path **fails as a dead grant**, because nobody removes a row that costs nothing to leave. **Two hazards this repo has already recorded were walked into and caught:** the first draft matched `T-[0-9]+`, which silently drops lettered sub-tasks — R9 itself names that as having bitten this queue twice, and it would have turned a granted sanction into a violation nobody could explain; and a bare `OUT4=$(validator)` under `set -e` **aborted the scenario mid-block while the suite still reported PASS on the checks that had run**, which is the exact shape of every "green over broken" finding today. MANIFEST`s own sentence — *"the map cannot express `sanctioned once`; that is the gap"* — is now false and was rewritten rather than left standing. **Template-owned: T-283 carries the port upstream (L-014).** |
| T-283 | Port the sanction register upstream before the next template sync erases it | assistant | — | orchestrator | P2 | T-282 | PENDING | — | `.claude/state/SANCTIONS.md`, the C6 patch in `.claude/hooks/validate-state.sh` and the six R9 guards all live under `.claude/`, which MANIFEST states plainly is **erased by the next `upgrade-project.sh` run** — *"a fix made HERE is erased by the next upgrade. Fix upstream, then sync."* **This is L-014`s shape and the third time this project has written the same patch twice:** `declared-elements.sh` line 73 was restored by a template sync and had to be re-applied, silently and size-dependently. Port to `claude-agents-final/templates/project-starter/` alongside T-156, which carries the same obligation for the spec-check fix — **one trip, not two.** Until then, an upgrade turns two recorded sanctions back into two violations and R9 goes red again with no visible cause. |
| T-285 | Build the inline add-task row on mobile | assistant | F-009 | mobile-agent | P0 | — | DONE | `src/assistant/mobile/components/TaskList.tsx` | Mobile half of T-284, same affordance, no backend. **On the phone this matters more than on web:** the header lost its Add-task button in the v3 layout and the floating button is voice, so **with the inline row unbuilt there is no hand path to add a task above the fold at all.** Draw from `app-shell-ios.html` / `app-shell-android.html`. **Closed on disk evidence, not re-dispatched: it had been sitting `IN_PROGRESS` with nothing running** — absent from `STATUS.md ## In-Flight`, inherited stale through the merge from the other session. `TaskList.tsx:277` carries the `InlineAdd` component, ported from web`s `TasksSurface.tsx`, so the work is real and complete. **This is the pattern recorded on 2026-08-21 when six rows sat IN_PROGRESS with five of them finished**, and nothing in the validator compares `IN_PROGRESS` against the In-Flight table. **Worth stating plainly: this built option A`s affordance, which the owner retired hours later in favour of B.** Not wasted — a drawn-and-built A is part of what made the comparison decidable — but the client-side retirement now belongs to the B promotion work, and this row should not read as shipping the current design.|
| T-286 | Architecture and API contracts for Lists and the list actions | assistant | F-008 | architect-agent | P0 | — | DONE | docs/specs/assistant/api-contracts.md, docs/specs/assistant/data-model.md, docs/specs/assistant/F-008-lists.md | **Both specs exist and both explicitly asked for this, and nothing has been dispatched.** F-008 (Lists) and F-009 (search, sort, hide completed, select) name the endpoints — `POST/GET/PATCH/DELETE /lists`, `list_id` on the task patch allowlist, `sort_order` on task, a bulk endpoint — but no contract shapes them and no data-model migration exists. **27 of the 35 unbuilt ids sit behind this**, so it is the gate on the whole v3 backlog. Also carries F-008's OQ-3, routed here by spec-agent: whether `Collection` widens to `string` or gains a branded type. **DONE — 6 endpoints shaped, 5 amended, both spec-checks pass. T-287 (backend) is unblocked, and with it the 27 ids behind it.** **Five decisions the owner can overturn:** `Collection` widens to **`string`**, not a branded type, because a list's collection id is its uuid and the `default` branch on every switch is what forces the handling; the **delete count comes from the client's own task data** with a `409 LIST_NOT_EMPTY` carrying the count as fallback — no preflight call, no stored counter to go stale; **bulk operations are all-or-nothing**, since per-item results would need an undo surface no spec draws, and one shared `delete_gesture_id` keeps restore a single action; **preferences live as columns on `account`** rather than a new entity; and **`sort_order` is deliberately invisible to the assistant** — not in `DIFF_FIELDS`, not in `ContextTask` — because reorder is cosmetic. |
| T-287 | Serve Lists and the list actions | assistant | F-008 | backend-agent | P1 | T-286 | DONE | `src/assistant/api/__tests__/lists.test.ts`, `src/assistant/api/app.ts`, `src/assistant/api/types.ts`, `src/assistant/api/store/store.ts`, `src/assistant/api/engine/serialize.ts`, `src/assistant/api/engine/apply.ts`, `src/assistant/api/engine/plan.ts`, `src/assistant/api/engine/turns.ts`, `src/assistant/api/engine/undo.ts`, `src/assistant/api/engine/sessions.ts`, `src/assistant/api/engine/task-equals.ts`, `src/assistant/api/engine/task-fields.ts`, `src/assistant/api/ports/interpreter.ts`, `src/assistant/api/ports/fixture-interpreter.ts`, `src/assistant/api/ports/fixture-table.ts`, `src/assistant/api/__tests__/wire.test.ts`, `src/assistant/_shared/controller.ts`, `src/assistant/_shared/testing/fixtures.ts` | **23 of the 26 ACs — every one tagged `(api)` — each with a test naming its id. Suite 1240 → 1275, +36 tests, verified by the orchestrator rather than taken on the return.** Endpoints for create / list / rename-recolour-reposition / delete; `PATCH /tasks/{id}` gains `list_id`; the turn path gains `list_create`, `list_move`, `list_refuse`. The three `(web)` ACs (AC-8, AC-15, AC-16) were correctly left alone. *(The return`s prose says 24 api ACs and its own list has 23; 23 is right — 26 minus the three web ones.)* **AC-7 has teeth as specified:** deleting a non-empty list unfiles its tasks in the same transaction instead of deleting them. **AC-9 holds:** a deleted list does not reach F-006`s trash and gets no soft-delete lifecycle. **The migration question was answered rather than obeyed** — the store is in memory, so `lists` is an optional `StoreState` key that `emptyState()` initialises, and no migration file was invented to satisfy the word in the task row. **The `_shared/testing/fixtures.ts` edit was checked specifically** because that file is the shared time bomb that produced today`s last red test: it is one additive default, `list_id: null`, and `upcomingTask` is untouched. `tsc --noEmit` still exits 2 on the two pre-existing mobile errors and no new ones. **One test left red on purpose** — see T-307. **One self-decision is wrong and is not the agent`s fault** — see T-308. |
| T-288 | Build Search, sort, hide completed and select on web | assistant | F-009 | web-agent | P1 | T-287 | PENDING | — | **20 of the 35 unbuilt ids.** Six components: SearchField, OverflowMenu, SelectMode, BulkToolbar, ConfirmDialog, and the sort controls. Drawn across `app-shell*.html`; testids already published in `components.md`. **Search needs its states, not just an icon** — field, mid-query, no-result. `shell-search-button` and `shell-overflow-button` render in the header today only in the mockups; in the build the header has them in the catalogue and nothing behind them. |
| T-289 | Build Lists on web | assistant | F-008 | web-agent | P1 | T-287 | PENDING | — | The 7 Lists ids, plus the four operations drawn in `lists.html` / `-ios` / `-android`: rename, recolour, delete with the count-naming confirmation, reposition by Move up / Move down. **The assistant may create a list and file tasks by voice and may NOT rename, recolour or delete one** (F-008 permissions table). Colour is a dot on the row, never a row background — `DESIGN.md` carries the rule and the reason. |
| T-290 | Every design pass from now on ends with an implementation task | assistant | — | claude | P1 | — | PENDING | — | **Process fix, and the failure was the orchestrator's.** Over two days the owner reviewed render after render; every piece of feedback was about how it looked, so every dispatch went back to design-agent. **Phase 3 ran perhaps a dozen times and phase 4 never ran once.** The signal existed and was misread: `app.test.tsx`'s `NOT_BUILT` map grew to 35 ids and was read as good hygiene rather than as a queue. **The structural reason it stays invisible: the mockups are HTML and the app is React and React Native, with nothing connecting them** — a mockup edit can never touch the build, so *design approved* never implies *app changed*. The rule: **a design task is not DONE until an implementation task exists naming what it drew.** |
| T-291 | Web styles hold 167 pixel literals the tokens should own | assistant | — | web-agent | P2 | — | PENDING | — | **Owner rule, 2026-08-23: never hardcode a pixel value — screens differ in size and density.** Measured: `src/assistant/web/styles.css` holds **167 `px` literals**; 60 are `1px` (hairlines, defensible) but the rest include `720px`, `12px`, `8px`, `6px` — spacing and measure values that `tokens.json` already defines. **Compare with mobile, which is nearly clean:** `components/styles.ts` reads **112 values from tokens** and holds 8 bare numbers, most of them `0`. **The web client drifted and the mobile one did not**, so this is a gap between two clients of the same token file rather than a missing rule. Replace with CSS custom properties fed from `tokens.json`; `design-check`'s token-literal rule already fails on values not present there, which is how the stale `dist/` build was caught. |
| T-292 | List every visible difference between the built app and the mockups | assistant | — | design-agent | P0 | — | CANCELLED | — | **The owner asked whether any task makes the UI match the design. There was none, and that is the gap this fills.** **Nothing compares the built app to the mockups.** `design-check` compares mockups against `tokens.json`; `app.test.tsx` compares the app's testid catalogue against the mockups' — **structural, id-level, and blind to how anything looks.** So both stayed green for two days while the owner found every visual defect by eye. **This task is the enumeration, not the repair:** render the built app and the matching mockup at the same widths, side by side, and write down each difference as a row — surface, what the mockup shows, what the app renders, and which token or rule the app is missing. Cover the states that exist in both today; skip anything in `app.test.tsx`'s `NOT_BUILT` map, which is already accounted for. **Deliver one HTML file the owner can scroll**, and a task-sized list of the fixes. **Do not fix anything in this pass** — an audit that also repairs cannot be checked, because its own output is the only record of what was wrong. |
| T-293 | A check that fails when the built app stops looking like the mockup | assistant | — | design-agent | P1 | T-292 | CANCELLED | — | **The reason the drift was invisible, stated as its own task.** Two checks exist and neither asks the question: mockup-vs-tokens, and testid-catalogue-vs-catalogue. **A third is missing — rendered app vs rendered mockup.** T-292's enumeration is the input: whatever classes of difference it finds are what this check has to catch. **Do not attempt pixel-perfect image diffing** — it fails on antialiasing and font hinting and gets switched off within a week. Compare **computed values against the token the mockup uses**: ground, radius, spacing, type size, hit area, per named state per platform. **The bar is that a defect the owner found by eye this week would have failed the check** — test it against the real ones: the ragged three-dot column, the oval swatches, the time column surviving on two platforms of three, the panel corners painted over by their own children. |
| T-294 | Enter creates the task twice on Android | assistant | F-003 | mobile-agent | P0 | — | CANCELLED | — | **Owner hit it: one Enter, two identical tasks.** Cause: `onSubmitEditing` and `onBlur` are wired to the same handler on the same field. Enter fires the first, the field then loses focus and fires the second. **Three sites, and one of them is older than the inline-add work:** `TasksSurface.tsx:181-182` (empty-state add, new), `TaskList.tsx:333-334` (inline add, new), and **`TaskList.tsx:161-162` — the rename field, which predates all of this and has the same shape.** So renaming by Enter on Android is likely double-committing too and nobody noticed; **check it rather than assuming, and fix all three.** The web version does not show the defect because its commit clears the draft before blur can run. **Guard the commit itself** — one commit per open field, whichever event arrives first — rather than removing `onBlur`, which is what makes tapping away save. **SUPERSEDED by T-298** — folded into the screen-1 mobile pass rather than dispatched alone; the detail above is the briefing for it. |
| T-295 | The header lost its title and its two controls | assistant | — | web-agent | P1 | — | CANCELLED | — | **Three deviations from the mockup, all in one bar, so one pass.** `app-shell.html`'s `<header class="topbar">` holds, in order: the Lists button, `<span class="bar-surface-title">Today</span>`, a spacer, then `tasks-search-input` and the overflow control. **The build has only the Lists button**, and renders `Today` as a hero heading in the content area below. **Two of the three are already known:** `shell-search-button` and `shell-overflow-button` sit in `NOT_BUILT` behind T-288. **The title's placement is not, and it is the cheap half** — moving it into the bar also removes the loose vertical rhythm the owner noticed on the empty state, because those gaps exist only to separate the hero heading from the count line. **SUPERSEDED by T-297** — one surface, one pass. |
| T-296 | The empty state says one thing twice and buries the voice | assistant | — | design-agent | P1 | — | CANCELLED | — | **Owner's reading of the empty screen, and the second point is a product problem rather than a layout one.** **(a)** *"0 tasks left today"* and *"No tasks yet"* carry the same information; the count line exists to say how many remain and is noise when the answer is none. **(b)** **This is a voice-first app whose empty state makes typing the loudest thing on screen** — a large filled `Add task` button — and puts speaking in the smallest, greyest line below it, naming a surface (*"on Talk"*) the user has not met yet. **The first screen a new user sees is teaching the wrong primary path.** **(c)** The vertical rhythm between heading, count and empty-state block is uneven — likely resolved for free by T-295 moving the title into the bar, so **do that first and re-look before spending anything here.** **SUPERSEDED by T-297** — same surface, same file, same pass. |
| T-297 | Screen 1, web: the task list matches the design | assistant | — | web-agent | P0 | — | IN_PROGRESS | — | **One surface, finished, so the owner can open it and sign it off.** Owner's direction: screen by screen, not defect by defect. **Five things, one file mostly:** (a) **base `fontSize` is 15 where the design says 16** — measured on 4 of 6 elements, so one value fixes four differences; (b) **hairlines the design retired are still painted** — `borderBottomWidth: 1px` on 3 elements the mockup renders at 0; (c) **checkbox radius 6 vs `radius.sm` 8, message bubble 18 vs `radius.md` 12**; (d) **the header** — `app-shell.html`'s topbar is Lists button, `bar-surface-title` "Today", spacer, search, overflow; the build has only the Lists button and renders Today as a hero heading below, which is also what makes the empty state's vertical rhythm uneven; (e) **the empty state** — *"0 tasks left today"* and *"No tasks yet"* say the same thing, and in a voice-first app the loudest control is for typing while speaking is the smallest grey line naming a surface the user has not met. **`shell-search-button` and `shell-overflow-button` stay in `NOT_BUILT`** — this task places the title and the bar, not what sits behind those two controls. **Measure the same six elements after and quote the difference count.** |
| T-298 | Screen 1, mobile: the task list matches the design | assistant | F-003 | mobile-agent | P0 | — | IN_PROGRESS | — | Mobile half of T-297, same surface. **Carries T-294 as its first item, which is a data defect and not cosmetic:** one Enter creates two tasks on Android, because `onSubmitEditing` and `onBlur` call the same handler — Enter fires one, the field then blurs and fires the other. **Three sites, and `TaskList.tsx:161-162` (rename) predates the inline-add work**, so renaming by Enter is likely doubling too; check rather than assume. **Guard the commit itself, one per open field**, rather than dropping `onBlur`, which is what makes tapping away save. Then the same visual reconciliation T-297 does on web: type scale, retired hairlines, radii, header title placement, empty state. **There is uncommitted work in `src/assistant/mobile/` from the inline-add port** — it carries this bug; fold it in rather than reverting. |
| T-299 | Screen 1 round 2, web: eleven defects the owner found in the render | assistant | — | web-agent | P0 | — | DONE | src/assistant/web/styles.css, src/assistant/web/components/TasksSurface.tsx, src/assistant/_shared/model/format.ts | **Owner reviewed the running app and found these; the previous pass reported `differences remaining: 0` while eight survived, so measure, do not assert.** **(1) `false` is printed on screen** in the *Added 1 task* block — an internal boolean reaching the user, and the only item here that is not cosmetic. **(2) `alignItems: flex-start` on the task row** puts the checkbox and the delete control against the top edge; measured 11px off centre on web, 36px on mobile. **(3) The due date wraps to a second line** under the title; the mockup right-aligns it on the same line, which is what makes the row 67px instead of 56 — **fix this first, then re-measure (2), because a single-line row may centre on its own.** **(4) Two ways to add on one screen** — the header `Add task` button goes, the inline row stays. **(5) The empty state's button becomes an unfocused inline field**, so the affordance is identical in both states: *always an inline field, never a button*. **(6) `0 tasks left today` duplicates `Nothing in Today`** — drop the count when it is zero. **(7) Drop `This list is empty. Your other tasks are still where you left them.`** — the first half repeats the heading and the second answers a worry the user does not have. **(8) Panels are not rounded** — `12px 12px 0 0` per the mockup. **(9) The message bubble keeps a 1px bottom border** the mockup renders at 0. **Search and the overflow menu stay unbuilt** — do not start them. |
| T-302 | Mobile: the empty state hangs at the top and out-ranks the title | assistant | F-003 | mobile-agent | P1 | — | PARTIAL | src/assistant/mobile/components/styles.ts | **Two defects seen in the render after T-300.** **(a)** `No tasks yet` and `+ Add a task` sit tight together just under the bar with the whole screen empty below — the block reads as debris at the top rather than as the screen's content. **The mockup decides whether it centres or anchors; say which rather than choosing by taste.** **(b)** `No tasks yet` renders larger than `Today` in the bar, so **a message about the screen being empty out-ranks the screen's own title.** Both sizes from tokens. |
| T-304 | The wide-desktop rule was fixed in the design and never reached the client | assistant | — | web-agent | P1 | T-200 | PENDING | — | **The inverse of what T-200 was filed as.** The mockup now centres a content column at every breakpoint — measured 0px asymmetry at 1024/1280/1440/1920. The client does not. `src/assistant/web/styles.css:856` still reads `.tasks-col { max-width: 720px; padding-left: var(--sp-xxl); padding-right: var(--sp-xxl); }` with **no `margin: 0 auto`**, under the same three-line comment T-200 quoted as the false justification — *"The list keeps a measure, left-anchored against the checkbox rail… The canvas is used by the SPLIT"* — and line 300 caps it at 900px. `TasksSurface.tsx:487` renders `<div className="tasks-col">`. **The design`s answer is `.content-col` with `max-width: var(--list-max)` and auto margins on both sides; the client needs the same rule and the same token, not a re-derivation.** Delete the comment too: it justifies a left anchor the design has now rejected, and a stale justification is what let this survive one round already. Measure the client at the same four widths and report asymmetry, the way the design half was measured. |
| T-305 | No state has ever been drawn with a list longer than the screen | assistant | — | design-agent | P1 | — | DONE | `docs/design/assistant/screens/app-shell.html`, `docs/design/assistant/screens/app-shell-ios.html`, `docs/design/assistant/screens/app-shell-android.html`, `docs/design/_shared/components.md` | **Found while answering an owner question about where the add-task control belongs, and it is the reason that question was hard to answer from the mockups.** Measured at 390x844 on `app-shell-ios.html`: the `.app` frame is **820px**, the document is **1560px**, and **there is no scrolling element anywhere in the mockup** — 18 rows simply spill past the frame. `.inline-new` lands at **y=1320, i.e. 476px below the fold**, in a mockup that cannot scroll to it. **Both shipped clients DO scroll** — `styles.css:299 .tasks-body{overflow-y:auto}` and `TaskList.tsx:406` wraps the list in a `ScrollView` — so the product has a scrolled long-list state that **no drawing has ever shown, and therefore no lens and no owner has ever reviewed.** What is unreviewed as a result: what the bottom edge looks like once scrolled, whether the voice FAB overlaps the last row, whether `.inline-new` is reachable without scrolling, and whether the group headings stay legible. **Draw the long list as its own state on all three platforms, with a real scroller**, and enumerate what sits at the bottom edge in it. This is upstream of the add-control question the owner raised, not a substitute for it. **Option A drawn on all three platforms, measured by the orchestrator rather than taken on the return.** Pinned while scrolled, released at the end, and a short list unchanged: `tasks-default` and `long-list-bottom` both measure **0 rows covered, gap 0** on web, iOS and Android — the property the owner was promised. While pinned the bar covers 1 row (2 mid-scroll; 2 at top on Android). **The FAB defect the orchestrator raised mid-flight is genuinely fixed, and the orchestrator`s first verification metric was the misleading one.** I measured "does the FAB overlap row text" and reported yes on all three platforms. The agent`s metric was "text the FAB covers that the BAR does not" — and that is the one that matters. Measured directly: the FAB now occupies the identical vertical band as the bar (iOS both 1420→1472), and the single text it touches, `row-due "Sat 10:00 AM"`, is inside that band and therefore already behind the bar. **Additional coverage: zero.** The render confirms it — `Sat 2:00 PM` on the Team meeting row reads fully, where before the fix the mic cut through it. **The agent chose the position by measuring three candidates rather than by taste:** FAB at `var(--s4)` covered 1 row`s due time, at `calc(--h-lg + --s4)` covered 2, at `bottom: 0` covers nothing extra — so the bar insets its right edge and they share the bottom band. **Two self-decisions worth keeping:** group headings became `position: sticky; top: 0` so the date group is always visible while scrolling, and the agent recorded the honest residue in `unresolved:` — while pinned the bar draws over the last visible row`s lower edge, no CSS fixes it, and padding does not help (which the briefing had already measured). `design-check` 202 passed / 0 failed / 4 skipped, testid catalogue 147/147, exit 0. It also carried the owner`s *"tạm thời"* forward itself: **nothing here forecloses B.**|
| T-306 | Owner question: should the add-task control be pinned rather than at the end of the list | Pin the add-task row to the bottom only once it scrolls out of view | — | design-agent | P2 | — | DONE | `docs/design/assistant/screens/app-shell.html`, `docs/design/assistant/screens/app-shell-ios.html`, `docs/design/assistant/screens/app-shell-android.html`, `docs/design/_shared/components.md` | **Raised by the owner from Microsoft To Do, whose `Add a Task` bar is fixed at the bottom.** Their argument, and it is measured on our side: our `InlineAdd` is a row at the END of the list (`TasksSurface.tsx:631`, `TaskList.tsx:277`) inside a scroller, so a long enough list costs a scroll before a task can be typed, and the control has no fixed position to build muscle memory on. **What the comparison does not carry over: their bottom edge is empty and ours is not.** We are voice-first — the phone puts the voice FAB at `right:s4; bottom:s4`, and Talk gives the whole bottom to the composer. A second permanent bottom affordance meaning "create a task" would put a typing control and a speaking control side by side with one meaning, which is the rule the Complete/Done fix was made under. **Also unlike My Day, our Tasks surface is grouped** (Overdue / Today / Upcoming): a pinned bar must choose the new task`s group silently, where an inline row shows the user where the task will land. **Option A** — keep inline, and let it pin to the bottom only once it scrolls out of view; the mechanism already exists in this shell (`.bulk-toolbar` is `position:sticky; bottom:0`). **Option B** — one bottom bar merging the field and the mic, shared with Talk`s composer; larger, and it risks one control shape carrying two meanings across surfaces. **Blocked on T-305: the long-list state has never been drawn, so neither option can be judged against a render.**  **OWNER CHOSE OPTION A on 2026-08-23: keep the row inline, let it pin only when it scrolls out of view.** Option B (one bottom bar merging field and mic, shared with Talk) is not taken. **Mechanics measured on a harness before writing this row, because two of the orchestrator`s predictions about them were wrong.** `position: sticky; bottom: 0` keeps the row in normal flow and **never lets it leave its containing block**: with 400px of content deliberately placed after the wrapper, the row sat 1px off the scroller`s bottom edge at every scroll position until the wrapper`s bottom came into view, then landed exactly on it (both measured 401px from the scroller bottom, identical). **So the release point is the bottom edge of the wrapper, and choosing that wrapper IS the decision** — it must be (rows + add row) with nothing after it. **No task moves:** gap from last row to the add row measured 0 in every case. **The cost is occlusion, not displacement:** while pinned the 52px bar draws over ~2 rows at the bottom of the viewport; rows keep their coordinates and scroll beneath it. **A short list is byte-for-byte today`s behaviour** — 0 rows covered, gap 0 — which is what makes this cheap. **Two orchestrator predictions the measurement killed:** reserving 52px of scroller padding does NOT stop the occlusion (still 2 rows — padding only adds room at the very end, where the bar has already released, so it is unnecessary; that is the difference from a `position: fixed` bar, which does need it), and a wrong containing block did NOT reproduce the float-down-with-a-gap that was warned about. **One thing genuinely open and not answerable without a drawing: the voice FAB sits at `right:s4; bottom:s4`, exactly where the pinned bar lands.** Full-width bar with the FAB floating over it, bar inset to clear the FAB, or FAB hidden while pinned — undecided, and it belongs to T-305`s long-list drawing. **Option B was re-examined on 2026-08-23 at the owner`s request, in the sharper form they proposed — the field and the voice button on ONE fixed bottom row — and the examination changed what B is.** That bar already exists: `app-shell-ios.html:1967` is a `composer-in` row holding the input and the mic side by side, placeholder *"Say or type what needs doing."* Measured **93px** (68px input row plus the home-bar area), i.e. **11% of an 844px phone, permanently**. **But B is not a layout change, it is a behaviour change, and that is the finding.** The two fields look alike and run different paths: `InlineAdd` calls `controller.addTask(title, collection)` — **title kept verbatim** after a trim, lands in the collection being viewed, cannot ask, cannot be refused. The composer calls `controller.send('typed')`, and `Composer.tsx`'s own opening comment says *"typed input takes the same interpretation path as speech (AC-17)"* — **the title is interpreted**, the destination follows from the interpretation, and the turn may come back as a clarify question or a refusal. Typing *"mua sữa ngày mai"* yields a task titled *"mua sữa ngày mai"* through one path and *"mua sữa"* due tomorrow through the other. **So B removes the literal-create path, and there is currently no way to say "do not interpret this".** **The hard blocker: `send()` can return a question, questions live in the conversation, and the Tasks surface has no conversation region** — `TasksSurface.tsx:3` records that it *"used to be a PANE beside the conversation. It is now a SURFACE."* **The trap is that the shape cannot be had without paying one of two prices:** wire the bar to `addTask` and the mic beside it is a lie about what the control does (the T-268 defect, fixed today), or wire it to `send()` and the surface owes somewhere for the assistant to answer. **What B genuinely buys:** one control instead of two — both the inline row and the FAB retire, which dissolves the FAB overlap for free — voice and text in one place on every surface, and the group-membership question answered aloud by the assistant instead of by a silent rule. **The real question B poses is not where the field goes; it is whether the Tasks surface gets an assistant at all.** Answer yes and it owes a conversation region; answer no and B collapses into A plus 93px of permanent chrome. Owner has not moved off A. **Owner confirmed A on 2026-08-23 with the word "tạm thời" — provisionally.** That qualifier is load-bearing and is recorded rather than smoothed away: A is the answer to the reach-and-scroll problem, **not a verdict against B**, and B`s own question (does the Tasks surface get an assistant) is untouched by it. **Nothing built for A may foreclose B** — in particular the inline row keeps calling `addTask` verbatim, and no mic is placed beside it. Dispatched together with T-305 as one drawing. **Option A drawn on all three platforms, measured by the orchestrator rather than taken on the return.** Pinned while scrolled, released at the end, and a short list unchanged: `tasks-default` and `long-list-bottom` both measure **0 rows covered, gap 0** on web, iOS and Android — the property the owner was promised. While pinned the bar covers 1 row (2 mid-scroll; 2 at top on Android). **The FAB defect the orchestrator raised mid-flight is genuinely fixed, and the orchestrator`s first verification metric was the misleading one.** I measured "does the FAB overlap row text" and reported yes on all three platforms. The agent`s metric was "text the FAB covers that the BAR does not" — and that is the one that matters. Measured directly: the FAB now occupies the identical vertical band as the bar (iOS both 1420→1472), and the single text it touches, `row-due "Sat 10:00 AM"`, is inside that band and therefore already behind the bar. **Additional coverage: zero.** The render confirms it — `Sat 2:00 PM` on the Team meeting row reads fully, where before the fix the mic cut through it. **The agent chose the position by measuring three candidates rather than by taste:** FAB at `var(--s4)` covered 1 row`s due time, at `calc(--h-lg + --s4)` covered 2, at `bottom: 0` covers nothing extra — so the bar insets its right edge and they share the bottom band. **Two self-decisions worth keeping:** group headings became `position: sticky; top: 0` so the date group is always visible while scrolling, and the agent recorded the honest residue in `unresolved:` — while pinned the bar draws over the last visible row`s lower edge, no CSS fixes it, and padding does not help (which the briefing had already measured). `design-check` 202 passed / 0 failed / 4 skipped, testid catalogue 147/147, exit 0. It also carried the owner`s *"tạm thời"* forward itself: **nothing here forecloses B.** **UX comparison, 2026-08-23, after the owner pointed out that the discussion had drifted into pixel accounting.** They were right: the height gap between A and B is now **0.05 rows**, which nobody can perceive, and it should not decide anything. Measured differences that actually belong to the experience: **(1) B closes the voice path while you type.** `optb-typing` has **no mic anywhere** — the morph replaces it with the send arrow, so switching to voice mid-sentence means clearing the field first. A keeps the FAB available throughout. **For a voice-first product this is the sharpest difference and it runs against B.** **(2) A`s target moves, B`s does not** — A`s row sits after the last task on a short list and pins on a long one, so its screen position depends on scroll state; B is always in one place. Muscle memory favours B. **(3) A hides 1–2 rows while pinned; B hides none** but makes the list ~1.3 rows shorter. Hidden is worse in principle than absent, though A hides at the bottom edge the user is scrolling toward anyway — close to a wash. **(4) A carries two bottom affordances** (pinned row plus floating FAB, which covered a due time until T-305 fixed it); B carries one. **(5) At 1440 the FAB is hidden and B still paints its bar** — so on desktop B pays for reach, which is not a desktop problem, in vertical space that is contested there. **And the comparison cannot honestly be finished yet: A has no typing state at all.** State enumeration shows B with three (`optb-typing`, `optb-typing-kb-short`, `optb-typing-kb-long`) and A with none — `list-new` is list creation, not add-task. **We are comparing a fully drawn B against a half drawn A, on the interaction that matters most.** T-319 draws it. **A possible resolution of (1) worth putting to the owner:** the morph exists to avoid a mic beside a text field implying the text goes through the assistant. But that trap is about **interpretation**, and this mic only means *"go to the conversation"* — so B could keep **both** mic and arrow, exactly as the Talk composer already does, which would also remove the two-bars-behave-differently divergence T-311 had to record. Cost: tapping the mic mid-typing abandons the draft unless it is preserved or confirmed. **The structural difference, raised by the owner asking whether a long list means handling both controls — it does, and unequally.** In A the two controls use two different mechanisms: the field is a row **inside** the scroller using `sticky`, so its screen position changes with scroll state, while the mic is an `absolute` FAB **floating over** the list — which is why it covered `row-due "Sat 2:00 PM"` until three candidate positions were measured to find one that ate no text. In B both sit in one bar **below** the scroller: neither moves, neither floats over content. **That is also the mechanical reason for the occlusion figures** — A`s bar paints over the list, B`s sits beneath it. **Adding the keyboard makes the asymmetry sharper: for a long list being typed into, A has four behaviours to define and B has two.** A owes an answer for the field (pinned and tapped — does the input stay at the bottom or does the list scroll to its natural place?) and one for the mic (is the FAB reachable above the keyboard?); B`s bar rides the keyboard and its mic morphs, both already drawn. **This is a simplicity argument rather than a pixel one, and it should have been in the UX comparison rather than arriving after it.** **2026-08-23, the owner overturned two of the orchestrator`s judgements and both were measured afterwards to check.** **(1) "Typing means the user does not want voice."** Correct, and it dissolves what had been recorded as A`s clearest advantage. Verified: B`s idle bar carries **both** labels, `Add a task` and `Talk`, so **the mic returns the moment the field is empty** — and the field empties on submit. **B closes the voice path only while text is being composed**, which is exactly when it is not wanted. The scenario weighted heavily against B — switching to voice mid-sentence — is both rare and self-contradicting. **(2) "B is easier one-handed; the user does not reach up for the field."** Measured, and it is stronger than stated. Distance from the bottom edge to the control the thumb must hit to start typing: **A gives 77px (9%) at 20 tasks, 242px (29%) at 5 tasks, and at 0 tasks the add row is not rendered at all** — the empty state replaces it. **B is 81px (10%) in every case.** So A`s target slides across a 20-point range with list length and then disappears; **the point is not that it is further, it is that its position cannot be learned.** Muscle memory needs a target that does not move. **With those two corrections B leads on every remaining line** — reach, occlusion, list share while typing (50% vs 36% on a long list), and one anchoring mechanism instead of two that currently disagree (T-320). Permanent cost is level at ~1.3 rows. **Recorded rather than quietly revised: this is the second time today a correct measurement led to a wrong conclusion — first counting pixels instead of comparing experience, now over-weighting a rare case.** If B is chosen, T-320 becomes moot and should be cancelled rather than fixed. **OWNER CHOSE OPTION B, 2026-08-23.** One fixed bottom bar; the mic becomes the send arrow while the field has text and navigates to Talk when it does not. A is retired. **The decision rests on two corrections the owner made to the orchestrator`s reading, both measured afterwards** — typing means voice is not wanted, so B closing that path while composing costs nothing; and A`s target slides from 9% to 29% of the screen and vanishes on an empty list, so its position cannot be learned. **Promotion is T-321 (design) and it needs T-322 (spec) first**, because AC-37 describes the control as *"the floating voice button"* and in B it neither floats nor keeps that id. |
| T-307 | A QA wire test hand-copies the contract, and the copy is now stale | assistant | F-008 | qa-api-agent | P1 | T-287 | DONE | `tests/assistant/api/F-005-task-detail.spec.ts` | `tests/assistant/api/F-005-task-detail.spec.ts` TC-33a fails: `expected [ 'completed_by_parent', …(23) ] to deeply equal [ …(22) ]`, the extra member being **`list_id`**. **Verified as stale, not as a leak, before filing** — the first read of the failure looks like an internal field escaping to the wire, which would be serious; it is not. `api-contracts.md:544` declares `list_id: uuid **`list_id` added; `INTERNAL_FIELDS` checked rather than assumed** — all four members still match the contract`s `# internal, never serialized` markers. **It declined to derive `WIRE_FIELDS` from the contract and the reason is itself a finding:** `api-contracts.md:549` declares **`sort_order`** on the wire, and the backend emits it nowhere — so a derived constant would fail immediately on a field that is correctly absent. Second reason: the internal-field marker is a YAML comment, not structured data. It left the constant hand-maintained with a 14-line comment naming the hazard and pointing at the contract, **so the next person to see this failure can tell drift from a leak in one read** — which is the thing that made this failure expensive twice today. See T-323.| null # F-008 AC-10; null = Inbox (unfiled)`, so the field is contract-declared and belongs on the wire. What is out of date is `WIRE_FIELDS` at `spec.ts:392`, **a hand-maintained copy of the contract`s wire shape**. backend-agent diagnosed it correctly and **did not cross into `tests/` to fix it**, which is the lane discipline two rows in `SANCTIONS.md` exist because an earlier dispatch did not keep. Add `list_id` to the constant. **The durable question is the copy itself:** a hand-maintained duplicate of a contract list drifts every time the contract gains a field, and this check exists precisely to catch drift — so consider deriving it from `api-contracts.md` rather than restating it. |
| T-308 | Two live refusals tell the user something false, because the enum has no member for what happened | assistant | F-008 | architect-agent | P1 | — | PENDING | — | **The third and fourth occurrence of the gap T-170 already names, and the first two that ship.** `RefusalReason` is closed at 12 members (`api-contracts.md:887`) and has **no member for "this operation is not permitted on this entity type"**. backend-agent had nothing to use, picked the closest, and flagged the choice — correctly. The result in `src/assistant/api/engine/turns.ts`: **line 746, a duplicate list name (AC-3), reported as `{reason: `structural_field_not_settable`, field: `name`}`** — but the field is perfectly settable, the value is taken; it is a uniqueness violation. **Line 827, the assistant asked to rename/recolour/delete a list (AC-20), reported as `{reason: `structural_field_not_settable`, field: null}`** — nothing about a field is involved, and `field: null` is the tell. Line 789 (filing a step, AC-13) is the one honest use, because AC-13 genuinely makes `list_id` unsettable on a step. **Same shape as the `skipped: modified_since_apply` defect T-191 is fixing right now** — a closed enum met a case it has no member for, and the test that passes certifies a wrong message. **Routed to the running T-191 architect** with instructions to settle it only if it falls inside the vocabulary work it is already doing, and to return it as `unresolved:` otherwise rather than widening scope. Fold into T-170 if that is where it lands. |
| T-309 | Build F-006: the trash read, the two deletes, the restore refusals and the harness door | assistant | F-006 | backend-agent | P1 | T-191 | DONE | `src/assistant/api/__tests__/trash.test.ts`, `src/assistant/api/app.ts`, `src/assistant/api/engine/turns.ts`, `src/assistant/api/engine/undo.ts`, `src/assistant/api/ports/interpreter.ts`, `src/assistant/api/ports/fixture-interpreter.ts`, `src/assistant/api/ports/fixture-table.ts`, `src/assistant/api/types.ts`, `src/assistant/_shared/model/messages.ts`, `tests/harness/qa-doors.ts` | **Unblocked by T-191 — the contracts are finished and nine shapes are specified, so this waits on nobody.** `GET /tasks/deleted` (grouped server-side, parent state, server-produced expiry, and the removal write on THIS read only), `DELETE /tasks/deleted/{id}`, `DELETE /tasks/deleted`, `GET /__qa__/raw-tasks`, restore outcomes (c) `409 RESTORE_EXPIRED` and (d) `409 RESTORE_PARENT_GONE`, processing rule 5`s `deleted_tasks` context, and the `trash_read` outcome. **Two rules that are easy to implement backwards:** the expiry *predicate* runs for both callers so the assistant never names an expired row, but the removal *write* happens only on the surface`s read — **a question purges nothing** (ADR-017). And a row in `deleted_tasks` can be recognised but **never targeted** (AC-4). **`GET /__qa__/raw-tasks` is what makes AC-12 and AC-17 assertable at all** — without it a test asserting reachability passes whether the purge ran or never existed, which the spec names as the failure mode. Clients and QA follow this. **The trash is served: four new routes, one amended, the QA door, the turn path and the undo. 273/273 in the api tier, 35 new tests, nine ACs each named in a test.** Five restore outcomes distinguishable at the door (`409 RESTORE_EXPIRED`, `409 RESTORE_PARENT_GONE`); `skipped.reason` gains `permanently_deleted`; `trash_read` lands as the eighth outcome member; `deleted_tasks` enters the interpretation context read-only. **It obeyed the instruction that mattered most: the RefusalReason members it needed are mentioned in the contract`s prose but absent from the formal closed enum, so it did NOT invent them in code** and left the two false refusals standing with a note. That is the correct call — a member invented in code is how a contract and an implementation drift apart. T-170 owns it. **Its own new red test is real and correctly diagnosed:** TC-22e now expects 3 ids in `changed` and gets 4, because ADR-012`s amendment clears `series_ended_at` on a done occurrence and the amendment says in its own words *"those rows appear in `changed` so both clients see them."* The fixture is stale, not the code — T-313. **But two of the four failures it reported as pre-existing are not, and are not its own either.** The web and mobile testid-catalogue tests fail because design-agent added two testids to the mockups **while this agent was running**, and its full-suite run picked them up. Verified by the orchestrator: `git stash` the design tree and both pass, 82/82. **L-022`s shape again — during parallel work even naming a failing file is unreliable, let alone attributing one.** The briefing should have told it to measure its own tier and treat the full suite as informational. **Lane crossing recorded, not hidden:** it wrote 20 lines into `tests/harness/qa-doors.ts`, which is `{tests}/`. **The cause was the orchestrator`s briefing contradicting itself** — it listed `GET /__qa__/raw-tasks` as in-scope while forbidding writes to `tests/`, and the `__qa__` doors live there. The agent flagged it in `files_modified` when told to. Sanctioned in `SANCTIONS.md`.|
| T-310 | F-006 still lists OQ2 as open, and the owner has answered it | assistant | F-006 | spec-agent | P2 | T-181 | PENDING | — | `F-006-recently-deleted.md:764` reads *"OQ2 is open and is the owner`s, not architecture`s: it changes a promise the user sees; it is assigned as T-181."* **The owner answered it on 2026-08-23: Option A — restoring a deleted repeating task brings the repeat back to life.** Close OQ2 the way this spec closes questions and says so in its own text: **kept, not deleted, showing what was decided and what it cost** — the file`s own words are *"a closed question that still shows what was decided, and what the decision cost, is what stops it being re-asked in three weeks."* The decision, its reasoning and the rejected option are already written in `ADR-012 § Amendment (2026-08-23, T-181)`; cite it rather than restating it. **Also check whether `F-005 AC-43` needs a word:** its claim *"reverses exactly the action it was offered for and nothing else"* is now true for the series class, which it was not — that is worth being visible at the AC, not only in an ADR two files away. |
| T-311 | Draw option B: one bottom bar where the mic becomes the send arrow | assistant | — | design-agent | P1 | T-305 | DONE | `docs/design/assistant/screens/app-shell.html`, `docs/design/assistant/screens/app-shell-ios.html`, `docs/design/assistant/screens/app-shell-android.html`, `docs/design/_shared/components.md` | **The owner's own design, and it dissolves the trap that stopped B before.** One fixed bottom row holding the field and the voice button. **Tap the field and the mic becomes a send arrow**; **tap the mic and it navigates to the conversation.** **Nothing new is introduced:** typing commits through `addTask` verbatim — today's InlineAdd path — and the mic navigates to Talk, today's FAB behaviour, already specified as F-001 AC-37 with the accessible name "Talk". **No conversation region is owed on Tasks and no control lies**, because the mic stops being an input method for that field: there is no state where a field with text and a mic are both on screen. **It also removes the defect that broke A's drawing rather than patching it** — measured 2026-08-23, the floating FAB covers `row-due "Sat 2:00 PM"` on a long list; in B the mic sits inside the bar and floats over nothing. **Two things to decide and write down, not to let happen.** (1) **Talk does NOT morph** — `Composer.tsx` renders input + mic + send together, send always present and merely disabled when empty. So B makes the two bottom bars behave differently. There is a real justification (on Talk the mic and send do different things at the same time; on Tasks the mic means "go elsewhere"), but it must be recorded in `components.md`, and the alternative — teaching Talk the same morph — considered and rejected in writing. (2) **The morphed button changes meaning, so its accessible name must change with it** — "Talk" when idle, "Add task" when the field has text — and it must never announce one while doing the other, which is the defect fixed earlier today. **Draw it beside A, not instead of A:** the owner needs both renders at the same three scroll positions to choose between. Report the same measurements as A — rows covered, gap last-row-to-bar, and whether anything overlaps row content — on all three platforms. **Drawn, five states on each of three platforms, and the trade is now measured on both sides.** Bar height **52px on web and iOS, 56px on Android** — sized to the InlineAdd row, not the Talk composer, on the orchestrator`s instruction: at **6.3%** of the frame it matches the 6.2% measured off Microsoft To Do`s own bar from the owner`s screenshot, which is the cost the owner had already looked at and accepted. **0.84–0.88 rows permanent.** **Its claim of "0 rows covered" is correct and the orchestrator`s first probe said otherwise — wrongly, for the third time today.** The bar sits **below** the scroller, not over it: measured on iOS, bar top 1449 = scroller bottom 1449. The row my probe counted is clipped by the scroller`s own edge, which is what happens at the bottom of any scroll area. **This is the B-cluster of `orchestrator-defects-2026-08-23.md` recurring within an hour of that file being written**, and it is the strongest argument yet for the visibility/clipping filter it proposes. **So the comparison the owner is choosing between, both halves measured:** A costs **0 permanently** and **occludes 1–2 rows while pinned**; B costs **0.85 rows permanently** and **occludes 0**. They are close to the same amount of list, differing in *when* and in *kind* — A hides content intermittently, B takes honest space always. **Three self-decisions, each answering a question the briefing asked:** the mic stays until the first character (no arrow on an empty field, so no empty submits); voice is unreachable while the field has text, and clearing it restores the mic — which is what keeps the mic from ever sitting beside a filled field; and **Talk was deliberately NOT taught the same morph**, with the rejected alternative written into `components.md` rather than left as an accident. **It also caused the suite to go red, and that is a real finding — see T-312.**|
| T-312 | An unchosen design option put its testids into the client contract, and the suite went red | assistant | — | design-agent | P1 | T-311 | PENDING | — | **Suite is 1307/1311 and two of the four failures are this.** `src/assistant/web/__tests__/app.test.tsx` (*"applies every one of the mockups` testids across the states, and invents none"*) and `src/assistant/mobile/__tests__/a11y.test.ts` (*"the shell mockups name nothing the client does not declare somewhere"*) both fail because **`tasks-bar-input` and `tasks-bar-action` were added to the mockups and the catalogue for option B — an option the owner has not chosen.** Verified by stashing `docs/design/` and re-running: **82/82 pass.** **The catalogue is a contract with the clients**, and putting a not-yet-chosen alternative into it makes both clients fail a contract nobody asked them to meet. **The design is not wrong; the contract entry is premature.** Two ways out and they are not equivalent: hold B`s testids out of `components.md` until B is chosen (and out of the mockup attributes, since the check reads the drawings), or mark them provisional in a way both catalogue checks understand. **Prefer the first** — a provisional tier in a contract is a second contract. **The general rule is worth writing down wherever comparison drawings are described:** an alternative drawn for a decision must not enter the client contract before the decision. |
| T-313 | TC-22e expects three ids where the amendment now produces four | assistant | F-005 | qa-api-agent | P1 | T-309 | DONE | `tests/assistant/api/F-005-task-detail.spec.ts` | `tests/assistant/api/F-005-task-detail.spec.ts` TC-22e asserts the `changed` array of a series-delete restore holds 3 ids; it now holds 4. **The code is right and the fixture is stale.** ADR-012`s 2026-08-23 amendment — owner-confirmed — makes the restore clear `series_ended_at` on the series, and says in its own text *"those rows appear in `changed` so both clients see them."* The fourth id is a **done occurrence** whose end-marker was cleared: it was marked by the series delete but never trashed, so it never appeared before. Add it to the expected set. **While there, TC-33a in the same file is the other stale fixture (T-307) — same file, same class, one dispatch.** **Suite 1311/1311, 43 files, 0 failed — verified by the orchestrator.** The id added is `occ1.id`, the **done** first occurrence: it was completed before the series delete, so the delete marked it with `series_ended_at` but never trashed it; the restore now clears that marker, and since its `deleted_at` was always `null` it passes the `backIds` filter and appears in `changed`. **The agent named which id and why it belongs rather than adjusting a count until it matched** — which was the thing the briefing asked for, because a fixture edited until green is not a fixture that was fixed.|
| T-314 | Option B's bar is flush against the bottom edge, with no clearance for the system indicator | assistant | — | design-agent | P1 | T-311 | DONE | `docs/design/assistant/screens/app-shell.html`, `docs/design/assistant/screens/app-shell-ios.html`, `docs/design/assistant/screens/app-shell-android.html`, `docs/design/_shared/components.md` | **Owner feedback on the B render, 2026-08-23: the field and the mic sit too low; raise them.** Measured, and it is more than a matter of taste: the bar`s bottom edge is **0px from the frame bottom on all three platforms**, with only `padding-bottom: 4px` inside it. **The Talk composer already solves this and B did not copy it** — `app-shell-ios.html:2249` puts a `.homebar` inside `.composer`, and `:943` gives it `height:5px; margin: var(--s3) auto var(--s2)`, with `.gesturebar` as the Android form and both hidden on web by the rule at `:945`. **B carries neither.** So on a real iPhone or Android the system home/gesture indicator would sit on top of the bar, over the field and the button. **The benchmark the owner has already accepted:** Microsoft To Do leaves **139px below its bar in a 2622px screen — 5.3%**, which on our 820px frame is about **43px**. Web has no system indicator, so its inset is a pure design choice and 5.3% is the number to beat; iOS and Android must additionally reserve the indicator the way `.composer` does rather than inventing a second mechanism. **Check the touch floor after raising it** — `control.minTarget` is 44 iOS / 48 Android — and report the bar`s new bottom gap and total height per platform, since the height is what the owner is trading against option A. **Bar raised, and the system indicator is reserved through the existing mechanism rather than a second one** — `.homebar` on iOS (25px), `.gesturebar` on Android (23px), and a deliberate `var(--s5)` 24px on web where there is no indicator. Verified by the orchestrator: bar now **80px web / 81px iOS / 83px Android**, and the outer box still reaches the frame edge while the *painted content* is raised 31–33px, which is the correct structure. **The agent raised its own NEEDS-OWNER and it was right to: B`s cost moved from 0.85 rows to 1.30–1.35.** It also declined to hit the 5.3% benchmark exactly on web, because `var(--s7)` 43px would have pushed B to 1.64 rows — a judgement it stated with the number rather than making quietly. Touch floor unchanged: the action button paints at 44px with a `::before` at `min-target` per platform.|
| T-315 | Hold option B's testids out of the client contract until B is chosen | assistant | — | design-agent | P1 | T-312 | DONE | `docs/design/assistant/screens/app-shell.html`, `docs/design/assistant/screens/app-shell-ios.html`, `docs/design/assistant/screens/app-shell-android.html`, `docs/design/_shared/components.md` | **Folded from T-312 — same files, same dispatch.** `tasks-bar-input` and `tasks-bar-action` entered `components.md` and the mockup attributes for an option **the owner has not chosen**, and both client contract tests went red: `src/assistant/web/__tests__/app.test.tsx` (*"applies every one of the mockups` testids across the states, and invents none"*) and `src/assistant/mobile/__tests__/a11y.test.ts` (*"the shell mockups name nothing the client does not declare somewhere"*). Verified by the orchestrator: stash `docs/design/` and both pass, **82/82**. **The catalogue is a contract with the clients**, so a comparison drawing must not enter it before the decision. Keep the states and the drawing; take the two ids out of the catalogue **and out of the mockup attributes**, since the checks read the drawings. Put them back in the same edit that implements whichever option wins. **Both ids removed from the catalogue AND from the mockup attributes** (`data-testid`, `accessibilityIdentifier`, `resource-id`), catalogue count 54 → 52. Verified: `app.test.tsx` + `a11y.test.ts` **82/82**, design-check 202/0/4, testid-catalogue back to **147/147**. **It did not take the exemption route** the briefing warned against — no carve-out was added to either check. Option A`s states verified untouched.|
| T-316 | No drawing models the keyboard, so the typing state is drawn for a screen that never happens | assistant | — | design-agent | P1 | — | DONE | `docs/design/assistant/screens/app-shell.html`, `docs/design/assistant/screens/app-shell-ios.html`, `docs/design/assistant/screens/app-shell-android.html`, `docs/design/_shared/components.md` | **Owner raised it from a Microsoft To Do screenshot; measured on both sides.** `grep -c "keyboard" app-shell-ios.html` returns **0** — the mockups model no keyboard anywhere. Our `optb-typing` therefore leaves the list **94% of the frame** while the field is focused. Microsoft`s real typing state, measured from the owner`s screenshot (1206x2622): **list 48%, input panel 14%, keyboard 38%.** So the state we drew for typing is a screen a phone never shows. **The client already handles what the drawing does not** — `src/assistant/mobile/components/useKeyboardInset.ts` exists from the "keyboard stops eating the composer" work — which is drift in the worst direction: the implementer had to invent what the designer never drew, and nothing compares the two. **Also owed and part of the same drawing, because the owner asked it as one question: what the list does with FEW tasks versus MANY while the keyboard is up.** With two tasks Microsoft leaves the rows at the top and the space below them empty; with many, the remaining 48% has to scroll. **Neither case is drawn on our side, at any breakpoint.** Draw the typing state with the keyboard reserved, at both list lengths, on iOS and Android (web has no on-screen keyboard — say so rather than drawing one). Report what fraction of the frame the list keeps in each. **Owner asked for it now, so it was routed to the agent already in these files (T-314/T-315) rather than queued behind it.** The two are one question asked twice: with the keyboard up the bar rides above the keyboard and the home indicator is irrelevant because the keyboard covers it — so the clearance rule and the typing state cannot be settled independently without the second agent re-deriving the first one`s answer. The agent was told to return the keyboard states in `unresolved:` rather than half-draw them if the scope does not fit. **Keyboard drawn on iOS and Android; web correctly got none, and the agent said so rather than inventing one.** Verified by the orchestrator: keyboard **38% of the frame**, list keeps **50% on iOS / 48% on Android** — which lands on Microsoft`s measured 48% almost exactly. Both list lengths drawn (`optb-typing-kb-short`, `optb-typing-kb-long`); with a short list the rows stay at the top and the space below is empty, with a long one the remaining half scrolls. **The rule it derived for the indicator is the right one and is now in CSS:** when the keyboard is up the system indicator is *behind* it, so `.homebar`/`.gesturebar` hide and the bar drops from 81/83px to **56/60px** — rendering the indicator there would spend 25px of list space on something invisible. **It also obeyed the prohibition:** the four quick-attribute buttons were not drawn, because that question is T-317`s and belongs to spec.|
| T-317 | Say it and you can set seven fields; type it and you get one, plus a date you did not choose | assistant | — | spec-agent | P1 | — | PENDING | — | **The owner asked for "the buttons to add more information" after seeing Microsoft`s four quick-attribute icons (My Day, reminder, due date, note) under the input. Measuring why they exist turned up an asymmetry in our own product that no spec names.** The voice path writes `TURN_WRITE_FIELDS = [title, note, due_at, reminder_at, priority, status, list_id]` (`api-contracts.md:923`) — **seven fields settable at creation.** The typed path is `controller.ts:903`: `createTask({ title, due_at: dueAtForCollection(collection, now) })` — **title, plus a due date derived from whichever collection you happen to be looking at, not one you chose.** No note, no reminder, no priority, no list. **So the same intent produces a richer task when spoken than when typed, and nothing in the specs records that as a decision.** It may be the right decision — voice-first is the product`s premise, and a typed quick-add that stays quick has real value. But it is currently an accident of two code paths, not a written rule. **Decide and write it down before anything is drawn**: either the typed path gains a way to attach the same attributes (Microsoft`s answer), or the spec states plainly that typing is deliberately the fast, attribute-free path and the detail surface is where the rest goes. **This is spec-agent`s before it is design`s** — a row of buttons drawn first would settle it by accident, which is how the `!!!` urgency scale and the "Start listening" label both got in. |
| T-318 | Option A does not reserve the system indicator, so the A-vs-B cost comparison is not like-for-like | assistant | — | design-agent | P1 | T-314 | PARTIAL | `docs/design/assistant/screens/app-shell-ios.html`, `docs/design/assistant/screens/app-shell-android.html`, `docs/design/_shared/components.md` | **Found by the orchestrator while checking whether B had genuinely got more expensive. It had not — A has simply not paid yet.** Measured on `app-shell-ios.html`: in `optb-long-list` the bar contains a `.homebar` near the bottom edge; in `tasks-long-list` **option A`s pinned `.inline-new` contains none.** So the defect the owner spotted in B by eye — the control sitting on the very bottom edge where the system home indicator lives — **is still present in A, unfixed and uncounted.** **This matters because it is the number the owner is choosing on.** B now reads 1.30–1.35 rows against A`s 0.85, which looks like B becoming half again as expensive. It is not: A`s pinned row is 52px and the indicator it must reserve is ~25px, which lands it at **~1.26 rows** — essentially level with B. **Fix A the same way** (reuse `.homebar`/`.gesturebar`, hide them when the keyboard is up, per T-316`s rule) and re-report A`s row cost, so the comparison is between two designs that have both paid for the platform. **Do not adjust B to compensate.** **A`s indicator reserved through the same mechanism as B** — `.inline-new` restructured to a column with `.homebar` (iOS 25px) / `.gesturebar` (Android 23px) beneath, and the FAB raised to clear it. **Measured by the orchestrator: A is now 77px = 1.24 rows on iOS, 75px = 1.17 on Android**, against B`s 81px = 1.31 and 83px = 1.30. **So the two are level, which was the point** — A read 0.85 against B`s 1.30 only because A had not paid for the platform. B was not adjusted to compensate.|
| T-319 | Option A has never been drawn while the user is typing | assistant | — | design-agent | P1 | T-316 | PARTIAL | `docs/design/assistant/screens/app-shell-ios.html`, `docs/design/assistant/screens/app-shell-android.html`, `docs/design/_shared/components.md` | **Found while answering the owner`s question about UX rather than pixels, and it is the reason that question cannot be closed.** State enumeration on `app-shell-ios.html`: option B has **three** typing states (`optb-typing`, `optb-typing-kb-short`, `optb-typing-kb-long`, the last two with the keyboard); **option A has none** — `list-new` is list creation, not add-task. So tapping `+ Add a task` in A leads somewhere nobody has drawn: **where the input appears, whether the pinned row jumps, what the keyboard pushes, and what the list keeps.** **The owner is being asked to choose between a fully drawn option and a half drawn one, on the interaction both options exist for.** Draw A`s typing state on iOS and Android with the keyboard reserved, at both list lengths, reusing T-316`s rule (the system indicator hides when the keyboard is up because it sits behind it). **Report the same numbers as B** — keyboard share, list share, rows visible at each length — so the two are comparable. **Also answer the question specific to A:** when the row is pinned and the user taps it, does the input stay pinned at the bottom, or does the list scroll to its natural place first? Those are different experiences and only one of them can be right. **Both A questions answered and drawn, and the important one came back in A`s favour — verified by the orchestrator, not taken on the return.** `opta-typing-kb-short/long` exist on iOS and Android. **The voice FAB is genuinely reachable above the keyboard** (`bottom: 312px`, sitting clear of it), so **A`s clearest advantage over B is real: you can switch to voice mid-sentence, where B has morphed the mic into the send arrow and the voice path is closed until the field is cleared.** Keyboard 38% on both platforms, matching B. **But the return`s single "List visible 50.2%" figure hides a 14-point spread, and the orchestrator`s own measurement found it:** A keeps **50% on a short list and only 36% on a long one** (iOS; 48%/34% Android), where B keeps **50% and 50%**. That is roughly two task rows lost, and it has a cause — see T-320. **Marked PARTIAL for that reason, not for the drawing**, which is otherwise complete. Web correctly got no typing state and the agent said so rather than inventing a keyboard.|
| T-320 | The same typing gesture renders two different layouts depending on list length | assistant | — | design-agent | P1 | T-319 | CANCELLED | — | **Found by the orchestrator measuring what the return reported as a single number. Option A only.** Measured on `app-shell-ios.html`: in `opta-typing-kb-short` the input and the voice FAB **sit on the same line** — both bottom at 1219, which is the keyboard`s top edge. In `opta-typing-kb-long` the input`s bottom is at **1103** while the FAB occupies **1167–1219**, so they are **not on the same line and 64px of dead space sits between them.** **The same interaction — tapping the add row and typing — produces two different layouts, decided by how many tasks the user happens to have.** The cause is that the two controls are anchored to different things: the FAB is `absolute` against the keyboard, the input is in flow / sticky against the scroller, and the two coincide only when the list is short enough. **This is what costs A the list space:** it keeps **50% of the frame on a short list and 36% on a long one** (48%/34% on Android) where B keeps 50% in both — about two task rows, spent on a gap. **It also weakens the return`s stated answer to "does the input stay pinned":** in the long state the input sits 116px above the keyboard, not against it. **Decide one anchor for both controls and use it at every list length.** Re-report the list share at both lengths afterwards, since that number is what the owner is comparing against B. **CANCELLED 2026-08-23: the owner chose option B, so option A`s two-anchor disagreement is no longer anything anyone will see.** The finding stands as recorded — it was real, and it was the measurement that exposed A costing 14 points of list space on a long list — but there is nothing left to fix. **Not deleted**, so the reason A lost is legible later. |
| T-322 | Two ACs describe the control that option B just moved | assistant | F-001 | spec-agent | P1 | T-306 | DONE | `docs/specs/assistant/F-001-voice-assistant-view.md`, `docs/specs/assistant/index.md` | **The owner chose option B on 2026-08-23 — one fixed bottom bar holding the field and the voice button, the mic becoming a send arrow while text is present. Two ACs were written against the shape B replaces, and spec decides this, not design.** **AC-37** (revision 9, written this morning) says *"**The floating voice button (`assistant-voice-fab`) navigates to the Talk surface**; it does not start microphone capture"* and that below the split breakpoint it is a navigation control with the accessible name **"Talk"**. **In B the control neither floats nor carries that id** — it sits inside the bar and the drawing calls it `tasks-bar-action`. **The guarantee AC-37 exists for is unchanged and must survive verbatim: this control navigates, it does not capture, and it is named "Talk".** What needs restating is only where it lives and what it is called — plus the new fact that **its accessible name changes to something like "Add task" while the field holds text**, which is a name change on a control and therefore squarely AC-37`s business (it is the AC that exists because a name once described an action the control did not perform). **AC-36** records *"there is no visible add-task affordance above the fold at `split` and above, as an accepted consequence"* — **B puts a permanent bar there, so check whether that consequence still holds** and say so either way rather than leaving the AC contradicted by a drawing. **Do not renumber or delete anything**; this spec`s revision history is emphatic about that. Related and already filed: T-317 (typing sets one field where speaking sets seven) — same surface, and worth reading before amending AC-37. **F-001 revision 10. Verified by the orchestrator: 37 ACs before, 37 after — nothing added, renumbered or deleted**, which is what this spec`s revision history demands in almost every entry. `declared-elements.sh` exit 0. **AC-37 keeps its guarantee verbatim** — *"it does not start microphone capture in either state"* — and now carries both identities: **"Talk"** when the field is empty and the button navigates, **"Add task"** when it holds text and the button submits. **It also writes down the rule rather than the instance:** each name *"would be a defect in the other`s state, and a name that does not track the function is the defect revision 9 was written to prevent"* — so the next control that changes function has a rule to obey instead of a precedent to copy. Morph trigger stated as **first character in / last character out, not focus or blur**, carried up from the design entry and flagged as self-decided so the owner can move it. **AC-36 confirmed still true rather than assumed** — the bar shows only below `split`. **Eight further ACs were considered and left alone, each named** (AC-1, AC-18, AC-19, AC-24, AC-25, AC-29, AC-30, and OQ9), which is the half that makes "no drift" checkable. **On whether T-317 should have gated this, it argued no and was right:** blocking a factual correction — the FAB no longer exists — on an open product question would leave the spec contradicting the drawing. But it raised T-317`s urgency for a concrete reason: **B puts the typed path permanently above the fold**, where A had it at the bottom of a scroller. |
| T-321 | Promote option B from an alternative to the design | assistant | — | design-agent | P1 | — | DONE | `docs/design/assistant/screens/app-shell.html`, `docs/design/assistant/screens/app-shell-ios.html`, `docs/design/assistant/screens/app-shell-android.html`, `docs/design/_shared/components.md` | **The owner chose B. The drawing exists; what is missing is making it canonical.** Today the `optb-*` states sit beside the `tasks-*` ones as a comparison. Retire A: `.inline-new` appears **4 times in the web shell and 8 times in each phone shell**, and the Tasks-surface `.voice-fab` once per file. The `.task-bar` becomes the add affordance on the Tasks surface and the mic inside it becomes the route to Talk. **Restore the two testids T-315 removed** — `tasks-bar-input` and `tasks-bar-action` — to `components.md` **and** to the mockup attributes, since B is now chosen and the reason for holding them out is gone. Retire the `InlineAdd` catalogue entry in favour of `TaskBottomBar`, keeping A`s entry as retired vocabulary rather than deleting it, the way `motion.rule` retires the aurora band. **The keyboard states become the canonical typing states.** **Blocked on T-322**: AC-37 describes the control as *"the floating voice button (`assistant-voice-fab`)"* and B`s neither floats nor keeps that id, so the spec must say what the control is before the drawing renames it. **Expect both client contract tests to go red the moment the testids return** — that is correct and it is what the client work then closes. **B is canonical. The FAB is gone (1 → 0 per shell, verified) and the testids are back; design-check 202/0/4, catalogue reconciled at 148.** **The return understated its own work and the orchestrator nearly filed a defect that does not exist.** It reported A as retired without the qualifier, and **skipped the success criterion that asked for grep counts before and after** — `.inline-new` is unchanged at 4 in the web shell and 8 in each phone shell, which reads as the retirement half-done. **Measured instead of assumed: it is visible in 1 of 60 states, `wide-tasks`, and `components.md:501` states the rule plainly** — *"Retired below `breakpoints.split` … At or above split, InlineAdd remains the add-task mechanism"*, because the bar is hidden there. **So the design is coherent and it is the report that was thin.** The lesson is the criterion, not the agent: a count asked for and not given is where a correct piece of work reads as a wrong one. **Three client contract tests are red on purpose and were reported as expected, with counts** — 1 web (catalogue 77 vs 76) and 2 mobile (`assistant-voice-fab` still in `SHELL_A11Y_IDS` but drawn nowhere). **No exemption was added to either check**, which the briefing forbade and which is the discipline that kept the last round honest. **One instruction for the client work, from its `a11y_review`:** four `.inline-new` elements stay focusable while `display:none` below split. In a static mockup that is unavoidable; **in the clients it is not, and they must keep those elements out of the tab order below split** — a keyboard user tabbing into an invisible control is exactly the class of defect this project keeps finding by hand.|
| T-323 | F-009 declares five api ACs and the queue has no backend row for any of them | assistant | F-009 | orchestrator | P1 | — | PENDING | — | **Surfaced by qa-api-agent explaining why it would not derive `WIRE_FIELDS` from the contract: `api-contracts.md:549` declares `sort_order` on the wire and the backend emits it nowhere** — absent from `serialize.ts` and from `types.ts`. Checked, and it is not drift: **`sort_order` is F-009 AC-5, tagged `(api, web, mobile)`, and simply unbuilt.** **The gap is in the queue, not the code.** F-009 has **16 ACs, 5 of them `(api)`**, and its six queue rows are four design (DONE), one mobile (T-285) and one web (T-288) — **no backend row exists.** So T-288 (*"Build Search, sort, hide completed and select on web"*) is scheduled to build a web surface over api ACs nobody has been asked to implement. **File the backend row and put T-288 behind it**, the way T-287 sat behind T-286`s contracts. **Also worth noting where this was caught:** TC-33a exists to catch exactly this kind of contract-versus-implementation divergence, and it could not, because its reference is a hand-copy of the contract rather than the contract. The copy and the code drifted in the same direction and cancelled out. |
| T-324 | Build the TaskBottomBar on web | assistant | F-001 | web-agent | P1 | T-321 | DONE | `src/assistant/web/components/TasksSurface.tsx`, `src/assistant/web/App.tsx`, `src/assistant/web/components/Chrome.tsx`, `src/assistant/web/styles.css`, `src/assistant/web/__tests__/app.test.tsx`, `src/assistant/web/__tests__/shell.test.tsx`, `src/assistant/web/__tests__/task-detail.test.tsx` | **The owner chose option B and the design is canonical. One web contract test is red on purpose until this lands:** `src/assistant/web/__tests__/app.test.tsx` reports catalogue size 77 against an expected 76 — the mockups now declare `tasks-bar-input` and `tasks-bar-action` and no longer declare `assistant-voice-fab`. **Retire the FAB in `Chrome.tsx:84`** and build the bar: a fixed bottom row below `breakpoints.split` holding the field and one morphing button. **`F-001 AC-37` (revision 10) is the contract and it is precise:** the button navigates to Talk with accessible name **"Talk"** when the field is empty, submits the typed title with accessible name **"Add task"** when it holds text, **and does not start microphone capture in either state.** The morph triggers on **the first character entering or the last character leaving the field — not on focus or blur.** **Typing commits through the existing literal path** (`controller.addTask`, title verbatim), not through `send()`. **At or above split the bar is hidden and `InlineAdd` remains the add mechanism** (`components.md:501`) — do not delete it. **And keep hidden `.inline-new` out of the tab order below split**: the mockup cannot manage tabindex, the client can, and a keyboard user reaching an invisible control is a real defect. **Web 370/370, tsc exit 0, and the contract test that was red on purpose is green — catalogue 76 → 77, built 42 → 43.** The FAB is gone from `App.tsx` and `Chrome.tsx`; three test files that reached for `assistant-voice-fab` now reach for `tasks-bar-action`. **It wrote the four tests that matter and wrote them as the rule, not the instance:** the accessible name tracking the function **in both directions** (empty→text and text→empty), plus navigation and submit-then-clear. A test that only checked one direction would pass on a control that never morphs back. **It reported the remaining red as "pre-existing" and it is not — second time today, after the briefing warned about this exact word.** Checked rather than accepted: `git stash` the mobile tree and `f005-mobile.test.ts` passes **34/34**, so the failure is mobile-agent`s in-flight work, running in parallel. **Its attribution ("mobile-agent`s domain") was right and its label was wrong**, which is the milder version of the same failure — but "pre-existing" closes an investigation and "another agent is writing right now" opens one. **The briefing warned in words and the words did not hold; this belongs in the defects file as a case for a check rather than a reminder.**|
| T-325 | Build the TaskBottomBar on mobile | assistant | F-001 | mobile-agent | P1 | T-321 | DONE | `src/assistant/mobile/components/TaskBottomBar.tsx`, `src/assistant/mobile/__tests__/task-bottom-bar.test.ts`, `src/assistant/mobile/model/a11y.ts`, `src/assistant/mobile/model/touch.ts`, `src/assistant/mobile/components/styles.ts`, `src/assistant/mobile/components/TasksSurface.tsx`, `src/assistant/mobile/components/TaskList.tsx`, `src/assistant/mobile/components/PathSwitch.tsx`, `src/assistant/mobile/__tests__/f005-mobile.test.ts` | **Mobile half of T-324; same contract, same design.** Two contract tests are red on purpose until this lands: `src/assistant/mobile/__tests__/a11y.test.ts` fails twice because **`assistant-voice-fab` is still in `SHELL_A11Y_IDS` but is drawn in no mockup.** Retire `VoiceFab.tsx` and add the bar`s two ids with the accessible names `F-001 AC-37` specifies. **The morphing button`s name must change with its function** — "Talk" when empty, "Add task" when the field holds text — which is the rule AC-37 states rather than the instance: *"a name that does not track the function is the defect revision 9 was written to prevent."* **T-285 built option A`s InlineAdd on mobile and it stays** — it is the add mechanism at split+ — so this retires the FAB, not the inline row. Typing commits through the literal add path, not the turn path. **Suite 1321/1321 and `tsc --noEmit` exit 0, both verified by the orchestrator. Both mobile contract failures cleared; `VoiceFab.tsx` deleted.** Six new tests for the morph contract, and it carried the change through `a11y.ts`, `touch.ts` and `styles.ts` rather than only the component — the id lives in three registries and missing one is how a control half-exists. **Its one self-decision is sound and it documented the reason in the code rather than only in the return:** InlineAdd is no longer *rendered* on mobile, but the component is kept — `TaskList.tsx:461` says *"still exists for split+ (tablet/desktop) reuse; it is simply not rendered here."* Rendering both would give the surface two add mechanisms. **This also means T-285`s work is not deleted, only unmounted**, which is the reversible version of retiring it. **One thing the orchestrator checked and it is not this task`s defect — see T-326:** the bar renders unconditionally, with no split check, so at `breakpoints.split` (1024) and above a large tablet would show it, which `AC-36` says should not happen. But the mobile client **has no width branching anywhere at all**, so this is not introduced here; it is that the split branch has never existed on mobile.|
| T-326 | The mobile client has no breakpoint branching at all, and AC-36 assumes one | assistant | F-001 | spec-agent | P2 | T-325 | PENDING | — | **Found while checking a mobile self-decision, and it is older and wider than that decision.** `AC-36` records *"there is no visible add-task affordance above the fold at `split` and above, as an accepted consequence"*, and `components.md:501` builds on it: below split the TaskBottomBar is the add mechanism, at split+ InlineAdd is. **The web client implements that branch. The mobile client implements no width branch whatsoever** — grepping `src/assistant/mobile/` for breakpoint logic turns up a string `split(",")` and comments, nothing else — so `TaskBottomBar` renders at every width and `InlineAdd` at none. `breakpoints.split` is **1024** while `tablet` is 768, so an iPad in landscape is comfortably above it. **The question is not the bar; it is whether the mobile client targets tablets at all.** If it does, the split branch is unimplemented there and AC-36 is unmet on that device class — and the bar is the least of it, since a phone-shaped layout at 1366pt is a much larger problem. **If it does not, say so in the spec**, so the next person to read AC-36 does not assume a branch that was never intended. **This is spec`s to answer before it is anyone`s to build.** |
| T-327 | UAT document: every drawn state, three platforms, ordered by flow | assistant | — | orchestrator | P1 | — | DONE | `docs/reports/uat/design-mockups.html`, `docs/reports/uat/build-uat.sh`, `docs/reports/uat/capture.mjs`, `docs/reports/uat/compress.py`, `docs/reports/uat/render.py`, `docs/reports/uat/flows.json`, `docs/reports/uat/style.css`, `docs/reports/uat/app.js` | **Owner asked for a UAT artifact organised by e2e flow rather than by screen, with special and negative states folded into the flow they belong to.** Built directly rather than dispatched: `{reports}/` is orchestrator-owned. **109 states captured across web, iOS and Android — 324 screenshots, 22MB in `docs/reports/uat/shots/`** — arranged as **13 flows**, each running its happy path in order and then its own failure and edge states beneath it. **The mapping was verified as data, not by eye:** the generator reports orphaned states and flows pointing at states that do not exist, and both came back **0** — which matters because a UAT document that silently drops a state is worse than one that never claimed coverage. **Three cells are labelled "không áp dụng" with a reason** (web has no on-screen keyboard ×2; swipe is a touch gesture ×1) rather than left blank: a blank cell reads as a missing drawing and gets filed as a bug. **It is an instrument, not a gallery** — per-step Đạt/Lỗi/Bỏ qua with a note, persisted in the viewer`s own browser storage, exported as text on demand; platform columns toggle; images enlarge. Verified by rendering: 13 flows, 109 steps, 324 images, 0 console errors, 0 broken images (the one that first reported broken was a lazy-load artifact and was re-checked with loading forced eager). **Built from the mockups as they stand today, which includes option B on the Tasks surface** — so it will need regenerating when the drawings move. **REBUILT 2026-08-23 on owner feedback — two faults, both the orchestrator`s.** **(1) Every image was broken when the file was opened.** It referenced a sibling `shots/` folder, and the orchestrator **said so in the delivery message and delivered it that way regardless** — naming a defect does not remove it. Now one self-contained file with WebP embedded; verified by copying it alone into an empty directory and rendering: **324 images, 0 broken, 0 console errors.** **(2) It read as a catalogue, not as flows.** The sidebar is gone; the thirteen flows are on the page at open, each a horizontal strip read left to right with arrows, its own failure and edge states directly beneath. **The change that mattered most was showing one platform at a time**, switched once for the whole page — three abreast at every step is what destroyed the sense of a sequence. Web is encoded at 760px so it stays readable when enlarged; **a lightbox holding only the thumbnail is a lightbox that does not help.** **Built as a script rather than by hand, which was not asked for and is the part that keeps it alive:** `bash docs/reports/uat/build-uat.sh` re-captures, compresses and regenerates in one command. **It reads states from the mockups` own `data-s` buttons**, so a state added to a drawing appears without anyone remembering to list it — the flow map in `flows.json` is the only hand-written part. **It refuses to finish if a captured state belongs to no flow or a flow names a state that does not exist, and that guard was verified by breaking it:** removing one step from `flows.json` stopped the build with `CHƯA XẾP FLOW: app-shell/tasks-empty-first`. **The 22MB of intermediate PNGs are not committed** — the mockups are the source and the script is the path. 10.0 MB, 109 steps, 13 flows.|
| T-328 | Build Lists on mobile | assistant | F-008 | mobile-agent | P1 | — | PENDING | — | **Mobile half of T-289, split out because that row said "web and mobile" and would have been dispatched to a web agent.** Backend landed with T-287, so nothing blocks it. **7 ids in `NOT_BUILT`** — `menu-list-row`, `menu-new-list-button`, `menu-retry-button`, `list-editor-name-input`, `list-editor-create-button`, `list-editor-cancel-button`, `list-editor-color-swatch`. **Drawn already** in `lists-ios.html` and `lists-android.html`: rename, recolour, delete with the count-naming confirmation, reposition by Move up / Move down. **The assistant may create a list and file tasks by voice and may NOT rename, recolour or delete one** (F-008 permissions). **Colour is a dot on the row, never a row background** — `DESIGN.md` carries the rule and the reason the owner approved it. Route to the drawer, not a swipe: inside the drawer a swipe collides with its own dismiss gesture. |
| T-329 | Build Search, sort, hide completed and select on mobile | assistant | F-009 | mobile-agent | P1 | T-328 | PENDING | — | **The missing half: `T-288` says "on web" and no mobile row existed, which is how 20 ids land on one client and not the other** — the exact divergence that produced yesterday's header mismatch. Six components: SearchField, OverflowMenu, SelectMode, BulkToolbar, ConfirmDialog, sort controls. **`shell-search-button` and `shell-overflow-button` already render in the mobile bar with nothing behind them.** Depends on T-328 because *Select → move to list* needs Lists. **Read the web implementation and match it rather than choosing a second answer** — that is the rule this row exists to enforce. |
| T-330 | The clarify chips stack out of their bubble on Android | assistant | F-003 | mobile-agent | P1 | — | IN_PROGRESS | — | **Owner found it in the acceptance book, M4 step 2.** When two tasks match, the question bubble holds `2 tasks match — which one?` and two chips. On Android the second chip **falls outside the bubble** and overlaps the timestamp below it, clipping the `PM`. The bubble sizes itself for the question and one chip. **The deeper half: the two clients lay the chips out on different axes.** Measured on iOS at the same state, both chips sit at `y=288` — side by side. Android stacks them vertically. **One of the two disagrees with the design, and neither was checked against it** — `docs/design/assistant/screens/voice-assistant-view*.html` has no `.chips` rule under that name, so start by finding what the mockup actually calls this row and what it draws. Evidence: `output/app-shots/mobile/android-talk-question-clarify.png`. |
| T-331 | Three iOS capture states produce one identical frame | assistant | — | mobile-agent | P1 | — | IN_PROGRESS | — | **Not an app defect — a capture defect, and the most dangerous kind this book can hold.** `ios-talk-question-confirm`, `ios-talk-applied-delete` and `ios-talk-reverted` are byte-identical (`md5 6f97aa9e…`). All three scenarios open with *delete the shopping tasks*; `applied-delete` must then tap the confirm chip and `reverted` must then tap Undo. **Neither tap fires on iOS**, so all three stop at the confirm question — and the book presents that one frame under three different captions. **A reviewer ticks pass on an undo they never saw.** Android produces three distinct frames from the same scenarios, so the fault is in the iOS path of `App.tsx`'s `play()` — `s.tapChip(0)` and `s.tapUndo()`. **Duplicate detection is now in `shoot-mobile.sh`** and catches it; the underlying taps still need fixing. |
| T-332 | Rewrite the specs that assume Talk is a peer surface | assistant | F-001 | spec-agent | P0 | — | PENDING | — | **Owner decision 2026-08-24: the task list is home at every width and Talk is summoned over it.** Model written in `information-architecture.md §4`; the superseded map is kept at `§4a`. **F-001 is built on the old assumption and has to move first, because the clients implement it:** `AC-24`'s reachability clause counts actions to reach the list from Talk — a count that means something different when the list is behind the overlay rather than beside it; the carried-notice rules say *appears on S1 and S2*, which are no longer peers; and the Talk landing copy is the app's first screen in the spec and will not be. **Two things the owner settled when deciding, so do not re-open them:** conversation history, session boundaries and carried notices are **unchanged — only the presentation moves**; and the landing copy that taught a first-time user they could speak is **not owed a new home, because there are no users yet.** |
| T-333 | Web: the list is home at narrow width too | assistant | F-001 | web-agent | P0 | T-332 | PENDING | — | **Wide is already correct and this task must not touch it:** the list is the centre and Talk is the right panel, per `owner-decision-2026-08-17-desktop-list-is-primary.md`. **Talk does NOT become an overlay at desktop width** — the model is one relationship drawn two ways, not one drawing. Narrow: Talk covers the list instead of replacing it as a surface. **The mic is the only way in; the `Talk` text button leaves the top bar** — two controls for one relationship is what made the old pair asymmetric, a text button one way and a floating mic the other. **Left of the bar is what changes where you are, right is what acts on what you see** (`§4`), so the collection picker moves left and search + overflow stay right. Back and Escape dismiss the overlay to the list. |
| T-334 | Mobile: the list is home and Talk is summoned | assistant | F-003 | mobile-agent | P0 | T-332 | PENDING | — | Mobile half of T-333 — **read that implementation and match it rather than choosing a second answer.** The phone is where the change is largest: Talk is the app's opening screen today and becomes an overlay over the list. **`shell-tasks-button` disappears** — there is no surface to switch to. The mic in the bottom bar summons Talk; close or system back dismisses it. **Check what system back does at each level**, since the collection is a state and must not unwind: back from Talk goes to the list, back from the list exits, and picking Inbox then pressing back does **not** return to Today. |
