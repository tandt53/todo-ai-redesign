# BRIEFING — T-275 + T-271 (two design-check blind spots)

- **Task IDs:** T-275, T-271
- **Module:** assistant · **Feature:** — · **Agent:** design-agent · **Date:** 2026-08-23
- **Scope:** `.claude/tools/design-check/` ONLY. Do not touch `docs/design/` or `src/` in this dispatch —
  three other agents are in flight on those trees right now.

## Context

`design-check` has now reported green over broken output three times today:
1. the render tier skipping silently and printing `13 passed, 0 failed`;
2. catalogue assertions scoped to the app shell, so a control drawn on any other surface is unchecked;
3. **today's live case** — `--dur-breath` was consumed 7 times and declared 0 times, seven CSS
   `animation:` declarations were invalid at computed-value time and the loops had stopped, and
   `design-check` reported **202 passed, 0 failed**.

Three checks were added earlier today (`token-literal` among them) and each was built the same way:
**break it first, watch it go red, restore, watch it go green.** Build these two the same way.

## T-275 — a CSS variable that is used but never defined

Assertion: **every `var(--x)` reference in a stylesheet resolves to a declaration in that same
stylesheet or in `:root`.** Pure text check, no browser needed. It would have caught the
`--dur-breath` deletion the moment it happened.

Prove it: delete a variable declaration in `src/assistant/web/styles.css`, run the check, confirm it
goes red and names the variable and the consuming line numbers; restore; confirm green.

Note `--dur-breath` is *currently* undefined and that is a real open defect owned by T-274 (blocked on
the owner). **Do not fix it here** — but your new check must go red on it today. If it does not, the
check does not work. If a temporary allowlist is the only way to land the check green, do not use one:
report the red as the check's first true finding.

## T-271 — a testid drawn in a mockup but absent from the catalogue

`testid-contract.sh` compares **mockup vs implementation** only. Nothing compares **mockup vs the
`components.md` catalogue**, so an id can be drawn and never declared. Live example: `detail-copy-button`
was added to three mockups by the T-265 fix and declared nowhere until an agent noticed by hand.

The catalogue assertions in `a11y.test.ts` are scoped to the APP SHELL, so a control drawn on any other
surface is unchecked in both directions. Your check must cover **every** mockup in
`docs/design/assistant/screens/`, not just the shells.

Prove it: remove `detail-copy-button` from `components.md`, run, confirm red; restore, confirm green.

## Read these files first

- `.claude/tools/design-check/check-design.mjs`
- `.claude/tools/design-check/run-design-check.sh`
- `.claude/tools/design-check/testid-contract.sh`
- `docs/design/_shared/components.md` (the catalogue — its shape decides how T-271 parses it)
- `src/assistant/web/styles.css` (the T-275 subject)
- `CLAUDE.md ## Known Blockers` — how to run the check with the browser reachable

## Write to

- `.claude/tools/design-check/` (new check files and/or edits to `check-design.mjs`)

## Success criteria

- Both checks demonstrated red-then-green on the named bad cases, with the transcript in your return.
- Full run: `DESIGN_CHECK_BROWSER=/opt/pw-browsers/chromium-1194/chrome-linux/chrome bash .claude/tools/design-check/run-design-check.sh`
  Baseline before your change is **202 passed, 0 failed**. Report the exact new numbers.
- T-275 going red on `--dur-breath` today is the **expected and correct** result. Say so plainly rather
  than suppressing it.
- No files written outside `.claude/tools/design-check/`.
