{
  "$meta": {
    "system": "todo-ai redesign — visual language v2 (Swiss)",
    "replaces": "v1 'Aurora, voice-first' (2026-08-17 .. 2026-08-19). Full replacement of colour, type, spacing, shape and layout, decided in docs/reports/owner-decision-2026-08-21-redesign-the-visual-language.md. The 52 published element ids are UNCHANGED — see components.md § Testid catalogue.",
    "themes": ["light", "dark"],
    "default_theme": "light",
    "rule": "Every value an implementer uses comes from this file. Reviewer C4 fails hardcoded design values.",
    "contrast": "Every text pair below computed by the WCAG 2.1 relative-luminance formula and listed in DESIGN.md ## Contrast. Minimum 4.5:1 normal text, 3:1 for rules and control boundaries that carry meaning.",
    "accents": "THE ACCENT SET IS CLOSED AT THREE, down from five. Meanings live in DESIGN.md ## Colour rules and are not restated here (L-004). green/success and the cyan|violet voice pair are RETIRED — DESIGN.md ## Colour rules 5 records what replaced each and why. Adding a fourth accent is a system decision that arrives with its own meaning.",
    "specimen": "docs/design/_shared/specimen.html — the rendered proof of this file. Every token below appears there in use."
  },

  "color": {
    "light": {
      "bg":   { "base": "#FFFFFF", "sunken": "#F2F2F4", "raised": "#FFFFFF", "ink": "#111318", "hairline": "#DCDCE1", "rule": "#94949E", "scrim": "rgba(17, 19, 24, 0.32)" },
      "text": { "primary": "#111318", "secondary": "#54545E", "muted": "#646470", "onInk": "#FFFFFF", "onAccent": "#FFFFFF" },
      "accent": "#002FA7", "accentTint": "#E6EAF6",
      "danger": "#C8102E", "dangerTint": "#FBE9EC",
      "attention": "#9A4C00", "attentionTint": "#FBEFE2",
      "focusRing": "#002FA7"
    },
    "dark": {
      "bg":   { "base": "#0E0E11", "sunken": "#17171C", "raised": "#17171C", "ink": "#F4F4F6", "hairline": "#2E2E38", "rule": "#63636F", "scrim": "rgba(0, 0, 0, 0.56)" },
      "text": { "primary": "#F4F4F6", "secondary": "#A8A8B4", "muted": "#9296A3", "onInk": "#0E0E11", "onAccent": "#0E0E11" },
      "accent": "#8AA6FF", "accentTint": "#1A2140",
      "danger": "#FF7183", "dangerTint": "#2C1519",
      "attention": "#FFB020", "attentionTint": "#2B2010",
      "focusRing": "#8AA6FF"
    }
  },

  "font": {
    "family": {
      "ui": "Inter, -apple-system, BlinkMacSystemFont, 'Helvetica Neue', 'Segoe UI', Roboto, system-ui, sans-serif",
      "numeric": "ui-monospace, 'SF Mono', 'JetBrains Mono', 'Roboto Mono', Menlo, Consolas, monospace"
    },
    "family_note": "ONE UI family, plus one utility face for numerals. The anchor locks a single grotesque for display and body. Product copy is English (ADR-008), but a TASK TITLE IS USER CONTENT in whatever script the user types — the live store carries Vietnamese titles today — so the face that sets titles must hold at body size across heavy diacritics, which is where a display face chosen for English headings breaks first. Hierarchy is carried by size, weight, case and tracking — the Swiss method — not by a second voice. The numeric face is legal ONLY on times, dates, counts, durations and ids. Justification recorded in DESIGN.md ## Type.",
    "scale_ratio": 1.25,
    "size": { "label": 11, "meta": 13, "body": 16, "lead": 20, "title": 25, "display": 31, "hero": 39, "mega": 49 },
    "size_ios": { "label": 11, "meta": 13, "body": 17, "lead": 20, "title": 28, "display": 34, "hero": 40, "mega": 50 },
    "size_android": { "label": 11, "meta": 14, "body": 16, "lead": 20, "title": 24, "display": 32, "hero": 40, "mega": 48 },
    "size_note": "ONE scale, three floors. The ramp and its ratio are shared; iOS lifts body to 17 (its own text baseline, and what Dynamic Type scales from) and Android sets meta to 14 (M3 bodyMedium). Nothing else diverges — platform paint never wins over the design system.",
    "weight": { "regular": 400, "medium": 500, "semibold": 600, "bold": 700 },
    "lineHeight": { "tight": 1.15, "title": 1.25, "body": 1.5, "meta": 1.4 },
    "letterSpacing": { "mega": "-0.035em", "hero": "-0.03em", "display": "-0.025em", "title": "-0.015em", "body": "0", "label": "0.08em" },
    "case": { "label": "uppercase" },
    "numeric": "tabular-nums",
    "note": "Body line-height 1.5 minimum, and body never below 16 on any platform. Both floors are set for arbitrary user-typed script, not for English: below 16 iOS zooms the page on focus and stacked tone marks lose their separation from the letter. label 11 is uppercase, tracked, and is for section eyebrows and NEW/EDITED markers only."
  },

  "space": { "0": 0, "1": 4, "2": 8, "3": 12, "4": 16, "5": 24, "6": 32, "7": 48, "8": 64, "9": 96 },
  "space_note": "4-based. gutter_mobile = 4, gutter_web = 5, section = 7. No value outside this scale appears in a mockup.",

  "radius": { "none": 0, "bubble": 4, "pill": 999, "circle": "50%",
    "note": "The app has NO rounded corners. Rows, cards, inputs, buttons, sheets, menus and dialogs are all 0 — that is the single biggest shape change from v1 (which had bubble 18, sheet 20, md 14). Three exceptions, each because the shape is intrinsic to the control and not decoration: bubble 4 (a message is a spoken block, squared not pilled), pill (Android M3 switch track and the sheet drag handle), circle (the mic control and Android's FAB)." },

  "border": { "hairline": 1, "rule": 1, "focus": 2, "mark": 2,
    "note": "Structure is 1px rules, never shadow. hairline is decorative separation (color.bg.hairline); rule is a boundary that carries meaning and uses color.bg.rule, which clears 3:1. mark 2 is the accent left-rule on an assistant message that changed something." },

  "shadow": { "overlay": "0 12px 32px -12px rgba(17, 19, 24, 0.28)", "overlayDark": "0 12px 32px -12px rgba(0, 0, 0, 0.64)",
    "note": "ONE shadow, and it is legal only on a layer that floats over another — sheet, menu, dialog, toast. Rows, cards, buttons and inputs carry no shadow at any state. A shadow used for emphasis instead of separation is a review failure." },

  "control": {
    "height": { "sm": 36, "md": 44, "lg": 52 },
    "minTarget": { "web": 40, "ios": 44, "android": 48 },
    "padding_x": { "sm": 12, "md": 20, "lg": 24 },
    "note": "minTarget is a hit-area floor met with hitSlop / padding, never by painting a bigger control. Row height is lg (52) on every platform, which already clears all three floors."
  },

  "field": {
    "height": 44, "height_multiline_min": 96,
    "padding_x": 12, "padding_y": 10,
    "label_gap": 6, "help_gap": 6, "row_gap": 24,
    "min_rendered_width": 240,
    "rule": "A field has NO intrinsic width. It is width:100% of its form row, always. The HTML size attribute and any fixed px width on an input are forbidden. CHECKABLE: inside a form row wider than 320, any field rendering below min_rendered_width is a failure. This rule exists because the shipped task detail rendered 197px input stubs that clipped the task's own title — the class was used in JSX and styled nowhere, so the input fell back to the HTML default size=20."
  },

  "layout": {
    "grid": { "columns": 12, "gutter": 24, "gutter_mobile": 16 },
    "measure": { "text_ch": 68, "list_min": 560, "list_max": 820, "form_max": 640, "panel_min": 380, "panel_max": 420, "rail": 240, "timerail": { "compact": 72, "default": 96, "wide": 136 } },
    "frame_max": 2200,
    "margin_max": 96,
    "fill_min_ratio": 0.9,
    "wide_rule": "ABOVE desktop THE APP ADDS A COLUMN, NEVER A GUTTER. Three frame tiers, and the third is new in v2. TIER 1, below split: one surface on screen, the frame is the pane. TIER 2, split up to wide: Tasks pane takes the frame minus the Talk panel. TIER 3, wide and above: a permanent Lists rail (measure.rail) joins on the left and the Tasks pane takes what is left. In every tier the pane then applies content_column_rule, so a pane wider than its measure centres its content instead of anchoring it left. Past frame_max the whole frame centres.",
    "content_column_rule": "ONE CENTRING, AND IT IS AT THE CONTENT COLUMN. Each frame column (Lists rail, Tasks pane, Talk panel) holds exactly ONE content column, width min(pane minus two gutters, its measure token), CENTRED in its pane. Everything inside a content column is left-aligned and nothing inside it centres again. Leftover width is therefore always symmetric and always at the same level.",
    "fill_rule": "CHECKABLE, and it is the underflow test design-check has never had. For every frame column, measure the leftover on each side of its content column; if the two differ by more than eight pixels, FAIL. v1's .tasks-col was max-width 720 with no margin auto, so at ultra it left zero on one side and 780 on the other — that asymmetry is the defect, and it is what this check names. A capped measure with symmetric leftover is not a defect and must not be reported as one.",
    "coverage_rule": "CHECKABLE, second half. A frame column's content must cover at least fill_min_ratio of the column's inner width UNLESS the shortfall is explained by a declared measure token (measure.list_max, measure.form_max, measure.text_ch). A shortfall with no measure token behind it is a column that stopped growing for no stated reason.",
    "gap_rule": "CHECKABLE: two text spans in the same row are never adjacent. Minimum gap between any two of them is space.2. v1's priority mark computed margin-left 0 and the glyphs touched the last letter of the task title."
  },

  "breakpoints": { "mobile": 375, "phone": 390, "tablet": 768, "split": 1024, "desktop": 1280, "laptop": 1440, "wide": 1536, "ultra": 1920,
    "note": "Every width in this block is MEASURED by design-check. split and wide are the two widths at which layout BRANCHES; the rest are measurement stops. The previous system declared four and stopped at desktop, which is why its worst defect was invisible to every check and present in its own mockup. mobile and phone are both here because the floor and the real device differ. KEEP THIS NOTE FREE OF DIGITS, INCLUDING VERSION NUMBERS: design-check strips non-digits from every value in this block, so writing the previous system as a v-plus-digit yields a phantom breakpoint measured at one pixel. That is not hypothetical; it happened while this file was being written. WHY wide SITS WHERE IT DOES: it is the narrowest frame at which the Lists rail can arrive without the Tasks list getting narrower than it was one breakpoint earlier — rail plus list_max plus two gutters plus panel_max. Put the rail in at laptop instead and a user who maximises their window watches the list shrink, which is a regression dressed as a feature." },

  "motion": {
    "duration_ms": { "micro": 100, "standard": 180, "emphasis": 280, "rulegrow": 900 },
    "easing": { "standard": "cubic-bezier(0.2, 0, 0, 1)", "exit": "cubic-bezier(0.4, 0, 1, 1)", "linear": "linear" },
    "rule": "animate transform and opacity only. NO spring, NO bounce, NO breathing loop — v1 had a 2400ms aurora loop and a spring curve; both are retired with the gradient. The one continuous animation left is the listening rule, which grows linearly along the composer while the microphone is live and is the only thing on screen that moves without the user acting.",
    "reducedMotion": "every animation collapses to an 80ms opacity change; end states (strikethrough, marks, the listening rule at full width) are fully kept."
  },

  "icon": { "set": "lucide", "stroke": 1.5, "cap": "square", "size": { "sm": 16, "md": 20, "lg": 24 },
    "note": "stroke 1.5 and square caps, down from v1's 1.8 round — the icons sit against 1px rules and a rounder heavier stroke reads as a different system. No emoji as UI icons; no unicode glyph standing in for an icon." },

  "haptic": {
    "note": "TARGET, not current behaviour — carried unchanged from v1 with its warning intact: no expo-haptics is in the build, so nothing below fires today. It binds whoever adds the first mobile animation, in the same change.",
    "listenStart": "impact.light", "listenEnd": "impact.light x2",
    "applied": "notification.success", "error": "notification.warning",
    "undo": "impact.medium", "optionTap": "selection"
  }
}
