# Project Learnings

<!--
Durable, human-curated insights about this codebase. Agents read this on startup
to avoid re-deriving lessons the pipeline has already paid for.

OWNERSHIP:
- reviewer-agent APPENDS entries when a C1–C14 failure reveals a pattern worth remembering
  (e.g., a bug with a recurring shape, a contract-drift class, a test anti-pattern).
- Humans CURATE: prune outdated entries, promote recurring ones into standards docs,
  correct framing.
- Spec-agent, architect-agent, and QA agents READ — they do not write.

SCOPE: entries should be general enough to guide future work, specific enough to act on.
"Be careful with async" is too vague. "In Next.js 14, page props `params` is a Promise and
must be awaited before destructuring" is actionable.
-->

## Format

Each entry is a level-2 heading (`## L-NNN — short title`) with:

- **Date added** — YYYY-MM-DD by {agent or human}
- **Trigger** — what prompted this lesson? (bug ID, review fail, task ID, spec revision)
- **Pattern** — the lesson, one paragraph. Phrase as *what went wrong* or *what works*.
- **How to apply** — concrete guidance for future work. Name files/functions/conventions.
- **Scope** — file globs or modules where this applies (or `project-wide`)
- **Stale check** — when should this be revisited? (specific date or `permanent`)

Entries are **append-only by agents**; humans may edit or delete.

---

## Entries

<!-- Agents append new entries below this line. Humans may reorder or prune. -->

### L-001 — C12's mutation tool destroys untracked files; back up before running it

> **RESOLVED 2026-08-17 (orchestrator).** The repo now has a baseline commit
> (`b83d9c6`), so `git checkout --` restores mutated files and
> `suite-can-fail.sh` can no longer leave source corrupted while reporting PASS.
> Verified by mutating `permissions.ts` and restoring it. The entry stays as the
> record of four consecutive reviews where a manual restore was the only thing
> preventing shipped mutations — and as the reason a fresh project should be
> committed before its first C12 run.

- **Date added** — 2026-08-16 by reviewer-agent
- **Trigger** — T-008, F-001 structural review. The C12 run mutated `src/assistant/api/errors.ts` and left it mutated on disk.
- **Pattern** — `.claude/tools/test-quality/suite-can-fail.sh` guards the working tree with `git diff --quiet` and restores with `git checkout -- <file>`. Both assume the target files are **tracked**. For an untracked file `git diff --quiet` reports clean (so the dirty-tree guard waves it through) and `git checkout --` cannot restore it (so the mutation is permanent). In a project whose first commit has not landed — which is every project during its first feature — the tool silently corrupts the implementation it was asked to evaluate. The check reports PASS while leaving broken code behind, which is worse than reporting nothing.
- **How to apply** — Before running C12 in a repo where `git status --porcelain` shows `??` for the implementation files: copy the source tree to a scratch directory, record `find <src> -type f -exec cksum {} \;` sorted to a manifest, run the tool, then diff the manifest and restore from the copy on any difference. Re-run the suite after restoring to confirm green. The durable fix is to make the tool's dirty-check use `git ls-files --error-unmatch` (or `git status --porcelain`) so untracked files trip the guard instead of slipping past it.
- **Scope** — project-wide; any C12 dispatch before the repo has a baseline commit
- **Stale check** — revisit once `suite-can-fail.sh` guards untracked files, or once this repo has a baseline commit

### L-002 — Tool verdicts that grep source text are evidence, not proof — check the rendered/executed observable

- **Date added** — 2026-08-16 by reviewer-agent
- **Trigger** — T-008, F-001 structural review. C14 reported 3 of 22 testids missing; all 3 were present. A first C12 web run reported FAIL having mutated only config files.
- **Pattern** — Two of the three tool-driven checks in this review returned a wrong verdict for the same underlying reason: they inspect **source text** and reason about it line by line, while the property they claim to check is a **runtime** one. `testid-contract.sh` matches `data-testid=` with a single-line regex, so an attribute computed by a multi-line JSX ternary (`data-testid={ cond ? 'a' : 'b' }`) is invisible and reads as absent. `suite-can-fail.sh` iterates files in `find` order with a 4-attempt budget, so a layer whose entry point, test seams, and build config sort first exhausts the budget on code no unit test covers and reports the suite cannot fail. Both failures point away from the real state of the code, and both are the *confident* kind of wrong — a specific list of named testids, a specific count of surviving mutations.
- **How to apply** — When a text-scanning tool reports a failure, confirm it against an execution observable before writing it into a report. For testids: render the component and query the DOM (`src/assistant/web/__tests__/app.test.tsx:310` does exactly this — parse the mockup catalogue, render every state, assert `missing` and `invented` are both empty; prefer this over the grep tool). For mutation checks: pass the behavioural core explicitly via `--files` (reducers, controllers, message builders, components) rather than a whole directory, and treat mutations landing in `main.*`, `*.config.*`, or seam/bootstrap files as inert rather than as findings — the tool's own header says a mutation that changes no behaviour does not count. Record which observable settled it, so the next reviewer inherits the resolution rather than the tool's verdict.
- **Scope** — project-wide; `.claude/tools/design-check/testid-contract.sh`, `.claude/tools/test-quality/suite-can-fail.sh`
- **Stale check** — permanent (the class of failure outlives either specific tool)

### L-003 — A test file that was never executed still counts as coverage everywhere except reality

- **Date added** — 2026-08-16 by reviewer-agent
- **Trigger** — T-008e, F-001 structural re-verify. Both automation suites named in `Links.tested_by` had never run: the api spec failed 46/46 on `require('../../src/assistant/api/store/memory')` (real path `store/memory-store.ts`, real export `MemoryStore`, and `require` in an ESM package), and the e2e spec collected 0 tests because `@playwright/test` was never installed and no Playwright config exists. The previous review pass reported C5 PASS on "167 passed".
- **Pattern** — The unit tiers and the automation tier were verified by *different commands*, and only the unit commands were ever run. `docs/specs/_shared/platform/*.md ## Test Harness` names `vitest run src/assistant/{api,web}` — both green, both honest, both scoped to `src/**`. Nothing in that harness contract reaches `docs/qa/**`, so an automation file can be authored, listed in `Links.tested_by`, counted by a coverage matrix, and cited in 72 TC markdown files while never once being imported. The failure is invisible from every direction that matters: C1 sees a non-empty path that exists, C2 sees P1 TCs that cite the right ACs, and C5 sees a green suite — because C5 was pointed at the other tier. The tell was `package.json`'s own aggregate command: `npm run test:all` (`vitest run`, no path) was red at `46 failed | 167 passed`, and nobody had run it.
- **How to apply** — For C5, run the platform-doc commands **and** every executable artifact named in `Links.tested_by`, separately, and report them as separate numbers. A `tested_by` entry pointing at a `.spec.ts`/`.test.ts` is a claim that the file runs; verify it by running it, not by confirming it exists and is non-empty. Run the project's own aggregate command (`test:all` or equivalent) at least once — it is the cheapest way to find a tier nobody is executing. When a QA agent authors automation against a layout it has not imported (watch for comments like "per backend.md store/ layout; align imports"), treat that as unrun until proven otherwise. Corollary: an AC whose only assertion lives in a dead suite is uncovered in practice — F-001's `undone: true` on the idempotent-replay path survived a C12 mutation for exactly this reason.
- **Scope** — project-wide; any feature whose `Links.tested_by` names files outside the platform docs' test-harness paths
- **Stale check** — revisit once C5's procedure requires executing `Links.tested_by` entries explicitly, or once a single test command covers every declared tier

### L-004 — A "single source of truth" that lives in two files announces its drift as a workaround, not as a failure

- **Date added** — 2026-08-16 by reviewer-agent
- **Trigger** — T-012, F-001 structural re-verify (pass 3). The canonical utterance→intent fixture table exists as `docs/qa/assistant/F-001/api/utterance-intent-fixtures.json` (24 rows) *and* `src/assistant/api/ports/fixture-table.ts` (23 rows); the QA copy declares `UT-DELETE-BULK-2` and the implementation copy does not.
- **Pattern** — The spec's Test strategy declares **one** table "shared by QA and implementers", and MANIFEST `## Ownership` exists precisely to stop a fact being copied between files. But the table has to be *data* for QA to read and *code* for the server to execute, so it was written twice, and the copies diverged by one row. The tell was not a red test. Both automation tiers hit the missing row, and both **independently invented a local replacement** — the web harness via `QA_EXTRA_ROWS` (`qa-test-server.ts:41`) and the api suite via an inline stub branch (`spec.ts:174`) — each using the extension mechanism the spec sanctions, each documented, each green. The suites pass, the coverage matrix is satisfied, and nothing anywhere reports that the shared artifact is no longer shared. Two agents solving the same missing precondition in two different places, both correctly, is the signature: when a divergence shows up as duplicated workarounds instead of a failure, the thing that diverged was a duplicated source of truth.
- **How to apply** — When a spec declares an artifact canonical and "shared" across roles, check early whether it is physically one file. If it cannot be (data on one side, executable on the other), make one the generated or imported product of the other rather than a hand-kept twin, and say so in `MANIFEST ## Ownership`. When reviewing, treat *any* `EXTRA_ROWS` / `extend the fixtures` / `local override` construct as a question to ask upstream, not as the accepted design it is documented to be — it is cheap to add and therefore records a gap silently. Note the reviewer-side limit that let this survive three passes: C9 sweeps endpoints against api-contracts.md and entities against data-model.md, so an artifact that is neither is outside every numbered check even when it is named canonical in the spec.
- **Scope** — project-wide; any artifact a spec calls canonical and shared that exists in more than one file. Currently: the F-001 fixture table.
- **Stale check** — revisit once the fixture table has one physical home (or a generated second copy), or once a check covers spec-declared canonical artifacts that are neither endpoints nor data-model entities

### L-005 — A rule enforced by one entry point and merely intended by another is the shape both F-003 bugs took

- **Date added** — 2026-08-17 by reviewer-agent
- **Trigger** — T-022, F-003 structural review. BUG-002 (cold open accepted input before the session read) and BUG-001's mobile half (offline creates never replayed) were filed independently, days apart, by different assertions — and turned out to be the same defect twice.
- **Pattern** — Both bugs are a state machine with **two doors into the same room, and the guard standing at only one of them**. `onForeground()` gated input on the session read via `foregroundSync`; `init()` ran the same reconciliation and never installed the gate, so a cold open dispatched a turn before `GET /assistant/session` and a previously closed session's boundary message was silently lost. Independently, `setOnline()` replayed offline-created tasks and `onForeground()` did not — so a user who reopened the app while still offline, then regained connectivity through the foreground path rather than a connectivity event, never got their tasks replayed. In both cases the AC named **two** triggers ("resume **or cold open**", "reconnect") and the implementation honoured one. The fix both times was the same move: stop duplicating the obligation at each entry point and route every entry through a **single shared installer** that performs it. The tell for reviewers is structural, not behavioural — when an AC's subject is a *transition* rather than an action, count the code paths that can produce that transition and check the obligation is attached to the transition, not to one of its callers.
- **How to apply** — When an AC names two or more triggers for one obligation (resume/cold-open, reconnect/foreground, retry/replay), write one test per trigger and make them **structurally different tests**, not one parameterised over a shared setup — a shared setup is exactly what hides the door nobody guarded. Every pre-existing AC-8 test `await`ed `init()` before asserting, which is precisely why the cold-open race survived authoring: awaiting the entry point collapses the race the AC is about. Drive the entry point **without** awaiting it and assert on what is observable mid-flight. In the implementation, prefer one installer (`foregroundSync`, `syncLocalTasks`) called by every entry point over the same three lines repeated per path; a grep for the installer's name should return every door.
- **Scope** — project-wide; `src/assistant/_shared/controller.ts` and `src/assistant/mobile/controller.ts` in particular, and any AC phrased as "every X transition …"
- **Stale check** — permanent (the shape outlives these two bugs)

### L-006 — An assertion that drives the wrong trigger passes whether or not the path it names works

- **Date added** — 2026-08-17 by reviewer-agent
- **Trigger** — T-022, F-003 structural review. Every reconnect assertion in TC-021 drove `connectivity.set(true)`, which always fires `onChange` — so the assertions were green whether or not the foreground reconnect path existed. The gap was found by mutation, not by the suite.
- **Pattern** — The test named one path and exercised another. `connectivity.set(true)` unconditionally fires the `onChange` callback, and the replay was wired to `onChange`; so the assertion proved "replay happens when `onChange` fires", which was never in doubt, while claiming to prove "a reconnect replays offline creates". The half of the AC that mattered — that a **foreground** is also a reconnect, with no connectivity event at all — had no assertion, and the missing behaviour (L-005) sat behind a green test for the whole authoring pass. This is worse than an untested AC, because the coverage matrix reports it covered. The fix was TC-040: a `Connectivity` double whose callback **never fires**, so the only way the replay can happen is the path under test. That is the general remedy — to test that path P causes effect E, disable every other cause of E rather than adding another assertion on E.
- **How to apply** — When a test double has a setter that also fires a notification (`set(true)` → `onChange`), treat it as testing the notification, not the setter. For any AC of the form "X is also a Y", build the double so the ordinary Y-trigger is inert — a no-op callback, a null listener — and let the test fail if the implementation was relying on it. Reviewers: when a mutation survives in a file whose ACs look well covered, suspect this before suspecting the mutation is inert; a suite that cannot distinguish two causes of the same effect will kill no mutant in either.
- **Scope** — project-wide; `tests/assistant/mobile/`, any test using the `Connectivity` / `AppLifecycle` doubles
- **Stale check** — permanent

### L-007 — `testid-contract.sh` silently scans nothing when handed a directory, and reads no id applied by a helper

- **Date added** — 2026-08-17 by reviewer-agent
- **Trigger** — T-022, F-003 structural review. C14 reported `0 honoured, 22 missing` — every id in the catalogue. All 22 are present and correct.
- **Pattern** — Two independent defects stack into one maximally-confident wrong answer. First, `ids_in()` iterates `for f in $1` and skips anything failing `[ -f "$f" ]`, so **directories are silently ignored** — and the reviewer protocol says to pass `Links.implemented_in`, which for F-003 is `[src/assistant/mobile/, src/assistant/_shared/]`, two directories. The tool scanned **zero bytes** and reported every declared id missing, with no warning that its input matched no files. Second, even given an explicit file list the regex is `(data-testid|testID|…)\s*=\s*…`, which only sees an id written as a literal attribute; the mobile implementation applies every id as `{...a11yProps(A11Y_IDS.micButton, {…})}`, a spread of a helper's return value, so the literal `testID=` never appears in a component at all. This is L-002's class, but the directory defect is worse than L-002's, because there the tool at least read the file it was wrong about — here a wrong argument shape produces a full-catalogue FAIL that looks exactly like a real contract breach.
- **How to apply** — Never accept a `testid-contract.sh` result without first confirming it read something: if `honoured + undeclared == 0` while the mockups declare a non-empty catalogue, assume the input matched no files and re-run with an explicit file list (`find <dir> -type f \( -name '*.ts' -o -name '*.tsx' \)`). Then settle the verdict against the executed observable, not the grep — for F-003 that is `src/assistant/mobile/__tests__/a11y.test.ts`, which parses both mockups and asserts the catalogue against `ALL_A11Y_IDS` in **both** directions, plus a check that every catalogue key is referenced in `components/`. The durable fixes are to make `ids_in()` recurse (or fail loudly on a path that matches no file) and to teach the regex the `a11yProps(A11Y_IDS.x)` / spread form.
- **Scope** — project-wide; `.claude/tools/design-check/testid-contract.sh`, and any feature whose `implemented_in` names directories rather than files
- **Stale check** — revisit once `testid-contract.sh` recurses directories and errors on an empty scan

### L-008 — Derived copy passes every test and hides the combinations nobody enumerated; literals + a parsed upstream catalogue expose them

- **Date added** — 2026-08-17 by reviewer-agent
- **Trigger** — T-032, F-003 structural re-verify (pass 2). Replacing an interpolating permission-copy template with literals cited by row ID exposed an eighth iOS tuple (mic `denied` · speech `undetermined`) that no spec, mockup, test or review had ever named — and, one step later, a live bug in the adjacent tuple.
- **Pattern** — Two distinct failures share one root, and both are invisible to a green suite. First: **derived copy silently satisfies an enumerated domain it does not cover.** A template that interpolates the missing capability into a sentence produces plausible output for *every* tuple, including the ones nobody thought about, so the gap has no observable — the suite asserts the three combinations someone listed and the template quietly serves the fourth. Deleting the template and demanding literals turns the unenumerated case into a compile-or-test failure, because a literal that was never written does not exist. Second: **a hand-transcribed expectation converts a contract check into a self-agreement check.** If the test retypes design's strings, design and implementation can drift apart while both halves of the test still agree with each other. The fix used here is the durable one: the test *parses* the upstream artifact (`docs/design/_shared/components.md` § MicControl) at run time and asserts per row ID, so it fails when the **upstream** artifact moves — the direction drift actually travels, and the direction almost every other cross-artifact check in this repo is blind to, because it compares two things the implementation controls.
- **How to apply** — When copy, messages, or any enumerated string set is owned by one artifact and consumed by another: (1) the consumer stores **literals** cited by a stable row ID, never a template that interpolates the varying part — templates are how an unenumerated combination ships fluent text nobody reviewed; (2) the test **reads and parses the owning artifact**, never a retyped copy — see `src/assistant/mobile/__tests__/permissions.test.ts:64-78` for the parser and `:106-113` for the per-row assertion; (3) add a test that reads the consumer as **text** and requires every published body to appear as a literal (`no message body is derived — every published body appears literally in the model`) — this is the assertion that makes rule 1 enforceable rather than a convention; (4) assert the *reason* a row differs from its siblings, not only its content, so a later "harmonising" edit fails instead of quietly changing what a user is told. **Reviewers: verify such a claim by mutation, not by reading.** Change one word in the upstream artifact, confirm the suite goes red, restore byte-identical (L-001 applies — checksum first), confirm green. A parser that silently matches nothing yields the same green as a parser that works, which is L-007's defect wearing different clothes. Corollary for implementers: when removing a derivation exposes a case with no specified answer, **route it — do not invent the row.** That refusal is what produced the correct row here, and the follow-up work found a real bug in the tuple next to it.
- **Scope** — project-wide; `docs/design/_shared/components.md` ↔ `src/assistant/mobile/model/permissions.ts` is the reference implementation. Applies to any spec-declared canonical artifact consumed by code — the same class L-004 names, solved rather than merely detected.
- **Stale check** — permanent (the shape outlives this catalogue)

### L-009 — Gate 1 clusters are a routing instrument, not a record; a finding that clusters with nothing disappears

- **Date added** — 2026-08-17 by orchestrator
- **Trigger** — T-050, F-002 Gate 1 round 2. **Four of seven lenses independently reported that their own round-1 findings had no recorded landing** — dev-mobile F5/F10, tester-web's two LOWs, design F4/F5, dev-web F7. Nobody had declined them; they were dropped in consolidation and the loss was invisible until the same lenses came back and looked for them.
- **Pattern** — Gate 1 lenses write no files by protocol (`_spec-review-protocol.md`), so their entire output exists only in the dispatch return. The orchestrator reads all seven returns, synthesises **convergence clusters**, and writes the clusters to `docs/reports/gate1-review-F-{id}.md`. That report is then the only surviving artifact. The clustering step is correct — it is what turns seven overlapping reviews into one revision work order — but it silently selects for **agreement**: a finding two lenses both raise becomes a cluster and survives; a finding one lens raises alone has nothing to cluster with and is preserved only if the synthesiser happens to put it in the MEDIUM/LOW table. Single-lens findings are exactly the ones a single-lens review would have missed, so the consolidation preferentially discards the highest-marginal-value output of the whole multi-lens design. Worse, the loss is **unfalsifiable from the report**: closure checks in round 2 compare against the report, so a finding absent from the report reads as "never raised" rather than "raised and lost". Two of the five recovered findings were HIGH-adjacent — dev-web F7 (Chromium drops `speak()` after `cancel()` in the same tick, which is literally the sequence AC-5 mandates) was rediscovered by chance in round 2 through a different route; had that route not existed it would have shipped.
- **How to apply** — **Persist every lens return verbatim at dispatch time, not at synthesis time.** Write each return to `docs/reports/gate1-lenses/F-{id}/round{n}-{lens}.md` as it arrives, before any clustering. Clusters remain the routing instrument and the report remains the work order; the returns are the record, and a round-2 closure check reads the returns rather than the report. Two checks follow: (1) after writing the synthesis, every finding id in every lens return must appear somewhere in the report — a cluster, the MEDIUM/LOW table, or an explicit "examined and declined, because…". An id in none of them is a dropped finding, not a low-priority one. (2) A finding raised by exactly one lens gets the *stricter* treatment, not the looser one — it is the marginal output of the extra lenses, which is the entire reason for dispatching seven instead of two. Orchestrators: "no lens-vs-lens conflict" is not evidence of a clean consolidation; the failure mode here is silent subtraction, not contradiction.
- **Scope** — project-wide; the orchestrator's Gate 1 synthesis step, `docs/reports/gate1-review-F-*.md`, `docs/reports/gate1-lenses/`. Applies to any dispatch pattern where N agents return findings and one agent consolidates them — the same shape recurs at Gate 2 when a reviewer merges tool verdicts.
- **Stale check** — permanent (the shape belongs to consolidation, not to Gate 1)

### L-010 — `git checkout --` on a TRACKED file with uncommitted changes destroys them, and L-001 does not cover that case

- **Date added** — 2026-08-17 by orchestrator
- **Trigger** — T-070c. qa-mobile-agent ran `git checkout -- docs/design/_shared/components.md` to undo a mutation check. The file was tracked, so the command **succeeded** — and discarded 57 lines of uncommitted work: T-062's entire English rewrite of the copy catalogue, and T-052's `## Spoken frames` section (18 `SPK-*` rows that F-002 AC-22's parse test reads). Nineteen mobile tests went red reporting `expected 'Asking for microphone access' to be 'Xin phép dùng micro'`, which reads like the agent's own edits broke something; the real cause was that the artifact underneath it had been reverted by four hours.
- **Pattern** — **L-001 names the wrong half of this hazard.** It warns that `git checkout --` *cannot restore an untracked file*, so the failure it describes is a restore that silently does nothing. The opposite case is worse and is not in the entry: for a **tracked file carrying uncommitted changes**, `git checkout --` works exactly as documented and throws away every change since the last commit. The agent gets no error, the tool reports success, and the damage is invisible until something downstream that parses the artifact starts failing — by which point the failure message points at the parser, not at the restore. Both halves share one root: **HEAD is not a backup.** In a project mid-feature, HEAD can be hours or days behind the working tree, and every restore-from-git is a restore to a state nobody intended. The blast radius scales with how many agents have written since the last commit — here, three tasks' output from two different agents, neither of them the one that ran the command.
- **How to apply** — **Never restore a mutation with `git checkout --`, `git restore`, or `git stash` in a working tree that is not clean.** Before mutating any file: `cp <file> <scratch>.bak`; after: `cp <scratch>.bak <file>` and confirm with `cksum` that it is byte-identical. This works for tracked and untracked files alike and does not depend on what HEAD holds, which is why it is the single rule to teach instead of two conditional ones. Two corollaries: (1) an artifact **owned by another agent** — a design catalogue, a spec, a fixture table — must never be restored by the agent that mutated it for a test; copy it first and put your own copy back. (2) When a parse-the-upstream-artifact test (L-008) fails with a *content* mismatch rather than a structural one, check the upstream artifact's mtime before believing the failure is about your own change; L-008's mechanism faithfully reports whatever the catalogue currently says, including a catalogue that was reverted underneath you.
- **Recovery, since this will happen again** — sub-agent transcripts hold full `Read` tool results. A file destroyed this way can usually be reconstructed from the most recent transcript that read it after the lost edits landed: search `~/.claude/projects/*/subagents/agent-*.jsonl` for a `tool_result` containing a distinctive string from the lost version, strip the `N\t` line-number prefixes the Read tool adds, and diff before writing it back. That is how the 57 lines here were recovered. Prefer a **later** reader over the writer's own transcript — the writer may only have a truncated diff, while a downstream agent that read the finished file has the whole thing.
- **Scope** — project-wide; any agent running a mutation check, and any orchestrator dispatching one while other tasks hold uncommitted work
- **Stale check** — permanent (the shape belongs to git, not to this repo). Revisit L-001's wording so the two halves are named together.

### L-011 — A stall with no output has several possible causes; measuring one of them is not diagnosing it

- **Date added** — 2026-08-17 by orchestrator. **Corrected the same day by the owner.**
- **Trigger** — T-087 and T-089 both died with `Agent stalled: no progress for 600s`, each having produced one line ("I'll start by reading my briefing"). Two identical failures on unrelated tasks. The orchestrator checked the host, found **127 MB free RAM and 13.7 GB of 15 GB swap in use**, concluded resource starvation, shut down the emulator/Simulator/Metro and orphaned Chromium processes, freed ~1.6 GB, re-dispatched, and wrote this entry attributing the stalls to memory. **The owner then said the real cause: the machine had been put to sleep.**
- **Pattern** — The memory pressure was real and worth fixing, and it was **not the cause**. The orchestrator had one hypothesis, took a measurement that confirmed it, and stopped looking — never asking what else produces the same symptom. It does not take much: `no progress for 600s` is **wall-clock**, so *anything* that suspends the host produces it identically — sleep, lid close, hibernation, a suspended VM, a paused container. A measurement that supports a hypothesis is more persuasive than a guess and exactly as wrong when the hypothesis was never the only candidate. **This is the failure the orchestrator had spent the whole session catching in other agents** — a green check that proves the thing it was pointed at rather than the thing in question — committed while writing the lesson about it.
- **How to apply** — On `Agent stalled: no progress`, enumerate before measuring. The candidates are at least: host suspended (sleep/lid/VM pause), resource starvation, network stall, and a genuine agent hang. **The orchestrator cannot distinguish the first from inside** — from its own view a sleeping host and a starved host look the same, because in both cases wall-clock advanced and nothing happened. So: **ask the human what happened to the machine before writing a cause into any durable record.** One sentence from them beats any amount of local measurement, and they are the only party who can see a closed lid. Two stalls in a row on unrelated tasks still means "environment, not agent" — that inference held; what did not hold was jumping from *environment* to a specific environmental cause.
- **The housekeeping half is still right, on its own merits** — an environment brought up for one step should be shut down at the end of it. Simulators, emulators, Metro and dev servers are cheap to restart and expensive to leave running; Playwright leaks orphaned Chromium across long runs (`adb emu kill`, `xcrun simctl shutdown all`, `pkill -f 'ms-playwright.*Chromium'`). Do that because leaving them running is wasteful, **not** because it explains a stall.
- **A second way the same negligence bites: abandoned wait-loops.** A poll written as
  `until grep -q <pattern> <log>; do sleep 10; done` is a reasonable way to wait for a build —
  until it outlives what it was waiting for. Two ways that happens, both seen here: the command
  exceeds its foreground timeout and the harness moves it to the background, where nothing ever
  reaps it; or the log it greps is later **truncated by a fresh run**, so the pattern it already
  matched never appears again and the loop waits forever. One such loop ran **5h40m** in this
  session, long after its build had finished and been reported. It cost almost no CPU, which is
  exactly why nobody noticed — the damage is that a stale "Running" entry makes the session's
  real state unreadable, and the owner had to ask. **Every wait-loop needs a bound**: give it a
  finite iteration count or a deadline rather than `until` alone, and stop it explicitly once the
  thing it waited for has been observed. When a backgrounded command never reports completion,
  that is not "still working" — check it, and reap it.
- **Scope** — project-wide; any orchestrator interpreting a subagent failure it did not witness
- **Stale check** — permanent

### L-012 — When two branches can end the same state, reaching it through one proves nothing about the other — and the sibling's side effects can make a dead assertion look alive

- **Date added** — 2026-08-17. **Found by mobile-agent (T-090) while porting a fix, confirmed and sharpened by web-agent (T-099).** Recorded by the orchestrator because no reviewer was in the loop; the finding is theirs.
- **Trigger** — AC-30's in-flight flag can be lowered by three different events: the scroll arriving, the user taking hold, and the surface being taken away mid-scroll. The guard named *"(c) still holds for an arrival long after the scroll has landed"* was written to cover the first. It never did: the run's later movement ended the flight through the **takeover** branch, so the arrival path was never exercised. Both clients had it, in the same shape, written by different agents.
- **The part that makes this hard to see** — the guard *did* go red when the arrival-lowering line was deleted, so a routine mutation check reported it healthy. The red came from the wrong place. Every behavioural assertion — view held, affordance raised — stayed **true** under the mutant; the only thing that moved was a raw count of scroll calls, and it moved because the sibling branch's cancel issues its own scroll. Relaxing that one count turned the whole suite green under the mutant. So the assertion was **already dead**, propped up by a side effect of the branch that was masking it, and it would have gone silently dead for real the day that cancel stopped being issued. A surviving mutant is a clear signal; **a mutant killed for the wrong reason is not, and it looks identical from the outside.**
- **Two more branches were fully unguarded** and only surfaced once each door was isolated: the takeover path stopping the animation but leaving the flag raised (the damage lands on the *next* arrival, which nothing exercised), and an instant scroll inheriting a flag from a previous animated one — a **handover between runs**, not a property of either branch, so no single-setting test could reach it.
- **How to apply** — For any state that more than one path can enter or leave, write **one case per path that can only reach it that way**, and shut the other doors explicitly in the setup rather than trusting the scenario not to open them. State in the test how each other door is closed — the three cases added here each say so, e.g. *"offset only ever moves forward while the flag is up; no gesture; `scroll.calls` is still 1, since a cancel would have added one"*. When mutating to check a guard, **read which assertion failed, not merely that one did**: if the failing assertion is a bookkeeping count rather than the behaviour the test is named for, the guard is decoration. And when one branch's implementation has an observable side effect, assume it is propping up its siblings' tests until shown otherwise.
- **Scope** — project-wide; anywhere a flag, mode or lifecycle can be exited more than one way. `src/assistant/{web,mobile}/follow.ts` and their tests are the reference case.
- **Stale check** — permanent

### L-013 — A feature that touches an existing one owes an impact section, and the pipeline does not ask for one

- **Date added** — 2026-08-18 by orchestrator. **The gap was named by the owner**, not found by the pipeline: *"F-005 có lẽ là feature mới nhưng lại liên quan tới những feature đã tồn tại nên nó phải handle được những gì đã có, những gì mới, impact changes."*
- **Trigger** — F-005 (task detail) was dispatched to spec-agent with a briefing that described the feature thoroughly and **never asked what it breaks**. The template's spec-agent brief, `_spec-review-protocol.md`'s five lenses, and the C1–C14 gate checks all read a spec for whether it is *internally* sound. Nothing in the pipeline asks whether the spec is sound **against the specs already built**.
- **Why it is not caught elsewhere** — Gate 2's C-checks run against code, so by the time they could notice, the impact has already been designed and built around. Gate 1's five lenses each read the spec *as written* and are told explicitly what is out of scope for them; "the other four features" is nobody's lens. And the individual collisions are all invisible from inside the new spec: they live in the *old* files.
- **The concrete shape it takes here**, as the reference case — each of these is a real collision found by hand, not a category:
  - **Closed field lists, in more than one place.** `apply.ts:28` and `app.ts:135` each hold the same five-field tuple. A new task field is either in them or explicitly out; silence means the field exists and no turn can ever change it, found later as a bug.
  - **A rule in one feature that a second feature is required to violate.** F-002 AC-22: an utterance with no declared frame **fails**. So the first turn that sets a new field has no frame and must fail — the rule working correctly, and a hard dependency F-002 would otherwise discover at build time.
  - **Records replayed verbatim.** `undo_snapshot` stores whole task rows; widening the task widens every stored snapshot, including ones already written. ADR-009 solved this once by narrowing the *write* vocabulary rather than deleting the value.
  - **A field with live data changing type** (a free string becoming an enum) is a migration, not an addition.
  - **A parity contract that enumerates.** F-003 counts F-001's ACs and was already stale.
- **How to apply** — Any feature that is not greenfield carries an **Impact** section naming, per existing artifact: what it touches, what changes there, and what breaks if nobody looks. Two failure modes to write against: a section listing only what the feature *adds* (the half that matters is what existing behaviour changes), and an impact silently *settled* by the spec author (where an impact forces a product decision, it is an open question for the owner). The orchestrator's job is to seed it with the collisions it can already name, marked explicitly as **a starting set, not the set** — a briefing that hands over a closed list invites the agent to stop at its end.
- **The durable fix is not in this project.** `_spec-review-protocol.md`, `ORCHESTRATION.md` and the agent files are **overwritten by `upgrade-project.sh`**, so a fix written into them here is erased at the next upgrade. This entry is in `LEARNINGS.md` because that file survives, and every agent's startup protocol skims it. The real repair — an impact lens at Gate 1, or an impact section required of spec-agent — belongs in `templates/project-starter/`.
- **PATCHED UPSTREAM 2026-08-18** (`claude-agents-final` commit `1aa28bf`), after the owner approved it. spec-agent now owes an `## Impact` section on any feature that is not the first in its module; `_spec-review-protocol.md` puts that section **in scope for every lens** rather than adding a sixth (an impact claim is a claim about an existing artifact, so the lens that owns the artifact is the one qualified to check it — and on the run that produced this entry, four lenses did exactly that unprompted). A **missing** Impact section is itself a HIGH finding, so absence cannot read as nothing to review. R15 asserts both halves, with four mutation cases proving it can fail.
- **This project has not received the patch, and receiving it is not free.** `.claude/agents/` here still carries the pre-patch spec-agent and protocol, so **an F-005 revision dispatched today does not inherit the Impact requirement** — the orchestrator must put it in the briefing by hand, exactly as it did the first time. `upgrade-project.sh` is what delivers it, **and it would also overwrite `.claude/tools/design-check/testid-contract.sh`, whose C14 identity-attribute fix exists here and NOT upstream** (see `STATUS.md ## Drift Log`, 2026-08-17). Upgrading before that fix is ported would silently regress C14: the checker would go back to treating `contentDescription` as an identity attribute and stop recognising `resource-id`. **Port the tool fix upstream first, then upgrade.**
- **Scope** — project-wide, and template-wide. Any feature after the first.
- **Stale check** — revisit once the template carries an impact step; this entry then documents why it exists.

### L-014 — A remedy adopted after an incident is worthless if the next command does not use it

- **Date added** — 2026-08-18 by orchestrator. **Second occurrence of the same failure in one session.**
- **Trigger** — A heredoc wrote `docs/reports/gate1-lenses/F-005-design.md` and committed it, and the working directory was `claude-agents-final`, not `todo-ai-redesign`. `mkdir -p` created the path rather than failing, `git add -A` swept **34 unrelated Playwright scratch files** in with it, and `git commit` reported success. The file was absent from the project it belonged to; nothing errored anywhere. It surfaced only because `validate-state.sh` checked an artifact path a TASKS row claimed — **the state validator caught a git mistake, which is not what it is for.**
- **The part that matters** — this had already happened earlier in the same session, with the same repository, the same 34 files and the same cause. The remedy was identified at the time and written down: **use `git -C <path>` for every git command.** Roughly forty tool calls later a command was written without it. So the lesson is not "remember the working directory" — that lesson was already learned and it did not hold. **A remedy that depends on remembering is not a remedy; it is the same exposure with a note attached.**
- **Why the working directory silently drifts** — the Bash tool persists cwd between calls, so a `cd` from many calls ago is load-bearing and invisible. Any call that does not restate it inherits whatever the last one left. Nothing in the output of `cat`, `mkdir -p`, `git add -A` or `git commit` mentions which repository it acted on, and all four succeed in the wrong one.
- **How to apply** — **Every** git invocation carries `git -C /absolute/path`, with no exception for "the previous command was in the right place". Every file-writing command that is part of a project's artifacts uses an absolute path or opens with `cd /absolute/path &&` in the *same* command. Do not rely on a `cd` in an earlier call. The check that actually catches this is cheap: after committing, `git -C <path> log --oneline -1` in the path you *meant*, not the one you were in.
- **Recovery, both times** — `git reset --mixed HEAD~1` in the wrong repository, then remove the misplaced file and leave the scratch files untracked as they were. Nothing was pushed either time. `--mixed` rather than `--hard`: the sweep included files that were untracked before the commit, and `--hard` would destroy them.
- **Scope** — project-wide, and orchestrator-wide; any session with more than one repository in reach.
- **Stale check** — permanent.

### L-015 — Two owner answers, each reviewed alone, can compose into a defect no lens could have found

- **Date added** — 2026-08-18 by orchestrator, from spec-agent's return on T-143.
- **Trigger** — F-005 Gate 1. Four escalated questions went to the owner and came back separately. Answer 1 shows passed reminders when the app opens; answer 3 makes `reminder_at` settable by voice. Voice runs on **both** clients. Scope the reminder surfacing web-only and a phone user can set a reminder the app never mentions again — **rebuilding, on mobile, the write-only data path answer 1 exists to close.** Neither answer is wrong. The defect exists only in their composition, and it became visible in the revision, not in the gate.
- **Pattern** — a gate reviews the spec against itself and, at Gate 2, against the code. **Nothing reviews one owner answer against another.** Each was escalated because no lens could settle it, each was answered on its own merits, and the lenses that would have caught the interaction had already returned. The revision agent is the only party holding all the answers at once, which makes it the only place the check can happen.
- **How to apply** — after folding N owner answers into a spec, read them **pairwise** before calling the revision done: for each pair, ask what one answer assumes about scope, platform or timing that the other changes. Where a pair collides, the spec **takes a default and names the alternative** in `## Open Questions` — do not block on it, and do not settle it silently. Flag it under `unresolved:` so the orchestrator escalates. This is the same shape as `## Impact` (L-013): a real gap that no role's lens is pointed at.
- **Second occurrence, same day, one task later (T-145)** — and it generalises the entry past owner answers. F-001 AC-24 requires the by-hand list to stay reachable in one action, with the reaching affordance *"neither hidden nor disabled by the failure being recovered from"*. F-005 AC-2 requires the detail **not to close while a write is unresolved** — on failure it stays, states it, and offers retry. Both were reviewed; both are right; **neither covers the composition**, in which the one action AC-24 promises is unavailable. What makes it more than a curiosity: **a server outage causes both failures at once**, so the composition is not a rare interleaving, it is the ordinary shape of the outage. Recorded as F-001 Open Question 10 rather than settled, because closing it means either weakening AC-24's condition or narrowing a reviewed AC in another spec.
- **The generalised form** — the parties that compose are not only owner answers. Any two independently-reviewed decisions can compose into an uncovered state: two ACs in different specs, an AC and a design rule, an owner answer and an AC. The invariant is that **review is per-artifact and composition is between artifacts**, so whoever holds both at once is the only one who can see it. That is the revision agent, the orchestrator, or nobody.
- **Scope** — project-wide; any Gate 1 revision that folds in more than one owner decision.
- **Stale check** — permanent, unless Gate 1 gains a step that re-reads answered escalations together.

### L-016 — A mechanical gate can fail on its own plumbing, and it names real fields when it does

- **Date added** — 2026-08-18 by orchestrator, from spec-agent's return on T-154, independently reproduced before being accepted.
- **Trigger** — `declared-elements.sh` reported nine of F-005's twenty-one declared fields as unaccounted orphans. All nine are in fact named in the ACs between 11 and 43 times.
- **The mechanism** — the check is `printf '%s' "$HAY_NORM" | grep -qF "$needle"` under `set -o pipefail`. Once the haystack exceeds the **64 KB pipe buffer**, `grep -q` exits at its first match, `printf` then takes **SIGPIPE (141)**, and `pipefail` promotes that to the pipeline's status. So the **earlier and more often a field appears, the more certainly it is reported missing** — the check inverts on exactly the fields the spec constrains most. Measured directly: the F-005 haystack is 118 KB normalised, and `grep -qF` returns **141** for `title`, `note`, `priority`, `due_at` and `status` while each string is present 11–43 times. Revision 2 of the same spec passed; revision 3's growth crossed the buffer.
- **Why it is worth a durable entry** — this script is also **C13 at Gate 2**. Any spec whose AC section passes ~64 KB fails structural review naming fields it does constrain, and *the failure message is credible*: "declared in ## Data, then never constrained by an AC" is exactly what a careless spec looks like. The likely response is to edit a correct spec until a broken checker goes quiet.
- **How to apply** — **when a deterministic check fails, verify the property it claims to test by an independent route before editing the artifact to satisfy it.** Here that was counting occurrences directly (`grep -c`), which contradicted the verdict in one command. More generally: a pipeline whose left side is a large `printf` and whose right side may exit early is unsafe under `pipefail`; use a shell substring test (`case "$hay" in *"$needle"*)`) which uses no pipe at all, or `grep -c … >/dev/null`, which consumes the whole input.
- **The fix, applied 2026-08-18** — `.claude/tools/spec-check/declared-elements.sh` now uses a `case` glob. Verified both directions: F-005 reports all 21 fields accounted (exit 0), an injected bogus field is still caught (exit 1), F-001/F-002/F-003 unchanged, eval 17/17.
- **Where the fix must also go** — `.claude/` is template-owned (`MANIFEST ## Paths.writers`). The copy in `claude-agents-final/templates/project-starter/` carries the same defect and **the next `upgrade-project.sh` will erase this fix unless it is ported upstream first** — the shape of L-004 and the reason C14's mapping fix was ported before the 2026-08-18 upgrade rather than after.
- **Scope** — project-wide; any spec review, and C13 at Gate 2.
- **Stale check** — permanent until the upstream template carries the fix.

### L-017 — A grant the map cannot express gets expressed as a repeated mistake instead

- **Date added** — 2026-08-18 by orchestrator, after making the same error three times in one day.
- **Trigger** — C6 (artifacts land inside the writing agent's subtree) failed on **T-143, T-153 and T-154**, each time because the orchestrator's own briefing told spec-agent to write a Gate 1 revision log into `docs/reports/gate1-lenses/`, which `MANIFEST ## Paths.writers` assigns to the orchestrator. Each time it was recorded as a drift entry and worked around — a one-off grant on T-143, a corrected Artifacts cell on T-153 — and each time the next dispatch made it again.
- **The pattern** — the crossing was **correct every time**. A revision log belongs beside the lens returns it answers, because that is where the next round's lenses look for it. What was wrong was the map: it had no way to say *"this agent may write this one kind of file in this one directory, always"*, only *"sanctioned once"*. So a standing, deliberate arrangement kept arriving as an error report against the agent that did what its briefing said.
- **Why the workaround was worse than the fix** — the drift log recorded three entries that read as agent misbehaviour, and the check attributed to spec-agent a decision the orchestrator made. Worse, C6 going red on a sanctioned write is exactly how a real out-of-subtree write becomes invisible: after the second identical FAIL, the next reader's instinct is to wave it through.
- **How to apply** — **when a deterministic check fails the same way twice for a reason you would sanction again, change the map, not the record.** A one-off grant is for a genuine exception (`T-121`, a change that had to land as a single unit); a pattern that recurs across dispatches is a missing entry. Write the reason into `MANIFEST` beside the entry so the widening stays auditable, and scope it as narrowly as the pattern actually is — here `docs/reports/gate1-lenses/`, not `docs/reports/`, so lens returns and consolidations stay the orchestrator's.
- **The tell** — a Drift Log accumulating near-identical entries about the same rule is not a record of carelessness; it is a design defect in the rule, reported by the only instrument that can see it.
- **Scope** — project-wide; orchestration and MANIFEST ownership.
- **Stale check** — permanent.

### L-018 — A stalled agent is not a failed agent; read the disk before you re-dispatch

- **Date added** — 2026-08-19 by orchestrator, from T-159.
- **Trigger** — spec-agent was dispatched to make F-005's final amendment. The harness reported `failed: agent stalled, no progress for 600s`, and the last thing it said was *"Now let me check a few exact strings I need to amend."* The obvious reading is that it stalled part-way through and the work is half-done or absent.
- **What was actually on disk** — the entire task, finished. The spec had grown 676 → 805 lines with all 48 AC ids intact and contiguous, the `## Links` block byte-identical, `declared-elements.sh` at exit 0, `index.md` updated, and a **22 KB revision log carrying all 58 findings** with a coherent closing section. The stall happened *after* the work, during a verification read.
- **What re-dispatching blindly would have cost** — not just the dispatch. The re-run would have started from the **already-amended** spec while being briefed to amend it, so every fix would have been applied to text that already carried it: duplicated clauses, withdrawn sentences re-withdrawn, and a second revision log disagreeing with the first. The failure would have looked like agent confusion rather than like an orchestrator error, which is the expensive kind.
- **Why the harness signal is not evidence about the artifact** — a stall is a fact about the *stream*, not about the *files*. The two are only correlated if the agent writes at the end, and these agents write throughout. `status: failed` answers "did the process finish cleanly", never "is the work done".
- **How to apply** — on any stall, timeout or crash, **measure the artifact before deciding anything**: does the target file exist, what is its size against its previous size, do its invariants hold (id sets, contiguity, the checks the briefing named), and does its last section read as finished or as cut off mid-sentence? Only then choose between accept-on-evidence, resume, and re-dispatch. This is L-011's rule — *measuring one possible cause is not diagnosing it* — applied to the artifact rather than to the stall.
- **The cost that is real** — the return itself is lost, and with it the `---METRICS---` block, `memory_entry:` and `unresolved:`. Record the task on evidence (the T-006c precedent), note the missing block in the Drift Log so the dashboard's `status: unknown` is explained, and accept that the agent's own judgement about what it left undone is gone. That is a reason to verify harder, not a reason to re-run.
- **Scope** — project-wide; any dispatch that ends abnormally.
- **Stale check** — permanent.

### L-019 — A rule outlives its reason, and nothing in the pipeline re-reads justifications

- **Date added** — 2026-08-19 by orchestrator, from spec-agent's return on T-157. The agent proposed this for `memory/` and said it probably belonged here instead; it was right, and the reason it gave is the entry's own point.
- **Trigger** — F-001 AC-31 gated the message door on the task being in the collection currently shown, and justified it: *"rendered as an inert control it would be an affordance that does nothing."* The rule survived three revisions. **Its reason did not.** Revision 6 gave the door a second postcondition — the open detail changes subject — which needs nothing from the list, and an owner direction supplied the collection change the first postcondition had been missing. The justification was false; the rule read exactly as it always had.
- **Why it is invisible** — a stale justification does not look stale. It looks like a rule that was written, reviewed and kept, and the sentence that would contradict it lives in a *different AC in a different spec*. Three Gate 1 rounds and nine lenses read AC-31's neighbourhood; the one that found it was a dev lens reading the **code** the gate compiles to, not the prose. The pipeline re-reads rules when they change; **nothing re-reads a reason when the thing it justified changes underneath it.**
- **The cost when it survives** — here it was measurable and ordinary: the default collection is Today, the assistant creates dateless tasks that land in Inbox, so under the retired gate **the one task in the message that was not a door was the task the assistant had just created**, with nothing on screen saying why.
- **How to apply** — when you amend a postcondition, add a route, or widen a rule, **grep the AC's *reason* clauses as well as its rules** — "because", "since", "otherwise", "which would". For each, ask whether the premise is still true under the new postcondition. And when you narrow a clause whose reason has gone false, **keep the reason with the strike-through** and say it was true when written: a later reader who finds a deleted justification concludes the earlier author was careless, which is both unfair and corrosive to the record.
- **Related** — the same family as **L-015** (review is per-artifact, composition is between artifacts), but the failure is a stale *justification* rather than two colliding decisions. Reviewer and dev lenses trip on it as readily as spec-agent does, which is why it is here and not in one role's file.
- **Scope** — project-wide; any spec with cross-referencing ACs.
- **Stale check** — permanent.

### L-020 — A spec that RECORDS an unstated mechanism hands architecture a work list; one that guesses hands it a guess to unpick

- **Date added** — 2026-08-19 by orchestrator, from architect-agent's return on T-160. The agent proposed this for LEARNINGS rather than `memory/` and gave the right reason: it crosses spec-agent, architect and every lens.
- **Trigger** — F-005 closed Gate 1 with the owner's split: *a contradiction must be fixed in the spec, because only the spec can say which of its two sentences is true; **an unstated mechanism is what the architecture phase exists to state.*** Revision 4 fixed 45 contradictions and left **twelve questions marked `Recorded, not answered`** in `## API Touch Points`, each in the finding lens's own words.
- **The result, which is the evidence for the entry** — **all twelve were answered on the first architecture dispatch**, in contracts and six ADRs, with **zero questions back to spec-agent and zero re-reading of the lens returns**. Two of them were answerable *only because a measurement travelled with the question*: the zone's write path (**there is no account entity in the store** — while ADR-005 had been scoping sessions to *the account* since 2026-08-16) and the restore's legacy case (**53 of 790 rows already soft-deleted with no membership, across 18 accounts, all predating the field**). A bare "TBD" on either would have cost a round.
- **How to apply — record three things, not one.** When a spec declines to answer: **what is unknown**, **what was measured**, and **what breaks if the wrong default is taken**. The third is what stops the next agent picking the cheap branch; the second is what makes the question answerable at all. A recorded gap with none of the three is indistinguishable from an oversight, and reads as one.
- **The inverse cost** — a spec that invents the mechanism hands architecture a guess to unpick, and the unpicking is invisible: the contract simply comes out different from the AC, and nobody re-reads the AC to notice.
- **Related** — **L-013** (a feature owes an impact section because nothing else asks) and **L-015**. This is the same family: work that no role's lens is pointed at, made visible by writing it down where the next role will look.
- **Scope** — project-wide; the spec → architecture handover.
- **Stale check** — permanent.

### L-021 — A truncated read is indistinguishable from a complete one, and specs put their prohibitions at the end

- **Date added** — 2026-08-19 by orchestrator, from design-agent's self-report on T-152.
- **Trigger** — design-agent drafted the urgency mark as `!` / `!!` / `!!!`, with a paragraph of reasoning about why inheriting that scale was right. **AC-9 forbids exactly that, by name**: *"deliberately one glyph, not Apple's graduated `!` / `!!` / `!!!`"*. The prohibition was in the part of the AC the agent's read had truncated. It caught it only by going back and reading AC-9's **tail** specifically.
- **Why nothing would have caught it** — the draft was internally coherent, cited the source audit, and would have passed every structural check. `design-check` verifies tokens, contrast, testid uniqueness and state coverage; **none of them knows what the AC forbids.** C11 renders the mockup and C14 checks the testid contract — a graduated glyph scale satisfies both. The earliest catch was a human reading the screens against an AC nobody was re-reading.
- **The mechanism, which generalises past design** — this spec is large: F-005 runs to 805 lines, single ACs exceed 6 KB, and a plain read of the file returns a preview. **The truncated part is not a random sample — it is the end.** And specs put their constraints at the end: the reasoning comes first, then the prohibitions, then the "which of the N states" clauses. So truncation removes disproportionately the sentences that would have stopped you, and leaves the ones that sound like agreement.
- **How to apply** — before publishing any artifact that implements an AC, **re-read that AC's last paragraphs specifically.** Extract one AC at a time (`awk '/^- \[ \] \*\*AC-9\*\*/{f=1} f&&/^- \[ \] \*\*AC-10/{exit} f'`) and check the extraction's byte count against what you were shown, or read the tail explicitly (`| tail -c 3000`). The same discipline applies to any long document read in full: **if a tool truncated, ask what was at the end, not whether the middle looked right.**
- **Related** — this is **L-007**'s shape (a check that silently scanned nothing) and **L-016**'s (a gate that failed on its own plumbing) moved to the reading step: a partial input that reports as a complete one. It is also why L-009 insists the individual lens returns are read rather than only the consolidation.
- **Scope** — project-wide; any agent reading a large spec, and any orchestrator briefing that says "read AC-N".
- **Stale check** — permanent.

### L-022 — During a parallel dispatch the test baseline is stale before the agent reads it, and a red suite is not attributable by reading it

- **Date added** — 2026-08-19 by orchestrator, from backend-agent's return on T-162. **The orchestrator caused the half of this that is a process defect**, and it is recorded that way.
- **Trigger** — phase 4 dispatched backend, web and design in parallel. Each briefing told the agent *"the baseline is 771/771 across 27 files"*. By the time any of them ran, it was false: web-agent was editing `src/assistant/_shared/`, which the mobile and web tests compile, so the full suite's failure set moved **12 → 6 → 28 → 9** across one task with nobody touching those tests. The suite ended at 898 tests because two agents were adding their own.
- **What the agent did instead of guessing** — `git worktree add <tmp> HEAD`, copied the *other* agents' current non-overlapping files in, and ran the suite there. The same 28 failures appeared **without any of its own changes**. That is what separated "mine" from "theirs", and it let it say *2 of the 11 are mine, in a subtree I may not touch, and here is the one-line fix* — rather than "pre-existing", which is the claim this project's ethos forbids without proof.
- **The orchestrator's half of the lesson** — **do not quote a baseline count into a briefing during a parallel dispatch.** It reads as ground truth, it is stale on arrival, and it invites the agent either to panic at someone else's red or to launder its own red as "pre-existing". Say instead: *other agents are editing X in parallel; establish your own baseline before you start, and attribute any failure you find with a counterfactual tree.* Naming the parallel agents and their subtrees is useful; naming a number is not.
- **The agent's half** — measure the baseline, do not accept it; diff the failure **sets**, never the counts; and **never fix a red test in another agent's subtree to make your own number look right.** Backend left two docs/qa/ failures it had caused, named them, and gave the one-line fix, because the file belongs to qa-api-agent.
- **Related** — **L-002** (a tool verdict is evidence, not proof) and **L-012**. Also the reason the queue rule forbids overlapping subtrees: this is the *unavoidable* residue when the subtrees are disjoint but the compile graph is not.
- **Scope** — project-wide; any phase-4-style parallel dispatch, and every briefing written for one.
- **Stale check** — permanent.

### L-023 — A harness with two clocks is green only while the view reads the wrong one

- **Date added** — 2026-08-19 by orchestrator, from web-agent's self-report on T-163.
- **Trigger** — `_shared/testing/fixtures.ts`'s `todayTask()` / `upcomingTask()` read the **real clock**, while the test harness pinned `ControllerDeps.now` to a fixed `T0`. **771 tests were green**, and they were green *because the view also read the real clock* — agreeing with the fixture and disagreeing with the controller. Routing the view onto the injected seam, which AC-44 requires, turned **29 tests red at once**, every one of them about a row that was "in Today" for one clock and "in Upcoming" for the other.
- **Why this is worse than an ordinary two-sources bug** — it is **L-004 living inside the instrument built to detect it.** The harness exists to pin time so date behaviour is assertable; a harness that pins the seam and leaves the fixtures on the wall clock does not fail, it *agrees with itself for the wrong reason*, and it converts the correct change into a 29-test regression that looks like the change broke something. The pressure at that moment is to revert the seam, which is exactly backwards.
- **How to apply** — **before routing any layer onto an injected clock, read what the fixtures read.** Anchor fixtures and seam to one instant *first*, confirm the suite is still green with the view unchanged, and only then move the view. If the suite goes red at the anchoring step, that redness is the pre-existing defect surfacing and is information — not something to route around.
- **The general form** — when a test's setup and its subject both read an ambient value, the test asserts their agreement, not the behaviour. Ask of any green date-, locale- or random-dependent test: *would this still pass if the subject read the injected value and the fixture did not?*
- **Related** — **L-004** (one fact, two sources), **L-012** (an assertion green for the wrong reason), and F-005's AC-44, whose whole purpose is one seam per side.
- **Scope** — project-wide; any suite with fixtures and an injectable ambient dependency.
- **Stale check** — permanent.

### L-024 — A held clock is a per-assertion choice, not a uniform good

- **Date added** — 2026-08-19 by orchestrator, from qa-api-agent's return on T-166.
- **Trigger** — F-005 mandates an injectable clock (AC-44) and **L-023** says anchor the fixtures and the seam to one instant. That is necessary and **not sufficient**. Two cases in the same suite need opposite things:
  - **AC-5's proof requires the instant HELD.** `task-equals` contains `updated_at`, so under a moving clock a hand edit is "modified-since" for a reason unrelated to the claim — it would pass for the wrong reason (tester T8 raised this at Gate 1).
  - **AC-28's third condition requires the clock ADVANCED.** The condition *is* `updated_at == created_at`, so under a held clock **an edit is byte-indistinguishable from no edit** and the state the condition describes **cannot be constructed at all.**
- **The deeper point, which is about the AC and not the harness** — a condition whose predicate is a timestamp comparison is **unconstructible under a frozen seam**, and in production it is blind to any edit inside one clock tick. That is a property of the condition, not a defect in the tooling; the ACs and ADR-013 chose the proxy deliberately, and the fixture has to work with it.
- **How to apply** — for every case, ask the extra question: **does this assertion turn on two timestamps being EQUAL or DIFFERENT?** Equal → hold the clock and assert the equality *explicitly* before the subject assertion. Different → advance it and assert the inequality first. **State which one the case needs, in the case**, because the next author will inherit the run's default and not know it was a choice.
- **Related** — **L-023** (two clocks in one harness), **L-012** (green for the wrong reason). L-023 is the floor; this is the part that is still per-case after the floor is in place.
- **Scope** — project-wide; any suite over an injectable clock.
- **Stale check** — permanent.

### L-025 — A file-watching runtime turns a git probe into a crash that looks like the product's

- **Date added** — 2026-08-19 by orchestrator, from a crash the owner saw on the iOS simulator.
- **Trigger** — the app was running on the simulator under Metro with Fast Refresh. To attribute a test regression, the orchestrator ran `git stash push src/assistant/mobile src/assistant/_shared`, re-ran the suite, and `git stash pop`. Metro saw the files change **twice**, hot-swapped modules into the live app both times, and the app red-boxed: `TypeError: Cannot read property 'used' of undefined` inside `carriedRows`, a function that had just been loaded in its **new** version against state built by the **old** reducer's `initialState`, which had no `undoOffer` field. Two module generations in one running process.
- **Why it is worth a lesson** — the crash is indistinguishable from a product defect at the point of observation: real stack, real component, real file, and the owner sees a red screen. Nothing in the red box says "you edited the working tree three minutes ago." The natural next move is to open `carried.ts` and start hardening `offer !== null` into `offer != null` — **fixing correct code to silence an artefact of the observer**, and shipping a weaker guard permanently.
- **How to apply** — **treat any running dev-server app as a consumer of the working tree, not just a producer of evidence.** Before `git stash`, `git checkout`, worktree swaps, or branch switches, either stop the watcher or accept that the running app is now invalid. And when a crash appears in an app that was healthy minutes ago with no deliberate code change, **relaunch on a clean bundle and re-drive the flow before diagnosing** — a defect that survives a cold start is real; one that does not was manufactured by the measurement.
- **The general form** — a diagnostic that mutates shared state is not read-only just because it restores what it touched. It is read-only only if nothing observed the interval.
- **Related** — **L-018** (measure the disk before re-dispatching), **L-022** (a baseline moves under you during parallel work). This is the same family: the act of measuring changed the thing measured.
- **Scope** — project-wide; any watcher-backed runtime (Metro, Vite HMR, `tsc --watch`, nodemon).
- **Stale check** — permanent.

### L-026 — A reversal is complete per FIELD, not per row — and the field the gesture is named after always comes back

- **Date added** — 2026-08-21 by orchestrator, from spec-agent's return on T-178. The agent proposed this for LEARNINGS rather than its own memory file, and was right: it crosses spec, architect, backend and QA, and it is the shape of a bug rather than a project convention.
- **Trigger** — a delete gesture in this app writes **two** things: `deleted_at` on every member row, and, when the scope is `series`, `series_ended_at` on the whole series (`engine/plan.ts:653`). The restore (`api/app.ts`) clears **`deleted_at` and `updated_at`, and nothing else**. So a restore that presents itself as reversing the gesture returns the occurrences **with the repeat permanently dead**, and `F-005 AC-43`'s *"it reverses exactly the action it was offered for and nothing else"* is false for that one class.
- **Why nothing caught it** — three reasons stacked, and the third is the one worth carrying. The AC reads as satisfied. The restore's own tests assert on `deleted_at`, which is the field that does come back. And **the window in which a human could have noticed was seconds wide**: the undo offer expires, so nobody ever restored a series days later. Verified as currently invisible — 0 rows in the live store carry `series_id` and `deleted_at` together.
- **What changes it from latent to real** — F-006's trash makes that restore a *deliberate act, days later*. The defect does not appear because code changed; it appears because a **new surface changes how long the old code is looked at.**
- **How to apply** — when a spec says a write is reversed, **enumerate every FIELD that write touched, not every ROW.** Write the reversal's postcondition as a field list and assert it. A gesture whose name describes one field (*delete* → `deleted_at`) is exactly where a second field gets written and forgotten, because the reversal *looks* complete: the field the gesture is named after did come back, and that is the one everybody checks.
- **The general form** — an inverse operation is only as complete as the enumeration it was written against. "Undo X" is not a specification; "restore these N fields to these values" is.
- **Related** — **L-005** (one door guarded, one not), **L-012** (an assertion green for the wrong reason), **L-021** (a rule landing while its predecessor stands). ADR-012 owns the restore; F-005 AC-43 owns the claim.
- **Scope** — project-wide; any undo, restore, revert or rollback path.
- **Stale check** — revisit when ADR-012 is amended.
