# Review — F-001 voice-assistant-view (re-verification, pass 3)

**Date**: 2026-08-16 **Agent**: reviewer-agent **Task**: T-012 **Result**: STRUCTURAL-PASS

## Summary

14/14 checks passing. **Both pass-2 failures are genuinely fixed, and the C5 fix is the real one** — the two automation tiers that had never executed now run for real and green (46/46 api, 34/34 e2e), verified by me from a clean process, and the project's own aggregate command `npm run test:all` is 213/213 where it was `46 failed | 167 passed`. C9.4's four `assistant_turn` fields are present and a fresh both-directions sweep of *every* entity found no fifth drift.

The two known non-blocking items from the briefing are confirmed real, confirmed harmless to all 14 checks, and recorded below with the arithmetic that shows no verdict depends on them.

C12's L-001 hazard reproduced on both runs again; both leftovers were caught by checksum manifest, restored from an out-of-tree copy, and the tree verified byte-identical. All suites re-run green afterwards. **No mutated code remains on disk.**

## Checks

| # | Check | Result | Notes |
|---|---|---|---|
| C1 | Links block populated and consistent | PASS | 15/15 declared paths exist and are non-empty (run records now among them); 8/8 `api_endpoints` match api-contracts.md rows; `known_bugs: []`; `tested_by.mobile: []` consistent — zero ACs carry a `mobile` tag |
| C2 | AC coverage (per-platform) | PASS | **38/38** AC×platform pairs (api 16 · web 22), every one with ≥1 **P1**. All 72 TCs are P1/active. AC-26/AC-27 untagged (RESERVED) and correctly excluded |
| C3 | API contract consistency | PASS | 8/8 endpoints: contract row + handler in `app.ts` + ≥1 P1 API TC; every per-endpoint error code has a triggering TC, including all three `UNDO_REFUSED` reasons. `INTERNAL` note carried from v2 |
| C4 | No hardcoded design values + component library | PASS | **Zero** colour literals in any `.ts`/`.tsx`; all 44 in `styles.css` sit inside the `:root` token block (lines 19–102, block spans 18–117). Radix `Toggle` used for both toggle affordances; Radix ships no button/input primitive |
| C5 | Test suite runs clean (real execution) | **PASS** | **Five real runs, all green** — see the table below. `tsc --noEmit` exit 0. Zero disabled-test markers anywhere in `src/` or `qa/**/automation/` |
| C6 | No TODO/FIXME without tracking | PASS | Zero TODO/FIXME/HACK/XXX across `Links.implemented_in` (`src/`, `package.json`) |
| C7 | Security sanity | PASS | `npm audit`: `{info:0, low:0, moderate:0, high:0, critical:0}`. No `dangerouslySetInnerHTML`/`innerHTML`/`eval`/`new Function`/`child_process`; no credential literals; no CORS headers emitted |
| C8 | Operational readiness | PASS | `## Ops` carries Observability (2 named counters), Rollback (N/A + prototype reason), Feature flag (N/A + reason) |
| C9 | Docs in sync with implementation | **PASS** | C9.4 fixed. Fresh both-directions sweep of **all six** shapes found zero drift — see detail below |
| C10 | Scope boundary declared | PASS | 8 Out-of-Scope items, each naming thing + reason; `**Considered and rejected:**` filled with two rationales. Mobile-deferral bullet and AC-26/AC-27's RESERVED markers cross-reference each other correctly |
| C11 | Design mockups render correctly | PASS | Render executed: 18 passed, 0 failed, 6 skipped. The contrast skip is now covered elsewhere — see note |
| C12 | The test suite can fail | PASS | api detected the break at mutation 1; web at mutation 1. L-001 hazard reproduced twice, contained — see detail |
| C13 | Every declared data element accounted for | PASS | `declared-elements.sh`: all 13 declared fields accounted for, exit 0 |
| C14 | Mockup's testid contract honoured | PASS | Tool reports 19 honoured / 3 missing; the 3 disproved at a **real browser DOM** this time — see detail |

## C5 in detail — the automation tier is alive

This was pass 2's headline failure. Every command below was run by me this dispatch, from the project root, output copied verbatim.

| Tier | Command | Result |
|---|---|---|
| api unit | `npx vitest run src/assistant/api` | `Test Files 6 passed (6)` · `Tests 73 passed (73)` |
| web unit | `npx vitest run src/assistant/web` | `Test Files 3 passed (3)` · `Tests 94 passed (94)` |
| **api automation** | `npx vitest run qa/assistant/automation/api` | `Test Files 1 passed (1)` · **`Tests 46 passed (46)`** |
| aggregate | `npm run test:all` | `Test Files 10 passed (10)` · **`Tests 213 passed (213)`**, exit 0 |
| **web e2e** | `npm run test:e2e` | **`34 passed (31.9s)`**, exit 0, real chromium |
| typecheck | `npx tsc --noEmit` | exit 0 |

The arithmetic closes: 73 + 94 + 46 = 213, so `test:all` is now demonstrably the union of the two unit tiers **and** the api automation tier — the aggregate command that was red at pass 2 is the same command that is green now, covering the same files. `vitest.config.ts` excludes only `qa/**/automation/e2e/**` and the harness entrypoint, with a comment explaining why (Playwright's `test()` is a different function than vitest's); the e2e tier is run by its own runner and its own config, which now exist. Nothing is excluded that nothing else runs.

All five commands were **re-run after C12's mutations were restored** and were green again: `213 passed`, `tsc` exit 0, `34 passed`. The green is not a build-cache artifact.

**Why I trust this more than pass 2's green.** Pass 2's C5 was measuring `src/**` while the spec claimed `qa/**` too (L-003). This pass I ran every executable artifact named in `Links.tested_by` explicitly, as separate commands with separate numbers, plus the aggregate. There is no tier left that nothing executes.

## C9 in detail — the fifth drift does not exist

Pass 2 found a fourth drift after fixing three; the honest question this pass was whether a fifth was waiting. I swept every persisted shape in both directions rather than only the four fields named in the prior report.

| Shape | Code | Document | Result |
|---|---|---|---|
| `TurnRow` | `types.ts:103–136` — 21 fields | `data-model.md` `assistant_turn` — 20 rows covering 21 fields | **exact match, both directions.** `post_apply`, `created_ids`, `pending_op`, `caused_resolutions` are present at `:64–:67`, marked internal in `last_foreground_at`'s style, with `post_apply` explicitly named as the AC-7 comparison baseline |
| `SessionRow` | `types.ts:150–167` — 10 fields | `assistant_session` — 10 rows | exact match, both directions |
| `TaskRow` | `types.ts:9–21` — 10 fields | `task` prose — 9 named + `user_id` in the ownership sentence | match |
| `Question` | `types.ts:82–91` — 6 fields | `Question` table — 6 rows | match |
| `UndoResultRec` | `types.ts:94–101` — 6 fields | `UndoResult` inline shape | match |
| `TurnOutcome` | `types.ts:52–63` — 6 variants | `TurnOutcome` block — 6 kinds | match |

Endpoint direction: all 8 `Links.api_endpoints` have a handler in `app.ts` (`:177`, `:205`, `:222`, `:257`, `:279`, `:289`, `:324`, `:322`) and a contract section or CRUD row in `api-contracts.md`; no handler exists that the contract does not document, and no documented endpoint lacks a handler.

## C12 in detail — L-001 reproduced twice, contained twice

Guarded exactly as L-001 prescribes: a `shasum -a 256` manifest of all 50 files under `src/` plus an out-of-tree copy, taken before the first run, with the copy independently verified byte-identical to the manifest before it was trusted as a restore source.

| Run | Files targeted | Verdict | Left mutated on disk |
|---|---|---|---|
| api | `engine/{turns,undo,apply,sessions,normalize}.ts` | PASS at mutation 1 | `engine/turns.ts:57` — `count === 1` → `count != 1` |
| web | `model/{reducer,messages,format}.ts`, `controller.ts` | PASS at mutation 1 | `model/reducer.ts:90` — `msgs.length === 0` → `!= 0` |

Per L-002 I passed the behavioural core explicitly rather than a directory, and both runs landed their first mutation on real behaviour and were detected immediately — no budget spent on bootstrap or config files, and no inert-mutation false accusation.

Both leftovers were detected by manifest diff (not by the tool, which exited 0 both times), restored from the verified copy, and the tree confirmed **byte-identical to its pre-C12 state**. `test:all` (213/213), `tsc` (exit 0) and `test:e2e` (34/34) were all re-run green afterwards.

L-001's stale-check conditions are still unmet: `suite-can-fail.sh` still guards with `git diff --quiet` and restores with `git checkout --`, and this repo still has **zero commits** (`fatal: your current branch 'main' does not have any commits yet`). The entry stays live and the next reviewer must guard the same way.

## C14 in detail — settled at a real browser this time

`testid-contract.sh` reports the same 3 missing as both prior passes: `assistant-chip-affirm`, `assistant-chip-negative`, `assistant-option-chip`. All three are present at `ConversationPane.tsx:130–136`, applied through a multi-line JSX ternary the tool's single-line regex cannot read — L-002, verbatim, third occurrence.

What is new is the strength of the disproof. Pass 2 settled it with a jsdom render (`app.test.tsx:310`, the 22-id mockup catalogue asserted against 17 rendered states — still passing this run, inside the green 94). This pass the **Playwright e2e suite resolves all three by `getByTestId` in a real chromium browser** (`qa/assistant/automation/pages/AssistantPage.ts:155–157`) and 34/34 pass, including TC-013a (taps `chipAffirm`), TC-013b (taps `chipNegative`) and TC-017 (taps `optionChips`). A selector that does not resolve cannot be clicked; these were clicked. PASS on execution evidence, and this is the strongest observable the project has produced for it.

*Method note:* `--impl` must be an explicit file list, not a directory (the tool skips non-files). Passed 8 `.tsx` files.

## The two known non-blocking items — checked, not waved through

The briefing named both and asked whether either confuses C2/C3. **Neither does**, and here is the arithmetic rather than the assurance.

**(1) TC-10 / TC-30 markdown describes a scenario the contract makes impossible.** Confirmed real. Both files' `## Preconditions` state two simultaneously-pending questions (TC-10: "Q1 = bulk_delete… then Q2 = clarify… Q2 is newer"; TC-30: "two pending questions"). Per AC-10's supersede rule, asking Q2 while Q1 is unresolved *declines* Q1, so the precondition is unreachable. The automation was corrected (`spec.ts:504` unclassifiable-then-tap; `:916` single-still-pending, with an inline comment naming the false premise) and the `.md` files were not synced — the api run record flags this under "Drift noted" and recommends the revision.

- **C2 unaffected.** C2 reads only the Metadata table. TC-10 cites `AC-10`; TC-30 cites `AC-28, AC-10`. Discount both entirely and AC-10 api still has **7** P1 TCs and AC-28 api still has **4**. No AC×platform pair drops to zero.
- **C3 unaffected.** TC-30 is one of three sources for `VALIDATION`; `TC-34-validation-400-matrix` is the dedicated owner. No error code is uniquely sourced from either file.
- **Recorded as a NOTE, not a WARNING**, because no check's logic is confused by it — but it *is* live documentation drift in the artifact QA hands to a human reader, and the queued follow-up should land.

**(2) The canonical fixture table has no exact-2-task-delete row.** Confirmed, and slightly larger than the briefing framed it. The spec's Test strategy declares **one** canonical table "shared by QA and implementers", but it physically exists in two files that have now diverged: `qa/assistant/F-001/api/utterance-intent-fixtures.json` carries 24 rows **including `UT-DELETE-BULK-2`**, while the implementation's `src/assistant/api/ports/fixture-table.ts` carries 23 and lacks it. Both automation tiers independently invented their own exact-2 row to probe AC-9's tightest boundary — the web harness via `QA_EXTRA_ROWS` (`qa-test-server.ts:41`, documented per row) and the api suite via an inline stub branch (`spec.ts:174`, `'delete both qaapi report tasks'`). Both use the extension mechanism the spec sanctions, and the web run record already files the follow-up ("add an exact 2-target delete row to `src/assistant/api/ports/fixture-table.ts`").

- **C2/C3 unaffected** — neither check reads fixture rows. AC-9 api retains 3 P1 TCs regardless.
- **C9 unaffected in scope** — the fixture table is neither an endpoint nor a `data-model.md` entity, so it falls outside C9's stated scope. I am not stretching C9 to reach it. It is named here because a fact the spec declares singular now has two copies that disagree, which is the shape of every drift C9 exists to catch, and the follow-up should collapse it to one.

## Notes on passing checks

- **C11 contrast — the gap narrowed since pass 2.** The tool still skips contrast on all 3 mockups (`DESIGN.md` states no target ratio) and overflow is still checked at 1280px only (`tokens.json` declares no breakpoints). But AC-19's 1.4.3 clause is no longer unverified: e2e **TC-023 measures contrast on transcript, diff and outcome messages in both themes** and passes. The mockups remain unmeasured; the shipped UI does not. Setting a target ratio in `DESIGN.md` would turn the mockup half on too.
- **C3 — `INTERNAL` still has no triggering TC**, unchanged from pass 2 and still not a fail: it is declared in `## Conventions` as a global catch-all, not in any endpoint's error table, and it is a defensive handler for a non-`ApiError` throw. Recorded so a human can overrule.
- **C2 — five TCs cite an AC not tagged for their platform** (api TC-04/TC-36 → AC-4, TC-38 → AC-18, TC-39 → AC-3; web TC-032 → AC-16). All five are *over*-coverage: an api TC additionally asserting a web-tagged AC's server-side half. No AC×platform pair depends on them. NOTE, not a finding.
- **AC-8's `not_newest` refusal is unreachable from the web UI** and the web run record discloses it rather than absorbing it (the Undo button only ever renders for the current newest mutating turn, so a stale-affordance race is impossible by construction). It is covered where it belongs: the api tier exercises `not_newest` in TC-18, TC-21 and TC-40, with 3 assertions in the api automation. No gap.
- **Pass-2's C12 escape is closed.** The undetected api mutation last pass flipped `undone: true` at `engine/undo.ts:43`, and the assertion that catches it lived at `qa/…/automation/api/…spec.ts:455` — in the suite that could not run. That suite runs now, 46/46, so the assertion is live. This is the clearest single measure of what repairing the automation tier bought.

## Human checklist

Nothing new blocking surfaced. Two items carried forward from prior reports remain open and are the human's to close:

```markdown
1. WCAG 1.4.3 on the *mockups* still has no automated check.
   Seen in: C11 — "DESIGN.md states no contrast ratio — not measured" on all 3
   Now partial: e2e TC-023 measures contrast on the shipped UI in both themes
   and passes, so AC-19's clause is verified where it matters most. The
   mockups themselves stay unmeasured until DESIGN.md declares a target ratio.
   Cost to close: one line in DESIGN.md.

2. The `X-User-Id` trust model (carried from pass 1, unchanged).
   Prototype auth accepts a caller-supplied user id with no verification.
   ADR-001 scopes this to prototype-grade and C7 finds no vulnerability
   *within* that scope. Confirm the boundary is intentional before anything
   resembling a deployment target exists.
```

Two follow-ups are already queued by the QA agents and need no new decision — the TC-10/TC-30 markdown sync, and collapsing the fixture table to one physical copy with the exact-2 row in it.

## Next step

**STRUCTURAL-PASS — 14/14.** This clears the structural gate. Recommended sequence:

1. **Gate 3 (product-agent)** — `MANIFEST ## Product.product_review: required`, so the final UX/value review is the next gate, not the last one.
2. **Human sign-off** — the reviewer does not approve merges. The checklist above is what needs a human, and neither item blocks Gate 3 running in parallel.
3. **The two queued follow-ups** (TC markdown sync, fixture-table consolidation) can land any time; neither gates anything.
4. **Land a baseline commit.** Independent of F-001, this repo having zero commits is what makes L-001's C12 hazard live, and it will reproduce for every reviewer on every feature until a first commit exists.

No product code, no test, and no document needs a change for this gate.
