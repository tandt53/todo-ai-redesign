node_modules/
data/
dist/

# playwright-cli scratch: console logs, screenshots, the published flow page
.playwright-cli/

# Playwright run output — regenerated per run
test-results/

# Ephemeral output — regenerable, never an input to anything. One rule decides
# what belongs here: could a tool make this again from what is in the repo?
# Reports and QA run summaries are AUTHORED records and stay in git, however
# much they look like output in a file tree.
#
# design-shots and mobile-preview moved here on 2026-08-19; 57 of their files
# had been tracked, which is 4.2 MB of regenerable renders sitting in history.
output/

# Render output from design-check / visual-check. Regenerated on every run,
# read back by nothing — no baseline comparison exists. 234 MB and 2268 files
# of churn per run, which is what jammed a stash restore on 2026-08-25.
docs/design/_shots/
